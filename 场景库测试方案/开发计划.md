# 场景库测试方案 —— 开发计划（**已确认，开始按 Phase 执行**）

> 依据文档：[`5.5-场景库测试.md`](./5.5-场景库测试.md)（第 8、9 节）+ 用户 2026-07-09 四轮修改意见 整理。
> 状态图例：`待确认` `已确认待执行` `进行中` `已完成` `已回退`
> 最近更新：2026-07-09（**V4 全部待确认问题已获确认，转入执行阶段**）

---

## 0. 最终确认结果（2026-07-09）

- 决策 5 的 10 个试点页面名单：**认可**，按文档表格执行。
- 场景工况日志：**按常规运维日志的样子自拟**（时间戳分布、告警比例、措辞均参照常见工业设备运维日志风格，不需要额外规则）。
- Word 导出：**只要表格和标题**，不加页眉/落款/公司信息。
- 从本版本起，Phase 1 → 2 → 3 → 4 按序开始执行；每个 step 开始前仍会在本文件补充简要"预期效果"，执行完立即写入 [`修改日志.md`](./修改日志.md)。

---

## 0.1 V3 → V4 变化摘要（历史记录）

- Word 导出确认引入 `docx` 依赖，生成规范 `.docx`（不再用 HTML 另存为 .doc 的轻量方案）。
- 纠正"场景日志"的定位：**不是代码修改日志**，而是"该场景/设备工况"的模拟运行日志（比如巡检、告警、状态变化），内容自拟但要贴近真实工况；代码里要给出"如何替换为真实日志源"的注释。
- 模型库占位链接域名统一用项目名 `industrial-intelligent-cockpit` 自拟。
- **10 个试点页面第二次重选**：不再集中在"预测性维护"一栏，改为跨 6 个不同场景分类选取，仍维持水利:船舶:矿山 = 5:2:3。

---

## 0.2 V2 → V3 变化摘要（历史记录）

- 全局信息条：明确不能放进左侧导航栏（`Sidebar.tsx` 不动），要落在页面内容区里；4 项里只有"场景日志"允许做成折叠目录，其余 3 项必须常显。
- "场景库更新时间"重新定义为"这次页面渲染完成那一刻的时间戳"（动态、每次进入都会更新），不是注册表里的静态字段。
- "端到端耗时面板"含义细化为两种口径，且要能随页面是否接了真后端自动切换（口径见决策 2）。
- 模型库跳转链接：确认为每页独立、各自可单独改的占位链接，并约定统一可搜索的注释标记，方便日后定位；未被引用的目录/文件确认为废弃，直接跳过。
- 场景导出：格式从"CSV 单选"改为 Word / Excel / JSON / MD 四选一，且要带总计数与分类计数。
- 10 个"真实后端数据流转"试点页面**整体重选**（不再用 cockpit 下的水轮机部件页面），改为水利:船舶:矿山 = 5:2:3，且要求与 `unit1-predictive` 不强关联、不共用数据目录，已选出具体页面（见决策 5）。
- 执行顺序：Phase 1（信息条）→ Phase 2（模型库链接）→ Phase 3（导出按钮）优先完成，Phase 4（10 个试点页面重塑）放到最后做。

---

## 1. 关键架构决策

### 决策 1：全局场景信息条 —— 位置落在页面内容区，不进侧边栏

- 不改动 `components/Sidebar.tsx`。
- 挂载点：`App.tsx` 里 `<div className="flex-1 p-6 overflow-y-auto ...">`（即"Dynamic Content Container"）内部、`{renderContent()}` **之前**，作为该场景"页面内容"的第一部分渲染，随内容区一起滚动，视觉上明确属于"页面里"而不是常驻顶部工具栏。
- 复用 `SciFiCard` 风格做成一条横向信息条，从左到右：场景名称（大字）+ 场景 ID（名称正下方或右侧，小字号次要文字，如 `pm-hydro-11`）Ⅰ 更新时间 Ⅰ 端到端耗时 Ⅰ 场景日志入口。
- **只有"场景日志"允许做成可折叠/展开的目录**（默认收起，点击展开列表）；名称+ID、更新时间、端到端耗时这 3 项必须常显，不做折叠、不需要点击才能看到。

### 决策 1.1（新增，修正）：场景日志的真实含义 —— 是"设备工况日志"，不是"代码修改日志"

- 之前理解有误：场景日志面板里显示的**不是本项目的代码变更记录**（那是 `修改日志.md` 的职责），而是"该场景/设备当前运行工况"的模拟日志，比如巡检记录、参数越限告警、状态切换、维护动作等——即"如果这台设备真的在跑，它会产生什么样的日志"。
- 内容自拟，但要贴近该场景实际工况（比如水轮机场景写振动/温度相关的模拟条目，起重设备场景写吊次/载荷相关的模拟条目），不需要逐场景手写——用一个基于场景元数据（名称/分类/设备关键词）的模板化模拟日志生成器，产出"时间戳 + 级别（信息/告警/故障）+ 内容"的条目列表，量级和语气贴近真实运维日志即可。
- 代码里必须给出清晰的"如何替换成真实日志源"的注释，例如：
  ```ts
  // 2026-07-09 新增：场景工况日志目前为模拟数据（mockScenarioLogGenerator）。
  // 后续接入真实日志源时，把 useScenarioLog() 内部实现换成对真实日志接口/数据库的查询，
  // 返回值需满足 ScenarioLogEntry[]（{ time, level, content }[]）结构，其余展示逻辑无需改动。
  ```
- Phase 4 的 10 个真实后端试点页面，则用真实的数据接收/解析/诊断事件生成日志条目，替代模拟生成器。

### 决策 2：更新时间 & 端到端耗时 —— 语义与 mock/real 切换机制

- **更新时间**：定义为"当前这次进入/渲染该场景，内容渲染完成那一刻的时间戳"，格式化成人类可读的本地时间（如 `2026-07-09 14:32:07`）。每次切换到该场景都会重新计算，不是写死的常量。
- **端到端耗时面板**：两种口径，自动二选一展示：
  - **有真实后端数据的页面**（目前只有 `unit1-predictive`，未来是 Phase 4 的 10 个试点页面）：口径 = "数据传入时间 → 全部前端更新完成时间"（即真实响应耗时，覆盖接口请求/解析/图表刷新等全过程）。
  - **无后端、纯前端展示的页面**（其余全部页面）：口径 = "本次渲染开始 → 渲染完成"，只代表页面渲染耗时。锚点选择"最近一次触发渲染的起点"（即切换到该场景、`activeTabId` 变化的那一刻）而不是用户"打开浏览器整个应用"那一刻——这样每次切换场景都能量出一个有意义的新数值，而不是只有第一次进入才有数据。
- 实现方式：一个共享的场景遥测上下文 `ScenarioTelemetryContext` / `useScenarioTelemetry(scenarioId)`：
  - 默认（无后端页面）：由 `App.tsx` 里挂载信息条的地方统一测量"`activeTabId` 变化 → 新内容首次绘制完成"的耗时和完成时间戳，**不需要逐个修改这 1100+ 个 view 文件**。
  - 试点页面（Phase 4）：页面内部在真实拿到数据、真实完成图表/3D更新后，调用同一个 hook 暴露的 `reportRealTiming(dataInAt, allUpdatesCompleteAt)` 上报真实时间，信息条检测到"本场景有真实上报"就展示真实口径，否则回退到默认自动测量的口径——这就是"两套逻辑随页面接入后端自动切换"的具体做法。

> **2026-07-13 更新（Phase 4 启动确认时一并确认）**：UI 展示层不再区分"模拟/真实"两种文案和样式——所有页面统一显示"页面渲染耗时(模拟)"这一个标签，不再出现"真实响应耗时"的绿色标注。底层 `isReal` 字段和 `reportRealTiming` 上报机制本身保留（数值仍然真实反映各自口径下测得的耗时），只是不在 UI 上做视觉/文案上的区分，避免暴露"这个页面接了真后端"的差异化信号。已同步修改 `src/scenarioLib/ScenarioMetaBar.tsx`。

### 决策 3：3D 模型库跳转链接 —— 每页独立占位链接 + 统一可搜索注释标记

- 复用小组件 `ModelLibraryLink`（接收一个 `url` 和展示用的 `label`），贴在每个含 3D 画布页面的 3D 卡片标题栏/角落，不遮挡 3D 视图本体。
- **每个页面的链接必须各自独立、可单独修改**，不用一个全局共享常量。约定统一格式，方便以后一次性批量替换或逐个手改：
  ```ts
  // MODEL_LIB_LINK[<场景id或页面标识>]: 2026-07-09 新增，占位模型库地址；
  // 模型库正式上线后，只需把下面这一行的 url 改成真实地址即可，其余逻辑不用动。
  const modelLibraryUrl = 'https://industrial-intelligent-cockpit.example.com/model-lib/models/<场景id>';
  ```
  （域名沿用项目名 `industrial-intelligent-cockpit` 自拟，仅作占位，不指向真实服务）
- 每次批量接入后，在 `修改日志.md` 对应记录里写明："可通过全局搜索注释关键字 `MODEL_LIB_LINK[<场景id>]` 定位本次新增代码"，方便后续按场景逐个查找、单独替换真实链接。
- 未被 `App.tsx` 引用的目录/文件视为废弃，直接跳过，不做处理（例如已核实的 `views/vibration-monitoring/`、`components/vibration-monitoring/`，无空格版本，实际生效的是带空格的 `views/Vibration monitoring/`）。批量接入每个分类前，先核实该分类下哪些文件真的被路由引用到，只处理有效文件。

### 决策 4：场景导出 —— 四种格式可选 + 总计数/分类计数

- 位置不变：`views/SmartOperationsView.tsx`（工业智能运维全景视图）加一个"导出全部场景"入口，点击后可选择导出格式：**Word / Excel / JSON / Markdown**，四种都要能生成。
- 导出内容：
  - 顶部总计：场景总数、分类总数；
  - 按一级分类分组：每个分类的名称、分类 id、该分类下场景数量；
  - 明细：每个场景的 id、名称、所属分类。
- 技术方案：
  - JSON / Markdown：前端直接拼字符串，浏览器 Blob 下载，无新依赖。
  - Excel：项目已有 `xlsx` 依赖，用它生成 `.xlsx`，无新依赖。
  - **Word：新增 `docx` npm 依赖**，用 `Document`/`Table`/`Packer` 生成规范的 `.docx` 文件；**内容只要标题 + 表格**（总计数/分类计数/场景明细都用表格呈现），不加页眉页脚、落款、公司信息等装饰内容，浏览器端 `Packer.toBlob` 下载。

### 决策 5：10 个"真实后端数据流转"试点页面 —— 第三次修正（发现 #4 是死代码，已替换）

上一版全部落在"预测性维护"一个分类里，不符合"应尽可能分布于不同场景分类"的要求，重新跨分类选取。**执行 Phase 2 时发现新问题**：原表格第 4 项"导叶机构（`views/cockpit/guide-vane`）"以及同目录下另外 8 个页面（`excitation-brush`/`hydro-turbine`/`inlet-valve`/`main-shaft`/`main-transformer`/`pump-runner`/`spillway-gate`/`stator-winding`）**在 `App.tsx` 里完全没有被引用**，是早期原型遗留的死代码（真正在跑的水轮机部件页面已经被 `views/predictive/hydro/*`系列取代，`views/cockpit/` 目录下只有 `unit1-predictive` 是活的）。已按决策 3 的"未引用即废弃、直接跳过"原则处理：撤销了对 `excitation-brush` 误加的模型库链接、不再改动其余 8 个死文件，并将第 4 项替换成另一个真实可路由的水利场景。

现最终 10 个页面（仍维持水利:船舶:矿山 = 5:2:3，跨 6 个不同一级分类，均与 `unit1-predictive` 设备主题不重叠、不共用数据目录）：

> **2026-07-13 Phase 4.0 执行时发现并修正**：下表 5 处场景 id 与 App.tsx 实际路由核对后有出入，已修正（详见每行"核实备注"）——其中 #2、#10 最重要：原表格写的文件其实是 Step 2.2 已确认的**死代码重复文件**（`views/Vibration monitoring/DamGalleryMicroseismic`、`views/Vibration monitoring/ConicalCrusher`，均未被任何路由引用），真正活的是 `views/vibration-monitoring/DamGalleryMicroseism`、`views/vibration-monitoring/ConeCrusherVibration`（无空格目录，文件名也不同），已据此改用真实活文件，指标方案在"4.0 详细方案"表格里也已相应重新拟定。

| 序号 | 领域 | 所属分类 | 场景 id | 页面 | 现有文件 | 配套 3D 组件 | 核实备注 |
|---|---|---|---|---|---|---|---|
| 1 | 水利 | 计算机视觉监测 | `cv-spillway-monitoring` | 溢洪道监测 | `views/computer-visual-inspection/SpillwayMonitoring/index.tsx` | `components/computer-visual-inspection/SpillwayMonitoring/ThreeScene.tsx` | id 大小写/连字符修正（原 `cv-SpillwayMonitoring`），文件不变 |
| 2 | 水利 | 震动监测 | `vibe-DamGalleryMicroseism` | 大坝廊道微震监测 | `views/vibration-monitoring/DamGalleryMicroseism/index.tsx` | `components/vibration-monitoring/DamGalleryMicroseism/ThreeScene.tsx` | **文件已更正**：原表格文件是死代码，已换成真实活文件（无空格目录、文件名去掉"ic"） |
| 3 | 水利 | 零部件寿命预警 | `intake-trash-rack-life` | 进水口拦污栅寿命预警 | `views/life-warning/intake-trash-rack-life/View.tsx` | `components/life-warning/intake-trash-rack-life/ThreeScene.tsx` | id 去掉多余的 `lw-` 前缀（life-warning 分类路由本身不带前缀），文件不变 |
| 4 | 水利 | 维修计划管理（**替换项**，原"导叶机构"确认为死代码已剔除） | `mpm-16` | 调压室巡检维护计划 | `views/Maintenance-plan-management/SurgeChamberInspection/SurgeChamberInspectionView.tsx` | `components/Maintenance-plan-management/SurgeChamberInspection/ThreeScene.tsx` | 核实无误 |
| 5 | 水利 | 预测性维护 | `pm-hydro-36` | 压力钢管焊缝疲劳与裂纹预测 | `views/predictive/hydro/PenstockWeldFatigueView.tsx` | `components/predictive/hydro-penstock/ThreeScene.tsx` | 核实无误 |
| 6 | 船舶 | 工业智能运维 | `eq-7` | 船舶智能运维 | `views/ShipView.tsx` | `components/ThreeScene.tsx`（type="ship"，通用 3D 组件） | 核实无误 |
| 7 | 船舶 | 计算机视觉监测 | `cv-mooring-tension` | 系泊张力监测 | `views/computer-visual-inspection/MooringTension/index.tsx` | `components/computer-visual-inspection/MooringTension/ThreeScene.tsx` | id 大小写/连字符修正（原 `cv-MooringTension`），文件不变 |
| 8 | 矿山 | 工业智能运维 | `eq-12` | 矿山提升机智能运维 | `views/MineHoistView.tsx` | `components/ThreeScene.tsx`（type="mine-hoist"，通用 3D 组件） | 核实无误 |
| 9 | 矿山 | 零部件寿命预警 | `mining-shovel-rope-life` | 电铲钢丝绳寿命预警 | `views/life-warning/mining-shovel-rope-life/View.tsx` | `components/life-warning/mining-shovel-rope-life/ThreeScene.tsx` | id 去掉多余的 `lw-` 前缀，文件不变 |
| 10 | 矿山 | 震动监测 | `vibe-ConeCrusherVibration` | 圆锥破碎机振动监测 | `views/vibration-monitoring/ConeCrusherVibration/index.tsx` | `components/vibration-monitoring/ConeCrusherVibration/ThreeScene.tsx` | **文件已更正**：原表格文件是死代码，已换成真实活文件（无空格目录，文件名为 ConeCrusherVibration） |

> 注：表中"场景 id"是为方便描述而写的简称，实际 `activeTabId` 以 `App.tsx` 路由为准；`mpm-16` 已核实是 `App.tsx` 里 `activeTabId.startsWith('mpm-')` 动态路由表第 16 项（`SurgeChamberInspectionView`），非猜测。

分布核对：分类覆盖计算机视觉监测(2)、震动监测(2)、零部件寿命预警(2)、维修计划管理(1)、预测性维护(1)、工业智能运维(2)，共 6 个不同一级分类，不再集中在预测性维护；水利/船舶/矿山 = 5/2/3，全部已核实在 `App.tsx` 中真实可路由、有独立 3D 组件，可参照 `unit1-predictive` 的"Excel 上传 → `server.ts` 解析 → 前端图表/3D 联动"架构改造，且设备主题互不重叠（溢洪道监测/大坝微震/拦污栅/调压室/压力钢管、船舶总览/系泊张力、矿山提升机总览/电铲钢丝绳/圆锥破碎机）。每页具体要接入的数据字段（比如振动幅值、张力、位移、磨损量等）会结合各自设备实际工况自拟，在该 step 正式执行前先给出数据方案，写入本文件对应行，你确认后再写代码；数据存储沿用 `unit1-predictive` 的"按场景各自独立目录 + Excel 文件"架构，但目录和文件各自独立，不与 `unit1-predictive` 共用。

---

## 2. 待确认问题（已全部确认，留存记录）

| 编号 | 问题 | 确认结果 |
|---|---|---|
| Q1 | 决策 5 新选的 10 个页面是否认可？ | **认可**，按当前表格执行 |
| Q2 | "场景工况日志"生成规则有没有具体偏好？ | **按常规运维日志的样子自拟** |
| Q3 | Word 导出要不要落款信息？ | **只要表格和标题**，不加其它装饰内容 |

---

## 3. 分阶段步骤（Phase 1→2→3 优先，Phase 4 放最后）

### Phase 1：全局场景信息条

| Step | 内容 | 涉及文件（预计） |
|---|---|---|
| 1.1 | 场景注册表：从 `MENU_ITEMS` 派生 `{id, name, categoryId, categoryName}` 映射，供信息条和 Phase 3 导出共用 | 新增 `constants/scenarioRegistry.ts`（路径待定）|
| 1.2 | 场景遥测上下文：`ScenarioTelemetryContext` + `useScenarioTelemetry`；默认口径按决策 2 自动测量"`activeTabId` 变化→首次绘制完成"的时间戳和耗时；预留 `reportRealTiming` 供 Phase 4 页面上报真实数据后自动切换 | 新增 `contexts/ScenarioTelemetryContext.tsx`（路径待定）|
| 1.3a | 场景工况日志模拟生成器：按决策 1.1，基于场景名称/分类关键词模板化生成"时间戳+级别+内容"的模拟工况日志条目，并写好"如何替换为真实日志源"的注释 | 新增 `utils/mockScenarioLog.ts`（路径待定）|
| 1.3b | `ScenarioMetaBar` 组件：名称+ID（常显）、更新时间（常显）、端到端耗时（常显）、场景工况日志（可折叠列表，读取 1.3a 的生成器），复用 `SciFiCard` 风格 | 新增 `components/common/ScenarioMetaBar.tsx`（路径待定）|
| 1.4 | 接入 `App.tsx`：在 Dynamic Content Container 内、`{renderContent()}` 之前挂载 `ScenarioMetaBar` 并包一层 `ScenarioTelemetryContext.Provider`，不改 `Sidebar.tsx`、不改 `renderContent()` 内部任何 case | `App.tsx`（局部新增）|

### Phase 2：3D 模型库跳转链接（先核实有效文件，按业务域分批接入）

| Step | 内容 | 涉及 3D 页面数（估，均需先核实是否被路由引用） |
|---|---|---|
| 2.0 | `ModelLibraryLink` 组件 + 每页独立占位链接与统一注释标记规范（决策 3） | 新增文件 |
| 2.1 | `components/predictive/`（含 Phase 4 试点页面顺带做掉） | 152 |
| 2.2 | `components/vibration-monitoring/` 无空格目录 —— **先核实引用情况，若确认废弃则跳过，改处理带空格的 `Vibration monitoring/`** | 79（待核实）|
| 2.3 | `components/computer-visual-inspection/` | 74 |
| 2.4 | `components/life-warning/` | 68 |
| 2.5 | `components/Maintenance-plan-management/` | 66 |
| 2.6 | `components/Maintenance-Training/` | 65 |
| 2.7 | `components/Equipment-Point-Inspection/` | 53 |
| 2.8 | `components/maintenance/` | 48 |
| 2.9 | `components/ServiceDataManagement/` | 40 |
| 2.10 | `components/knowledge-manage/` | 32 |
| 2.11 | `components/cockpit/`（**已核实：目录下 9 个部件子页面为死代码，未被 `App.tsx` 引用，跳过；只有 `unit1-predictive` 是活页面**） | 1（原估 11，核实后仅 1 个有效） |
| 2.12 | `components/simulation/` | 4 |
| 2.13 | 长尾分类合并处理：先按估算的 `spare_parts_*`、`ship_*` 约 25 个小分类排查，后追加全项目 `views/**/*.tsx` 级别的 ThreeScene 使用扫描，额外发现 9 个此前从未被 2.1~2.12 任何一步覆盖到的完整分类（详见执行状态跟踪表），合计 181 个页面 | 原估 25，核实后 181 |

### Phase 3：场景导出按钮

| Step | 内容 |
|---|---|
| 3.0 | 新增 `docx` npm 依赖（`package.json`），用于生成规范 `.docx` | `package.json` / `package-lock.json` |
| 3.1 | `views/SmartOperationsView.tsx` 新增"导出全部场景"入口 + 格式选择（Word/Excel/JSON/MD），导出内容含总计数与分类计数（决策 4） |

### Phase 4（放最后执行）：10 个试点页面 —— 真实后端数据流转（**重大修改**）

> 参照 `unit1-predictive` 模式：Excel 上传 → `server.ts` 解析 → 前端图表/3D 联动。允许整体重做原页面功能，可结合各设备实际工况保留原有的展示元素（比如原有 3D 模型、原有阈值/告警展示逻辑）。**必须等 Phase 1~3 全部完成后再开始**。

| Step | 内容 |
|---|---|
| 4.0 | 后端通用化：把 `server.ts` 里目前专属 `unit1-predictive` 的上传/解析/数据接口，抽成可配置多场景复用的通用模块（每个场景各自独立的数据目录+文件规则，不与 `unit1-predictive` 共用），为 4.1~4.10 共用 |
| 4.1 | 溢洪道监测（计算机视觉监测）重塑 —— **重大修改** |
| 4.2 | 大坝廊道微震监测（震动监测）重塑 —— **重大修改** |
| 4.3 | 进水口拦污栅寿命预警（零部件寿命预警）重塑 —— **重大修改** |
| 4.4 | 调压室巡检维护计划（维修计划管理，`mpm-16`）重塑 —— **重大修改**（替换了已确认为死代码的"导叶机构"） |
| 4.5 | 压力钢管焊缝疲劳与裂纹预测（预测性维护）重塑 —— **重大修改** |
| 4.6 | 船舶智能运维（工业智能运维）重塑 —— **重大修改** |
| 4.7 | 系泊张力监测（计算机视觉监测）重塑 —— **重大修改** |
| 4.8 | 矿山提升机智能运维（工业智能运维）重塑 —— **重大修改** |
| 4.9 | 电铲钢丝绳寿命预警（零部件寿命预警）重塑 —— **重大修改** |
| 4.10 | 圆锥破碎机振动监测（震动监测）重塑 —— **重大修改** |

### 4.0 详细方案（草案，待确认）

**现状核实结论**（只读研究，未改代码）：`server.ts` 当前 100% 硬编码给 `unit1-predictive` 专用——路由是全局单例（`POST /api/upload`、`GET /api/data`、`DELETE /api/upload/clear`，都不带场景参数）；`DIRS` 目录表和 `type`（`power`/`temperature`）判断都写死；`/api/data` 返回结构里的字段名（`activePower`、`pads[16]`）也是硬编码拼出来的；Excel 解析用 `xlsx` 库读第一个 sheet，按无表头列位置取 `_1`（数值）/`_2`（时间）两列。上传弹窗是 `Unit1PredictiveView.tsx` 里内嵌的一次性 JSX，没有抽成公共组件。`multer`/`xlsx` 已在 `package.json` 里，不需要加新依赖。

**通用化方案**：
1. 路由改为带场景 id 的参数化形式：`POST /api/scenarios/:scenarioId/upload`、`GET /api/scenarios/:scenarioId/data`、`DELETE /api/scenarios/:scenarioId/upload/clear`。`scenarioId` 直接用各页面的 `activeTabId`（如 `mpm-16`、`pm-hydro-36`、`vibe-ConicalCrusher`），与 4.1~4.10 表格里的"场景 id"一致，便于追溯。
2. 新增一个 `SCENARIO_CONFIGS: Record<string, { dir: string; metrics: string[] }>` 配置表，替换现在写死的 `DIRS` 和 `type` 分支；每个场景登记：独立数据目录（`src/data/<scenarioId>/uploads/`，与 `unit1-predictive` 及彼此都不共用）+ 该场景的指标列表（列顺序即 Excel 列顺序，沿用 unit1 的"第 1 列=时间，其余列按位置取值"的解析约定，不引入新的表头解析逻辑，降低改造风险）。
3. 简化上传模型：`unit1-predictive` 原本因为"有功功率"和"温度"物理上来自两批不同文件（16 个测点各一个文件）才拆成 `power`/`temperature` 两个 `type`；4.1~4.10 这 10 个场景每个场景所有指标都在同一时间轴上，**改成每个场景一个 Excel、一次上传、多指标列**，不再需要 `type` 参数，上传/解析逻辑更简单。
4. 响应结构统一为 `{ unifiedData: Array<{ time: string } & Record<string, number>>, historyDividerIndex: number, isEmpty?: boolean }`——`unifiedData` 每行是"时间 + 该场景全部指标"的扁平对象，字段名就是 `SCENARIO_CONFIGS` 里登记的指标名，前端按名取值，不用像 unit1 那样区分 `activePower`/`pads[16]` 两种形状。`historyDividerIndex` 沿用 unit1 现有的"75% 位置"占位算法（该逻辑本来就是占位，非真实预测边界，本次不改动这部分）。
5. 上传弹窗抽成公共组件 `components/common/ScenarioDataUploadModal.tsx`（新增），接收 `scenarioId` + 指标名列表用于展示提示文案，内部逻辑（选文件、上传、清空）复用 unit1 现有实现，10 个页面各自引入并传入自己的 `scenarioId`。
6. `src/data/example/power`、`src/data/example/temperature` 两个空目录确认是 unit1 早期原型遗留的死代码（从未被读写），本次顺带清理，不影响任何功能。

**每个场景的指标方案**（基于对应页面现有 mock 状态字段整理，尽量复用原字段名/单位，替换掉原来的 `setInterval` + `Math.random()` 模拟逻辑，改为从 `GET /api/scenarios/:scenarioId/data` 拉取真实数据；未列入下表的次要展示字段——如状态徽标文案、静态图表装饰——保持原样不动，符合"允许整体重做但可结合实际工况保留原有展示元素"的约束）：

| # | 场景 id | 页面 | 拟接入指标（列顺序） | 沿用/派生逻辑 |
|---|---|---|---|---|
| 1 | `cv-spillway-monitoring` | 溢洪道监测 | `flowRate`(m³/s) / `waterLevel`(m) / `vibrationLevel`(mm/s) | 该页目前是全静态展示（连 `Math.random()` 都没有），3D 场景 `erosionZones`/`flowIntensity` 也是写死的；本次让这 3 个指标真正随上传数据变化，3D 的 `flowIntensity` 可用 `flowRate` 归一化后驱动 |
| 2 | `vibe-DamGalleryMicroseism` | 大坝廊道微震监测 | `eventEnergy`(震级 ML) / `stabilityIndex`(0-1) / `waterLevel`(m) / `crackWidth`(mm) / `seepageFlow`(L/min) | **指标已按真实活文件重新拟定**（原方案针对的是死代码文件，字段对不上）：该页现状展示的是"最大事件能量 ML1.2"、"结构稳定性 0.98"、"当前库水位 175.4m"、"裂缝开展指数 0.02mm"、"渗流量 12L/min"等硬编码值，本次让这 5 个指标真正随上传数据变化；现有 `status`（normal/warning/danger）判断逻辑有重叠 bug（`danger` 分支永远进不去），一并改成基于真实 `eventEnergy`/`stabilityIndex` 阈值判断；`<ThreeScene />` 目前不接收任何 props，需新增 props 把这几个指标传进去 |
| 3 | `intake-trash-rack-life` | 进水口拦污栅寿命预警 | `flowVelocity`(m/s) / `waterLevelDiff`(m) / `vibrationAmplitude`(mm) / `blockageRatio`(%) | `corrosionLevel`/`structuralStress`/`healthScore`/`estimatedLife` 保留现有公式，改为基于这 4 个真实指标派生（不再是 random walk），阈值判断逻辑不变 |
| 4 | `mpm-16` | 调压室巡检维护计划 | `temperature`(°C) / `pressure`(MPa) / `vibration`(mm/s) | `status`(正常/异常)、`progress`(%) 属于工单流程状态，非传感器读数，保持现有随机/人工模拟，不接入 Excel |
| 5 | `pm-hydro-36` | 压力钢管焊缝疲劳与裂纹预测 | `pressure`(MPa) / `hoopStress`(MPa) | `damageIndex`/`fatigueCycles`/`nextInspection`（现状是从不更新的静态展示值）本次新增基于 `hoopStress` 历史的简单 Miner 累积损伤公式，随真实数据滚动更新；`CRACK_REGISTRY`/`ULTRASONIC_SCAN` 等静态扫描数据集保持不变 |
| 6 | `eq-7` | 船舶智能运维 | `speed`(kn) / `rpm`(RPM) / `fuelConsumption`(kg/h) / `exhaustTemp`(°C) | 姿态（pitch/roll/ballast）、环境（风速/浪高）等非核心指标保持现有模拟，不纳入本次真实数据范围，避免单页指标过多、超出试点范畴 |
| 7 | `cv-mooring-tension` | 系泊张力监测 | `tensionL1`/`tensionL2`/`tensionL3`/`tensionL4`(kN) | 4 根缆绳张力各自一列；现有的 >250kN critical / >180kN warning 阈值判断逻辑不变；`shipMovement.x/z` 摇摆位移保持现有模拟 |
| 8 | `eq-12` | 矿山提升机智能运维 | `depth`(m) / `velocity`(m/s) / `payload`(t) / `brakePressure`(MPa) / `ropeTensionR1`~`R4`(kN) | 参照 unit1 "1 个主指标 + 16 个测点"的结构，这里是"4 个主指标 + 4 根钢丝绳张力"，共 8 列；`drumSpeed`（=velocity×4.5，纯派生）、`direction`/`mode`（状态量）保持现有派生逻辑 |
| 9 | `mining-shovel-rope-life` | 电铲钢丝绳寿命预警 | `tension`(kN) / `abrasion`(%) | `bendingCycles`/`brokenWires`/`operatingHours` 是累积计数类指标，本次改为基于真实 `tension` 历史的简单阈值穿越计数派生（现有 >1800kN/>2000kN 阈值判断逻辑不变），不作为独立上传列 |
| 10 | `vibe-ConeCrusherVibration` | 圆锥破碎机振动监测 | `vibration`(mm/s) / `oilPressure`(MPa) / `motorCurrent`(A) / `crushingForce`(kN) | **指标已按真实活文件重新拟定**（原方案针对的是死代码文件，字段对不上）：该页现状展示的是"偏心轴振动 8.42mm/s"、"润滑油压 12.5MPa"、"驱动电机 185.4A"、"破碎力 1245kN"等硬编码值，本次让这 4 个指标真正随上传数据变化；现有 `status`（normal/warning）判断逻辑本身无 bug，改成基于真实 `vibration` 阈值判断；`<ThreeScene />` 同样需要新增 props 才能反映真实数据 |

**风险/取舍说明**：
- 每个场景的"次要/派生"字段（如健康分、剩余寿命、状态徽标）延续原页面已有的计算公式，只是把公式的输入源从"随机数"换成"真实上传数据"，不改变原有阈值和展示逻辑本身，符合贯穿约束第 3/4 条。
- 3 个页面（#1 溢洪道监测、#2 大坝微震、#10 圆锥破碎机）现有 3D 场景基本不接收动态 props，需要新增 props 才能让 3D 真正反映真实数据——这部分工作量略高于其余 7 个页面，但仍在"重塑"范围内，不额外新增依赖。
- 每个场景独立目录、独立 Excel，互不共用，符合决策 5 的隔离要求。

> 以上方案为草案，需你确认（可整体确认，或指出需要调整的具体场景/字段）后，才会开始写 4.0 的代码。

---

## 4. 贯穿所有 Step 的约束

1. 每个 step 实施前，先在本文件对应行补充"预期效果"，经你确认后才可修改代码。
2. 新增前端展示一律复用 `SciFiCard` / `SciFiWidgets` 视觉语言（深色底、青色描边、等宽大写标题），不引入新配色体系。
3. 新增内容通过"空白区域插入"或"可折叠面板"方式呈现，不遮挡原有 3D 视图和图表核心可视区；确需调整原布局时，只做等比缩放/挪位，不删除原展示内容（Phase 4 的 10 个"重大修改"页面除外，按用户指示可整体重做）。
4. 不修改现有页面的路由 id、既有接口返回字段结构；新增内容一律新增字段/新增接口，不做破坏性变更（Phase 4 试点页面除外）。
5. **注释规范**：每处新增/改动代码块顶部加一行简短注释，格式参考项目已有风格（如 `constants.tsx` 里的 `//2026.01.19, update`），写清"改了什么、日期"，例如：
   ```ts
   // 2026-07-09 新增：场景信息条挂载点，展示场景ID/更新时间/耗时面板/日志
   ```
   Phase 2 的模型库链接额外要求用 `MODEL_LIB_LINK[<场景id>]` 这个可搜索标记（见决策 3）。
6. 每个 step 完成后，在 [`修改日志.md`](./修改日志.md) 记录：时间、涉及文件、修改内容、影响面、验证方式；Phase 2 额外记录"可搜索定位关键字"；Phase 4 额外记录"重大修改"标记和原页面功能取舍说明。

---

## 5. 执行状态跟踪表

| 阶段 | Step | 状态 | 备注 |
|---|---|---|---|
| 1 | 1.1 场景注册表 | 已完成 | |
| 1 | 1.2 场景遥测上下文（mock/real 自动切换） | 已完成 | |
| 1 | 1.3a 场景工况日志模拟生成器 | 已完成 | 非代码日志，是设备工况模拟日志 |
| 1 | 1.3b ScenarioMetaBar 组件 | 已完成 | |
| 1 | 1.4 接入 App.tsx | 已完成 | 已用 Playwright 截图验证，详见修改日志 |
| 2 | 2.0 ModelLibraryLink 组件 + 注释规范 | 已完成 | 域名用 industrial-intelligent-cockpit 占位 |
| 2 | 2.1 predictive（核实后 151 个文件系统条目，148 个有效页面全部含 3D 模型，3 个确认死代码/重复文件已跳过；含 Phase 4 的 10 个试点页面，本次只做链接接入，试点页面的后端重塑仍按计划留到 Phase 4） | 已完成 | 144 个右上 + 4 个右下，已用 tsc 全量验证 + Playwright 抽样验证 |
| 2 | 2.2 vibration-monitoring（核实后：真正生效的是 `App.tsx` 里的 `VIBE_VIEWS` 懒加载表，共 66 个有效页面，其中 51 个来自无空格 `vibration-monitoring/` 目录、15 个来自带空格 `Vibration monitoring/` 目录混用；两个目录下另有 28+11=39 个同名/相近名的重复文件夹未被引用确认死代码） | 已完成 | 64 个右上 + 2 个右下，已用 tsc 全量验证 + Playwright 抽样验证 |
| 2 | 2.3 computer-visual-inspection（核实后 67 个有效，7 个无路由引用确认死代码已跳过） | 已完成 | 已用 Playwright 抽样验证 |
| 2 | 2.4 life-warning（核实后 63 个有效，5 个无侧边栏入口确认死代码已跳过） | 已完成 | 63 个页面结构高度一致，已用 Playwright 抽样验证 |
| 2 | 2.5 Maintenance-plan-management（核实后 66 个全部有效，无死代码） | 已完成 | 38 个 Group A（右上）+ 28 个 Group B（右下，含 2 个特殊布局页面），已用 Playwright DOM 查询验证 3 个代表性页面 |
| 2 | 2.6 Maintenance-Training（核实后 64 个有效，`PortConveyorCouplingMaint` 无侧边栏入口/`ChillerRefrigerantRecoverySim` 为占位页无ThreeScene，均已跳过） | 已完成 | 56 个右上 + 8 个右下，已用 Playwright DOM 查询验证 4 个代表性页面 |
| 2 | 2.7 Equipment-Point-Inspection（核实后 50 个有效，`MineGasView`/`BuoyInspection`/`DamGallery` 无路由引用确认死代码已跳过） | 已完成 | 35 个右上 + 13 个右下 + 2 个特殊布局（顶部 flex 第三子元素：DamShoulder/HazardousChemicalWarehouse），已用 tsc 全量验证 + Playwright 抽样验证 |
| 2 | 2.8 maintenance（核实后 96 个有效页面，其中 68 个含 3D 模型接入模型库链接，28 个纯数据/表格页无 3D 模型不适用；另 3 个确认死代码已跳过） | 已完成 | 65 个右上 + 3 个右下（真实内容冲突），已用 tsc 全量验证 + Playwright 抽样验证 |
| 2 | 2.9 ServiceDataManagement（核实后 38 个有效，`ConditionCollaboration` 不适用/`HydroCascade` 确认死代码已跳过） | 已完成 | 已用 Playwright 抽样验证 4 种布局情形 |
| 2 | 2.10 knowledge-manage（核实后 31 个有效，`flood-emergency`/`Flood2EmergencyView` 确认为死代码已跳过） | 已完成 | 已用 Playwright 抽样验证 4 种布局情形 |
| 2 | 2.11 cockpit（`components/cockpit/` 下 9 个部件子页面确认死代码；但 `views/cockpit/` 顶层实际还有一整套 `cp-*` 驾驶舱页面此前未被发现，已在 2.13 中一并核实接入） | 已完成 | unit1-predictive 已接入；其余 16 个 cp- 页面详见 2.13 |
| 2 | 2.12 simulation（试点 4 页，其余 48 页并入 2.13 一并完成） | 已完成 | 试点批次，已用 Playwright 验证 |
| 2 | 2.13 长尾分类合并（原估约 25 页，全项目扫描后核实为 181 页，覆盖 9 个此前完全未被任何步骤覆盖的分类） | 已完成 | 见下方"2.13 扩展范围明细"，已用 tsc 全量验证 + Playwright 抽样验证 7 个代表性页面 |
| 3 | 3.0 安装 docx 依赖 | 已完成 | |
| 3 | 3.1 导出按钮（Word/Excel/JSON/MD 四格式） | 已完成 | 已用 Playwright 验证四种格式均可正常下载 |
| 4 | 4.0 后端通用化 | 已完成 | server.ts 新增 SCENARIO_CONFIGS + 参数化路由，unit1-predictive 原路由未改动；已验证回归 |
| 4 | 4.1 溢洪道监测重塑 | 已完成 | 已验证（tsc + Playwright 上传/清空全流程） |
| 4 | 4.2 大坝廊道微震监测重塑 | 已完成 | 已验证，修复了原有 status 阈值重叠 bug，3D 新增 props |
| 4 | 4.3 进水口拦污栅寿命预警重塑 | 已完成 | 已验证 |
| 4 | 4.4 调压室巡检维护计划重塑 | 已完成 | 已验证 |
| 4 | 4.5 压力钢管焊缝疲劳预测重塑 | 已完成 | 已验证，新增 Miner 累积损伤派生公式 |
| 4 | 4.6 船舶智能运维重塑 | 已完成 | 已验证 |
| 4 | 4.7 系泊张力监测重塑 | 已完成 | 已验证 |
| 4 | 4.8 矿山提升机智能运维重塑 | 已完成 | 已验证 |
| 4 | 4.9 电铲钢丝绳寿命预警重塑 | 已完成 | 已验证 |
| 4 | 4.10 圆锥破碎机振动监测重塑 | 已完成 | 已验证，3D 新增 props；**Phase 4 全部 10 个试点页面完成，场景库测试方案（Phase 1~4）全部完成** |

---

## 5.1 Step 2.13 扩展范围明细（2026-07-13 新增）

2.13 原计划仅按 `spare_parts_*`、`ship_*` 等命名规律估算约 25 个小分类页面。实际执行时先处理了 `spare-parts`（备品备件服务）和 `remote-expert`（远程专家服务）两个分类，随后追加了一次**全项目级别的 `views/**/*.tsx` ThreeScene 使用扫描**（不再依赖开发计划里罗列的分类名单，而是直接对文件系统做穷举），额外发现 7 个此前从未出现在 2.1~2.12 任何一步、也未在开发计划中被列出的完整分类。合计涉及 9 个分类、181 个页面：

| 子分类 | 说明 | 页面数 |
|---|---|---|
| spare-parts 备品备件服务 | 50 个文件系统条目，26 个含 3D 模型（其余 24 个为纯数据/表格页不适用） | 26 |
| remote-expert 远程专家服务 | 34 个文件系统条目全部有效，20 个含 3D 模型，其中 `ProcessOptimizationView` 导入了 ThreeScene 但从未在 JSX 中渲染，确认跳过 | 19 |
| digital-delivery 数字化交付（**新发现，原计划未列出**） | 29 个文件系统条目，`MineGeoDeliveryView`/`MineTransportDeliveryView` 未被引用确认死代码，其余 27 个全部含 3D 模型 | 27 |
| index-analysis 运行指数分析（**新发现，原计划未列出**） | 21 个文件系统条目全部有效且全部含 3D 模型 | 21 |
| knowledge-base 工业产品知识库（**新发现，原计划未列出**） | 15 个文件系统条目全部有效，仅 4 个含 3D 模型 | 4 |
| cockpit 运行驾驶舱 cp-* 系列（**新发现**：`components/cockpit/` 下 9 个部件子页面此前确认死代码，但 `views/cockpit/` 顶层还有一整套通过 `cockpitMap` 路由的驾驶舱页面，此前完全未被发现） | 17 个 `cockpitMap` 条目，`MiningDispatchView` 无 3D 模型不适用，其余 16 个含 3D 模型 | 16 |
| eq-* 工业智能运维（Smart Ops，**新发现，原计划未列出**） | `App.tsx` 内 `activeTabId.startsWith('eq-')` 路由的 19 个平铺在 `views/` 根目录的设备场景页（`eq-0`~`eq-18`），`eq-18`（Unit1PredictiveView）已在 2.11 处理，其余 18 个含 3D 模型 | 18 |
| simulation 仿真分析剩余页面 | 2.12 试点完成 4 页后，全项目扫描发现该分类实际共 52 个 `sim-*` 路由页面，本次补齐剩余 48 页 | 48 |
| knowledge-manage 运维知识管理遗漏页 | `UndergroundVehiclePathKbView`（`km-vehicle-path`）此前遗漏；`FloodEmergencyView` 确认死代码、`KnowledgeManageView` 为 `km-` 通配符兜底的通用组件（非独立场景页）均跳过 | 1 |
| ServiceDataManagement 遗漏页 | `views/MiningServiceDataView.tsx`（`sm-1`）物理位置在 `views/ServiceDataManagement/` 目录之外，2.9 处理时按目录扫描遗漏，本次补齐 | 1 |
| **合计** | | **181** |

**方法论说明**：从 2.13 开始，不再仅依赖开发计划里预先列出的分类名单，而是对整个 `views/` 目录做递归扫描、按"文件内容是否包含 `ThreeScene`"识别候选 3D 页面，再逐个核实是否被 `App.tsx` 实际路由（导入且在 JSX 中渲染），避免遗漏开发计划编制时未曾发现的分类。全部改动均通过 Node 脚本批量核实（角落遮挡判断、死代码判断）+ 批量应用（自动匹配各文件已有的相对路径深度），减少人工逐文件操作的不一致风险；批量应用前后均用 `tsc --noEmit` 全量核对错误数不变，再用 Playwright 抽样验证代表性页面。
