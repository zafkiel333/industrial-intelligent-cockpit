# 场景库测试方案 —— 修改日志

> 每完成一个 [`开发计划.md`](./开发计划.md) 中的 step，就在下方追加一条记录。倒序排列（最新的在最上面）。

## 记录格式

```
### [YYYY-MM-DD HH:mm] Step 编号 - 步骤名称
- 状态: 完成 / 部分完成 / 回退
- 涉及文件: 
- 修改内容: 
- 影响面（是否触碰现有功能/接口/样式）: 
- 验证方式（如何确认未破坏原功能）: 
- 备注:
```

---

### [2026-07-14 17:30] 补充 - Phase 4 十个试点页面差异化整改（真实趋势图 + 数据驱动结论 + 现场报告导出）
- 状态: 完成
- 涉及文件:
  - 新增 `src/scenarioLib/scenarioFieldReport.ts`（10 个试点页面共用的 Markdown 现场报告导出工具）
  - 10 个试点页面全部改动：`views/computer-visual-inspection/SpillwayMonitoring/index.tsx`、`views/vibration-monitoring/DamGalleryMicroseism/index.tsx`、`views/life-warning/intake-trash-rack-life/View.tsx`、`views/Maintenance-plan-management/SurgeChamberInspection/SurgeChamberInspectionView.tsx`、`views/predictive/hydro/PenstockWeldFatigueView.tsx`、`views/ShipView.tsx`、`views/computer-visual-inspection/MooringTension/index.tsx`、`views/MineHoistView.tsx`、`views/life-warning/mining-shovel-rope-life/View.tsx`、`views/vibration-monitoring/ConeCrusherVibration/index.tsx`
- 修改内容:
  - 用户反馈 Phase 4 十个试点页面"同质化严重、缺少数据直观展示、报告导出按钮点击无效"。核实后确认三处系统性问题：① 所有页面此前只把 `unifiedData` 最新一行取出显示几个数字，图表组件仍渲染与上传数据无关的静态 mock 数组；② 多个"生成/导出报告"按钮没有绑定 `onClick`；③ 部分"智能决策建议"文案是与实际数值无关的固定字符串。
  - 按每个场景的物理特性做差异化真实图表（不再是同一套模板复制 10 份）：溢洪道监测用流量/水位双轴趋势；大坝微震用事件能量时间线（柱状，替换原随机散点）；拦污栅寿命预警新增健康度衰减曲线；调压室巡检新增温度/压力/振动三线趋势 + 真实阈值风险列表；焊缝疲劳预测用真实 Miner 法则累积损伤曲线（逐条真实数据滚动计算，替换原装饰性指数公式数组）；船舶运维新增转速/排烟温度趋势 + 燃油效率（转速-油耗）散点图；系泊张力监测新增 4 线极坐标张力雷达图；提升机运维用深度-时间真实运行轨迹（替换原固定 S 型曲线）；电铲钢丝绳预警新增张力分布直方图；圆锥破碎机振动监测改为振动/油压真实双轴趋势。
  - 所有页面的"生成/导出报告"按钮接入共用的 `downloadScenarioReport()`，点击后基于当前真实指标 + 数据驱动结论生成 Markdown 文件并触发浏览器下载；同时把多处与数值无关的固定"建议/结论"文案改为按真实阈值判断动态生成。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增/替换图表数据源与结论文案逻辑、新增报告导出功能，未改动后端接口与既有阈值判断的业务规则本身。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（基线），0 条新增错误。
  - Playwright 对 10 个页面逐一验证：图表 SVG 正常渲染、点击报告按钮均触发真实文件下载（文件名各不相同、内容基于当前数据生成）、0 条控制台报错。
  - 过程中通过 Playwright 实测发现并修复 2 处真实 bug（均为 tsc 未能捕获的运行时错误）：① `MineHoistView.tsx`、`ConeCrusherVibration/index.tsx` 在早前编辑中意外删除了 `const SCENARIO_ID = '...'` 声明，导致页面运行时报 `SCENARIO_ID is not defined` 崩溃；② `PenstockWeldFatigueView.tsx` 无真实上传数据时的占位演示曲线数值范围超出预期的 0-1 损伤指数量级（recharts 的 `domain` 在数据超出时会自动外扩而非裁剪），已改为正确归一化，使占位曲线也保持在 0-1 范围内。
- 备注: 本次是响应用户直接反馈的整改，非新计划项；后续如再新增试点页面，应从一开始就使用真实历史数据驱动图表 + 数据驱动结论文案，避免重复"只更新数字、图表仍是静态 mock"的问题。

---

### [2026-07-14 09:20] 补充 - Phase 4 使用说明文档 + 清理遗留空目录
- 状态: 完成
- 涉及文件: 新增 `场景库测试方案/Phase4-真实数据接入使用说明.md`；删除 6 个遗留空目录（`src/data/cv-MooringTension`、`src/data/cv-SpillwayMonitoring`、`src/data/lw-intake-trash-rack-life`、`src/data/lw-mining-shovel-rope-life`、`src/data/vibe-DamGalleryMicroseismic`、`src/data/vibe-ConicalCrusher`，均为 Phase 4 施工过程中场景 id 命名修正后遗留的空壳目录，未被 `server.ts` 的 `SCENARIO_CONFIGS` 引用）
- 修改内容: 编写完整的 Phase 4 使用说明文档，涵盖：11 个页面（`unit1-predictive` + 10 个试点页面）的通用上传/查看/清空流程、`端到端耗时` 统一标注为"模拟"（不区分真实/模拟标签，仅数值不同）、各场景数据落盘的磁盘目录、Excel 文件格式的硬性要求（表头行必须是字面文本 `_1`/`_2`/`_3`...，已用 node 脚本实测 `xlsx.utils.sheet_to_json` 的实际行为确认，非猜测）、10 个场景各自的数据字段与预测/派生公式（含 `pm-hydro-36` 的简化 Miner 累积损伤公式、`mining-shovel-rope-life` 的阈值穿越计数法等具体推导过程）、通用后端接口列表、已知局限（`historyDividerIndex` 占位算法、真实计时未实现等）
- 影响面（是否触碰现有功能/接口/样式）: 纯文档新增，无代码改动；清理的 6 个目录经核实为空且未被任何代码引用，删除不影响任何功能
- 验证方式（如何确认未破坏原功能）: 文档中的技术细节（Excel 解析行为、各场景公式、目录路径）均直接对照 `server.ts`/各页面源码及本地 node 脚本实测结果编写，未凭猜测；删除目录前用 `find`/`grep` 确认目录为空且无代码引用
- 备注: 端到端耗时标签统一为"模拟"的调整，是本次按用户要求追加的展示层面修改，不涉及计时逻辑本身（计时逻辑的局限性已在文档第 7 节注明）

---

### [2026-07-14 02:55] Step 4.10 - 圆锥破碎机振动监测重塑：接入真实数据（重大修改，Phase 4 全部完成）
- 状态: 完成
- 涉及文件: `views/vibration-monitoring/ConeCrusherVibration/index.tsx`、`components/vibration-monitoring/ConeCrusherVibration/ThreeScene.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('vibe-ConeCrusherVibration')` + `ScenarioDataUploadModal`，接入 4 个真实指标：`vibration`(mm/s)、`oilPressure`(MPa)、`motorCurrent`(A)、`crushingForce`(kN)，替换原来"偏心轴振动 8.42"/"润滑油压 12.5"/"驱动电机 185.4A"/"破碎力 1245kN"等硬编码展示值；"电机功率(kW)"一项改为"驱动电机电流(A)"以对齐真实指标口径。
  - `status`（normal/warning）原逻辑本身无重叠 bug，改成基于真实 `vibration>10` 阈值判断；`load`（运行负荷%）为操作状态量保持原有模拟。
  - `ThreeScene.tsx` 原本不接收任何 props，新增 `vibration`/`status` 两个 prop：`vibration` 归一化后驱动偏心振动幅度（`vibScale`，缩放机头摆动/机身振动动画），`status` 驱动机头材质告警变色（normal 橙色/warning 红色），真正让 3D 场景反映真实数据。
  - 页头新增"数据入库"/"一键清空"按钮；关键点温度、维护记录建议等其余静态展示元素保持不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id 不变；仅核心 4 指标数据源、status 判断逻辑、3D props 有变化。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传 vibration=12.5（>10 阈值）后页面正确显示 12.50/13.0/190.0/1,350，且正确触发 SYSTEM WARNING 状态，控制台仅 1 条与外部 CDN 相关的既有环境限制报错，与本次改动无关。
- 备注: 本 step 标记为"重大修改"（Phase 4.10）。**至此，场景库测试方案 Phase 4 全部 10 个试点页面（4.1~4.10）均已完成真实后端数据流转重塑并逐一验证，整个场景库测试方案（Phase 1~4）全部完成。**

---

### [2026-07-14 02:25] Step 4.9 - 电铲钢丝绳寿命预警重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/life-warning/mining-shovel-rope-life/View.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('mining-shovel-rope-life')` + `ScenarioDataUploadModal`，接入 2 个真实指标：`tension`(kN)、`abrasion`(%)，替换原来的 `setInterval` + `Math.random()` 模拟。
  - `bendingCycles`/`operatingHours`（原为"随时间快进累加"）改为按上传数据序号派生（`150000+序号×150`、`1200+序号`）；`brokenWires`（原为概率触发）改为基于真实 `tension` 历史的简单阈值穿越计数（每出现一次 `tension>1800kN` 的读数，累计断丝数 +1，基础值 2 根），健康度/剩余寿命公式及 >1800kN/>2000kN 阈值判断逻辑保持不变。
  - 页头新增"数据入库"/"一键清空"按钮；原有"穿新绳作业"操作按钮及 `ParameterControl` 手动滑块保留不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、阈值判断、诊断建议文案均未改动；仅核心指标数据源及断丝数派生方式改为真实驱动。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传 2 条 tension>1800 的读数后页面正确显示 tension=1850、abrasion=25.0、brokenWires 正确派生为 4（基础 2 + 2 次穿越），控制台仅有 2 条与 Google Fonts/Tailwind CDN 相关的既有环境限制报错，与本次改动无关。
- 备注: 本 step 标记为"重大修改"（Phase 4.9）。

---

### [2026-07-14 02:00] Step 4.8 - 矿山提升机智能运维重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/MineHoistView.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('eq-12')` + `ScenarioDataUploadModal`，接入 8 个真实指标：`depth`(m)、`velocity`(m/s)、`payload`(t)、`brakePressure`(MPa)、`ropeTensionR1`~`R4`(kN)。仿照 unit1-predictive"1 个主指标 + 16 个测点"的结构，本页为"4 个主指标 + 4 根钢丝绳张力"。
  - `drumSpeed`（=velocity×4.5）、`direction`（由 velocity 符号派生：|v|<0.5→STOP，v>0→DOWN，v<0→UP）沿用原有派生逻辑，只是输入源从模拟改为真实值；`mode`（AUTO/MAN/INSPECT）为操作状态量，原逻辑本就从未被 interval 更新，保持不变。
  - 页头新增"数据入库"/"一键清空"按钮；"提升速度曲线"(S-Curve)图表、液压制动站/主电机状态面板中的次要硬编码数值（Armature Current、Stator Temp、Brake Shoe Gap 等）保持不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、3D 场景（`type="mine-hoist"`通用组件）均未改动；仅核心 8 个指标数据源改为真实拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传 velocity=-6 后页面正确显示 depth=320.0、方向判断正确翻转为 UP、drumSpeed 正确派生为 -27，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.8）。

---

### [2026-07-14 01:30] Step 4.7 - 系泊张力监测重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/computer-visual-inspection/MooringTension/index.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('cv-mooring-tension')` + `ScenarioDataUploadModal`，接入 4 根缆绳张力真实指标：`tensionL1`~`tensionL4`(kN)，替换原来 `setInterval` 里对 4 根缆绳统一用 `100+Math.random()*200` 生成张力的模拟逻辑。
  - 沿用原有 >250kN critical / >180kN warning 阈值判断逻辑（抽成 `lineStatus` 函数复用）；`shipMovement.x/z`（船体纵向/横向摇摆位移）保持原有 `Math.random()` 模拟不变。
  - 页头新增"数据入库"/"一键清空"按钮；"平均张力"/"张力不平衡度"/"缆绳疲劳指数"/"累计冲击"/"安全系数"/"张力事件流"等次要展示元素保持不变（均为硬编码静态值，不在本次真实数据范围内）。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、阈值判断逻辑均未改动；仅 4 根缆绳张力数据源改为真实拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传 tensionL3=270（>250 阈值）后页面正确显示 270 且正确触发"极限张力告警"状态，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.7）。

---

### [2026-07-14 01:05] Step 4.6 - 船舶智能运维重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/ShipView.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('eq-7')` + `ScenarioDataUploadModal`，接入 4 个真实指标：`speed`(kn)、`rpm`(RPM)、`fuelConsumption`(kg/h)、`exhaustTemp`(°C)。
  - 姿态（pitch/roll/ballast/trim）、环境（风速/浪高/风向）、`heading`/`depth`/`load`（航向/水深/主机负荷）等次要指标按计划保持原有 `Math.sin`/`Math.random` 模拟，不纳入本次真实数据范围（避免单页指标过多超出试点范畴）。
  - 新增 `latestRow` 同步 `useEffect`：数据上传/清空后自动把最新一行的 4 个真实指标写入 `navData.speed`/`engineData.rpm`/`fuelConsumption`/`exhaustTemp`，与模拟中的其余字段（通过 `...prev` 展开）互不覆盖。
  - 页头新增"数据入库"/"一键清空"按钮；`engineTrend`（RPM&负荷趋势图）、货舱温度、压载水位等展示元素保持不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、3D 场景（`type="ship"`通用组件）均未改动；仅 4 个核心指标数据源改为真实拉取，其余模拟逻辑原样保留。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传后页面正确显示 speed=21.5、rpm=96.5、fuelConsumption=136(135.5取整)，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.6）。

---

### [2026-07-14 00:35] Step 4.5 - 压力钢管焊缝疲劳与裂纹预测重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/predictive/hydro/PenstockWeldFatigueView.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('pm-hydro-36')` + `ScenarioDataUploadModal`，接入 2 个真实指标：`pressure`(MPa)、`hoopStress`(MPa)，替换原来的 `setInterval` 遥测模拟。
  - `damageIndex`/`fatigueCycles`/`nextInspection`（原为从不更新的静态展示值）新增基于 `hoopStress` 历史的简化 Miner 累积损伤公式：`damageIndex = Σ(hoopStress/300)³ × 0.0005`（参考应力沿用既有 `stressFactor=hoopStress/300` 换算基准，S-N 指数取 3），随每次上传的数据量滚动更新；"剩余寿命 Hours Remaining"也据此改为 `20000×(1-damageIndex)`。
  - `CRACK_REGISTRY`（裂纹缺陷记录）、`ULTRASONIC_SCAN`（相控阵超声成像）、`PARIS_LAW_DATA`（裂纹扩展预测图）、`LOAD_CYCLE_STATS`（应力幅值循环统计）等静态数据集及 NDT 扫描交互逻辑保持不变。
  - 页头新增"数据入库"/"一键清空"按钮。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、NDT 扫描交互均未改动；仅 2 个核心指标及其派生的损伤/寿命计算数据源改为真实拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传后页面正确显示 hoopStress=160.0，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.5）。累积损伤公式的具体系数（0.0005、S-N 指数 3）为自拟演示数值，非工程真实取值，仅用于呈现"真实数据驱动派生指标滚动更新"的效果。

---

### [2026-07-14 00:05] Step 4.4 - 调压室巡检维护计划重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/Maintenance-plan-management/SurgeChamberInspection/SurgeChamberInspectionView.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('mpm-16')` + `ScenarioDataUploadModal`，接入 3 个真实指标：`temperature`(°C)、`pressure`(MPa)、`vibration`(mm/s)。
  - `status`(工单状态：正常/异常)、`progress`(检修进度%) 属于工单流程状态而非传感器读数，按计划保持原有 `setInterval` 随机模拟不变（拆分成独立的 `workOrderState`，与真实传感器数据解耦）。
  - 页头新增"数据入库"/"一键清空"按钮；`ParameterWidget`/`ChartWidget`/`RiskWidget`/`TimelineWidget` 等其余展示组件保持不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、工单模拟逻辑均未改动；仅 3 个核心指标数据源改为真实拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传后页面正确显示 58.5°C / 13.20MPa / 1.40mm/s，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.4）。

---

### [2026-07-13 23:35] Step 4.3 - 进水口拦污栅寿命预警重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/life-warning/intake-trash-rack-life/View.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('intake-trash-rack-life')` + `ScenarioDataUploadModal`，接入 4 个真实指标：`flowVelocity`(m/s)、`waterLevelDiff`(m)、`vibrationAmplitude`(mm)、`blockageRatio`(%)，替换原来的 `setInterval` + `Math.random()` 模拟。
  - `structuralStress` 沿用原公式（`20 + waterLevelDiff*100 + flowVelocity*10`），`corrosionLevel` 因原逻辑依赖"真实时钟缓慢增长"、现无持续计时概念，改为按上传数据序列进度派生（`0.1 + 序号占比*0.3`），`healthScore`/`estimatedLife` 沿用原有的应力/振动/腐蚀惩罚公式，全部阈值判断（1.5m/2.5m 水头损失、3.0mm 振动、150/180MPa 应力、0.4 堵塞率等）保持不变。
  - 页头新增"数据入库"/"一键清空"按钮；原有"执行清污"/"更换栅体"操作按钮及 `ParameterControl` 手动滑块保留不变（滑块可在两次真实数据刷新之间临时预览调整，不影响真实数据展示的正确性）。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id、阈值判断、诊断建议文案均未改动；仅核心 4 指标数据来源改为真实拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 验证 upload→data 字段正确；Playwright 验证上传后页面正确显示 waterLevelDiff=1.80、按原公式计算的 structuralStress=235.0、blockageRatio=45，0 条控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.3）。

---

### [2026-07-13 23:00] Step 4.2 - 大坝廊道微震监测重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/vibration-monitoring/DamGalleryMicroseism/index.tsx`、`components/vibration-monitoring/DamGalleryMicroseism/ThreeScene.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('vibe-DamGalleryMicroseism')` + `ScenarioDataUploadModal`，接入 5 个真实指标：`eventEnergy`(ML 震级)、`stabilityIndex`(0-1)、`waterLevel`(m)、`crackWidth`(mm)、`seepageFlow`(L/min)，替换原来"最大事件能量 ML1.2"、"结构稳定性 0.98"、"当前库水位 175.4m"、"裂缝开展指数 0.02mm"、"渗流量 12L/min"、"24h事件统计 124"等硬编码展示值。
  - 修复原有 `status`（normal/warning/danger）判断的重叠 bug（`rand>0.95 warning` 判断在 `rand>0.99 danger` 之前，导致 danger 分支永远进不去），改成基于真实 `eventEnergy`/`stabilityIndex` 的正确阈值判断（danger: energy>2.0 或 stability<0.85；warning: energy>1.0 或 stability<0.95）。
  - `ThreeScene.tsx` 原本不接收任何 props，新增 `status`/`intensity` 两个 prop：`intensity`（由 `eventEnergy` 归一化）驱动微震光球出现频率，`status` 驱动光球颜色（danger/warning 变色），真正让 3D 场景反映真实数据。
  - `mockEventData`（事件空间分布散点图）、"库水位关联度 75%"等次要展示元素按约束保持不变。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、路由 id 不变；仅核心指标数据源、status 判断逻辑、3D props 有变化。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - curl 直接验证 upload→data 返回字段名、数值正确；Playwright 验证页面上传后正确显示 ML 2.4 / 0.80 / 181.0 / 0.08 / 18，且因 eventEnergy=2.4>2.0 正确触发 DANGER 状态（验证了修复后的阈值判断逻辑），0 条与本次改动相关的控制台错误。
- 备注: 本 step 标记为"重大修改"（Phase 4.2）。

---

### [2026-07-13 22:20] Step 4.1 - 溢洪道监测重塑：接入真实数据（重大修改）
- 状态: 完成
- 涉及文件: `views/computer-visual-inspection/SpillwayMonitoring/index.tsx`
- 修改内容:
  - 引入 `useScenarioRealData('cv-spillway-monitoring')` + `ScenarioDataUploadModal`，替换原来写死的 `useState<SpillwayState>({flowRate: 1245.5, waterLevel: 15.2, vibrationLevel: 2.4})` 静态值（该页此前甚至没有 `setInterval`/`Math.random()`，是全站唯一的"纯静态无模拟"页面）。
  - 取最新一行真实数据的 `flowRate`/`waterLevel`/`vibrationLevel` 填入原 `state` 对象，无数据时保留原静态值兜底展示。
  - 3D 场景 `flowIntensity`（原写死 0.8）改为按 `flowRate / 1500` 归一化后驱动，随真实流量变化。
  - 页头新增"数据入库"/"一键清空"按钮（样式对齐 unit1-predictive）。
  - `MOCK_ZONES`（异常区域清单）、`RADAR_DATA`（冲刷深度多维分析）、`FLOW_HISTORY`（泄洪流量历史趋势图）等次要展示元素按约束保持不变，未接入本次真实数据范围。
- 影响面（是否触碰现有功能/接口/样式）: 页面结构、其余展示元素、路由 id 均未改动；仅将 3 个核心指标的数据来源从写死常量换成真实后端拉取。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - Playwright 完整跑通：上传测试 Excel（flowRate=1010.5, waterLevel=16.8, vibrationLevel=3.5）后刷新页面，页面正确显示 1010.5 和 3.5（waterLevel 本身在原设计里就不渲染在 UI 上，与本次改动无关）；点击"一键清空"后正确恢复默认值 1245.5。
  - 排查出的唯一控制台错误（`fonts.googleapis.com` 的 `ERR_CONNECTION_CLOSED`）经核实是沙盒环境无法访问 Google Fonts CDN 导致，与本次代码改动无关，其余页面同样会有此现象。
- 备注: 本 step 标记为"重大修改"（Phase 4.1）。

---

### [2026-07-13 21:00] Step 4.0 - 后端通用化：多场景数据接口 + 共享上传组件（重大修改）
- 状态: 完成
- 涉及文件: `server.ts`（新增代码，未删改 unit1-predictive 专用路由）、新增 `src/scenarioLib/useScenarioRealData.ts`、新增 `src/scenarioLib/ScenarioDataUploadModal.tsx`
- 修改内容:
  - **现状核实**：`server.ts` 原有 `/api/upload`、`/api/data`、`/api/upload/clear` 三个路由 100% 硬编码给 `unit1-predictive` 专用（`DIRS` 目录表、`type`（power/temperature）判断、`activePower`/`pads[16]` 响应字段均写死）。此前遗留的 `DIRS.powerExample`/`DIRS.tempExample` 两个空目录确认是早期原型死代码（从未被读写），本次一并清理。
  - **新增通用路由**（与原有 unit1 路由并存，不冲突、不改动）：`POST /api/scenarios/:scenarioId/upload`、`GET /api/scenarios/:scenarioId/data`、`DELETE /api/scenarios/:scenarioId/upload/clear`。
  - 新增 `SCENARIO_CONFIGS` 配置表（10 个试点场景各自登记独立数据目录 `src/data/<scenarioId>/uploads/` + 指标列表），替换原来的硬编码 `DIRS`/`type` 分支。
  - 简化上传模型：每个场景所有指标在同一时间轴、同一张 Excel 多列里（不再像 unit1 那样按 power/temperature 拆两类文件），解析约定沿用 unit1 的"第 1 列 `_1` 固定为时间，其余列 `_2、_3...` 按场景声明的指标顺序取值"，未引入新的表头解析逻辑。
  - 响应结构统一为 `{ unifiedData: [{time, ...指标名:数值}], historyDividerIndex, isEmpty? }`；`historyDividerIndex` 沿用 unit1 现有的"75% 位置"占位算法（本身就是占位，非真实预测边界，本次未改动这部分逻辑）。
  - 新增前端复用件：`useScenarioRealData(scenarioId)` hook（封装 fetch/清空逻辑）+ `ScenarioDataUploadModal` 组件（从 `Unit1PredictiveView.tsx` 原内嵌上传弹窗抽出，去掉了 unit1 特有的"归类类型"选择器，改成单文件多列上传）。
- 影响面（是否触碰现有功能/接口/样式）: 新增路由/文件为主，`unit1-predictive` 原有路由、字段结构、上传弹窗均未改动一行；`DIRS` 里移除的两个空目录未被任何代码读写，移除不影响功能。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit`：72/0 新增错误基线保持不变。
  - 手工构造测试 Excel（`eq-7` 场景，4 列指标），完整跑通 `POST upload → GET data → DELETE clear` 全流程，返回字段名、排序、清空后归零均符合预期。
  - 重启 dev server 后用 Playwright 回归验证 `unit1-predictive`（1号机组异常预测分析）页面：真实功率数据正常渲染、0 条控制台报错，确认通用化改造未影响原有页面。
- 备注: 本 step 标记为"重大修改"（Phase 4）。10 个试点场景的指标方案已在 `开发计划.md` "4.0 详细方案"小节列出并经用户确认；4.1~4.10 每个页面的具体重塑将逐个记录到本文件。

**2026-07-13 22:20 补充**：Phase 4.1（溢洪道监测）已实施并验证完成，详见下方独立条目。

**2026-07-13 21:40 补充修正**：正式接入 `SCENARIO_CONFIGS` 时逐个核对 `App.tsx` 实际路由，发现决策 5 表格里有 5 处出入，已修正：3 处是 id 拼写/大小写错误（`cv-SpillwayMonitoring`→`cv-spillway-monitoring`、`cv-MooringTension`→`cv-mooring-tension`、`lw-`前缀多余的两个 life-warning 场景 id），文件本身不变；另外 2 处（#2 大坝廊道微震监测、#10 圆锥破碎机振动监测）更严重——决策 5 表格里写的文件其实是 Step 2.2 已确认的**死代码重复文件**（`views/Vibration monitoring/DamGalleryMicroseismic`、`views/Vibration monitoring/ConicalCrusher`，均未被任何路由引用），真正活的是 `views/vibration-monitoring/DamGalleryMicroseism`、`views/vibration-monitoring/ConeCrusherVibration`（无空格目录，文件名也不同）。已改用真实活文件，并按活文件的实际展示内容重新拟定了指标方案（详见 `开发计划.md` 决策 5 表格及"4.0 详细方案"表格的更新）。`server.ts` 的 `SCENARIO_CONFIGS` 已同步更正为正确的 10 个 id。

---

### [2026-07-13 20:10] 决策 2 修订 - 端到端耗时面板不再区分"模拟/真实"展示样式
- 状态: 完成
- 涉及文件: `src/scenarioLib/ScenarioMetaBar.tsx`
- 修改内容: 用户确认 Phase 4 启动方案时一并要求：所有场景页面的"端到端耗时"标签统一显示为"页面渲染耗时(模拟)"（原有灰色样式），不再对接了真实后端的页面显示绿色"真实响应耗时"样式/文案。删除了原来基于 `snapshot?.isReal` 的三元条件渲染（原第 67-75 行），改成固定的单一样式/文案。数值本身仍然是各自口径下真实测得的耗时（不受影响），只是 UI 不再暴露"这个页面是否接了真实后端"这一差异化信号。底层 `ScenarioTelemetryContext` 的 `isReal` 字段与 `reportRealTiming` 上报机制本身未删除，仍保留供将来内部诊断使用，只是 `ScenarioMetaBar` 不再读取 `isReal` 来决定样式。
- 影响面（是否触碰现有功能/接口/样式）: 仅影响 UI 展示，不影响耗时数值计算逻辑、不影响 `ScenarioTelemetryContext` 的数据结构或 API。
- 验证方式（如何确认未破坏原功能）: `npx tsc --noEmit` 保持 72/0 新增错误基线。
- 备注: 已同步更新 `开发计划.md` 决策 2 小节，补充"2026-07-13 更新"说明本次修订的背景和范围。

---

### [2026-07-13 19:30] Step 2.13 - 长尾分类合并接入模型库链接（181 页，本项目最终收尾批次，覆盖 9 个新发现分类）
- 状态: 完成
- 涉及文件: `views/spare-parts/*.tsx`（26）、`views/remote-expert/*.tsx`（19）、`views/digital-delivery/*.tsx`（27）、`views/index-analysis/*.tsx`（21）、`views/knowledge-base/*.tsx`（4）、`views/cockpit/*.tsx` 顶层驾驶舱页（16）、`views/{EquipmentView,GeneratorView,...}.tsx` 等 18 个平铺在 views 根目录的 eq-* 页面、`views/simulation/*.tsx` 剩余（48）、`views/knowledge-manage/UndergroundVehiclePathKbView.tsx`（1）、`views/MiningServiceDataView.tsx`（1）
- 修改内容:
  - Step 2.13 原计划仅是"合并处理约 25 个长尾小分类"。实际先完成了 `spare-parts`（备品备件服务，50 个文件系统条目中 26 个含 3D 模型）与 `remote-expert`（远程专家服务，34 个文件系统条目中 20 个含 3D 模型，其中 `ProcessOptimizationView` 导入了 `ThreeScene` 但从未在 JSX 渲染，确认跳过，实接 19 页）。
  - 之后改用**全项目级别扫描**（递归遍历 `views/**/*.tsx`，按文件是否包含 `ThreeScene` 识别候选 3D 页面，再逐一核实是否被 `App.tsx` 实际路由），而不再依赖开发计划里预先列出的分类名单，从而发现了 7 个此前 2.1~2.12 全程未覆盖、开发计划里也从未列出过的完整分类/遗漏页面：
    - `digital-delivery`（数字化交付）：29 个文件系统条目，2 个（`MineGeoDeliveryView`/`MineTransportDeliveryView`）未被引用确认死代码，其余 27 个全部含 3D 模型，全部接入。
    - `index-analysis`（运行指数分析）：21 个文件系统条目全部有效且全部含 3D 模型，全部接入。
    - `knowledge-base`（工业产品知识库）：15 个文件系统条目全部有效，仅 4 个含 3D 模型，其余 11 个纯数据页不适用。
    - `views/cockpit/` 顶层驾驶舱页（`cp-*` 系列，通过 `App.tsx` 里的 `cockpitMap` 路由）：此前 Step 2.11 只发现了 `components/cockpit/` 下 9 个部件子页面为死代码、`unit1-predictive` 为唯一活页面的结论并不完整——`views/cockpit/` 目录下实际还有 17 个通过 `cockpitMap` 独立路由的驾驶舱页面（`MiningDispatchView` 无 3D 模型不适用，其余 16 个接入）。
    - `eq-*` 工业智能运维（Smart Ops）：`App.tsx` 中 `activeTabId.startsWith('eq-')` 路由的 19 个页面，物理位置是平铺在 `views/` 根目录的独立文件（`EquipmentView.tsx`、`GeneratorView.tsx`、`CraneView.tsx` 等），`eq-18`（Unit1PredictiveView）已在 2.11 处理过，其余 18 个全部含 3D 模型，全部接入。
    - `simulation`（仿真分析）剩余页面：2.12 只做了 4 页试点，全项目扫描发现该分类实际共 52 个 `sim-*` 路由页面，本次补齐剩余 48 页。
    - `knowledge-manage` 遗漏页：`UndergroundVehiclePathKbView`（`km-vehicle-path`）此前遗漏，本次补齐；同时确认 `KnowledgeManageView` 是 `km-` 通配符的通用兜底组件（非独立场景页，类比 `GenericView`），不适用该功能，未做修改。
    - `ServiceDataManagement` 遗漏页：`views/MiningServiceDataView.tsx`（`sm-1`）物理位置在 `views/ServiceDataManagement/` 目录之外（直接放在 `views/` 根目录），Step 2.9 按目录扫描时遗漏，本次补齐。
  - 181 个页面每个新增独立 `ModelLibraryLink` 引入 + `MODEL_LIB_URL` 常量（`MODEL_LIB_LINK[<id>]` 搜索注释），相对路径深度按各文件实际嵌套层级自动匹配。
  - 角落占用判断：绝大多数（约 179 个）右上角空闲用默认 `absolute top-4 right-4 z-20`；仅 2 个页面（`digital-delivery/HydroBimDeliveryView`/dd-hydro-bim、`UndergroundVehiclePathKbView`/km-vehicle-path 分别因右上角有真实工具栏/左上角有信息卡）改用其他角落。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增 UI 元素，未改动任何现有状态逻辑、路由或样式类；所有确认死代码/通用兜底组件均未触碰。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（基线），0 条新增错误。
  - 全项目 `grep -rl "ModelLibraryLink" views/` 统计从本批次开始前的 665 个增长到 846 个，与 181 的批次总量一致。
  - Playwright 抽样验证 7 个代表性页面，覆盖新发现的全部 4 个大分类（`cp-bulk-terminal`、`cp-maritime-safety`、`eq-0`、`eq-9`、`sim-mine-vent`、`sim-hydro-flood`、`sim-port-flow`）：均恰好渲染 1 个 `ModelLibraryLink`，href 正确。其中 `sim-hydro-flood` 页面触发了 1 条控制台警告（"Received `%s` for a non-boolean attribute"），经排查是该页面 `<Play size={24} ml-1/>` 的既有代码笔误（`ml-1` 应为 className 却被误写成裸属性），与本次改动无关，不在本次修复范围内。
- 备注: 本条目标志着场景库测试方案 Phase 2（模型库跳转链接接入）全部完成，全项目 846 个含 3D 模型的场景页面均已接入独立可替换的模型库占位链接。下一步进入 Phase 4（十个试点页面真实后端数据流转重塑）。

---

### [2026-07-13 17:50] Step 2.1 - 预测性维护分类接入模型库链接（148 页，本项目最大批次）
- 状态: 完成
- 涉及文件: `views/predictive/**/*.tsx`（共 151 个文件系统条目：52 个平铺在 `views/predictive/` 根目录、50 个在 `hydro/` 子目录、20 个在 `mining/` 子目录、其余分布在约 20 个"每个子目录仅 1 个文件"的小分类里，如 `hatch-cover/`、`winch-gearbox-brake/`、`crane-trolley/` 等）
- 修改内容:
  - 路由方式与此前分类不同：分三段处理——79 个 `pm-pmOther-N`（if 链）、50 个 `pm-hydro-N`（switch）、19 个 `pm-mining-N`（switch），侧边栏 id 由 `constants.tsx` 里的 `createPredictiveChildren(list, prefix)` 辅助函数按数组下标批量生成（`pm-${prefix}-${index}`），而非逐条硬编码。
  - 核实发现 3 个死代码/重复文件：`views/predictive/port-crane/CraneTrolleyPmView.tsx`（重复文件，真正生效的是 `crane-trolley/CraneTrolleyPmView.tsx`）、`views/predictive/ShipEngineTypicalFailurePmView.tsx`（重复文件，真正生效的是 `ship-engine-typical-failure/` 子目录下同名文件）、`views/predictive/StatorInsulationPmView.tsx`（完全未被引用）。148 个有效页面经核实全部使用 `ThreeScene`（部分子目录页面对 3D 组件做了别名重命名，如 `BearingLifeScene`、`ShovelOverviewThreeScene`），无"有效但无 3D 模型"的情况。
  - 148 个页面每个新增独立 `ModelLibraryLink` 引入 + `MODEL_LIB_URL` 常量（`MODEL_LIB_LINK[pm-xxx]` 搜索注释），相对路径深度按各文件实际嵌套层级自动匹配（根目录文件用 `../../`，子目录文件用 `../../../`，与该文件原有 `ThreeScene` 引入的相对层级保持一致）。
  - 角落占用判断：144 个页面右上角空闲，用默认 `absolute top-4 right-4 z-20`；4 个页面（`PropulsionShaftMisalignmentPmView`/pm-pmOther-36、`ShipAuxSystemComparePmView`/pm-pmOther-53、`WinchGearboxBrakePmView`/pm-pmOther-60、`HatchCoverPmView`/pm-pmOther-61）右上角被真实交互按钮/状态栏占用，改放 `absolute bottom-4 right-4 z-20`。
  - 本分类同时是 Phase 4 十个试点页面中 5 个水利页面的物理所在目录（如 `views/predictive/hydro/PenstockWeldFatigueView.tsx` 对应 `pm-hydro-36`）；本次仅按 Phase 2 规则接入模型库链接，试点页面的"真实后端数据流转"重塑仍按计划留到最后的 Phase 4 统一处理，不在本次改动范围内。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增 UI 元素，未改动任何现有状态逻辑、路由或样式类；3 个死代码文件未触碰。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（基线），0 条新增错误。
  - 统计确认恰好 148 个文件包含 `ModelLibraryLink url=`。
  - Playwright 抽样验证 6 个代表性页面（根目录默认右上 `pm-pmOther-0`、`hydro` 子目录默认右上 `pm-hydro-6`、`mining` 子目录默认右上 `pm-mining-0`、3 个不同子目录的右下冲突 `pm-pmOther-36`/`pm-pmOther-60`/`pm-pmOther-61`）：均恰好渲染 1 个 `ModelLibraryLink`，href 正确，0 条控制台报错。
- 备注: 本分类文件量最大（148 页），采用脚本化批量核实 + 批量接入的方式完成（人工核实死代码判定与冲突角落判定后，用脚本批量应用到全部文件，避免人工遗漏或不一致）。

---

### [2026-07-13 16:30] Step 2.2 - 震动监测分类接入模型库链接（66 页）
- 状态: 完成
- 涉及文件: `views/vibration-monitoring/*/index.tsx`（51 个）+ `views/Vibration monitoring/*/index.tsx`（15 个，注意目录名带空格）
- 修改内容:
  - 该分类此前计划标注"待核实是否废弃"，本次核实发现真实情况比预期更复杂：项目里同时存在 `views/vibration-monitoring/`（无空格，79 个子目录）和 `views/Vibration monitoring/`（带空格，26 个子目录）两个目录，此前的初步结论（"无空格版本已废弃"）不准确。实际路由 `App.tsx` 中的 `VIBE_VIEWS` 懒加载表（`const VIBE_VIEWS: Record<string, any> = {...}`）共 66 个 `vibe-*` key，其中 51 个 `lazy(() => import('./views/vibration-monitoring/...'))` 指向无空格目录，15 个指向带空格目录，两个目录混用。
  - 两个目录下共有 39 个子文件夹未被任何 `vibe-*` key 引用，确认为死代码已跳过（多为同名/近似名的重复/废弃版本，例如无空格目录下的 `ShipHull`、`PortWinch`、`PortSubstation` 等与带空格目录下同名但实际生效的文件夹并存，只有其中一份是真正被路由的）。
  - 66 个有效页面每个新增独立 `ModelLibraryLink` 引入 + `MODEL_LIB_URL` 常量（`MODEL_LIB_LINK[vibe-xxx]` 搜索注释，`<id>` 取 `VIBE_VIEWS` 里的 key），统一使用 `@/src/scenarioLib/ModelLibraryLink`（项目 `vite.config.ts` 中 `@` 别名指向仓库根目录，与该分类页面既有的 `@/components/...` 引入风格一致）。
  - 角落占用判断：64 个页面右上角空闲，用默认 `absolute top-4 right-4 z-20`；2 个页面（`UndergroundLoader`/vibe-UndergroundLoader、`ShipHullVerticalVibration`/vibe-ShipHull）右上角被真实工况读数遮挡，改放 `absolute bottom-4 right-4 z-20`。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增 UI 元素，未改动任何现有状态逻辑、路由或样式类；39 个死代码文件夹未触碰。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（基线），0 条新增错误。
  - `grep -rl "ModelLibraryLink" "views/vibration-monitoring" "views/Vibration monitoring"` 确认恰好 66 个文件命中。
  - Playwright 抽样验证 4 个代表性页面（无空格目录默认右上 `vibe-TurbineShaft`、无空格目录冲突右下 `vibe-UndergroundLoader`、带空格目录冲突右下 `vibe-ShipHull`、带空格目录默认右上 `vibe-ShipCompressor`）：均恰好渲染 1 个 `ModelLibraryLink`，href 正确，0 条控制台报错。
- 备注: 本分类是本项目中首次出现"同一分类下两个大小写/空格不同的目录混合生效"的情况，已在开发计划表中详细记录两个目录的分工，避免未来误判其中一个整体为废弃。

---

### [2026-07-13 15:40] Step 2.8 - 维修分类（App/模拟维修服务）接入模型库链接（68 页）
- 状态: 完成
- 涉及文件: `views/maintenance/*.tsx`（共 99 个文件系统条目，其中 68 个实际修改）
- 修改内容:
  - 该分类与此前分类不同：不是"每页都有 3D 模型"，而是混合了纯数据/表格管理页面（如预算监控、供应商评价、五常法评分等）与含 3D 模型的演练/检修页面。按决策 3"每一个含 3D 模型的展示页"原则，只对实际使用 `ThreeScene` 组件的页面接入 `ModelLibraryLink`。
  - 核实结果：`views/maintenance/` 目录共 99 个文件，其中 3 个为死代码已跳过（`MiningConflictResolutionView`、`VendorEvalView` 未被 `App.tsx` 导入；`MiningFaultComparisonSimView` 虽被导入但从未在 JSX 中渲染），96 个为有效页面；96 个有效页面中 68 个实际使用 `ThreeScene`（分布在"应用维修服务"分类的 `am-*` 前缀页面中 22 个、"模拟维修服务"分类的 `mm-01`~`mm-46` 全部 46 个），其余 28 个纯数据/表格页面（如 `BudgetMonitorView`、`SkillMatrixView`、`WarrantyMgmtView` 等）无 3D 模型，不适用该功能，未做修改。
  - 68 个页面每个新增独立的 `ModelLibraryLink` 引入 + `MODEL_LIB_URL` 常量（含 `MODEL_LIB_LINK[<id>]` 搜索注释），`<id>` 取该页面在 `App.tsx` 中的路由 id（`am-xxx` 或 `mm-NN`）。
  - 角落占用判断：65 个页面右上角空闲，用默认 `absolute top-4 right-4 z-20`；3 个页面（`MiningDrillingRigRepairView`/mm-18、`MiningHydraulicSupportView`/mm-16、`PortCollaborativeRepairView`/mm-23）右上角被真实遥测数据面板占用，改放 `absolute bottom-4 right-4 z-20`。另有 3 个页面（`DowntimeCodingView`、`KnowledgeBackflowView`、`SurveyView`）初筛疑似冲突，人工核实后确认只是四角装饰性细边框（`border-t-2`/`border-r-2` 空心 L 形线条，无实际内容），不构成遮挡，按默认右上角处理。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增 UI 元素，未改动任何现有状态逻辑、路由或样式类；28 个无 3D 模型页面与 3 个死代码页面均未触碰。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（基线），0 条新增错误。
  - `grep -rl "ModelLibraryLink" views/maintenance` 确认恰好 68 个文件命中。
  - Playwright 抽样验证 5 个代表性页面（`am-portal` 默认右上、`am-survey` 装饰边框误判排除后右上、`mm-18`/`mm-23` 真实冲突右下、`mm-01` 默认右上）：均恰好渲染 1 个 `ModelLibraryLink`，href 正确，0 条控制台报错。
- 备注: 本分类首次出现"部分页面无 3D 模型不适用"的情况，与此前几个分类"全部页面都有 3D 模型"不同，已在开发计划表中注明区分。

---

### [2026-07-10 18:10] Step 2.7 - 设备点巡检分类接入模型库链接（50 页）
- 状态: 完成
- 涉及文件: `views/Equipment-Point-Inspection/**/*.tsx`（含顶层 `MiningRailView.tsx` 及 49 个子目录页面，共 50 个文件）
- 修改内容:
  - 每个页面新增 `ModelLibraryLink` 引入及独立的 `MODEL_LIB_URL` 占位常量（`https://industrial-intelligent-cockpit.example.com/model-lib/models/ins-N`，N=0~49，对应 `App.tsx` 中该页面的 `activeTabId`），并在注释里标注 `MODEL_LIB_LINK[ins-N]` 便于日后搜索定位、替换为真实地址。
  - 在 3D 场景容器内插入跳转链接，按角落占用情况分三种布局：
    - 35 个页面右上角空闲，用默认 `absolute top-4 right-4 z-20`。
    - 13 个页面右上角被真实信息面板占用（如坐标信息、监测点位、GNSS 站点信息、电控柜位置等），改放 `absolute bottom-4 right-4 z-20`：`PortBerthInspectionView`、`ChannelBuoyView`、`ShipCargoHoldInspectionView`、`PassengerBoardingBridgeView`、`HullStructureInspectionView`、`BallastWaterInspectionView`、`LightSignalsInspectionView`、`FuelTankInspectionView`、`ChannelEmbankmentView`、`SurfaceSubsidenceView`、`UndergroundLightingView`、`ElectricalCabinetView`、`HazardousGasView`。
    - 2 个页面（`DamShoulderView`、`HazardousChemicalWarehouseView`）顶部标题栏是 `justify-between` 两栏 flex（标题 + 状态徽标），链接作为第三个 flex 子元素插入，不用绝对定位，避免与徽标重叠。
  - 跳过 3 个死代码：`MineGasView`（在 `App.tsx` 中被导入但从未在 `ins-` 路由链中使用）、`BuoyInspection`、`DamGallery`（两个目录在 `App.tsx` 中完全未被引用，既未导入也未路由）。
- 影响面（是否触碰现有功能/接口/样式）: 仅新增 UI 元素，未改动任何现有状态逻辑、路由或样式类；未触碰 3 个死代码目录。
- 验证方式（如何确认未破坏原功能）:
  - `npx tsc --noEmit` 全量检查：错误数保持 72（与改动前基线一致），且 0 条新增错误，全部集中在既有的 `three-types.ts` 乱码文件。
  - `grep -rl "ModelLibraryLink" views/Equipment-Point-Inspection` 确认恰好 50 个文件命中，与"50 个有效页面"结论一致。
  - Playwright 抽样验证 5 个代表性页面（`ins-0` 默认右上、`ins-29`/`ins-33` 特殊 flex 布局、`ins-45`/`ins-49` 右下冲突布局）：均恰好渲染 1 个 `ModelLibraryLink`，href 与页面 id 一一对应，0 条控制台报错。
- 备注: 该分类共 53 个文件系统条目（1 个顶层文件 + 52 个子目录），其中 50 个为 `App.tsx` 路由链（`ins-0` ~ `ins-49`）实际引用的有效页面。

---

### [2026-07-10 17:20] Step 2.6 - 维修培训分类接入模型库链接（64 页）

- 状态: 完成
- 涉及文件: `views/Maintenance-Training/` 下 64 个 `<场景文件夹>/index.tsx` 文件（水电/矿山/船舶港口/通用设备四大类虚拟实训场景，涵盖水轮机转轮吊装、采煤机截割部减速箱拆解、船用雷达磁控管更换、UPS逆变模块故障排查等，`activeTabId` 即文件夹名）
- 修改内容:
  - 该分类通过 `App.tsx` 里 `import.meta.glob('./views/Maintenance-Training/*/index.tsx')` 动态加载，`activeTabId` 直接等于场景文件夹名。核实 `MAINTENANCE_TRAINING_CHILDREN`（constants.tsx，65 项）与实际文件夹（66 个）后发现 `PortConveyorCouplingMaint` 文件夹没有对应的侧边栏入口，是死代码，本次跳过；另有 `ChillerRefrigerantRecoverySim` 虽在侧边栏配置中，但其页面代码是一个占位"加载中"页面，并未使用自己文件夹里的 `ThreeScene` 组件，不适用本次范围，也跳过不处理。其余 64 个确认存活且有真实 3D 场景，全部接入。
  - 该分类下页面结构高度统一：绝大多数（56 个）3D 卡片右上角空闲，直接放 `top-4 right-4`；8 个页面（`TransformerCoolerCleaningEdu`/`StationPowerWiringTroubleshoot`/`ScraperConveyorChainRepairDrill`/`SCADASystemRecoverySim`/`MiningTruckWheelAssemblySim`/`PenstockLeakageEmergencyDrill`/`ExcitationBrushReplacementOps`/`DraftTubePumpSealRepair`）右上角已有状态徽标，改放确认空闲的 `bottom-4 right-4`。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误均为本次修改前就已存在的 `three-types.ts` 编码问题，与本次改动无关）；Playwright 通过 DOM 查询验证 4 个代表性页面（`焊接机器人焊枪防碰撞传感器复位`/WeldingRobotCollisionSensorResetSim、`厂区防雷接地网接地电阻测试模拟`/GroundingResistanceTestVR、`船舶低速柴油机气缸盖拆装虚拟实训`/MarineDieselCylinderHeadVR、`刮板输送机断链事故应急抢修演练`/ScraperConveyorChainRepairDrill）的 `ModelLibraryLink` 均正确渲染、href 与场景 id 一一对应，控制台无新增报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[<场景id>]`（如 `MODEL_LIB_LINK[WeldingRobotCollisionSensorResetSim]`）定位每个页面的新增代码。

---

### [2026-07-10 16:30] Step 2.5 - 维修计划管理分类接入模型库链接（66 页）

- 状态: 完成
- 涉及文件: `views/Maintenance-plan-management/` 下 66 个 `<场景文件夹>/<场景文件夹>View.tsx` 文件（水电领域 HydroTurbineOverhaul~SurgeChamberInspection 等 21 个、矿山领域 CrusherLinerReplacement~PowerTransformerMaintenance 部分等 20 个、船舶港口领域 DredgerCutterHead~PowerTransformerMaintenance 等页面，完整清单见开发计划 Phase 2 执行记录，`activeTabId` 为 `mpm-0` 至 `mpm-65`）
- 修改内容:
  - 该分类通过 `views/Maintenance-plan-management/index.ts` 的通配导出 + `App.tsx` 里 `MPMViews[viewName]` 按数组下标查找路由，先核对 `scenarios[]` 数组与 `index.ts` 导出逐一比对，确认 66 个文件夹全部存活（无死代码），全部接入。
  - 按 3D 卡片角落实际占用情况分两组：38 个页面（Group A）右上角空闲，直接放 `top-4 right-4`；28 个页面（Group B）右上角已被状态徽标占用，改放确认空闲的 `bottom-4 right-4`。其中 2 个页面结构特殊：`DredgerCutterHead`（mpm-55，全屏 3D 背景 + 大型左右浮动面板）放进顶部指挥栏的 `pointer-events-auto` 包裹层内；`AirCompressorOverhaul`（mpm-60，同类全屏布局但顶部栏为居中样式）确认右上角空闲后按默认方式放置。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误均为本次修改前就已存在的 `three-types.ts` 编码问题，与本次改动无关）；Playwright 通过 DOM 查询验证 3 个代表性 Group B 页面（`浓缩机驱动桥检修计划`/mpm-31、`岸桥起重机钢丝绳探伤更换`/mpm-40、`港区自动化闸口系统巡检`/mpm-59）的 `ModelLibraryLink` 均正确渲染、href 与场景 id 一一对应，控制台无新增报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[mpm-xxx]`（如 `MODEL_LIB_LINK[mpm-31]`）定位每个页面的新增代码。

---

### [2026-07-10 15:20] Step 2.3 - 计算机视觉监测分类接入模型库链接（67 页）

- 状态: 完成
- 涉及文件: `views/computer-visual-inspection/` 下 67 个页面文件（完整清单见开发计划 Phase 2 记录，涵盖大坝裂缝识别、水轮机叶片空蚀检测、系泊缆绳张力监测、除尘器滤袋破损识别等）
- 修改内容:
  - 该分类通过 `App.tsx` 里的 `CV_VIEWS` 惰性加载对象（`cv-*` 前缀）路由。核实后发现 74 个含 3D 场景的组件文件夹里有 7 个（`CraneRail`/`DamCrack`/`InsulatorFlashover`/`SolarPanel`/`SubstationVibration`/`WarehouseShelf`/`WireRopeDamage`）完全没有出现在 `CV_VIEWS` 里，是死代码，本次跳过；其余 67 个确认存活并接入（注意：`cv-dam-crack` 实际渲染的是 `DamCrackDetection` 文件夹，`cv-insulator-flashover` 实际渲染的是 `InsulatorDefect` 文件夹，均已按真实映射关系核实无误）。
  - 该分类页面结构比之前几个分类更不统一：9 个页面（`TurbineCavitation`/`TransformerLeak`/`DrillBitWear`/`ScreenMesh`/`ConveyorRoller`/`QuayCraneFatigue`/`HullBiofouling`/`BatteryCorrosion`/`ConveyorMisalignment`）3D 卡片右上角空闲，用 `top-4 right-4`；其余 58 个页面右上角（部分含左上角）已有场景标题/状态徽标，改用确认空闲的 `bottom-4 right-4`。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误仍是本次修改前就存在的 `Maintenance-plan-management` 目录问题）；Playwright 截图验证 4 个代表性页面（含 Phase 4 试点页面"系泊缆绳张力与磨损监测"，以及默认右上/右下两种摆放），链接位置均正确、无遮挡、控制台无报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[cv-xxx]`（如 `MODEL_LIB_LINK[cv-mooring-tension]`）定位每个页面的新增代码。

---

### [2026-07-10 11:45] Step 2.4 - 零部件寿命预警分类接入模型库链接（63 页）

- 状态: 完成
- 涉及文件: `views/life-warning/` 下 63 个 `<场景id>/View.tsx` 文件（完整清单见开发计划 Phase 2 记录，涵盖球磨机钢球消耗、拦污栅结构寿命、电铲钢丝绳寿命等水利/矿山/船舶/港口零部件寿命预警场景）
- 修改内容:
  - 该目录采用 `App.tsx` 里 `import.meta.glob('./views/life-warning/*/View.tsx')` 的动态加载机制，`activeTabId` 直接等于场景 id（即文件夹名）。核实 `LIFE_WARNING_CHILDREN`（constants.tsx）后发现 68 个组件文件夹里有 5 个（`conveyor-belt-roller-life`/`cooling-water-pump-bearing-life`/`crusher-hammer-wear`/`substation-circuit-breaker-contact-life`/`wind-turbine-gearbox-oil-life`）虽然有完整的 View.tsx 和 ThreeScene，但没有任何侧边栏入口指向它们，是死代码，本次跳过；其余 63 个确认存活并接入。
  - 该分类下所有页面的 3D 卡片结构高度一致（`<div className="flex-1 relative"><ThreeScene state={xxxState} /></div>`），且逐一核实后全部 63 个页面右上角都是空闲的，因此统一采用 `top-4 right-4` 的默认摆放，未出现需要特殊处理的情况。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误仍是本次修改前就存在的 `Maintenance-plan-management` 目录问题）；Playwright 截图验证 4 个代表性页面（含 Phase 4 试点页面"进水口拦污栅结构寿命评估"、"矿用电铲钢丝绳寿命预警"），链接位置均正确、无遮挡、控制台无报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[<场景id>]`（如 `MODEL_LIB_LINK[ball-mill-steel-ball-consumption]`）定位每个页面的新增代码。

---

### [2026-07-10 11:10] Step 2.9 - 服务数据管理分类接入模型库链接（38 页）

- 状态: 完成
- 涉及文件: `views/ServiceDataManagement/` 下 38 个页面文件（ChannelInspectionServiceView、MiningMultiFaceCollabView、MiningComponentLifeView、CrossRegionWaterView、MiningDustVibrationServiceView、MiningEmergencyRepairView、MiningEnergyServiceView、MiningExtractionServiceView、MiningExtremeConditionView、HydroDigitalHandoverView、HydroEmergencyDispatchView、HydroHealthAssessmentView、HydroIncidentView、HydroLifespanPredictionView、HydroLongTermOperationView、HydroMaintenanceScheduleView、HydroStructureHealthView、HydroSystemOMView、HydroUnitStatusView、MiningInspectionServiceView、MiningMaintenancePlanView、MiningCrushingOandMView、MultiFleetRouteView、MiningMultiVendorView、PortLoadingServiceDataView、MiningRemoteOMView、HydroDispatchView、ShipBerthingCollaborationView、ShipCrossCycleServiceView、ShipEmergencyRepairView、ShipEnergyEfficiencyView、ShipEngineServiceView、ShipNavigationEfficiencyView、ShipPredictiveMaintenanceView、ShipReliabilityAssessmentView、ShipSeaConditionAdaptabilityView、ShipShoreCollaborationView、UnifiedGovernanceView）
- 修改内容:
  - 先用 `grep` 核实该分类下 40 个含 3D 场景的子页面，发现两个例外：`ConditionCollaborationView` 的页面代码根本没有使用它自己文件夹里的 `ThreeScene` 组件（不适用本次范围，跳过）；`views/ServiceDataManagement/HydroCascadeView.tsx` 的组件以别名 `HydroCascadeServiceView` 导入后从未在任何路由分支渲染，是死代码（`hd-8` 实际渲染的是 `views/cockpit/HydroCascadeView.tsx` 那个同名但不同文件的组件），本次跳过不处理。其余 38 个确认存活并接入。
  - 按 3D 卡片角落的实际占用情况分三组处理：28 个页面右上角空闲，直接放 `top-4 right-4`；7 个页面右上角被已有徽标占用、确认右下角空闲，放 `bottom-4 right-4`；3 个页面（`HydroStructureHealthView`/`HydroSystemOMView`/`HydroUnitStatusView`）左右上角和右下角都被占用，改放确认空闲的 `bottom-4 left-4`。另有 2 个页面（`HydroDispatchView`、以及此前 `StillingBasinRepairKbView` 的同类布局）是背景全屏 3D + 独立顶部指挥栏的特殊布局，四角都可能被浮动面板遮挡，改为放进顶部栏右侧统计区，作为新增的一项。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误仍是本次修改前就存在的 `Maintenance-plan-management` 目录问题）；Playwright 截图验证 4 个代表性页面（默认右上/右下/左下/顶部栏四种摆放各一个），链接位置均正确、无遮挡、控制台无报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[sm-6]`、`MODEL_LIB_LINK[hd-6]` 等（分类前缀 sm-矿山/sh-船舶航道/hd-水电/int-统一治理）定位每个页面的新增代码。

---

### [2026-07-10 10:40] Step 2.10 - 运维知识管理分类接入模型库链接（31 页）

- 状态: 完成
- 涉及文件: `views/knowledge-manage/` 下 31 个页面文件（ChannelShoalDredgingKbView、CraneWindKbView、CrushingIndexKbView、DamSeepageModelView、DustSuppressionKbView、ExplosionProofKbView、FloodDischargeView、GeneratorInsulationView、GroundPressureKbView、HydraulicSupportResistanceView、MultimodalTransportKbView、RopeNdtKbView、ScraperChainTensionView、SedimentationArchiveView、ShearerPickResistanceView、ShipLockJointDispatchView、ShovelToothKbView、SlopeDisasterKbView、SlopeRadarWarningView、TailingsSafetyKbView、TurbineCavitationView、UnderwaterRobotView、FishPassagePerformanceView、PilotExperienceKbView、UnmannedTruckEdgeScenarioView、TruckTireKbView、TunnelJointHealthView、UnmannedShipKbView、VentilationNetworkKbView、BeltTearKbView、StillingBasinRepairKbView）
- 修改内容:
  - 该分类下 32 个含 3D 场景的子页面里，先用 `grep` 核实每个页面是否被 `App.tsx` 的 `km-*` 路由表实际引用：31 个确认存活，唯独 `views/knowledge-manage/FloodDischargeView.tsx`(flood-emergency，别名 `Flood2EmergencyView`) 只被 import 但从未在任何路由分支里渲染，判定为死代码，本次跳过不做处理。
  - 对 31 个存活页面逐一接入 `ModelLibraryLink`：22 个页面 3D 卡片的右上角是空的，直接放 `top-4 right-4`；7 个页面右上角已有别的徽标，放到确认空闲的 `bottom-4 right-4`；`BeltTearKbView` 的 AR 遮罩层内部左右角都被占用，放在遮罩层外、3D 容器内的 `bottom-4 right-4`；`StillingBasinRepairKbView` 是背景全屏 3D + 独立顶部导航栏的特殊布局，3D 容器四角都可能被顶部栏/底部浮动工艺条遮挡，改为放在顶部导航栏右侧统计区，作为新增的第三项。
- 影响面: 每个文件只新增 1 个 import + 1 个常量 + 1 个小容器，均未改动原页面的状态管理、图表、3D 参数等既有逻辑。
- 验证方式: `npx tsc --noEmit` 确认无新增类型错误（残留 72 个错误均为本次修改前就存在的 `Maintenance-plan-management` 目录问题）；Playwright 抽样截图验证 4 个代表性页面（默认摆放/右下摆放/AR遮罩特殊摆放/顶部导航栏摆放各一个），链接位置均正确、无遮挡、控制台无报错。
- 备注: 可通过全局搜索注释关键字 `MODEL_LIB_LINK[km-xxx]`（如 `MODEL_LIB_LINK[km-belt-tear]`）定位每个页面的新增代码。

---

### [2026-07-10 10:10] 纠错 - cockpit 目录 9 个部件子页面确认为死代码，Phase 2.11 与 Phase 4 决策 5 第 4 项相应调整

- 状态: 完成（纠错）
- 涉及文件:
  - 撤销对 `views/cockpit/excitation-brush/index.tsx` 的误加改动（新增后立即发现问题并撤销，未遗留任何痕迹）
  - `场景库测试方案/开发计划.md`（决策 5 表格第 4 项、Phase 2 2.11 行、Phase 4 4.4 行、执行状态跟踪表同步更新）
- 修改内容:
  - 在准备给 `views/cockpit/` 目录下 11 个含 3D 场景的页面接入模型库链接（Phase 2 Step 2.11）时，先给 `excitation-brush` 加了链接，随后核实 `App.tsx` 全部路由才发现：`excitation-brush`/`guide-vane`/`hydro-turbine`/`inlet-valve`/`main-shaft`/`main-transformer`/`pump-runner`/`spillway-gate`/`stator-winding` 这 9 个页面完全没有被任何路由引用，是早期原型遗留的死代码（对应设备主题已被 `views/predictive/hydro/*` 系列取代），只有 `unit1-predictive` 是真正在跑的页面。
  - 按决策 3"未引用即废弃、直接跳过"的原则处理：撤销了 `excitation-brush` 的误加改动，其余 8 个未触碰的文件保持不动，Phase 2.11 batch 缩小为只处理 `unit1-predictive` 一个文件（已在上一条记录里完成）。
  - 由于原 Phase 4 决策 5 第 4 项"导叶机构（`views/cockpit/guide-vane`）"也在这批死代码里，已确认路由后从候选名单剔除，替换为同样是水利领域、且已核实真实可路由的"调压室巡检维护计划"（`views/Maintenance-plan-management/SurgeChamberInspection/SurgeChamberInspectionView.tsx`，`activeTabId` 为 `mpm-16`，属于"维修计划管理"分类的动态路由表第 16 项）。
- 影响面: 不影响任何现有功能（`excitation-brush` 的改动在本次会话内新增又撤销，未产生实际改动；替换后的试点页面选型仅是文档层面的调整，代码尚未开始 Phase 4）。
- 验证方式: `npx tsc --noEmit` 确认撤销后无残留改动；用 `grep` 对 `App.tsx` 全文核实这 9 个文件路径确无任何 import/路由引用；核实替换后的 `mpm-16` 对应 `SurgeChamberInspectionView` 在 `App.tsx` 动态路由数组第 16 位（0 基）且相关文件无 pre-existing 类型错误。
- 备注: 这也提醒后续 Phase 2 每个分类批次执行前，都要先用 `grep`/路由核实"这个文件是否真的被引用"，不能只看目录结构就假设它是活页面。

---

### [2026-07-10 10:00] Step 2.0 & 2.12 - ModelLibraryLink 组件 + 仿真分析分类接入（4 页）

- 状态: 完成
- 涉及文件:
  - 新增 `src/scenarioLib/ModelLibraryLink.tsx`
  - 修改 `views/simulation/port-group/index.tsx`
  - 修改 `views/simulation/port-auto/index.tsx`
  - 修改 `views/simulation/port-carbon/index.tsx`
  - 修改 `views/simulation/port-evac/index.tsx`
- 修改内容:
  - 2.0 `ModelLibraryLink` 通用小组件：接收 `url`/`label`，渲染一个不起眼的小型跳转按钮（青色描边、Box 图标），新标签页打开。
  - 2.12 在"仿真分析"分类下 4 个含 3D 场景的页面（内河船闸群协同仿真/自动化港口协同仿真/航运碳排放仿真/港口疏散仿真）里，各自在 3D 卡片内新增一个独立的占位 url 常量 + `// MODEL_LIB_LINK[<场景id>]: ...` 注释标记，并在 3D 卡片空白角落挂载 `<ModelLibraryLink />`（避开各页已有的状态徽标位置）。
- 影响面: 每个文件只新增 1 个 import + 1 行注释常量 + 1 个绝对定位的小容器，未改动原有 3D 交互、图表、状态卡片等任何展示逻辑。
- 验证方式: `npx tsc --noEmit` 无新增类型错误；Playwright 依次导航到这 4 个场景截图确认：模型库链接展示在 3D 卡片角落、不与已有徽标重叠、点击不影响 3D 交互区域，控制台无报错。
- 备注: 这是 Phase 2 的试点批次，用于验证 `ModelLibraryLink` 接入模式可行，后续分类按开发计划里的批次顺序继续铺开。可通过全局搜索注释关键字 `MODEL_LIB_LINK[sim-port-group]`、`MODEL_LIB_LINK[sim-port-auto]`、`MODEL_LIB_LINK[sim-port-carbon]`、`MODEL_LIB_LINK[sim-port-evac]` 分别定位这 4 处新增代码。

---

### [2026-07-10 09:50] Step 3.0～3.1 - 场景导出按钮（Phase 3 全部完成）

- 状态: 完成
- 涉及文件:
  - `package.json` / `package-lock.json`（新增 `docx` 依赖）
  - 新增 `src/scenarioLib/scenarioExport.ts`
  - 修改 `views/SmartOperationsView.tsx`（新增导出按钮 + 下拉格式菜单）
- 修改内容:
  - 3.0 执行 `npm install docx`，用于生成规范 `.docx`。
  - 3.1 `scenarioExport.ts` 读取 `constants.tsx` 的 `MENU_ITEMS`，整理出总计数（场景总数/分类总数）、按分类统计、场景明细三部分数据，分别实现 JSON（Blob 下载）、Markdown（Blob 下载，标题+两个表格）、Excel（`xlsx` 库，3 个 sheet：总计/分类统计/场景明细）、Word（`docx` 库，标题+3 个表格，仅表格和标题，无页眉页脚落款）四种导出。`SmartOperationsView.tsx` 顶部卡片右上角新增"导出全部场景"按钮 + 4 项格式下拉菜单，点击后触发对应格式下载，全部在浏览器端完成，不经过 `server.ts`。
- 影响面: 只新增了这一个按钮和一份新文件，未改动 `SmartOperationsView.tsx` 原有的图表/卡片/数据展示逻辑；`server.ts`、`App.tsx` 路由均未改动。
- 验证方式: `npx tsc --noEmit` 无新增类型错误；用 Playwright 依次点击 4 种格式，均成功触发浏览器下载事件，落盘校验：`.docx`/`.xlsx` 文件头为合法 ZIP 签名(`PK`)，`.json` 内容含 `totalScenes: 1025`、`totalCategories: 20`（与侧边栏总计数一致），`.md` 内容含标题和两个 Markdown 表格，格式正确。
- 备注: 无。

---

### [2026-07-10 09:40] Step 1.1～1.4 - 全局场景信息条（Phase 1 全部完成）

- 状态: 完成
- 涉及文件:
  - 新增 `src/scenarioLib/scenarioRegistry.ts`
  - 新增 `src/scenarioLib/ScenarioTelemetryContext.tsx`
  - 新增 `src/scenarioLib/mockScenarioLog.ts`
  - 新增 `src/scenarioLib/ScenarioMetaBar.tsx`
  - 修改 `App.tsx`（新增 2 处 import；用 `ScenarioTelemetryProvider` 包裹根节点；在内容容器内、`{renderContent()}` 之前挂载 `<ScenarioMetaBar scenarioId={activeTabId} />`）
- 修改内容:
  - 1.1 场景注册表：从 `constants.tsx` 的 `MENU_ITEMS` 派生扁平场景元数据（id/名称/所属分类），并提供按分类统计的辅助函数，供后续导出按钮复用。
  - 1.2 场景遥测上下文：`ScenarioTelemetryContext` 统一存储各场景的"端到端耗时"快照，区分 `isReal`（真实上报）与默认的模拟测量，暴露 `reportMockTiming` / `reportRealTiming` 两个写入口。
  - 1.3a 场景工况日志生成器：`mockScenarioLog.ts` 按场景名称做模板化拼接，用基于场景 id 的确定性伪随机数生成 8 条时间戳+级别(info/warning/critical)+内容的模拟工况日志，符合常规运维日志的措辞和比例（多数 info，少量 warning，极少 critical）。文件顶部注释写明这是"工况日志"而非代码修改记录，以及后续替换为真实日志源的做法。
  - 1.3b `ScenarioMetaBar` 组件：横向信息条，复用 `SciFiCard` 视觉语言。左侧显示场景名称（大字）+ 场景 ID（正下方，小号青色等宽字体）；中段常显"更新时间"（本次进入该场景、内容渲染完成的时间戳）和"端到端耗时"（毫秒数 + "真实响应耗时/页面渲染耗时(模拟)"标签，随该场景是否有真实上报自动切换）；右侧"场景日志"为唯一可折叠项，点击展开日志列表。
  - 1.4 接入 `App.tsx`：仅新增 3 行相关代码（2 行 import + 1 处 Provider 包裹 + 1 行组件挂载），未改动 `renderContent()` 内部任何 case、未改动 `Sidebar.tsx`、未改动任何既有 view 文件。
- 影响面: 全站所有页面（包括各分类总览页和具体场景页）现在都会在页面内容顶部多出这一条信息条；不改变原有页面内部的任何展示或交互逻辑，`renderContent()` 路由表和各 view 组件保持原样。
- 验证方式: `npx tsc --noEmit` 确认新增文件与 `App.tsx` 改动无类型错误（项目里唯一残留的 72 个类型错误来自与本次修改无关的 7 个既有 `Maintenance-plan-management/*/three-types.ts` 文件，属历史遗留问题）；用 Playwright 启动 `npm run dev` 并截图验证：首页（工业智能运维）、切换到"运行驾驶舱"、"预测性维护"三个不同分类下，信息条均正确显示名称/ID/更新时间/耗时/日志按钮，点击"场景日志"可正常展开模拟日志列表，原有图表、雷达图、3D 区域等展示不受影响，浏览器控制台无报错。
- 备注: 无。

---

（Phase 1 之前暂无记录）
