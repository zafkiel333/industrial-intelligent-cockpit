# 新增模型配对与主平台排序交付

## 1. 模型库增量筛选

2026-09-01 通过主项目只读列表接口取得 1,162 个模型，较 2026-08-26 快照的 1,140 个增加 22 个。新增项集中在矿山、破碎、输送和筛分设备。

场景库 BFF 当前单文件安全上限为 50 MiB。22 个新增模型中只有以下 4 个不超过上限且文件头有效：

| 模型 ID | 名称 | 大小 | 处理 |
|---:|---|---:|---|
| 8736 | 矿石球磨机 | 20.83 MiB | 接入 `eq-16` |
| 8740 | 筛沙机 | 21.15 MiB | 接入 `eq-17` |
| 8741 | 粗粉碎机 | 7.81 MiB | 接入 `eq-15` |
| 8743 | 振动给料机 | 22.49 MiB | 暂缓：没有不牵强且不破坏既定位置约束的页面 |

其余新增模型为 63.56 MiB～1,025.99 MiB，直接接入会超过现有文件上限并放大 1.6 GiB 云主机内存风险，本轮不强行配对。

## 2. 新增页面配对

| 前置顺序 | 页面 | 新模型 | 等级 | 页面适配 |
|---:|---|---|---|---|
| 13 | `eq-15 破碎设备智能运维` | `8741 粗粉碎机` | A | 直接匹配进料箱、粉碎腔、传动轴和支架 |
| 14 | `eq-16 选矿设备智能运维` | `8736 矿石球磨机` | A | 收敛为磨矿核心设备，指标限于筒体、齿圈、轴承和驱动 |
| 15 | `eq-17 制砂机智能运维` | `8740 筛沙机` | B | 改为制砂筛分单元，指标限于筛面、输送、给料和振动 |

没有给 `eq-13 掘进设备智能运维` 强行匹配大型采煤设备，也没有为振动给料机新造牵强页面。

智能运维栏目冻结顺序为：

```text
eq-0, eq-1, eq-2, eq-3, eq-4, eq-5, eq-7, eq-8, eq-9, eq-10,
eq-12, eq-14, eq-15, eq-16, eq-17, eq-6, eq-11, eq-13, eq-18,
eq-unit1-model
```

硬约束保持：`eq-18` 原一号机组数据页仍是第 19 位且代码未动；独立模型页 `eq-unit1-model` 为第 20 位；仿真模拟前四页不变；总菜单仍为 20 个根节点、1,050 个节点。

## 3. 主项目端必须接收的交付物

建议直接交付以下两个文件：

```text
主项目模型库扩展接入-20260826/主平台导航正式交付包-20260901.zip
主项目模型库扩展接入-20260826/主平台导航正式交付包-20260901.zip.sha256
```

ZIP 内有：

```text
README.md
SHA256SUMS.txt
scene-library-menu.manifest.json
scene-library-model-allowlist.json
```

主项目必须同时整体替换 manifest 和 allowlist：

- manifest 决定新接入页面在各栏目中的顺序；
- allowlist 把模型详情白名单更新为 106 个唯一模型，并移除失效模型 ID；
- 不能只改主平台硬编码数组，否则下次 manifest 更新会再次丢顺序；
- 保留 `viewId`、动态 Origin、`event.source`、协议版本、详情路径、模型 ID 和名称校验；
- 构建后清理主平台静态资源/CDN/Service Worker 的旧版本缓存，并让用户关闭旧标签页重新进入。

正式验收值：

```text
交付 ZIP 字节=34321
交付 ZIP SHA-256=9d68ba09b32e5be935d6d21077ede2ca3243a3216417758a82e5427fe3f75c09
menuVersion=20260901-093430
menuContentSha256=d664531146fd033c7b0a23bda35f997e7adfe1396a7e7d58536dc6334b5d9abf
manifest SHA-256=73da22e4f3fc9e3f0673461e11e07263d3b1e8a174c76009380db100a600850c
模型数=106
allowlist SHA-256=22d0e7e952b5f9383179bdf809bd331114c43232e87ac3fbed6e253e6c32bce3
```

主项目团队自行备份、构建、发布和回滚主平台；场景库本轮不会登录或修改 `8.146.211.204`。
