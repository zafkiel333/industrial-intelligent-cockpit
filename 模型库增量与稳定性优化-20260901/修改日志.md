# 修改日志

## 2026-09-01

- 核实 `eq-0/2353` 上游下载内容不是有效 FBX，以 `2363 轴流式水轮机` 替换。
- 新增 `audit:model-assets`，扫描全部模型展示配置；发现并替换 `turbine-blade-erosion/2286`，最终 106/106 资源有效。
- 将 `turbine-blade-erosion` 改绑 `2306 弗朗西斯水轮机转轮`，吊装实训改绑唯一的 `7193 混流式转轮`。
- 对主项目 1,162 个模型与 1,140 条旧快照做差分；从 22 个新增模型中选择 `8741/8736/8740` 接入 `eq-15/eq-16/eq-17`。
- 将新增模型页前置到智能运维第 13～15 位；保持 `eq-18` 第 19 位、`eq-unit1-model` 第 20 位和仿真前四页不变。
- 修复服务端无界模型 Buffer 缓存和全量持久缓存定时加载，增加条目/字节双预算、LRU 和健康指标。
- 浏览器 ArrayBuffer 缓存降为 1，卸载时释放 WebGL context，无效资源自动重试限制为 2 次。
- 本地连续 10 模型和并发 6 模型压力验证通过；严格 3 项/64 MiB 预算下未超限。
- 运行代码提交：`07005f60131f8a202d6de1c257d098bfb2ad9ed0`。
- 生成菜单版本 `20260901-093430` 和主平台 106 模型正式交付目录；ZIP 34,321 字节，SHA-256 `9d68ba09b32e5be935d6d21077ede2ca3243a3216417758a82e5427fe3f75c09`。
- 新建本目录，记录模型审计、稳定性、配对排序、主项目交付和本地开发/云发布流程。

## 验证结果

```text
UPSTREAM_MODEL_ASSET_AUDIT total=106 valid=106 failures=0 concurrency=3
MODEL_LIBRARY_EXPANSION_VERIFY_OK bindings=102 showcasePages=106 menuNodes=1050 legacySwitch=hidden
MODEL_LOADING_OPTIMIZATION_VERIFY_OK ... browserBuffers=1 serverLru=bounded autoRetry=2
MODEL_VIEWER_STABILITY_VERIFY_OK centerDrift<1e-6 rotation30=0.360000 rotation120=0.360000
HTTPS_INTEGRATION_VERIFY_OK models=106 files=20
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1050
```

`npm run build:all` 通过。全项目类型检查仍被 7 个既有维修计划 `three-types.ts` 非法字符错误阻塞，本轮未修改这些无关历史文件。
