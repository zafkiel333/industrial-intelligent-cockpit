# 修改日志

## 2026-09-11（业务标题保护与全页面稳定性验收）

- 固定场景库 20 个一级分类名称及顺序，第 19 类继续使用既定名称“震动监测”；外部模型页标题与全局可见文本规范化逻辑分离，防止业务名称被通用术语规则改写。
- 增加覆盖全部 106 个外部模型页的公网浏览器门禁，逐页检查页面主体、模型页头、数据工作区、导入入口、六项指标、六张图、阈值诊断版本、精确标题和脚本异常。
- 106 页公网浏览器验收全部通过，最慢页面 2.868 秒；生产 API 审计无问题，专业文案、响应时延和水轮机统一工作流专项检查均通过。
- Release `20260911-083049` 已上线，服务保持 active/enabled、`NRestarts=0`，直接回退版本为 `20260910-194728`；模型、预测数据和日志继续使用共享持久化目录。

## 2026-09-10（106 页模型数据与预测工作区全量推广）

- 106 个外部模型页现均使用非阻塞模型加载和统一数据预测工作区；模型资源、历史数据、预测区域可独立进入可用状态，短时上游波动不再阻塞整个页面。
- 全部 16 类模型使用独立业务指标、故障知识和公开说明，扩展页与原四页均经过配置归一化，不再显示研发替换、接口错误或通用占位版本。
- 全量浏览器回归覆盖 16 类代表页，首次代表页约 2.3 秒，其余约 0.6～0.9 秒；8 个重点页面通过专业文案与状态表达复核。
- Release `20260910-194728` 已上线，当前 106 个模型目录、315 套非水轮机配对数据和 106 份场景日志均已在共享目录预置；直接回退版本为 `20260910-185005`。

## 2026-09-09（工业智能运维前三页加载排查与修复）

- 排查范围：工业智能运维首页后的 `eq-0 水轮机智能运维`、`eq-1 发电机智能运维`、`eq-2 输电装置智能运维`。
- 主平台集成根因：线上主平台当前把 `sceneLibraryEntry` 配置为相对路径 `/cockpit/`，实际 iframe 因此指向 `https://8.146.211.204:3100/cockpit/`；该地址返回主平台自身的 Vite HTML 回退页，不是场景库应用。场景库正确入口为 `https://47.122.104.52/cockpit/`。主平台配置或同源代理需由主平台侧修正，本轮未越权修改主平台服务器。
- 场景库响应优化：Bootstrap 立即使用最近可用 Dashboard 或空数据状态进入页面，实时数据请求转为后台同步；同一场景并发 Dashboard 请求合并，避免初始化、立即刷新和轮询在接口波动时叠加等待。
- 切页稳定性：浏览器模型字节缓存由最近 1 个调整为最近 3 个，覆盖前三页模型；在主平台切页和 React 重挂载期间为在途下载保留 2 秒接续窗口，降低往返切换触发重复下载或误取消的概率。
- 文案边界：Bootstrap 固定使用场景库审核后的页面说明，不再透传上游模型元数据中的描述字段；前三页配置独立业务文案，仅说明设备对象、关键结构、运行指标、趋势与风险信息。
- 验证门禁：模型加载专项检查新增三模型缓存、下载接续、Dashboard 请求合并、非阻塞 Bootstrap 和审核文案来源断言；专业页面浏览器回归范围加入 `eq-1`、`eq-2` 及三页精确文案检查。
- 验证结果：定向服务端 TypeScript 检查、`verify:model-loading`、`verify:model-library`、standalone/microapp 双构建与 8 个代表页面浏览器文案回归通过；新版三页 Bootstrap 本地响应为 0.05～0.20 秒。无生产缓存的本地模型完整渲染仍会受上游下载超时影响，生产共享缓存中的三份模型文件已独立验证可用。
- 当前状态：场景库侧代码修复与验证已完成，尚未发布；主平台入口配置修正前，主平台内嵌空白问题仍不能由场景库单侧消除。

## 2026-09-09

- 状态：外部模型页面专业化文案与加载稳定性修复已在本地完成并通过验证，尚未执行云端发布。
- 根因一：`modelCatalog.ts` 将 `pageModelBindings.ts` 中用于模型筛选、替换和研发适配的 `note/adaptation` 直接赋给页面公开描述，导致 102 个扩展模型页可能显示文件校验、上游端点、替换原因等内部信息。
- 根因二：模型接口在已有同模型持久缓存时仍等待上游元数据；Bootstrap 同时受最长 10 秒的元数据和 Dashboard 请求阻塞；模型首次下载最多执行两次 30 秒请求，浏览器失败后再等待 30 秒重试，形成长时间加载和稍后自行恢复的现象。
- 根因三：上游设备已配置但测点值为 `null` 时，诊断快照仍被记录，证据生成对空值调用 `toFixed()`，造成诊断接口 500 并被轮询重复触发。
- 文案修复：扩展模型公开描述改为按页面业务与模型对象集中生成，内部 `note/adaptation` 仅保留为研发配置；页面、模型视窗、资源协同和通道抽屉不再展示原始错误码、下载失败原因、内部占位版本或“模拟”耗时标签。
- 状态表达：瞬时波动统一使用“运行数据同步中”“模型资源正在同步”“等待首次同步”等业务状态；有可用版本或最近数据时明确保持展示，不使用红色故障措辞暴露底层实现。
- 加载修复：同模型且模型 ID 已核准的内存/磁盘缓存直接返回，过期检查转为后台执行；旧清单在元数据指纹核验通过后自动补写模型 ID；同场景元数据请求合并，失败退避 8 秒；元数据与 Dashboard 默认超时收敛至 4.5 秒。
- 下载修复：首次模型下载改为默认单次 20 秒有界请求，浏览器准备上限改为 30 秒，失败后以 8 秒间隔进行最多 2 次有限重试；可通过 `MODEL_DOWNLOAD_TIMEOUT_MS`、`MODEL_DOWNLOAD_ATTEMPTS`、`MODEL_METADATA_TIMEOUT_MS`、`MODEL_DASHBOARD_TIMEOUT_MS` 调整。
- 数据回退：服务端保留最近一次可用 Dashboard，短时接口波动时继续返回最近数据；无历史数据时只返回空测点状态，不虚构实时数值。
- 诊断修复：空值测点不进入诊断历史；证据数值增加有限数回退，避免历史脏数据再次触发异常；前端仅在至少存在一个来源数值时请求诊断，消除空数据状态下的五秒重复请求。
- 图表稳定性：历史点不足两条时展示“等待运行数据形成趋势”，达到可绘制条件后再挂载响应式图表，并增加最小宽高约束，避免空图误导及页面切换时产生负尺寸告警。
- 新增约束：`verify:model-library` 检查 102 个公开描述不等于内部备注且不含内部实施词；新增 `verify:model-showcase-ui` 浏览器覆盖 6 个代表页面；新增 `verify:diagnostic-empty-values` 覆盖空值与恢复场景；加载校验增加缓存优先、请求合并、失败退避和超时边界断言。
- 验证：`npm run build`、`npm run verify:model-library`、`npm run verify:model-loading`、`npm run verify:diagnostic-empty-values`、`node scripts/verify-dashboard-isolation.mjs`、`npm run verify:model-showcase-ui` 均通过；浏览器 6 页未发现内部实施措辞、原始故障原因或占位版本泄露。
- 已知检查项：全项目 `npm run typecheck` 仍被 7 个既有维修计划 `three-types.ts` 的非法字符阻断，本轮未修改这些无关文件；Vite 生产构建通过。

## 2026-09-01

- 核实 `eq-0/2353` 上游下载内容不是有效 FBX，以 `2363 轴流式水轮机` 替换。
- 新增 `audit:model-assets`，扫描全部模型展示配置；发现并替换 `turbine-blade-erosion/2286`，最终 106/106 资源有效。
- 将 `turbine-blade-erosion` 改绑 `2306 弗朗西斯水轮机转轮`，吊装实训改绑唯一的 `7193 混流式转轮`。
- 对主项目 1,162 个模型与 1,140 条旧快照做差分；从 22 个新增模型中选择 `8741/8736/8740` 接入 `eq-15/eq-16/eq-17`。
- 将新增模型页前置到智能运维第 13～15 位；保持 `eq-18` 第 19 位、`eq-unit1-model` 第 20 位和仿真前四页不变。
- 修复服务端无界模型 Buffer 缓存和全量持久缓存定时加载，增加条目/字节双预算、LRU 和健康指标。
- 浏览器 ArrayBuffer 缓存降为 1，卸载时释放 WebGL context，无效资源自动重试限制为 2 次。
- 本地连续 10 模型和并发 6 模型压力验证通过；严格 3 项/64 MiB 预算下未超限。
- 运行代码提交：`07005f60131f8a202d6de1c257d098bfb2ad9ed0`。
- 生成菜单版本 `20260901-093430` 和主平台 106 模型正式交付目录；ZIP 34,321 字节，SHA-256 `9d68ba09b32e5be935d6d21077ede2ca3243a3216417758a82e5427fe3f75c09`。
- 新建本目录，记录模型审计、稳定性、配对排序、主项目交付和本地开发/云发布流程。
- 发布包 `scene-library-release-20260901-093430.tar.gz` 为 39,995,208 字节、326 条目，SHA-256 `6dcaa849b8807be3747b11c813b262ec3307e7f58947a174a9bf00d21a043b25`。
- 3103 预检与正式公网均通过 102/102 Bootstrap 和 6 个关键 FBX 完整下载；Release 已切换，`previous=20260828-145036`，服务 active/enabled、`NRestarts=0`、3103 已停止。

## 验证结果

```text
UPSTREAM_MODEL_ASSET_AUDIT total=106 valid=106 failures=0 concurrency=3
MODEL_LIBRARY_EXPANSION_VERIFY_OK bindings=102 showcasePages=106 menuNodes=1050 legacySwitch=hidden
MODEL_LOADING_OPTIMIZATION_VERIFY_OK ... browserBuffers=1 serverLru=bounded autoRetry=2
MODEL_VIEWER_STABILITY_VERIFY_OK centerDrift<1e-6 rotation30=0.360000 rotation120=0.360000
HTTPS_INTEGRATION_VERIFY_OK models=106 files=20
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1050
```

`npm run build:all` 通过。全项目类型检查仍被 7 个既有维修计划 `three-types.ts` 非法字符错误阻塞，本轮未修改这些无关历史文件。
