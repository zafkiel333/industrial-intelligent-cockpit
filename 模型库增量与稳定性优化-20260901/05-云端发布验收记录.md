# 云端发布验收记录

> 正式 Release：`20260901-093430`
> 部署对象：仅场景库服务器 `47.122.104.52`  
> 当前状态：已上线；主项目服务器未登录、未修改

## 发布包

```text
运行代码提交：07005f60131f8a202d6de1c257d098bfb2ad9ed0
发布包源码提交：aa244232d0f937e6ba2a7f6405873d6fc90b8bae
发布包：scene-library-release-20260901-093430.tar.gz
大小：39,995,208 字节
条目：326
SHA-256：6dcaa849b8807be3747b11c813b262ec3307e7f58947a174a9bf00d21a043b25
安装目录：/nlplabProj/scene-library/releases/20260901-093430
```

## 发布前基线

```text
current=/nlplabProj/scene-library/releases/20260828-145036
service=active/enabled
NRestarts=0
MemoryCurrent=469,688,320 bytes
MemoryPeak=474,189,824 bytes
listen=127.0.0.1:3102
3103=空闲
主机内存=1.6 GiB，无 Swap
磁盘使用率=50%
```

## 3103 隔离预检

预检期间旧生产保持 `active` 且健康版本仍为 `20260828-145036`。结果：

```text
health.version=20260901-093430
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=102 base=http://127.0.0.1:33103/api
eq-0=7,665,980 bytes，有效 FBX
eq-15=8,192,944 bytes，有效 FBX
eq-16=21,846,688 bytes，有效 FBX
eq-17=22,175,856 bytes，有效 FBX
turbine-blade-erosion=8,679,052 bytes，有效 FBX
TurbineRunnerHoistingTraining=1,135,500 bytes，有效 FBX
连续 10 模型后缓存=8 项/114,535,408 bytes，上限=8 项/134,217,728 bytes
```

预检进程和 SSH 隧道均已停止，3103 无残留。

## 正式切换

切换前保存旧 `current`、`previous` 和 `/etc/scene-library-next.env`，脚本为链接、环境、服务和入口失败设置自动回滚。本次一次通过：

```text
current=/nlplabProj/scene-library/releases/20260901-093430
previous=/nlplabProj/scene-library/releases/20260828-145036
APP_VERSION=20260901-093430
RELEASE_VERSION=20260828-145036
```

systemd 在计划内 restart 时记录旧进程 `status=143`，随后立即启动新进程；这不是新版崩溃。新服务 `NRestarts=0`。

## 公网验收

```text
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=102 base=https://47.122.104.52/scene-library-api
standalone=HTTP 200
eq-15 embedded=HTTP 200
microapp HTML/JS=HTTP 200
公开 health=HTTP 200，version=20260901-093430
HTTP 8081 compatibility=HTTP 200
关键 6 模型完整下载=HTTP 200，字节与预检一致，全部为有效 FBX
modelMemoryCache=8 项/80,564,628 bytes，上限=8 项/134,217,728 bytes
service=active/enabled
NRestarts=0
listen=127.0.0.1:3102
3103=stopped
nginx -t=successful
```

公网验证后的服务内存 `MemoryCurrent=369,889,280`、`MemoryPeak=398,376,960` 字节；其中已含 102 页 Bootstrap 和 6 个完整模型请求。修复前旧服务约为 `469,688,320/474,189,824` 字节，且旧逻辑会随历史缓存继续增长。

## 回滚

直接回滚目标为 `20260828-145036`。发布包、旧 Release、共享 data/model-cache 和环境备份均保留。回滚时必须一起恢复 `current`、环境版本并重启服务，再复核内部和公网健康；不得删除共享缓存。
