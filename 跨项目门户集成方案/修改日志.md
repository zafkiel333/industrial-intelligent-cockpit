# 工业智能驾驶舱跨项目门户集成 —— 修改日志

> 对应计划：[`开发计划.md`](./开发计划.md)。
> 记录规则：按时间倒序记录实际修改；不得把计划中的待办事项写成已经完成。
> 当前有效方案以最新一条记录及双方最新交付说明为准；2026-08-17 的 qiankun 部署记录现作为历史记录保留。

---

### [2026-08-18] 完成仿真前四页模型详情宿主导航适配

- 状态：场景库侧代码已完成并通过 standalone、microapp 双构建；主平台仍需按《主平台侧代码资料与内标签页适配说明-20260818.md》第 6.2 节在 iframe 宿主页接入消息监听后联合验证。
- 最新集成事实：主平台当前使用 iframe 直连场景库独立版，不采用 qiankun；本条结论替代 2026-08-17 记录中的 qiankun 正式集成判断。
- 严格范围：只修改仿真分析前四页共用的两处“查看资源详情”入口，覆盖模型 2326、2328、2316、2310；其他场景库模型链接和主平台页面不受影响。
- 跳转协议：新增 `src/integration/hostModelNavigation.ts`，嵌入模式向宿主发送 `scene-library:navigate` 协议消息，固定目标为 `/three-model/detail`，并校验模型 ID、模型名称及详情地址一致性。
- 开关适配：场景库只表达导航意图；主平台统一执行 `router.push({ path, query })`。内标签开关开启时由 `MultipleTabs` 自动新增/激活标签，关闭时退化为当前浏览器标签内的普通同页路由跳转。
- 旧行为移除：`ProjectConnectionMap.tsx` 与 `ConnectionDetailDrawer.tsx` 中的 `_blank` 链接已改为统一按钮处理器，不再新建浏览器标签。
- 独立模式：场景库不在 iframe 中时使用 `window.location.assign`，在当前浏览器标签直接进入完整模型详情地址。
- 失败处理：宿主拒绝、导航失败或 5 秒无回执时显示页面错误提示并记录日志，不回退到 `_blank`；等待期间禁用两个入口，避免重复请求。
- 安全限制：只允许 `/three-model/detail` 及四个固定模型；发送目标 Origin 优先从父页面 Origin/`document.referrer` 获取，响应同时校验父窗口、Origin、协议版本和 requestId。
- 已知行为：主平台场景库路由当前 `ignoreKeepAlive: true`，从模型详情返回“场景库”标签时 iframe 会重新加载；本次未扩大范围修改主平台缓存策略或动态标签标题。
- 修改文件：
  - `src/integration/hostModelNavigation.ts`
  - `components/remote-model-showcase/ProjectConnectionMap.tsx`
  - `components/remote-model-showcase/ConnectionDetailDrawer.tsx`
  - `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - `跨项目门户集成方案/修改日志.md`
- 验证结果：`npm run build:all` 成功，standalone 与 microapp 构建均通过；`git diff --check` 通过。全仓库 `tsc --noEmit` 仍被既有 `components/Maintenance-plan-management/*/three-types.ts` 非法字符错误阻断，本次未修改这些文件。

### [2026-08-17] 完成场景库微前端集成版部署并形成主项目专项交付文档

- 状态：场景库微前端集成版及其 API 已部署并完成源站核查；主项目侧的同源代理、菜单、权限、Vue Router 容器和 qiankun 挂载仍由主项目团队按新说明实施。
- 当前版本：发布版本为 `20260817-154326`，协议版本为 `1.0`；微应用入口为 `/microapps/scene-library/`，API 前缀为 `/scene-library-api/`，源站为 `47.122.104.52:8081`。
- 后端切换：新 `scene-library.service` 已设置为 `enabled`、`active`，仅监听源站 `127.0.0.1:3102`；旧 `cockpit.service` 已 `disabled`、`inactive`，旧端口 `3101` 已关闭。
- 已完成验证：微应用 HTML、JavaScript、CSS、API 健康检查、仪表盘、诊断和模型下载均返回 HTTP 200；健康接口返回版本 `20260817-154326`。
- 主项目交付边界：只交付 qiankun 微应用和场景库 API；本项目自用独立版不进入主项目配置、开发、部署或验收范围，正式集成不使用 iframe。
- 新增交付手册：`主项目微前端集成与运维交付手册-20260817.md`，记录已部署制品、校验值、生命周期、props、页面 ID、API、服务器状态、运维检查、安全限制和验收清单。
- 新增主项目说明：`主项目场景库调整开发与部署说明-20260817.md`，指导 C-Y-D-G 主项目完成菜单/权限、Vue Router、qiankun 容器、路由同步、主题、同源 Nginx、发布、验收、故障定位和回滚。
- 旧方案替代：既往文档中的“同机部署”前提、内部端口 `3101`、生命周期待实现、双构建脚本缺失、旧主题对象和主项目独立版地址配置均不再作为当前实施依据；地址、版本和协议以两份 2026-08-17 交付文档为准。
- 已知限制：微应用 JavaScript 约 17.1 MiB 且使用固定文件名；当前依赖 `https://cdn.tailwindcss.com`；API 需由主项目网关补充鉴权和审计；源站 `8081` 的公网暴露应收敛；历史 TypeScript 非法字符问题不影响本次 Vite 构建，但全量类型检查尚未通过。
- 运维待办：按用户要求暂不清理本次部署使用的临时 SSH 密钥；该事项需进入后续安全变更窗口，并且不得在交付文档中记录私钥内容。
- 本次文档：
  - `跨项目门户集成方案/主项目微前端集成与运维交付手册-20260817.md`
  - `跨项目门户集成方案/主项目场景库调整开发与部署说明-20260817.md`
  - `跨项目门户集成方案/修改日志.md`

### [2026-08-13] 将服务器部署指导简化为可复制执行手册

- 状态：部署文档已重写为面向不了解项目的操作手册；未修改业务代码、依赖、构建产物、Nginx 或生产服务器。
- 简化方式：固定默认目录、端口、服务名和公开路径，把首次部署压缩为“上传包、检查环境、建目录、安装版本、环境变量、systemd、Nginx、验证”八个顺序步骤。
- 命令完善：为首次部署、后续更新、版本回退、日志、端口和磁盘检查提供可直接复制的命令；仅服务器账号、IP、域名、主平台 Nginx 文件和回退版本号需要替换。
- 安全边界：Express 使用 `127.0.0.1:3101`，模型缓存和上传数据位于 `/var/lib/scene-library`，发布采用独立版本目录与 `current` 软链接，禁止覆盖主平台 `dist`。
- 数据持久化：补充 `SCENE_DATA_DIRECTORY=/var/lib/scene-library/data` 开发前置要求，防止当前写在 `src/data` 的上传数据随版本发布或回退丢失。
- API 统一：明确同服务器部署时 standalone 和 microapp 都使用 `/scene-library-api/`，避免独立版根 `/api/` 误入主平台后端。
- 修改文件：
  - `跨项目门户集成方案/本项目服务器端前后端部署指导.md`
  - `跨项目门户集成方案/开发计划.md`
  - `跨项目门户集成方案/工业智能驾驶舱项目开发指导说明.md`
  - `跨项目门户集成方案/修改日志.md`

### [2026-08-13] 改为同服务器分路径部署与 qiankun 微前端方案

- 状态：该目录全部现行方案文档已更新；新增服务器部署指导；尚未修改主平台或本项目业务代码、依赖、构建配置和生产服务器。
- 用户确认的新方向：两个项目部署在同一服务器但不合并源码，主平台通过自身路由进入“场景库”，使用 qiankun 在内容区挂载本项目 React 页面，正常流程不使用 iframe。
- 总计划：重写 `开发计划.md`，将 qiankun 从二期备选提升为正式方案；重新定义总体架构、固定路径、双方职责、实施阶段、验收、风险和回退。
- 主平台指导：重写 `主平台项目开发指导说明.md`，明确“场景库”菜单、`/scene-library`、qiankun 容器、props 契约、装卸载、权限、错误和最小影响要求。
- 本项目指导：重写 `工业智能驾驶舱项目开发指导说明.md`，明确 standalone/microapp 双模式、生命周期、紧凑导航、样式隔离、API Base、双构建、Express 和资源释放。
- 新增部署文档：`本项目服务器端前后端部署指导.md`，包含目录与端口规划、构建产物、Express 环境变量、systemd、同域 Nginx 完整示例、缓存、安全、发布、验证、监控和回退。
- 统一默认路径：
  - 主平台路由：`/scene-library`；
  - 微应用入口：`/microapps/scene-library/`；
  - 独立入口：`/cockpit/`；
  - API 前缀：`/scene-library-api/`；
  - Express 内部路由继续使用 `/api/`。
- 名称边界：主平台显示“场景库”；本项目独立入口保持“工业智能运维驾驶舱”；微应用技术标识保持 `industrial-intelligent-cockpit`。
- 兼容与影响：两个项目继续独立代码库、独立构建、独立发布和独立回退；iframe 只保留为重大兼容问题下的临时应急方式，不进入正常实现。
- 部署事实说明：当前 `package.json` 尚无 `build:standalone`、`build:microapp`，当前代码也尚未实现 qiankun 生命周期和 `/api/health`；部署文档已明确这些命令和接口需在开发完成后才能使用。
- 影响面：本次仅修改 `跨项目门户集成方案` 中 Markdown 文档。

### [2026-08-13] 明确主平台展示名称为“场景库”

- 状态：开发指导和总计划中的名称边界已更新；未修改任何业务代码、路由实现、接口、构建产物或部署配置。
- 主平台展示：一级菜单、默认页面标题、标签页和面包屑统一使用“场景库”。
- 本项目展示：独立入口继续使用“工业智能运维驾驶舱”及现有品牌文案，不进行批量改名。
- 技术契约：`industrial-intelligent-cockpit`、`/industrial-cockpit`、嵌入路径、API 前缀和消息协议保持不变，避免展示名称调整扩大到代码与部署层。
- 标题协调：本项目只上报内部业务页面标题；主平台需要展示细分页面时可使用“场景库 / 当前页面标题”，不得修改一级菜单名称。
- 修改文件：
  - `跨项目门户集成方案/主平台项目开发指导说明.md`
  - `跨项目门户集成方案/工业智能驾驶舱项目开发指导说明.md`
  - `跨项目门户集成方案/开发计划.md`
  - `跨项目门户集成方案/修改日志.md`

### [2026-08-13] 按两个项目职责拆分独立开发指导说明

- 状态：两份开发指导文档已建立；未修改任何业务代码、依赖、构建产物、接口或部署配置。
- 用户要求：把总体开发计划划分为两个项目各自可执行的说明，使双方开发人员无需二次沟通即可完成职责内开发，同时把影响范围控制到最小。
- 新增文件：
  - `跨项目门户集成方案/主平台项目开发指导说明.md`
  - `跨项目门户集成方案/工业智能驾驶舱项目开发指导说明.md`
- 主平台说明覆盖：开发原因、驾驶舱技术情况、冻结契约、菜单与 Vue Router、原生容器、运行配置、地址同步、主题、错误降级、postMessage、同源网关、缓存/大文件、安全、权限、最小影响、阶段和验收。
- 驾驶舱说明覆盖：开发原因、主平台技术情况、冻结契约、最小外壳改造、standalone/embedded、原导航紧凑化、页面 ID同步、postMessage、主题、API Base、双构建、Express/安全、资源释放、兼容降级、阶段和验收。
- 协调约定：两份文档统一使用 `industrial-intelligent-cockpit`、协议 `1.0`、主路由 `/industrial-cockpit`、参数 `view`、默认页 `smart-ops`、嵌入前端 `/integrations/cockpit/embed/` 和 API `/integrations/cockpit-api`。
- 最小影响原则：主平台以新增菜单/路由/容器/代理为主，不改已有模型管理和全局布局；本项目以新增嵌入外壳、紧凑导航、bridge、API Base 和双构建为主，独立外壳和业务页面保持。
- 总计划调整：在 `开发计划.md` 顶部增加两份开发指导的入口和共同契约说明。
- 影响面：本次仅新增和修改 `跨项目门户集成方案` 内 Markdown 文档。

### [2026-08-13] 调整为保留本项目原导航结构的紧凑集成方案

- 状态：开发计划已调整，等待用户继续确认；未修改任何业务代码、依赖、构建产物或部署配置。
- 用户要求：进入主平台后仍保留本项目原有导航结构，只对显示长度进行限制。
- 方案调整：
  - 集成版从“隐藏本项目导航、主要使用场景抽屉”调整为“主平台全局导航 + 本项目紧凑模块导航”；
  - 本项目继续复用同一份 `MENU_ITEMS`、递归父子结构、页面 ID、数量徽标、展开状态和选中状态；
  - 嵌入模式只去除重复品牌区和底部状态区，不删除任何业务栏目或页面入口；
  - 导航大屏宽度 220px、中等宽度 200px、折叠宽度 52～56px；
  - 菜单名称最多两行并提供完整名称提示，每个分组默认显示 8 项，支持展开剩余项；
  - 导航高度固定并独立滚动，当前页面自动展开和定位；低于 1600px 时默认折叠并可覆盖式展开；
  - 主平台默认只增加一个“智能运维驾驶舱”入口，不复制本项目全部导航到主平台菜单。
- 开发计划同步修改：最终效果示意、导航处理、路由职责、双外壳、工具条、响应式规则、预计文件、阶段任务、验收和风险。
- 影响面：本次只修改 `跨项目门户集成方案/开发计划.md` 和本修改日志。

### [2026-08-13] 明确由主平台导航直接路由进入驾驶舱

- 状态：开发计划已调整，等待用户继续确认；未修改任何业务代码、依赖、构建产物或部署配置。
- 用户想法：两个项目均有导航栏，希望集成后直接使用主平台导航路由进入本项目。
- 方案结论：
  - 采纳“主平台导航接管”思路；
  - 主平台菜单指向自身 `/industrial-cockpit` Vue 路由，不直接填写本项目 IP，也不执行整页外链跳转；
  - 主平台路由打开原生容器页，再加载本项目 `embedded` 模式；
  - 集成版隐藏本项目原顶栏和永久侧栏，独立版继续完整保留；
  - 可在一级菜单下配置少量稳定二级快捷入口，通过 `?view=<页面ID>` 直接定位；
  - 不把全部叶子页面复制进主平台菜单，全部目录继续由搜索、最近访问、收藏和场景抽屉承担。
- 开发计划新增：导航接管定义、直接路由时序、双模式导航边界、快捷入口示例、静态/动态菜单适配、地址同步规则和阶段验收标准。
- 影响面：本次只修改 `跨项目门户集成方案/开发计划.md` 和本修改日志。

### [2026-08-12] 基于两个线上项目重新制定集成计划

- 状态：新计划已完成，等待用户确认；未修改任何业务代码、依赖、构建产物或服务器配置。
- 用户要求：更换环境后重新执行线上调查，不沿用上一版计划，从零给出更美观、和谐、方便且兼容性更好的合并方案。
- 线上核查：
  - `http://8.146.211.204:3100/three-model/overview` 可访问，确认主平台采用 Vue 3、Vue Router、Pinia、Ant Design Vue 和 Vite；
  - 主平台已有 iframe 页面机制，安装了 qiankun 相关依赖并预留微前端容器；
  - 主平台当前 `3100` 服务实际为 Vite 开发服务，正式集成前应改为生产构建并关闭公网开发源文件访问；
  - `http://47.122.104.52:8081/` 可访问，确认本项目生产页面由 Nginx 提供；
  - 核对本地 `package.json`、`index.html`、`App.tsx`、`components/Sidebar.tsx` 和 `server.ts`，确认 React 19 + Vite + Express、Tailwind CDN、根路径 API、大量页面状态和 3D 资源等现状。
- 重写文件：`跨项目门户集成方案/开发计划.md`。
- 新计划结论：
  - 首期采用“主平台原生页面壳 + 同源反向代理 + 本项目嵌入模式 + 版本化页面通信”；
  - 主平台只增加一个一级菜单，本项目大量页面由场景抽屉、搜索、最近访问和收藏承载；
  - 本项目使用单代码库、standalone/embedded 两个构建目标，保留原独立部署；
  - Express 后端继续独立运行，由主平台网关代理固定 API 前缀；
  - qiankun 作为完成 CSS、生命周期和 3D 资源治理后的二期升级，而非首期直接启用。
- 文档已覆盖：线上现状、最终效果、方案比较、前后端边界、双外壳、导航、路由同步、通信协议、主题、网关、权限、安全、双模式部署、qiankun 升级、预计文件、实施阶段、验收、风险和回退。
- 影响面：本次仅修改计划与修改日志，没有修改本项目页面、接口、3D、部署或主平台代码。

### [2026-08-12] 建立跨项目门户集成与双模式部署计划

- 状态：计划已建立，等待用户确认；未修改业务代码。
- 新增文件：
  - `跨项目门户集成方案/开发计划.md`
  - `跨项目门户集成方案/修改日志.md`
- 调查范围：
  - 核对 `package.json`，确认本项目为 React 19 + TypeScript + Vite 前端及 Node.js + Express 后端；
  - 核对 `App.tsx`，确认当前由 `activeTabId` 选择页面，并直接组合侧栏、顶栏和业务内容；
  - 核对 `components/Sidebar.tsx` 和 `constants.tsx`，确认导航项数量多、采用递归展开且菜单数据集中维护；
  - 核对前端 API 调用，确认存在多个根路径 `/api/...` 请求，子路径部署前需要集中适配；
  - 核对现有部署说明，确认生产环境必须保留 `server.ts` 和相关 `src`，不能仅复制 `dist`。
- 计划结论：
  - 推荐“同源反向代理 + iframe 页面容器 + 单代码库双外壳模式”；
  - 独立入口继续显示现有完整平台外壳；
  - 嵌入入口隐藏重复顶栏，并使用紧凑、可折叠、默认限量 8 项和可搜索的内部导航；
  - 保留本项目后端服务边界，由宿主网关转发固定子路径；
  - 不推荐首次接入就复制全部源码或使用复杂微前端。
- 文档内容：已记录方案比较、前后端路径、宿主改动、双模式部署、导航限制、视觉融合、安全、预计文件、阶段、验收、风险和回退方案。
- 影响面：没有修改现有前端、后端、依赖、构建产物、接口、导航或部署配置。
- 下一步：用户确认总体方案，并提供对方项目代码/技术栈、导航位置、域名、代理能力和登录方式后，再开始业务代码修改。
