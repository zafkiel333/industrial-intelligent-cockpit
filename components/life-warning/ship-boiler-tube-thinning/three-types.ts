export interface BoilerTubeState {
  steamPressure: number; // MPa
  exhaustGasTemp: number; // Celsius
  feedwaterPh: number; // pH
  tubeThickness: number; // mm
  operatingHours: number; // hours
}
