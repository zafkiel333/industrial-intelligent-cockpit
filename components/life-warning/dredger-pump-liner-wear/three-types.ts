export interface DredgerPumpState {
  slurryDensity: number; // kg/m3
  flowVelocity: number; // m/s
  particleSize: number; // mm
  linerThickness: number; // mm
  operatingHours: number; // hours
}
