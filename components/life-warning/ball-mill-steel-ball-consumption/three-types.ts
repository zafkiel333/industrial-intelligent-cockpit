export interface BallState {
  oreHardness: number; // Bond Work Index (kWh/t)
  millSpeed: number; // % of critical speed
  slurryDensity: number; // % solids
  ballWearRate: number; // g/t
  operatingHours: number; // hours
}
