export interface WireRopeState {
  hoistLoad: number; // t
  ropeSpeed: number; // m/s
  bendingCycles: number; // count
  brokenWires: number; // count per lay length
  operatingHours: number; // hours
}
