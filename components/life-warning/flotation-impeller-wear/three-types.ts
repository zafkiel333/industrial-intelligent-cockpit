export interface ImpellerState {
  slurryDensity: number; // % solids
  particleSize: number; // mm (P80)
  rotationSpeed: number; // RPM
  wearDepth: number; // mm
  operatingHours: number; // hours
}
