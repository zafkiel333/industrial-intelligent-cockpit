# 分区响应时延与阈值诊断版本

Release：20260910-003316。阶段：已切换并完成公网验收。

## 修改范围

- 水轮机页面的文件上传解析、历史入库、数据读取、预测、验证、下载准备、基础信息、三维资源和手动刷新测量响应时延。
- 按用户最新要求，各环节只在对应区域显示T_start、T_end和Δt，不设统一总览。
- 文件选择完成即自动上传并解析，一个计时段覆盖至预检显示；查看预检及等待确认不计入，最终确认后再测入库/验证响应。
- 结束点在结果DOM就绪并经过浏览器绘制机会后；失败、中止不标成功。时间使用统一浏览器单调时钟，起止差值与显示毫秒一致。
- 删除、重置及其自动刷新不新建计时；后台轮询不覆盖手动测量。下载测量只到文件准备并交给浏览器，不包含保存位置选择或磁盘写入。
- 阈值诊断模型HD-TD v1.0.0对应实际运行的水轮机阈值与故障关联规则，显示于预警诊断区域，新预测与报告携带版本；旧快照不重写。
- 实现口径见外部模型数据上传与预测诊断-20260903/12-分区响应时延与诊断版本说明.md。

## 产物

- 基准Git 5870ee02b123bbdbe379c018ff5439557fbafdea，工作区快照。
- 包名scene-library-release-20260910-003316.tar.gz，54,329,056字节，485条目。
- SHA256：0CEB2C12F05CA7DE7DFF884BF6E1C36D2578815623FE754B4A460EE434B857A4，远端与本地一致。
- 双构建、定向TypeScript、diff格式检查通过。

## 本地验证

- verify-response-timing-ui.mjs：起止差值一致、200ms注入延迟被包含、700ms确认停留被排除、自动上传解析、入库、预测、验证、下载、失败提示与删除不触发时延通过。
- SCL解析和IEC61850自动上传预检实际流程通过，重新选择/切换来源清除旧时延，过期SCL读取不覆盖新摘要。
- 预测与验证时延分别位于对应面板，无全局总览；1600/390视口与截图检查通过。
- 原统一上传/预测/验证API回归、浏览器闭环、52项下载和32份协议样例通过。
- 所有写入验收使用临时数据目录，不操作生产设备数据。

## 生产阶段

- 3103隔离预检通过：四试点模型完整读取，正常HT-01/异常HT-41导入、预测、核验及6种结果/报告下载通过。
- current=20260910-003316；previous=20260909-201950；未触发回滚。
- 环境备份：/etc/scene-library-next.env.before-20260910-003316。
- 切换前后生产设备state文件哈希一致；旧预测快照、核验和累计统计保留，未在生产执行测试写入。
- 公网只读时延检查通过：设备数据读取与三维显示完成时延、T_start/T_end/Δt算术、无统一总览、HD-TD v1.0.0版本显示正常。
- 公网52项下载及32份协议样例检查通过。
- 公网四试点三维模型实际渲染成功，时延条未造成视窗高度塌陷或脚本异常。
- 服务active，NRestarts=0，正式监听127.0.0.1:3102；3103预检已停止。
