# 云端更新执行记录：数据工作区视觉升级

> 执行日期：2026-09-04（Asia/Shanghai）  
> Release：`20260904-163312`  
> 最终状态：已上线并完成发布后独立缺陷检查  
> 范围：仅场景库服务器 `47.122.104.52`；未登录、未修改主平台服务器

## 1. 发布内容

- 四个试点页的下载格式由原生下拉框改为可直接点击的选项，主标题直接显示 `.pdf`、`.docx`、`.csv`、`.xlsx`、`.json`、`.zip` 后缀，次标题使用查看、编辑、导入、系统对接和全格式打包等直观用途说明。
- 下载弹窗增加完整文件名预览；设备 ID 与格式变化会即时更新，预览名称和最终下载名称保持一致。
- 数据工作区重构为与周边一致的浅工业蓝灰界面，形成标题和链路状态、六项数据指标、运行趋势与预测、健康诊断、故障概率及批次记录的清晰层级。
- 上传、下载、危险操作确认弹窗统一控件和状态色；原有上传、解析、增加、覆盖、冲突处理、重置和删除逻辑不变。
- 响应式布局针对 1440 桌面和 390 移动视口分别调整，移动端操作区、指标区、图表和格式选项均无横向溢出。
- 仍只覆盖水轮机 `2326`、污水泵 `2328`、桥式起重机 `2316`、矿卡/牵引车 `2310` 四个试点页，没有推广到其余 102 页。

## 2. 本地验证

```text
npm run build:all=通过
npm run typecheck:navigation=通过
npm run verify:navigation=通过，roots=20 nodes=1050
npm run verify:model-loading=通过
npm run verify:model-stability=通过
git diff --check=通过
Playwright 只读=4 页 × 桌面/移动通过
Playwright 完整模式=上传、解析、批次落表、危险操作取消焦点通过
下载交互=逐格式后缀、选中态、文件名预览、16 次实际下载名称通过
截图=桌面/移动工作区、下载弹窗、上传弹窗通过
```

依赖清单没有因本轮视觉改造发生变化。`npm audit --omit=dev --audit-level=low` 本轮因 npm 仓库服务端长时间无响应主动终止；上一 Release 的相同锁文件生产依赖审计为 0 个漏洞。

## 3. 发布包

```text
Release=20260904-163312
sourceCommit=04209ba1eda3a74ff54e0010befda078bfb6ac6b
工作区=dirty，按用户要求发布当前工作区快照；未清理或覆盖其他改动
package=scene-library-release-20260904-163312.tar.gz
bytes=53,754,040
entries=319
SHA-256=A42E766BA6ED54D6656C931B372ECBF278F62A14B60BF55CB7B3AF4913DC31CA
menuVersion=20260904-163312
menuContentSha256=2df4367183435500d65a9ea095a6d78d9ea6ba0c1e488b097ee8f9f9b139ca2d
manifest SHA-256=8A556DAF3E9AA5239F14C97ACC02CF6DD154AD76E56313BABBDB905ED3A3B1D2
```

发布包由仓库脚本生成，包含 standalone、microapp、服务器运行源码、npm 清单和固定 SheetJS 包，不包含 `.git`、`node_modules` 或环境秘密。远端包字节数和 SHA-256 与本地完全一致后才解压。

## 4. 云端基线与隔离预检

切换前现场状态：

```text
current=20260904-152456
previous=20260904-131956
APP_VERSION=20260904-152456
RELEASE_VERSION=20260904-131956
service=active/enabled
NRestarts=0
listen=127.0.0.1:3102
3103=空闲
磁盘=40 GiB，已用 21 GiB，剩余 17 GiB，使用率 55%
nginx -t=successful
shared/data=cockpit:cockpit
shared/model-cache=cockpit:cockpit
```

新 Release 以 `cockpit` 用户安装 413 个包，使用 Node 22.23.2 和 tsx 4.23.12。随后从新 release 工作目录在 `127.0.0.1:3103` 启动，并使用 `/tmp/scene-library-preflight-data-20260904-163312` 隔离生产数据。四模型概况、四类代表下载、新 CSS 选择器和文件名预览构建标记均通过。预检期间 `current` 和 3102 生产服务不变，结束后 3103 已停止。

## 5. 正式切换

脚本现场解析旧 `current`，将其设为直接回滚目标，备份环境文件，原子更新 `previous/current` 和版本字段，重启 `scene-library.service`。内部健康版本、Nginx standalone、microapp、微应用脚本和公开健康接口均通过；本次一次切换成功，没有触发自动回滚。

```text
current=/nlplabProj/scene-library/releases/20260904-163312
previous=/nlplabProj/scene-library/releases/20260904-152456
APP_VERSION=20260904-163312
RELEASE_VERSION=20260904-152456
health.version=20260904-163312
service=active/enabled
NRestarts=0
```

## 6. 发布后独立缺陷检查

```text
公网下载矩阵=4 模型 × 13 种格式，52/52 通过
中文命名=模型 ID、中文模型名、设备 ID、中文资料名和后缀全部通过
Playwright=4 页 × 1440/390 视口只读回归通过
格式交互=后缀、用途说明、选中态、文件名预览和实际下载名称通过
视觉检查=生产桌面工作区、移动下载弹窗无遮挡且无横向溢出
HTTPS=健康接口、standalone、microapp、微应用脚本均 HTTP 200
HTTP 8081=健康接口、standalone、microapp 均 HTTP 200
生产数据=四页均为 120 条/1 台设备/1 个批次，检查前后未变化
服务=active/enabled，NRestarts=0，3102 仅监听 127.0.0.1，3103 已停止
journal=近 15 分钟无 error 级记录
```

Nginx error log 中同日记录只涉及同机的 `threeDemo` 虚拟主机及公网扫描请求，不涉及场景库路径、release、3102 或 3103，本轮场景库验收未发现新增缺陷。

## 7. 回滚与边界

直接回滚目标为 `20260904-152456`。旧 release、环境备份、共享数据和共享模型缓存均保留；代码回滚不自动回滚或删除数据。不得删除或覆盖 `/nlplabProj/scene-library/shared/data` 与 `shared/model-cache`。

本次没有登录、修改或部署主平台服务器 `8.146.211.204`。菜单结构和模型范围未改变；新生成 manifest 仅同步 Release 版本标识，主平台是否导入仍由主平台团队决定。
