# 云端更新执行记录：模拟维修与运维知识视觉修复

> 执行日期：2026-08-21
> 发布服务器：场景库 `47.122.104.52`
> 发布结果：已完成生产切换和技术验收

## 1. 正式发布信息

| 项目 | 值 |
|---|---|
| Release ID | `20260821-154225` |
| 源码提交 | `a6d867ef4e867206badfe96167f8216f6717c7d3` |
| 发布包 | `scene-library-release-20260821-154225.tar.gz` |
| 发布包大小 | `39,952,329` 字节 |
| 发布包条目数 | `324` |
| 发布包 SHA-256 | `eb8337a2c57beed6c43ebe503dc8b51367cdd120f4a2d08c528f70f7576bfb03` |
| 菜单版本 | `20260821-154225` |
| 菜单根节点/总节点 | `20 / 1049` |
| 菜单内容 SHA-256 | `b7de7be4e41977d0e9fdfc75c204ff93086d4a33b57f9c33e0351cd1fa05b021` |
| 菜单清单文件 SHA-256 | `2d4c0952ca94880cde84526bff2766cc37ab374bc813da63e780024de8e98f84` |

本次只部署场景库服务器，没有登录、修改或发布主平台服务器 `8.146.211.204`。

## 2. 本次发布范围

- “模拟维修服务”实际路由 `mm-01` 至 `mm-46` 共 46 个 Three.js 页面统一使用浅工业蓝灰背景 `#E8F1F6`；
- 同步修正上述页面 49 处 `THREE.Fog`/`THREE.FogExp2`，防止切换工况后恢复深色；
- “运维知识管理”实际创建 Three.js 渲染器的 32 个页面统一使用 `#E8F1F6` 背景及同色雾效；
- 仅在 `ops-knowledge` 和 `km-*` 页面增加 `knowledge-management-theme` 栏目作用域；
- 修复该作用域内按钮在常态、悬停、选中、告警和禁用状态下的浅底浅字问题，不改动其他栏位按钮；
- 不改变三维模型几何、材质、灯光、动画、业务数据、菜单、路由、模型库通信协议或 API。

## 3. 本地验证证据

依赖安装、正式菜单清单生成和独立版/微应用版双构建均完成。主要专项结果：

```text
KNOWLEDGE_MANAGEMENT_VISUALS_OK scenes=32 fogs=32 color=#E8F1F6 buttons=state-paired
SIMULATED_MAINTENANCE_BACKGROUND_OK scenes=46 fogs=49 color=#E8F1F6
SCENE_BACKGROUND_VERIFY_OK scenes=305 fogs=124 color=#315268
MAINTENANCE_PLANNING_REDESIGN_OK pages=10 kpis=40 signals=40 stages=50
LIFE_WARNING_REDESIGN_OK pages=10 kpis=40 signals=40 mechanisms=30 thresholds=30 actions=40
CV_MONITORING_IMAGES_OK pages=6 assets=6 randomSources=0 baseUrl=verified bytes=16316892
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1049
HTTPS_INTEGRATION_VERIFY_OK models=4 files=17
```

- 导航专项 TypeScript 检查通过；
- `git diff --check` 通过；
- 独立版构建转换 5528 个模块，微应用版构建转换 5523 个模块；
- 全量 `npm run typecheck` 仍仅被项目既有 7 个 `components/Maintenance-plan-management/*/three-types.ts` 非法字符文件阻断，本次修改没有新增类型错误；
- 安装依赖时出现 `esbuild` 安装脚本未列入 `allowScripts` 的 npm 警告，与既有发布一致，`tsx` 运行及双构建均正常。

## 4. 上传、安装与独立端口预检

- SSH 使用既有 Ed25519 私钥并强制公钥认证；
- 上传到 `/tmp/scene-library-release-20260821-154225.tar.gz` 后，远端 SHA-256 与本地完全一致；
- 安装目录为 `/nlplabProj/scene-library/releases/20260821-154225`，属主为 `cockpit:cockpit`，安装后约 504 MB；
- 远端安装 362 个 npm 包，`tsx v4.23.12`、`node v22.23.2` 可正常运行；
- 新版本先按 systemd 等价启动方式监听 `127.0.0.1:3103`，健康检查返回：

```json
{"status":"ok","version":"20260821-154225"}
```

- 预检结束后测试进程已停止，3103 无残留监听；预检期间旧版本始终在线。

## 5. 生产切换结果

切换前：

```text
current  → /nlplabProj/scene-library/releases/20260821-120215
previous → /nlplabProj/scene-library/releases/20260821-112458
```

切换后：

```text
current  → /nlplabProj/scene-library/releases/20260821-154225
previous → /nlplabProj/scene-library/releases/20260821-120215
```

环境文件当前公开字段：

```text
HOST=127.0.0.1
PORT=3102
APP_VERSION=20260821-154225
RELEASE_VERSION=20260821-120215
```

环境文件切换前备份：

```text
/tmp/scene-library-next.env.before-20260821-154225
```

`scene-library.service` 为 `active`、`enabled`；3102 只监听 `127.0.0.1`；Nginx 配置检查通过。内部和公网健康检查均返回：

```json
{"status":"ok","version":"20260821-154225"}
```

## 6. 公网技术验收

以下入口均返回 HTTP 200：

```text
https://47.122.104.52/cockpit/
https://47.122.104.52/cockpit/?embedded=1&viewId=mm-03
https://47.122.104.52/cockpit/?embedded=1&viewId=mm-46
https://47.122.104.52/cockpit/?embedded=1&viewId=ops-knowledge
https://47.122.104.52/cockpit/?embedded=1&viewId=km-dam-seepage
https://47.122.104.52/cockpit/?embedded=1&viewId=km-turbine-cavitation
https://47.122.104.52/microapps/scene-library/
https://47.122.104.52/microapps/scene-library/scene-library.js
https://47.122.104.52/scene-library-api/health
http://47.122.104.52:8081/cockpit/
http://47.122.104.52:8081/cockpit/?embedded=1&viewId=km-dam-seepage
http://47.122.104.52:8081/microapps/scene-library/
http://47.122.104.52:8081/microapps/scene-library/scene-library.js
http://47.122.104.52:8081/scene-library-api/health
```

- 独立版当前主资源为 `/cockpit/assets/index-DLoXx7xG.js`，公网返回 200；
- 公开 JS 和 CSS 均包含 `knowledge-management-theme`；
- 公开 CSS 命中按钮深色文字、常态浅色底、悬停/选中状态等关键规则；
- Three.js 背景颜色编译在 JS 中，不以 CSS 色值是否存在作为背景验收依据。

## 7. 服务与数据安全审计

- systemd 日志显示旧进程在正常重启时以 143 结束，新服务随后正常监听 `127.0.0.1:3102`，没有启动异常；
- `/nlplabProj/scene-library/shared/data` 与 `shared/model-cache` 均保留；
- 未删除发布包、旧 release 或回滚版本；
- 如需立即回滚，目标为 `/nlplabProj/scene-library/releases/20260821-120215`。

## 8. 尚需人工完成的目视验收

当前会话没有可连接的浏览器实例，因此没有把自动资源校验冒充为像素级验收。用户应在目标浏览器按 `Ctrl+F5` 强制刷新后抽查：

- 模拟维修：`mm-01`、`mm-03`、`mm-30`、`mm-44`；
- 运维知识：`km-dam-seepage`、`km-turbine-cavitation` 及一个包含常态、悬停、选中、禁用按钮的代表页面；
- 主平台嵌入模式与独立模式均应确认三维背景为浅色、按钮文字清晰可见。

若仅主平台仍显示旧样式，而上述 HTTPS 独立入口已是新样式，应优先清理浏览器缓存并检查主平台 iframe/微应用资源缓存，不要直接认定场景库回滚。
