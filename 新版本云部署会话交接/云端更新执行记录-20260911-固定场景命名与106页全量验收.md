# 云端更新执行记录：固定场景命名与 106 页全量验收

执行日期：2026-09-11

最终 Release：`20260911-083049`

直接回退 Release：`20260910-194728`

场景库服务器：`47.122.104.52`

主平台服务器：本次未登录、未修改

## 一、发布目标与边界

本轮在外部模型预测诊断能力已经全量推广的基础上，完成固定业务名称保护、全 106 页逐页浏览器验收和正式发布。改动不调整数据格式、预测算法、设备数据或主平台服务，仅强化页面业务标题边界和全页面验收门禁。

场景库导航的 20 个一级分类名称及顺序属于既定业务信息，必须保持为：

1. 工业智能运维
2. 工业产品知识库
3. 运行驾驶舱
4. 运行指数分析
5. 数字化交付
6. 仿真分析
7. 客户数据管理
8. 远程专家服务
9. 预测性维护
10. 应用维修服务
11. 备品备件服务
12. 模拟维修服务
13. 运维知识管理
14. 服务数据管理
15. 设备点巡检
16. 维修计划管理
17. 零部件寿命预警
18. 计算机视觉监测
19. 震动监测
20. 维修培训

第 19 类的业务名称固定为“震动监测”。页面中的“振动幅值”“振动频谱”等属于设备技术指标和专业术语，可按对应模型语义使用；两者作用域不同，不得用技术术语改写导航分类名称。

## 二、实现内容

- 外部模型页面主标题增加本地化保护，始终显示页面配置中的真实业务标题，不再被全局可见文本转换规则二次改写。
- 中文审计新增两项强制断言：20 个一级分类名称与顺序完全一致；外部模型业务标题已经与全局术语规范化逻辑分离。
- 新增 `scripts/verify-model-operational-ui-all.ts`，逐页访问 106 个外部模型页，检查：
  - 页面主体、模型页头和数据预测工作区能够进入可用状态；
  - “导入历史数据”“导入验证数据”“数据管理”三个入口存在；
  - 六项测量概览卡和六张指标时序图完整显示；
  - 阈值诊断模型版本为页面实际配置；
  - 页面标题与配置完全一致，各模型字段名称完整；
  - 不含研发替换、上游错误、捏造、AI 会话或内部实施类公开措辞；
  - 浏览器无页面脚本错误。

## 三、本地验收

以下检查全部通过：

```text
中文文案转换审计：69 个代表性转换通过，20 个一级分类名称保持不变
模型库检查：bindings=102，showcasePages=106，menuNodes=1050
导航检查：roots=20，nodes=1050
standalone / microapp 双构建：通过
预测回归：scenes=106，pairedGroups=318
状态准确率：97.48%
综合结论准确率：97.48%
预测区间点级覆盖率：99.13%
平均归一化 MAE：7.74%
```

全量浏览器脚本在本地开发服务的冷编译阶段有 5 页首次等待超过并发检查窗口；将这 5 页改为单并发复核后均在 4.0～7.4 秒内通过。正式生产环境的 106 页验收结果见第六节，并作为最终结论。

## 四、发布包与菜单清单

正式包：

```text
文件：scene-library-release-20260911-083049.tar.gz
大小：54,350,195 bytes
条目：486
SHA-256：7E1AAD1F90D38B4EE3B0BEC3A210B52CA5F092BAF823138DC095725EBDEDB61F
源代码提交：4e3bd42e76a6decedc366ea8dcec7ccc2aa4460e
```

本地包与服务器 `/tmp` 中上传包的大小、条目数和 SHA-256 完全一致。

菜单清单：

```text
menuVersion：20260911-083049
一级节点：20
totalNodeCount：1050
menuContentSha256：2df4367183435500d65a9ea095a6d78d9ea6ba0c1e488b097ee8f9f9b139ca2d
sourceCommit：4e3bd42e76a6decedc366ea8dcec7ccc2aa4460e
```

菜单内容哈希与上一版一致，说明本轮没有改变导航树内容、节点数量或顺序；更新的是发布版本信息和业务名称保护门禁。

## 五、隔离预检与正式切换

Release 安装后先在 `127.0.0.1:3103` 启动隔离预检，检查结果：

- 健康检查版本为 `20260911-083049`；
- 四个试点页面的 Bootstrap 和完整 FBX 下载通过；
- 水轮机预测汇总接口通过；
- `HT-01` 正常数据和 `HT-41` 异常数据完成预测、状态判断与验证；
- 每组生成 2160 个预测点，验证覆盖率为 1；
- 预测结果与分析报告的规定格式下载通过。

预检通过后执行正式切换，部署脚本确认共享数据切换前后未改变。切换完成状态：

```text
current  -> /nlplabProj/scene-library/releases/20260911-083049
previous -> /nlplabProj/scene-library/releases/20260910-194728
scene-library.service = active / enabled
NRestarts = 0
监听 = 127.0.0.1:3102
health.version = 20260911-083049
3103 隔离预检端口已关闭
```

## 六、发布后全量验收

生产 API 审计：

```text
scenes = 106
issues = []
```

生产公网浏览器逐页验收：

```text
MODEL_OPERATIONAL_UI_ALL_OK
scenes = 106
concurrency = 3
通过 = 106 / 106
最慢页面 = sim-visual-hydro-turbine，2.868 秒
其次 = sim-visual-wastewater-pump，2.748 秒
其次 = sim-visual-bridge-crane，2.729 秒
```

全部页面均在 15 秒门限内完成业务工作区检查。浏览器检查以数据工作区可用为完成条件，不要求等待三维资源完全下载，因此模型资源的后台加载不会阻塞数据、预测和诊断功能。

补充专项验收：

```text
MODEL_OPERATIONAL_UI_OK                 通过
MODEL_SHOWCASE_PROFESSIONAL_UI_OK       通过
RESPONSE_TIMING_UI_OK（只读）           通过
UNIFIED_HYDRO_UI_OK（只读）             通过
```

公网入口：

```text
https://47.122.104.52/cockpit/                    HTTP 200
https://47.122.104.52/microapps/scene-library/    HTTP 200
/api/health.version                               20260911-083049
```

共享持久化数据复核：

```text
模型目录          106
非水轮机配对目录  315
配对数据文件      945
场景日志          106
数据目录大小      55 MiB
```

共享根目录为：

```text
/nlplabProj/scene-library/shared/data/model-showcase/
```

## 七、回退方法

如需回退，只切换应用 Release，不删除共享数据：

```powershell
ssh -i C:\Users\Administrator\.ssh\scene-library-deploy-ed25519 root@47.122.104.52 "/tmp/deploy-unified-hydro.sh switch 20260910-194728"
```

回退后必须重新检查 `/api/health`、`current/previous`、`NRestarts`、两个公网入口以及 106 页代表性浏览器验收。共享模型数据、协议文件、预测结果和两类日志不随 Release 链接切换删除。

## 八、Git 与交付文件

业务标题保护和全页验收脚本提交：

```text
4e3bd42e76a6decedc366ea8dcec7ccc2aa4460e
fix: 保护场景业务标题并补充全页验收
```

本次发布应保留以下交付物：

```text
scene-library-release-20260911-083049.tar.gz
主平台统一导航拆分方案-20260820/scene-library-menu.manifest.json
新版本云部署会话交接/云端更新执行记录-20260911-固定场景命名与106页全量验收.md
新版本云部署会话交接/README-新会话部署前必读.md
外部模型数据上传与预测诊断-20260903/修改日志.md
模型库增量与稳定性优化-20260901/修改日志.md
```

正式发布包属于本地发布产物，不加入 Git；菜单清单、修改日志、执行记录和接手说明加入 `origin/main`。工作区内其他未跟踪的 Word、PDF、旧发布资料和历史压缩包均未修改、未删除、未提交。
