# 2026-09-09 预测工作台与模型稳定性发布记录

发布 ID：`20260909-083801`  
当前阶段：生产发布与公网验收已完成，Git 提交与推送进行中。

## 发布范围

- 四个试点模型页面的实测数据、长周期预测、预测结论、预警及预警部位工作台。
- 水轮机 50 组观测/验证数据、冻结预测、单组对照和累计准确率/误差工作台。
- Modbus、IEC 61850 文件导入与转换、SCL 归档、异常数据基础清洗、设备与批次管理、多格式下载。
- 外部模型页面专业化文案、断连缓存、请求合并、超时退避和空数据诊断修复。
- 发布包仅携带水轮机版本化参考数据，不携带运行期 `current`、`batches`、`imports`、`protocol` 或 `validation` 数据。

## 发布前基线

- 场景库服务器：`47.122.104.52`；未访问或修改主平台服务器。
- `current`：`/nlplabProj/scene-library/releases/20260908-194130`
- `previous`：`/nlplabProj/scene-library/releases/20260908-192630`
- `scene-library.service`：active / enabled，`NRestarts=0`
- 内部健康接口：`http://127.0.0.1:3102/api/health` 返回 200，版本 `20260908-194130`
- Nginx 配置检查通过；磁盘可用约 11 GiB。
- 生产共享数据根目录存在四个试点模型目录；发布切换前将再次记录四份 `current/state.json` 哈希。

## 阶段记录

### 1. 本地验证

状态：通过（全量类型检查的既有缺陷除外）。

- `npm ci`：成功，审计 0 项漏洞。
- 导航类型检查与导航验证：通过，20 个根节点、1050 个节点。
- 模型库、模型加载、空值诊断、断连隔离、四模型时序接口、52 项下载与 32 份协议样例：通过。
- 水轮机 50 组数据：状态准确率 96%、综合结论准确率 94%、异常故障类型准确率 91.3%、归一化 MAE 18.26%、SMAPE 20.20%。
- `npm run build:all`：standalone 与 microapp 均成功；仅有既有大 chunk 警告。
- 水轮机工作台桌面/移动端浏览器回归通过；外部模型专业文案与恢复态浏览器回归通过。
- 四试点页面布局与交互均完成，但本地上游模型接口在回归期间对 3 个模型文件返回 504，严格脚本因此退出 1；生产隔离预检必须使用共享持久缓存重新验证四页模型后才能切换。
- `npm run typecheck` 仍只报告既有 7 个维修计划目录下 `three-types.ts` 首行非法字符，本轮未修改这些文件。
- `git diff --check`：通过。

### 2. 发布包与上传

状态：发布包已生成、上传并完成远端哈希复核。

- 包名：`scene-library-release-20260909-083801.tar.gz`
- 大小：54,292,356 字节
- 条目：480
- SHA-256：`C6944A7606F10287922B71A108BA0F19158C5081BDCEA4E33D03E6964E85CEDF`
- 水轮机参考数据：154 个条目，包含 50 组观测/验证 CSV、清单、说明和中文 XLSX 索引。
- 包内运行期数据条目：0。
- 正式菜单：1050 节点，`menuVersion=20260909-083801`，内容哈希 `2df4367183435500d65a9ea095a6d78d9ea6ba0c1e488b097ee8f9f9b139ca2d`。
- 远端 `/tmp` 文件大小与 SHA-256 和本地一致；目标 Release 安装前确认不存在。

### 3. 隔离安装与预检

状态：通过。

- 新目录独立解包，归属 `cockpit:cockpit`；`npm ci --include=dev` 安装 413 个包。
- `tsx v4.23.12`、Node `v22.23.2`，生产 `current` 在预检期间保持 `20260908-194130`。
- 3103 使用临时数据目录和隔离模型缓存启动，健康版本为 `20260909-083801`，`NRestarts=0`。
- 四个试点 FBX 完整返回：7,661,788 / 13,082,652 / 3,777,248 / 6,175,120 字节。
- 50 组清单、103 文件 ZIP、四模型时序预测、数据检视、52 项下载及 32 份协议样例通过。
- ZIP 实际为 447,424 字节，高压缩比低于最初 1 MiB 检查阈值；经 HTTP 200、ZIP 完整性和 103 条目复核确认无缺失，不属于接口错误。

### 4. 生产切换

状态：完成，未触发回滚。

- `current`：`/nlplabProj/scene-library/releases/20260909-083801`
- `previous`：`/nlplabProj/scene-library/releases/20260908-194130`
- `APP_VERSION` / `RELEASE_VERSION`：`20260909-083801`
- 旧环境备份：`/etc/scene-library-next.env.before-20260909-083801`，权限 600。
- 水轮机参考数据以不存在为前提原子增加 103 个文件；没有覆盖运行期数据。
- 切换前后四份 `current/state.json` SHA-256 完全一致。

### 5. 线上验收

状态：通过。

- HTTPS 与 8081 健康版本均为 `20260909-083801`。
- standalone 与 microapp 的 HTML、JS、CSS 公网内容哈希和本地构建完全一致。
- 四试点页 1440px/390px 公网只读 Playwright 通过，实际模型资源均成功加载。
- 水轮机工作台 1600px/390px 均显示 50 组，关键区块齐全且横向溢出为 0；生产验证台账保持 0 条，未执行上传或删除。
- 六个代表性外部模型页未出现内部实现、原始错误或不专业措辞，首屏约 0.7 至 5.7 秒。
- 服务 active/enabled、`NRestarts=0`，仅 `127.0.0.1:3102` 监听；3103 已停止。
- 应用启动/运行级错误计数 0，Nginx error/crit/alert/emerg 计数 0。

### 6. Git 提交与推送

状态：未开始。

## 回退约束

生产切换时以现场旧 `current` 为准设置 `previous`。若健康检查、静态入口或关键业务接口失败，恢复旧软链接与旧版本环境变量并重启服务。共享数据不参与版本切换，也不得在回退时删除。
