# 云端更新执行记录：黑色模型替换与材质优化

> 执行日期：2026-08-28（Asia/Shanghai）  
> 最终状态：Release `20260828-145036` 已上线；主项目服务器未登录、未修改  
> 业务边界：服务器切换和技术验收完成；主平台目标浏览器强制刷新后的最终目视验收待用户执行

## 1. 发布内容

- `eq-5`：`2304 工业污水处理设备` → `2327 污水处理设备`；
- `eq-7`：`2374 集装箱船` → `2373 集装箱式远航跨洋货轮`；
- `ia-ship-cii`：`2373` → `2283 万吨级排水作业货轮`；
- diffuse map 统一使用 sRGB，贴图材质增加低强度可读性底色，告警状态结束后恢复正常底色；
- 服务端持久模型缓存增加绑定资源指纹核对，禁止改绑后继续返回旧二进制；
- 正式菜单 manifest 和页面顺序不变，主平台 103 模型详情白名单重新生成。

## 2. 发布包

```text
源码提交：a205c525ea3ade15718e537fcaab559d5565b66d
发布包：scene-library-release-20260828-145036.tar.gz
本地字节：39,994,695
条目数：326
SHA-256：0ffecf027e04b0315694f563cf00f35f08b1aca237a6495ccd1555630c2f1a73
安装目录：/nlplabProj/scene-library/releases/20260828-145036
上传暂存：/tmp/scene-library-release-20260828-145036.tar.gz
```

首次 `scp` 超时只留下错误哈希的未完成暂存文件，未安装、未切换。重新上传后先在远端核对字节与 SHA-256 完全一致，才继续安装。

## 3. 发布前现场基线

```text
current  → /nlplabProj/scene-library/releases/20260828-103420
previous → /nlplabProj/scene-library/releases/20260827-155745
service  → active/enabled
listen   → 127.0.0.1:3102
3103     → 空闲
NRestarts=0
nginx -t → successful
```

共享目录为 `/nlplabProj/scene-library/shared/data` 和 `/nlplabProj/scene-library/shared/model-cache`。新 Release 独立解压、使用共享 npm cache 安装依赖，并建立相同共享链接；此阶段没有改变 `current`。

## 4. 3103 隔离预检

隔离实例返回：

```text
health.version=20260828-145036
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=http://127.0.0.1:33103/api
eq-5 modelId=2327 declared=11328764 downloaded=11328764
eq-7 modelId=2373 declared=2960608 downloaded=2960608
ia-ship-cii modelId=2283 declared=4353952 downloaded=4353952
```

standalone 和 microapp 均检出材质增强构建标记，服务端检出 `MODEL_BINDING_CACHE_MISMATCH`。预检期间 `current` 保持旧版且正式服务 active；通过后已停止 3103。

## 5. 正式切换

切换使用旧 `current`、旧 `previous` 和环境文件备份组成的自动回滚脚本；若链接、服务、健康或 Nginx 任一检查失败，会恢复旧指针与旧环境并重启旧服务。本次一次通过，没有触发回滚。

```text
current  → /nlplabProj/scene-library/releases/20260828-145036
previous → /nlplabProj/scene-library/releases/20260828-103420
APP_VERSION=20260828-145036
RELEASE_VERSION=20260828-103420
环境备份=/tmp/scene-library-next.env.before-20260828-145036
```

## 6. 切换后技术验收

```text
internal/public health.version=20260828-145036
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=https://47.122.104.52/scene-library-api
standalone=HTTP 200
microapp=HTTP 200
HTTP 8081 compatibility=HTTP 200
scene-library.service=active/enabled
NRestarts=0
listen=127.0.0.1:3102
3103=stopped
nginx -t=successful
```

公网再次完整下载三项模型，结果仍为 11,328,764、2,960,608、4,353,952 字节，与 Bootstrap 声明一致。切换日志中 systemd 的 143 是计划内 restart 对旧进程发送 SIGTERM，不是新进程崩溃；新进程启动后无应用错误。

## 7. 主平台正式交付物

```text
交付目录：主项目模型库扩展接入-20260826/主平台导航正式交付包-20260828/
ZIP：主平台导航正式交付包-20260828.zip
ZIP 字节：34,286
ZIP SHA-256：481e896ad4ff04be17cf31cf2c1882de0016221211ffb22023488843d9f1595e
manifest SHA-256：c54f5375db0b99460d5848895b436b47ffe0848aea69590a7d3719e24b7d14bd
allowlist SHA-256：2df40d23bd5377214cf785fda249f65319b5b56424a5bb779ae9948dfbadd881
```

白名单删除 `2304/2374`，新增 `2327/2283`，并把 `2373` 的 `sceneId` 调整为 `eq-7`；总数仍为 103。旧 ZIP 哈希 `61f0a8e...` 已作废。主平台只需更新白名单交付物，菜单 manifest 的 20 个根节点、1,050 个节点、顺序和哈希均不变。

## 8. 回滚与剩余验收

如场景库自身发生故障，可把 `current` 和环境版本恢复到 `20260828-103420` 后重启服务；共享数据和模型缓存均未删除。发布包、旧 Release 和环境备份均保留。

目标浏览器需强制刷新或关闭旧主平台标签页重新打开，再抽查 `eq-5`、`eq-7`、`ia-ship-cii` 的正面、侧面和连续旋转效果。若仍显示旧黑模，先核对 Bootstrap 的模型 ID；新服务不会把旧二进制作为新 ID 返回，若 ID 仍旧则是主平台页面/Service Worker/iframe 入口缓存尚未刷新。
