# 场景库云端更新执行记录：主平台统一导航拆分

> 创建日期：2026-08-20
> 更新方式：部署过程中逐阶段补充，保留关键命令输出和判断依据。
> 严格口径：本地开发、构建、打包、上传、远端安装、生产切换、技术验收、业务验收是不同状态。没有对应证据时，不得写“已更新到云端”。

## 1. 本次发布目标

本次只更新场景库服务器 `47.122.104.52`，不登录、不修改、不部署主平台服务器 `8.146.211.204`。

场景库侧目标：

- 主平台 iframe 使用 `/cockpit/?embedded=1&viewId=<页面ID>` 时，本项目隐藏自身导航栏，只显示业务内容；
- 直接访问 `/cockpit/` 时仍保留原导航栏；
- 本项目原导航树由主平台呈现在“场景库”一级入口之下；
- 合法 `viewId` 精确打开页面，非法值安全回退；
- 保留既有模型详情通信协议 `scene-library:navigate`；
- 同时生成并部署 standalone、microapp 两套构建产物；
- 生成与最终发布版本一致的菜单清单，供主平台团队集成。

## 2. “已更新到云端”的判定

只有阶段 7“生产切换”和阶段 8“切换后技术验收”均完成，才能说代码已更新到场景库云端。

只有阶段 9“浏览器业务验收”也完成，才能说场景库侧部署完整完成。

只有阶段 10“主平台交付与联合验收”完成，才能说两平台导航拆分整体完成。

## 3. 发布身份信息

| 项目 | 当前记录 |
|---|---|
| 本地分支 | `main` |
| 当前部署源码提交 | `e612f665874cfe983edf449674ca5cb96b08f6f2` |
| HTTPS 功能提交 | `46d14c1209d387ea3a6e677af4b6b6bd1aa99597` |
| 工作区状态 | 部署源码提交后干净；本执行记录属于部署结果补记，不改变已发布运行代码 |
| 本地 Node / npm | `Node v24.12.0` / `npm 11.19.0` |
| 本地 C 盘可用空间 | `31.06 GB`（2026-08-20 核查） |
| Release ID | `20260821-085852` |
| 发布包文件名 | `scene-library-release-20260821-085852.tar.gz` |
| 发布包 SHA-256 | `A19133C58A88CD9845737745D621128A3F9C3B085356ED28918F9DD93734A07B` |
| 正式菜单版本 | `20260820-173922` |
| 菜单内容 SHA-256 | `b7de7be4e41977d0e9fdfc75c204ff93086d4a33b57f9c33e0351cd1fa05b021` |
| 菜单文件 SHA-256 | `B9492C17F9A0CCED84140A80082D6E6B5EB957A3773F33DB966B1D395F35649E` |
| 本轮切换前线上版本 | `20260821-084944` |
| 当前线上版本 | `20260821-085852`（一号机组 3D 浮层二次修复已通过技术验收） |
| 当前回滚目标 | `/nlplabProj/scene-library/releases/20260821-084944` |

## 4. 当前阶段总表

状态只能使用：`未开始`、`进行中`、`已完成`、`失败待处理`、`不适用`。

| 阶段 | 状态 | 当前结论/证据 |
|---|---|---|
| 0. 场景库侧开发调整 | 已完成 | 已实现 `embedded`、`viewId`、隐藏导航、严格页面定位和菜单清单生成 |
| 1. 本地发布前核查与版本确认 | 已完成 | 发布提交 `018f89a` 已创建，14 个目标文件，提交后已跟踪工作区干净 |
| 2. 最终依赖检查、专项验证与双构建 | 已完成 | 导航专项检查和双构建通过；全量检查的 72 条错误仅来自 7 个已知历史文件 |
| 3. 正式菜单清单、发布包与本地 SHA-256 | 已完成 | 正式清单和发布包已生成；协议、结构、大小和 SHA-256 均已复核 |
| 4. SSH 密钥验证与远端只读基线 | 已完成 | 公钥认证通过；current/previous、服务、入口、监听、磁盘和目标缺失状态均已现场核验 |
| 5. 上传 `/tmp` 与远端 SHA-256 复核 | 已完成 | 远端文件 7646495 字节，SHA-256 与本地一致 |
| 6. 安装新 release、但不切换 | 已完成 | 新目录 473M、依赖及协议检查通过；current 和旧服务保持不变 |
| 7. 切换前预检与生产切换 | 已完成 | 3103 预检通过；生产切换成功，current/previous 与环境版本正确 |
| 8. 切换后技术验收 | 已完成 | 版本、服务、6 个 HTTP 入口、协议、软链接、持久目录、监听和日志均已复核 |
| 9. 浏览器业务验收 | 失败待处理 | 独立版正常；主平台合并页出现缺少子菜单、错误映射及场景库 Sidebar 重现 |
| 10. 主平台清单交付与联合验收 | 进行中 | 确认正式清单在初次方案交付后才生成；需补交正式清单并核对主平台递归导入和 iframe 参数代码 |

## 5. 已经更新到云端的部分

截至 2026-08-21 09:10 本次生产切换后，以下内容已经更新到场景库云端：

- `/nlplabProj/scene-library/releases/20260821-085852` 已安装，目录归属 `cockpit:cockpit`；
- `/nlplabProj/scene-library/current` 已指向新版 `20260821-085852`；
- `/nlplabProj/scene-library/previous` 已指向切换前真实旧版 `20260821-084944`；
- `/etc/scene-library-next.env` 的 `APP_VERSION`、`RELEASE_VERSION` 已更新为 `20260821-085852`；
- `/etc/scene-library-next.env` 的模型上游已更新为 `https://8.146.211.204:3100/three-model-api`；
- `scene-library.service` 已重启并运行新版；
- 内部和 HTTPS 公开健康接口均返回版本 `20260821-085852`；
- HTTPS `/cockpit/`、microapp HTML、`scene-library.js` 和新版 standalone 主资源均返回 200；
- 前四个外部模型页面的公开 HTTPS `bootstrap` 均返回 200，模型 ID 2326、2328、2316、2310 匹配；
- HTTP 8081 兼容入口仍返回 200，但 HTTPS 主平台必须使用 `https://47.122.104.52`；
- 3102 仍只监听 `127.0.0.1`；
- 一号机组两个浮层已移除旧 HUD 与全部深色背景类，并对所有可能隐藏的文字提供专用深色样式；现网 CSS 为 `/cockpit/assets/index-CH0Eb6DW.css`；
- 切换前环境备份保留在 `/tmp/scene-library-next.env.before-20260821-085852`；
- 本轮发布包保留在 `/tmp/scene-library-release-20260821-085852.tar.gz`。

场景库侧 HTTPS、前四页模型链路和新版诊断资源的服务器技术验收已经完成。主平台需按 07、08、09 文档修改分类首页和 iframe 消息 Origin 后，才能完成联合浏览器验收。

## 6. 本地已经完成的部分

### 6.1 代码调整

- `src/integration/launchOptions.ts`：解析并校验 `embedded=1`、`viewId`；
- `index.tsx`：读取启动参数并传入应用；
- `App.tsx`：集成模式隐藏本项目 Sidebar，独立模式保留；
- `src/integration/menu.ts`：提供递归页面 ID 查询；
- `scripts/generate-scene-library-menu.ts`：生成主平台菜单清单；
- `scripts/verify-navigation-split.ts`：验证菜单唯一性、默认页和页面定位协议；
- `tsconfig.navigation.json`：导航专项类型检查；
- `package.json`：新增专项验证和清单生成脚本。

### 6.2 开发阶段验证

曾经通过：

```text
npm run verify:navigation
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1049

npm run typecheck:navigation
通过

npm run build:all
standalone 与 microapp 均通过
```

本地 HTTP 曾验证 `/`、合法集成入口、叶子入口、非法 `viewId` 回退和 `/api/health` 均返回 200。正式发布前仍必须基于最终工作区重跑。

全量 `npm run typecheck` 仍存在 7 个与本次导航无关的历史 `three-types.ts` 非法字符错误，详见交接 README。该结果必须如实记录，不能假装通过，也不能误判为本次导航回归。

### 6.3 当前开发态菜单清单

```text
文件：主平台统一导航拆分方案-20260820/scene-library-menu.manifest.json
menuVersion：dev-46e7605-dirty
sourceCommit：46e7605c8c6f1102fdeb6aba1a7984b2e1b062ba
sourceDirty：true
一级节点：20
总节点：1049
menuContentSha256：b7de7be4e41977d0e9fdfc75c204ff93086d4a33b57f9c33e0351cd1fa05b021
文件 SHA-256：46097072D7DEE3A335AAA2A2E9D0074BFB527B6214D823347E4C9B9E75EFC769
```

这是开发态清单，正式发布前必须按最终 Release ID 重新生成。

## 7. 最后已知云端基线（只作历史参考）

2026-08-18 最后验证：

```text
current  -> /nlplabProj/scene-library/releases/20260818-144540
previous -> /nlplabProj/scene-library/releases/20260817-154326
scene-library.service = active / enabled
HOST=127.0.0.1
PORT=3102
APP_VERSION=20260818-144540
health = {"status":"ok","version":"20260818-144540"}
```

任何部署命令前必须重新只读核查，现场结果优先。

## 8. 本次验收清单

### 8.1 场景库独立入口

- `/cockpit/` 返回 200；
- 页面仍显示本项目导航栏；
- 原有页面和常用操作未回归。

### 8.2 场景库集成入口

- `/cockpit/?embedded=1&viewId=smart-ops`：隐藏本项目导航，显示目标页面；
- `/cockpit/?embedded=1&viewId=eq-0`：隐藏本项目导航，直接显示叶子页面；
- 非法 `viewId`：安全回退默认页，不白屏、不跳往任意地址；
- 刷新后仍保持当前 `viewId` 页面；
- 控制台没有新增严重错误。

### 8.3 原有集成功能

- microapp 入口和 `scene-library.js` 均返回 200；
- `scene-library:navigate` 仍存在于构建产物；
- 从前四个仿真页面点击“查看资源详情”时，主平台能按其内标签页开关处理；
- 场景数据、模型资源、三维展示及 API 健康检查没有回归。

### 8.4 主平台联合验收

- 主平台只有“场景库”作为本项目一级导航；
- 原 20 个根菜单及其子项整体位于“场景库”下；
- 主平台菜单点击传入正确的 `embedded=1&viewId=...`；
- 主平台内切换页面不会重新显示本项目导航栏；
- 主平台刷新、返回、内标签页行为符合对方平台规范。

## 9. 执行日志（按时间追加，不覆盖历史）

每完成一个阶段，在这里追加一条。建议格式：

```text
### YYYY-MM-DD HH:mm - 阶段 N：阶段名称
- 操作者：用户 / AI 引导
- 本地或远端：
- 执行内容摘要：
- 关键输出：
- 结论：成功 / 失败 / 需复核
- 对 current 的影响：无 / 已切换到 ...
- 下一步：
```

### 2026-08-20 - 文档与部署准备开始

- 操作者：AI 整理，用户尚未执行新的云端命令；
- 本地或远端：本地文档；
- 执行内容摘要：更新部署交接说明，创建本执行记录；
- 关键输出：确认本次云端上传、安装、切换均尚未开始；
- 结论：部署记录已建立；
- 对 `current` 的影响：无；
- 下一步：用户在本机 PowerShell 执行阶段 1 的只读核查并贴回输出。

### 2026-08-20 - 阶段 1：本地只读核查

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：核对分支、基准提交、工作区、差异格式、Node/npm 和磁盘空间；
- 关键输出：`main`；`46e7605c8c6f1102fdeb6aba1a7984b2e1b062ba`；`git diff --check` 无报错；Node `v24.12.0`；npm `11.19.0`；C 盘可用 `31.06 GB`；结尾为 `LOCAL_READONLY_CHECK_OK`；
- 变更范围：4 个核心源码/配置文件已修改，3 份主平台方案文档和部署 README 已修改；导航脚本、启动参数模块、专项 tsconfig、菜单清单和本执行记录为未跟踪文件；
- 结论：只读核查成功，空间充足，可以进入最终依赖和构建验证；
- 对 `current` 的影响：无，未连接云服务器；
- 下一步：执行导航专项类型检查、协议验证和 standalone/microapp 双构建。

### 2026-08-20 - 阶段 2 第一次尝试：依赖安装失败

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：运行 `npm ci --no-audit --no-fund`，计划随后执行专项检查和双构建；
- 关键输出：`EPERM: operation not permitted, unlink node_modules/@rolldown/binding-win32-x64-msvc/rolldown-binding.win32-x64-msvc.node`；
- 结论：依赖安装失败。最可能原因是本项目的 Node/Vite 进程仍加载该 Windows 原生模块，不属于源码编译失败；因命令在 `throw` 处停止，专项类型检查、协议验证和双构建均未执行；
- 对 `current` 的影响：无，未连接云服务器；
- 安全处理原则：先只读定位与本项目相关的 Node 进程，不结束不相关 Node 进程，也不直接删除整个 `node_modules`；
- 下一步：列出 Node 进程 PID 和命令行，确认占用来源后再决定精确停止哪个本地进程。

### 2026-08-20 - 阶段 2 故障诊断：确认本地文件占用者

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：使用 `tasklist /m` 检查加载 Rolldown 原生模块的进程，并读取相关 Node 命令行；
- 关键输出：PID `24424`、`13904` 均直接加载 `rolldown-binding.win32-x64-msvc.node`，命令行均为 `node scripts/run-tsx.mjs server.ts`；
- 结论：占用者是本项目此前启动后遗留的两个本地服务进程。Codex runtime Node 进程虽然工作目录相同，但未被 `tasklist /m` 判定为模块占用者，不得结束；
- 对 `current` 的影响：无，仍未连接云服务器；
- 下一步：在停止前再次核对 PID、进程名和命令行，只精确停止这两个本项目服务进程，然后确认模块占用已经消失。

### 2026-08-20 - 阶段 2 故障处理：释放本地构建文件锁

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：再次核验进程名与命令行后，精确停止 PID `24424`、`13904`；
- 关键输出：两个 PID 均已停止；`tasklist /m rolldown-binding.win32-x64-msvc.node` 返回“没有运行的任务匹配指定标准”；结尾为 `PROJECT_SERVER_LOCK_RELEASED`；
- 结论：Rolldown 原生模块文件锁已释放，可以重试 `npm ci`；
- 对 `current` 的影响：无；只停止本机开发服务，未连接云服务器；
- 下一步：重新执行依赖安装、导航专项检查和两套正式构建。

### 2026-08-20 - 阶段 2 第二次尝试：依赖、专项验证和双构建通过

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：重新运行 `npm ci`、导航专项类型检查、导航协议验证、standalone/microapp 双构建和锁文件差异检查；
- 关键输出：`npm ci` 安装 360 个包；`NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1049`；standalone 转换 5519 个模块并成功构建；microapp 转换 5514 个模块并成功构建；结尾为 `LOCAL_BUILD_VERIFY_OK`；
- 关键产物：`dist-standalone/index.html` 3872 字节；`dist-microapp/index.html` 463 字节；`dist-microapp/scene-library.js` 18080142 字节；standalone 主入口为 `assets/index-C7pH7zo-.js`；
- 警告说明：`node-domexception` 弃用、`esbuild@0.28.2` install script 未列入 allowScripts、插件耗时和大 chunk 均为非阻断警告；构建实际成功；
- 锁文件：`git diff --exit-code -- package-lock.json` 无差异；
- 结论：本次导航相关硬性检查及两套构建通过，可以继续全量类型检查诊断；
- 对 `current` 的影响：无，尚未连接或更新云服务器；
- 下一步：执行全项目 `npm run typecheck`，确认失败项是否仍严格限定为已知 7 个历史 `three-types.ts` 文件。

### 2026-08-20 - 阶段 2：全量类型检查历史基线确认

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 执行内容摘要：运行全项目 `npm run typecheck` 并完整收集 TypeScript 错误；
- 关键输出：退出码 `2`，共 72 条 TypeScript 错误；错误只来自以下 7 个 `components/Maintenance-plan-management/*/three-types.ts` 文件：`CoolingWaterSystemCleaning`、`DamSeepageMonitoring`、`ExcitationSystemUpgrade`、`GeneratorRotorReplacement`、`GovernorSystemCalibration`、`HydraulicHoistMaintenance`、`HydrologicalStationCalibration`；
- 结论：错误范围与部署前已知历史基线完全一致，没有任何本次导航拆分文件报错。导航专项检查和双构建已通过，本次发布可继续；全量类型检查不能标记为成功，只能记录为“已完成诊断、仅命中历史基线”；
- 对 `current` 的影响：无，尚未连接云服务器；
- 下一步：由用户确认是否按推荐方式先创建 Git 发布提交，再生成正式 Release ID、正式菜单清单和发布包。

### 2026-08-20 - 阶段 1 决策：采用 Git 发布提交

- 用户决定：同意在打包前创建 Git 发布提交；
- 目的：使云端 Release ID、源代码、正式菜单清单和回滚审计具有明确对应关系；
- 范围原则：只暂存本次导航拆分代码、对接资料和部署交接记录；不包含 `node_modules`、`dist-*`、`.claude/` 或其他无关内容；
- 下一步：使用显式文件白名单加入暂存区，自动检查是否缺失或混入文件，用户复核后再真正提交。

### 2026-08-20 - 阶段 1：暂存核对脚本中文路径误判

- 操作者：用户执行白名单暂存和自动核对；
- 现象：脚本报告 6 个中文路径文件“缺失”并主动停止；
- 复核结果：文件实际上已经成功暂存。现场 `git diff --cached --name-status` 显示全部 14 个目标文件，暂存文件总数也是 14；
- 原因：Git 中文路径输出进入 PowerShell 变量后的编码/字符串表现与原始白名单字符串不一致，导致 `-notcontains` 比较误判；不是 `git add` 失败，也不是文件缺失；
- 安全状态：尚未创建提交；`.claude/` 仍为未跟踪且未暂存，不在本次发布范围；
- 处理：不清空暂存区，不重复批量加入文件；改用“逐路径检查是否存在暂存差异 + 暂存总数必须等于 14”的方式复核。

### 2026-08-20 - 阶段 1：暂存内容空白检查修正

- 现象：14 个文件的逐路径核对和数量核对通过，随后 `git diff --cached --check` 报告本记录第 3、4 行存在 trailing whitespace；
- 原因：文档引用行末使用了两个 Markdown 强制换行空格；不影响功能，但不符合本次提交的 Git 空白检查要求；
- 处理：移除两个行尾空格，仅重新暂存本执行记录，然后再次执行暂存总数和空白检查；
- 安全状态：尚未提交、推送或连接云服务器。

### 2026-08-20 - 阶段 1：发布提交范围复核通过

- 操作者：用户按 AI 引导执行；
- 关键输出：暂存文件总数为 14；`git diff --cached --check` 无输出；结尾为 `STAGED_RELEASE_SCOPE_VERIFIED`；
- 暂存范围：4 个修改的核心源码/配置文件、4 个新增导航实现/验证文件、4 个主平台对接资料、2 个部署交接与执行记录文件；
- 排除范围：`node_modules`、`dist-standalone`、`dist-microapp` 和 `.claude/` 均未进入提交；
- 结论：暂存范围和格式检查通过，可以创建正式 Git 发布提交；
- 对云端影响：无，尚未连接云服务器。

### 2026-08-20 17:36:28 - 阶段 1：创建 Git 发布提交

- 操作者：用户按 AI 引导执行；
- 提交：`018f89a254f3093fb6d8e2f0221782a91e334282`；
- 提交说明：`feat: 适配主平台统一导航并补充部署交接`；
- 关键输出：14 个文件，8112 行新增、38 行删除；结尾为 `GIT_RELEASE_COMMIT_CREATED`；
- 提交后状态：没有已跟踪文件变化；未推送远端 Git 仓库；
- 对云端影响：无，尚未连接云服务器。

### 2026-08-20 17:39:22 - 阶段 3：生成正式菜单清单

- 操作者：用户按 AI 引导执行；
- Release ID / menuVersion：`20260820-173922`；
- sourceCommit：`018f89a254f3093fb6d8e2f0221782a91e334282`；
- sourceDirty：`False`；
- 菜单规模：一级节点 20，总节点 1049；
- 菜单内容 SHA-256：`b7de7be4e41977d0e9fdfc75c204ff93086d4a33b57f9c33e0351cd1fa05b021`；
- 清单文件 SHA-256：`B9492C17F9A0CCED84140A80082D6E6B5EB957A3773F33DB966B1D395F35649E`；
- 关键输出：`FORMAL_MANIFEST_READY`；
- 对云端影响：无；正式清单是主平台交付物，尚未上传场景库服务器。

### 2026-08-20 - 阶段 3 第一次尝试：发布包已生成但协议检查需重做

- 操作者：用户按 AI 引导执行；
- 本地或远端：本地 PowerShell；
- 现象：本机找不到 `rg` 命令，入口路径和四个协议关键字检查均未真正执行；PowerShell 的命令不存在异常没有可靠刷新 `$LASTEXITCODE`，原脚本因此继续运行并错误打印“构建入口路径正确”；
- 已完成证据：发布包 `scene-library-release-20260820-173922.tar.gz` 已生成；大小 7646495 字节；目录项 306；结构白名单/禁止项检查完成；SHA-256 为 `2D18718BCC1CC4BC17AC100A3B3627032695D93CB16846DA62A0B811FE02A41D`；
- 当前判定：不得因出现 `LOCAL_RELEASE_PACKAGE_READY` 就认为阶段 3 完成。包暂不覆盖、暂不上传，先使用纯 PowerShell 重新验证当前构建产物的基础路径、`embedded`、`viewId`、`scene-library:navigate` 和 `/scene-library-api/`，并复核包哈希与目录；
- 对云端影响：无，尚未连接或上传云服务器。

### 2026-08-20 - 阶段 3：发布包协议与哈希复核通过

- 操作者：用户按 AI 引导执行；
- 检查方式：不依赖本机缺失的 `rg`，改用纯 PowerShell/.NET 读取构建文件；
- 构建路径：standalone `/cockpit/` 与 microapp `/microapps/scene-library/` 均通过；
- 协议检查：`embedded`、`viewId`、`scene-library:navigate`、`/scene-library-api/` 均在 standalone 主入口 `assets/index-C7pH7zo-.js` 中找到；
- 发布包：`scene-library-release-20260820-173922.tar.gz`；大小 7646495 字节；目录项 306；
- SHA-256：`2D18718BCC1CC4BC17AC100A3B3627032695D93CB16846DA62A0B811FE02A41D`；
- 结构检查：关键运行文件齐全，未发现 `node_modules`、`.git` 或 `.env`；
- 关键输出：`PACKAGE_PROTOCOL_AND_HASH_VERIFIED`；
- 结论：本地正式产物可用于上传；
- 对云端影响：无，尚未连接或上传服务器。

### 2026-08-20 - 阶段 4 第一次尝试：当前会话缺少 KeyPath 变量

- 操作者：用户按 AI 引导执行；
- 现象：命令在本地前置检查处停止，提示“当前 PowerShell 中没有设置 `$KeyPath`”；
- 原因：`$KeyPath` 是 PowerShell 会话变量，新会话不会自动继承；不代表原 SSH 密钥丢失或失效；
- 安全处理：不重新生成密钥、不读取或粘贴私钥正文，先根据用户 `.ssh` 目录中的公私钥文件对恢复 `$KeyPath`；
- 对云端影响：无。命令在调用 `ssh` 前已经停止，没有连接服务器。

### 2026-08-20 - 阶段 4：恢复现有 SSH 密钥路径

- 操作者：用户按 AI 引导执行；
- 找到的密钥对：`C:\Users\Administrator\.ssh\scene-library-deploy-ed25519` 及其 `.pub` 文件；
- 私钥文件大小：419 字节；最后修改时间：2026-08-18 15:27:45；
- 关键输出：`KEY_PATH_RESTORED=C:\Users\Administrator\.ssh\scene-library-deploy-ed25519`；
- 安全说明：只列出路径和文件元数据，没有输出私钥正文，也没有生成或安装新密钥；
- 下一步：使用该密钥，强制仅公钥认证并执行云端只读基线检查。

### 2026-08-20 17:47:01 - 阶段 4：SSH 与云端只读基线通过

- SSH：`KEY_AUTH_OK`，仅使用现有公钥认证；远端主机 `iZn4acystzayers4egsmyeZ`；
- current：`/nlplabProj/scene-library/releases/20260818-144540`；
- previous：`/nlplabProj/scene-library/releases/20260817-154326`；
- 服务：`scene-library.service` 为 `active`、`enabled`；
- 环境公开字段：`HOST=127.0.0.1`、`PORT=3102`、`APP_VERSION=20260818-144540`；
- 健康检查：内部和 Nginx 公开接口均返回 `{"status":"ok","version":"20260818-144540"}`；
- 公开入口：`/cockpit/`、microapp HTML、`scene-library.js`、公开 health 均为 HTTP 200；
- 监听：Node 仅监听 `127.0.0.1:3102`；
- 运行时：Node `v22.23.2`，npm `12.0.2`；
- 磁盘：根分区 40G，已用 11G，可用 27G，使用率 29%；
- 目标状态：新 release 目录不存在，`/tmp` 中新包不存在；
- Nginx：`nginx -t` 通过；
- 关键输出：`REMOTE_BASELINE_OK`、`SSH_AND_REMOTE_BASELINE_VERIFIED`；
- 结论：可以上传发布包到 `/tmp`；上传不改变 current，不重启服务。

### 2026-08-20 - 阶段 5：上传与远端 SHA-256 复核通过

- 操作者：用户按 AI 引导执行；
- 远端文件：`/tmp/scene-library-release-20260820-173922.tar.gz`；
- 上传前检查：`REMOTE_UPLOAD_TARGET_CLEAR`，没有覆盖远端同名文件；
- 文件大小：7646495 字节；所有者 `root:root`；权限 `644`；
- 远端 SHA-256：`2D18718BCC1CC4BC17AC100A3B3627032695D93CB16846DA62A0B811FE02A41D`，`sha256sum -c` 返回 `OK`；
- 关键输出：`UPLOAD_AND_REMOTE_HASH_VERIFIED`；
- 云端影响：只在 `/tmp` 新增发布包；未创建 release 目录，未修改 `current`/`previous`，未重启服务；
- 下一步：安装到 `/nlplabProj/scene-library/releases/20260820-173922` 并安装依赖，但继续保持旧版本在线。

### 2026-08-20 - 阶段 6：安装新 Release、未切换

- 操作者：用户按 AI 引导执行；
- 安装目录：`/nlplabProj/scene-library/releases/20260820-173922`；
- 安装前：远端包 SHA-256 再次返回 `OK`，`CURRENT_BEFORE` 为旧版 `20260818-144540`；
- 依赖：新增 362 个包；`tsx v4.23.12`，Node `v22.23.2`；`esbuild` install script 未被 allowScripts 覆盖是非阻断警告；
- 产物：standalone 与 microapp 关键文件齐全，`embedded`、`viewId`、`scene-library:navigate` 远端检查通过；
- 目录：所有者 `cockpit:cockpit`，根目录权限 `755`，大小 473M；
- 线上保护：`CURRENT_AFTER` 仍为 `/nlplabProj/scene-library/releases/20260818-144540`；旧健康接口仍返回 `20260818-144540`；
- 关键输出：`RELEASE_INSTALLED_NOT_ACTIVE`、`REMOTE_RELEASE_INSTALLED_NOT_ACTIVE`；
- 结论：新版本已安装但没有接管流量；
- 下一步：在独立端口 `127.0.0.1:3103` 临时启动新 Release，验证其后端健康与版本，然后自动停止。

### 2026-08-20 - 阶段 7 第一次预检：新 Release 启动失败，未切换

- 操作者：用户按 AI 引导执行；
- 预检方式：计划在 `127.0.0.1:3103` 临时启动新 Release；
- 失败位置：`node scripts/run-tsx.mjs server.ts` 的 tsx 加载阶段；
- 错误：`ENOENT: no such file or directory, open 'node:child_process?tsx-namespace=...'`，Node `v22.23.2`；
- 关键输出：`SMOKE_START_FAILED`；本地 PowerShell 随后抛出“新版本独立端口检查失败”；
- 安全结果：脚本的退出清理已触发；新版本未接管流量，`current` 未切换，systemd 未重启；
- 当前判断：不能进入生产切换。错误更接近 tsx/Node 启动加载链问题，而不是导航前端构建问题；
- 下一步：只读比较新旧 Release 的启动命令、`run-tsx.mjs`、锁文件、已安装 tsx/esbuild 版本及关键文件哈希，并确认 3103 没有残留监听。定位前不修改依赖、不重试切换。

### 2026-08-20 - 阶段 7：只读启动诊断完成

- 线上真实 ExecStart：`/usr/bin/node /nlplabProj/scene-library/current/node_modules/tsx/dist/cli.mjs /nlplabProj/scene-library/current/server.ts`；生产 systemd 不调用 `npm start` 或 `scripts/run-tsx.mjs`；
- 运行状态：旧服务 active，3102 正常；3103 已清理；旧健康接口仍返回 `20260818-144540`；
- 新旧依赖：`package-lock.json` 完全一致；tsx 均为 `4.23.12`；tsx dist 聚合 SHA-256 均为 `6418c0910e6138bb1fc88e75f673684c246892523768796c11c4cbbff3171021`；
- 关键对照：`tsImport('node:child_process')` 在旧、新 Release 中都因 `ERR_UNKNOWN_BUILTIN_MODULE` 失败，证明第一次失败来自 `scripts/run-tsx.mjs` 所用的 tsx API 路径，不是新 Release 特有差异；
- 文件差异：旧 Release 没有 `scripts/run-tsx.mjs`；新旧 `server.ts`、`package.json` 不同，`package-lock.json` 相同；
- esbuild 诊断说明：顶层 `require('esbuild')` 在两边都找不到，是诊断命令假定了错误的依赖提升位置，不作为启动失败证据；
- 结论：第一次冒烟测试没有复刻 systemd 的真实启动方式，因此结果不能用于否定新 Release。无需修改导航代码或发布包；应改用 tsx CLI 直接启动新 Release，再做 3103 冒烟检查；
- 关键输出：`SMOKE_3103_CLEAN`、`READ_ONLY_STARTUP_DIAGNOSIS_DONE`、`REMOTE_STARTUP_DIAGNOSIS_COLLECTED`；
- 对线上影响：无，current 仍为旧版，未重启或切换。

### 2026-08-20 - 阶段 7：按 systemd 真实入口预检通过

- 操作者：用户按 AI 引导执行；
- 启动方式：`/usr/bin/node <new-release>/node_modules/tsx/dist/cli.mjs <new-release>/server.ts`，与 systemd ExecStart 等价；
- 独立监听：`127.0.0.1:3103`，Node 监听 PID `128116`；
- 新版本健康：`{"status":"ok","version":"20260820-173922"}`；
- 清理：临时进程停止后 3103 无残留监听，输出 `SMOKE_PROCESS_STOPPED`；
- 线上保护：`CURRENT_BEFORE` 和 `CURRENT_AFTER` 均为旧版 `20260818-144540`；旧 3102 健康正常；
- Nginx：配置检查通过；
- 关键输出：`PREFLIGHT_EXACT_EXECSTART_OK`、`REMOTE_EXACT_PREFLIGHT_VERIFIED`；
- 结论：新 Release 能按真实生产入口启动，已满足切换前技术条件；
- 下一步：执行带自动回滚的生产切换。切换会把 `previous` 指向真实旧 current、把 `current` 指向新 Release、更新环境版本并重启服务。

### 2026-08-20 - 阶段 7：生产切换成功

- 操作者：用户按 AI 引导执行；
- 切换前 current：`/nlplabProj/scene-library/releases/20260818-144540`；
- 切换前 previous：`/nlplabProj/scene-library/releases/20260817-154326`；
- 切换后 current：`/nlplabProj/scene-library/releases/20260820-173922`；
- 切换后 previous / 回滚目标：`/nlplabProj/scene-library/releases/20260818-144540`；
- 环境备份：`/tmp/scene-library-next.env.before-20260820-173922`；
- 内部健康：`{"status":"ok","version":"20260820-173922"}`；
- 公开健康：`{"status":"ok","version":"20260820-173922"}`；
- HTTP：standalone、microapp HTML、`scene-library.js`、standalone 主资源 `index-C7pH7zo-.js` 均为 200；
- 服务与监听：服务 active；Node 监听 `127.0.0.1:3102`，未暴露到 `0.0.0.0:3102`；
- Nginx：切换前后配置检查均通过；
- 回滚：未触发；自动回滚脚本处于本次切换保护路径中；
- 关键输出：`CUTOVER_OK`、`PRODUCTION_CUTOVER_VERIFIED`；
- 结论：场景库云端已切换至新版本。仍需扩展技术复核和浏览器业务验收，暂不宣布整体部署完成。

### 2026-08-20 - 阶段 8：切换后扩展技术验收通过

- current / previous：分别为新版本 `20260820-173922` 和回滚版本 `20260818-144540`；
- 环境：`HOST=127.0.0.1`、`PORT=3102`、`APP_VERSION=20260820-173922`、`RELEASE_VERSION=20260820-173922`；
- 运行进程：主进程工作目录解析到新 Release，命令行使用 systemd 配置的 tsx CLI；
- 健康：内部和公开 health 均返回新版本；
- HTTP：独立入口、`smart-ops` 集成入口、`eq-0` 叶子入口、非法 viewId 入口、microapp HTML、`scene-library.js` 共 6 个 URL 均为 200；
- 部署协议：`embedded`、`viewId`、`scene-library:navigate` 均存在于现网产物；
- 静态软链接：standalone 与 microapp 均解析到新 Release；
- 持久目录：`shared/data`、`shared/model-cache`、`shared/npm-cache` 均存在且归属 `cockpit:cockpit`；其中 `shared/data` 权限为 `777`，是既有权限风险，本次未改动，后续可单独评估收紧；
- 回滚材料：环境备份和发布包仍在 `/tmp`；
- 监听：仅 `127.0.0.1:3102`，3103 无残留；磁盘可用 27G；Nginx 配置通过；
- systemd 日志判断：切换时旧主进程因 SIGTERM 以 143 退出并被 systemd 暂记 failed，紧接着新版成功启动并监听 3102；属于本次主动 restart 的停止过程，不是当前故障；
- Nginx 日志判断：所列错误属于另一套 `threedemo`、端口 8082，发生在本次切换前，与场景库 8081 无关；
- 关键输出：`POST_CUTOVER_TECHNICAL_AUDIT_OK`、`POST_CUTOVER_TECHNICAL_VERIFIED`；
- 结论：服务器技术验收完成；下一步进行浏览器视觉和交互验收。

### 2026-08-20 - 阶段 9：浏览器业务验收发现主平台合并异常

- 用户确认：独立入口 `/cockpit/` 页面、原导航和业务内容正常；
- 主平台异常 1：多个原子菜单未出现在主平台导航，只剩类目名称；
- 主平台异常 2：部分主平台菜单点击后进入错误场景；
- 主平台异常 3：部分合并页内再次出现场景库浅色 Sidebar，与主平台深色 Sidebar 同时显示；
- 截图判断：浅色 Sidebar 重现直接说明对应 iframe 未以 `embedded=1` 启动，或主平台仍在使用未适配的旧宿主页地址；错误页面通常对应主平台 route query/iframe src 未传或传错叶子 `viewId`；
- 清单核对：正式 manifest 为 `menuVersion=20260820-173922`，一级 items 20 个、总节点 1049；`smart-ops` 明确包含 19 个 children。缺少子菜单不是场景库正式清单本身丢失；
- 交付时序：用户第一次向主平台交付方案发生在场景库正式开发和正式 manifest 生成之前；当前尚无证据证明主平台已取得并递归导入正式 manifest；
- 当前结论：场景库云端发布与独立版验收正常，但双平台导航拆分联合验收失败，整体任务不能标记完成；
- 回滚判断：暂不回滚场景库。回滚不会让主平台获得缺失的菜单树或修复其 iframe 参数，且会失去已上线的嵌入协议；
- 下一步：先用场景库源站直接打开合法 embedded URL，证明隐藏 Sidebar 和 viewId 定位是否正常；再从主平台页面读取实际 iframe `src`，形成给主平台团队的一次性整改证据包。

### 2026-08-20 - 阶段 9：场景库 embedded 直连隔离验证通过

- 验证地址：`http://47.122.104.52:8081/cockpit/?embedded=1&viewId=eq-0`；
- 视觉结果：场景库自身 Sidebar 和顶部平台栏均隐藏，只显示业务内容；
- 页面定位：直接打开 `eq-0`，标题为“水轮机智能运维”，水轮机页面内容正常；
- 结论：场景库端 `embedded=1` 识别、隐藏导航和合法 `viewId` 定位均正确。主平台截图中的双导航、缺失子菜单和错误页面不是场景库解析失败，问题范围已收敛到主平台菜单数据/菜单渲染、主平台 route query 以及 iframe `src` 生成；
- 下一步：在主平台异常页读取 iframe 元素的实际 `src`，确认它是否缺少 `embedded=1` 或使用错误/缺失的 `viewId`。

### 2026-08-20 - 阶段 9：主平台二级分类首页不可点击问题定位

- 用户进一步确认：主平台传递的页面路由和场景库 `viewId` 定位正常；三级叶子“水轮机智能运维”（`eq-0`）能够在嵌入模式下正确显示；
- 菜单层级：主平台一级为“场景库”，原场景库根菜单“工业智能运维”“仿真分析”等成为二级分类，其原有子项成为三级菜单；
- 复现规律：二级分类存在 `children` 时，只能展开/收起，不能选中并显示该分类自身的首页；二级分类没有 `children` 时可以正常进入自身首页，但自然没有三级菜单；
- 原因定位：场景库原导航允许同一节点同时具有“自己的页面”和“子菜单”；主平台菜单组件把有子项的节点当作纯 `SubMenu`，标题点击只控制展开，不执行该节点保存的 `viewId` 路由；
- 文档缺口：既有协议要求保留父子结构，也要求 `smart-ops` 等根节点可打开，但没有明确规定当主平台菜单组件不支持“可点击父节点”时应如何转换；
- 图中“水轮机多工况数字孪生仿真”已按 `sim-visual-hydro-turbine` 正确定位；页面内部提示“外部模型服务暂不可用”属于外部模型接口状态，不是菜单路由错误；
- 当前结论：这是主平台菜单交互模型与场景库菜单数据模型不兼容，不是场景库云端版本、iframe 页面定位或 Vue/React 路由故障；
- 已选整改方案：主平台导入/转换 manifest 时，对全部 20 个二级分类统一增加一个排在首位的“首页”叶子；分类节点只负责展开，首页沿用分类原 `viewId`，其他 children 原样保留；即使分类原来没有 children，也生成唯一的“首页”；
- 备选整改：主平台改造菜单组件，使父级标题在保留展开能力的同时也能执行自身路由。该方案会触及主平台通用菜单行为，影响面更大，不作为首选；
- 发布判断：上述推荐方案只需主平台修改菜单转换/配置并重新发布菜单数据；场景库运行时代码和当前云端 Release 暂不需要重新部署。
- 交付文档：新增 `主平台统一导航拆分方案-20260820/07-主平台分类首页子菜单整改说明.md`，并同步修订方案总览、主平台指导、定位协议、验收说明和修改日志；

### 2026-08-20 - 阶段 9：前四个外部模型仿真页初始化故障定位

- 影响范围：`外部模型仿真展示方案` 主导的前四页，对应模型 `2326`、`2328`、`2316`、`2310`；
- 直接复现：场景库源站以 `embedded=1&viewId=sim-visual-hydro-turbine` 打开后仍显示“外部模型服务暂不可用”，因此排除主平台菜单和 iframe 路由；
- 场景库服务：内部健康接口返回 200、版本 `20260820-173922`；水轮机 bootstrap 返回 HTTP 502、错误码 `UPSTREAM_ERROR`；
- 当前上游配置：`VISUAL_MODEL_API_BASE_URL=http://8.146.211.204:3100/three-model-api`；
- 根因：主平台 `8.146.211.204:3100` 当前只接受 HTTPS。TCP 3100 可连通，但明文 HTTP 请求均被直接关闭并得到 `Empty reply from server`；
- HTTPS证书：系统校验通过，CN/SAN 均匹配 IP `8.146.211.204`，有效期 2026-08-19 至 2026-11-17；不需要使用不安全的跳过证书校验方案；
- 上游实测：使用场景库服务器实际 Node 运行时访问 HTTPS，四个模型的元数据与 Dashboard 共 8 个请求全部为 HTTP 200、业务 `code=200`；
- 缓存状态：水轮机模型二进制仍为持久缓存命中，活动版本 `504adbdbf7159ba8`，文件大小 7,661,788 字节；页面全屏失败是因为初始化同时依赖的元数据和 Dashboard 离线；
- 结论：属于场景库侧云端运行配置的上游协议不匹配，不是主平台菜单、场景库 `viewId`、模型页面代码或主平台 HTTPS API 本身故障；该配置会共同影响前四页；
- 附带代码问题：全屏初始化错误状态中的“查看资源详情”仍使用 `<a target="_blank">`，没有复用正常页面的主平台导航请求；该问题不造成接口离线，但不符合既定跨项目跳转要求；
- 待修复：备份 `/etc/scene-library-next.env`，把上游地址协议改成 `https://`，重启服务并执行四页专项验收；
- 待开发：将错误状态按钮接入 `handleOpenModelDetail/openHostModelDetail`，完成构建、发布和主平台内标签开关验收；
- 本次影响：只读诊断，没有修改环境、代码、current/previous、Nginx 或 systemd 服务状态。

### 2026-08-20 - 阶段 9：场景库 HTTPS 入口与证书就绪

- 整改原因：主平台已经使用 HTTPS；HTTPS 页面继续嵌入 `http://47.122.104.52:8081` 会被现代浏览器作为混合内容阻止；
- 兼容策略：保留原 HTTP 8081，不强制跳转；新增标准 HTTPS 443，主平台集成必须改用 `https://47.122.104.52`；
- 证书：使用 Certbot 5.7.0 为公网 IP `47.122.104.52` 申请受信任的 Let's Encrypt 短期 IP 证书；证书链和 IP SAN 校验通过；
- Nginx：实际生效配置 `/etc/nginx/sites-enabled/cockpit` 已增加 443 TLS 监听，仍保留 8081；修改前备份为 `/etc/nginx/backups/cockpit.enabled.before-https-20260820-1918`；
- 自动续期：`snap.certbot.renew.timer` 已启用；deploy hook 位于 `/etc/letsencrypt/renewal-hooks/deploy/reload-scene-library-nginx.sh`，续期后先执行 `nginx -t` 再 reload；
- 续期演练：`certbot renew --cert-name 47.122.104.52 --dry-run --run-deploy-hooks` 最终记录为所有模拟续期成功；
- 技术验证：外部 Windows 客户端无需跳过证书校验即可访问；HTTPS 独立页和公开健康接口均为 200；HTTP 8081 兼容入口仍为 200；Nginx active；
- 未修改：没有登录、部署或修改主平台 `8.146.211.204`；没有在此步骤切换场景库 Release；
- 主平台待办：iframe/微应用入口、消息 origin 白名单和 CSP `frame-src` 统一切换到 `https://47.122.104.52`；不得使用 `https://47.122.104.52:8081`；
- 交付文档：新增 `主平台统一导航拆分方案-20260820/08-HTTPS协议切换与混合内容整改说明.md`。

### 2026-08-20 - HTTPS 修复首个发布包预检失败，未切换

- 首个修复 Release：`20260820-193837`；Git 提交 `46d14c1209d387ea3a6e677af4b6b6bd1aa99597`；
- 本地专项检查、导航检查、双构建、构建产物协议检查均通过；发布包 SHA-256 为 `94C883A7275DA7FF58FE8DB90B31C3031E565E6CD636779FE295298A356B9813`；
- 上传、远端哈希和独立 Release 安装成功，`current` 始终保持 `20260820-173922`；
- 第一次使用 `runuser` 预检两次出现 esbuild `EACCES`；新旧 esbuild 权限均为 755、哈希完全相同，运行用户直接执行均成功，说明 `runuser` 不是可靠的 systemd 等价预检方式；
- 改用临时 systemd 单元复刻 `User=cockpit`、`Group=cockpit`、工作目录和 `NoNewPrivileges=true` 后，获得真正阻断错误：`ERR_MODULE_NOT_FOUND`，缺少 `src/remoteModelShowcase/modelCatalog`；
- 根因：旧手工 tar 清单只包含双 dist、`server.ts` 和 npm 清单，没有明确包含 `server.ts` 在生产运行时直接导入的 `src/remoteModelShowcase`；
- 安全结果：Release `20260820-193837` 从未激活，3103 临时单元已停止清理，旧服务健康正常；
- 整改：新增 `scripts/create-release-package.ps1`，强制打包并校验服务端运行源码；下一次使用新 Release ID 重新生成、上传、安装和预检，不在失败 Release 中手工补文件。

### 2026-08-20 - Release 20260820-195135 上线与技术验收完成

- 最终 Release：`20260820-195135`；部署源码提交 `eb1aeca8c1f904047b4570c27ad61f3aa544031d`；
- 发布包：`scene-library-release-20260820-195135.tar.gz`，大小 7,642,522 字节，目录项 305，SHA-256 为 `40C692F89850A1B85DA529C7D3E62F92990ECFF792C07B04A18D6B6AE240E944`；
- 自动打包检查：输出 `SERVER_RUNTIME_SOURCES_INCLUDED`，四个服务端必需 TypeScript 文件均在包中；
- 上传安装：远端 SHA-256 一致，新目录 473M；安装期间 `current` 保持旧版，旧健康接口正常；
- 生产等价预检：临时 systemd 单元使用 `cockpit:cockpit`、新 Release 工作目录和 `NoNewPrivileges=true` 在 `127.0.0.1:3103` 启动成功；
- 四页预检：水轮机 2326、污水泵 2328、桥式起重机 2316、矿卡/拖车牵引车 2310 的 `bootstrap` 均为 200，模型名称与缓存版本匹配；
- 生产切换：`current` 从 `20260820-173922` 切至 `20260820-195135`；`previous` 指向 `20260820-173922`；环境备份为 `/tmp/scene-library-next.env.before-20260820-195135`；自动回滚未触发；
- 运行环境：`VISUAL_MODEL_API_BASE_URL=https://8.146.211.204:3100/three-model-api`，内部健康返回 `20260820-195135`，3102 仅监听回环地址，3103 无残留；
- 外网验收：HTTPS 独立页、microapp HTML、`scene-library.js`、公开健康和四个 `bootstrap` 全部为 200；证书由外部客户端正常信任；HTTP 8081 兼容入口仍为 200；
- 构建产物：现网主资源 `/cockpit/assets/index-3OsWkdp_.js` 含 HTTPS 主平台地址和 `scene-library:navigate`，不含旧 HTTP 主平台地址；
- 证书续期：Let's Encrypt IP 证书 SAN 为 `47.122.104.52`；Certbot renew timer active/enabled，模拟续期已通过，deploy hook 会在续期后校验并 reload Nginx；
- 日志：服务切换时旧进程收到 SIGTERM 后以 143 退出，随后新版正常监听，属于主动 restart；切换后无应用启动错误；
- 场景库侧结论：协议修复、发布和服务器技术验收完成；主平台仍需按 `07-主平台分类首页子菜单整改说明.md` 和 `08-HTTPS协议切换与混合内容整改说明.md` 发布自身配置/代码并完成联合浏览器验收。

### 2026-08-20 - Release 20260820-201251 模型详情导航诊断增强上线

- 故障复核：四个模型详情 URL 已是 `https://8.146.211.204:3100/three-model/detail?id=...`；场景库请求只携带 `/three-model/detail` 和模型 ID；
- 主平台根因：当前 iframe 配置为同源 `/cockpit/`，浏览器实际消息来源为 `https://8.146.211.204:3100`，但主平台运行代码仍以 `http://47.122.104.52:8081` 做接收白名单和返回消息目标；
- 场景库修改：超时提示明确指向 iframe Origin 白名单，并在控制台记录 `iframeOrigin`、`expectedParentOrigin`、路径和模型 ID；协议版本和安全白名单未放宽；
- 主平台说明：新增 `09-模型详情跳转消息Origin热修说明.md`，给出接收校验与返回 `targetOrigin` 的精确修改方式；
- 本地验证：`typecheck:navigation`、`verify:navigation`、`verify:https-integration` 与 `build:all` 全部通过；
- 部署源码提交：`a6273187f62d7146326f61b7526b7040df3446f2`；
- 发布包：`scene-library-release-20260820-201251.tar.gz`，大小 7,643,435 字节、目录项 305、SHA-256 为 `ED0C1E777BA9316ABC00A8F8A1BE83CEDD310703E49439C9868742E2EE885B26`；
- 上传安装：本地与远端 SHA-256 一致，新目录 473M；安装期间正式 `current` 仍为 `20260820-195135`；
- 首次 3103 预检：失败但未切换。原因是临时 systemd 命令中的 `--setenv PORT=3103` 被 `EnvironmentFile` 中的 `PORT=3102` 覆盖，新进程没有在 3103 监听；正式旧服务持续健康；
- 修正预检：使用 `/usr/bin/env HOST=127.0.0.1 PORT=3103 APP_VERSION=...` 在加载环境文件后覆盖临时字段；健康版本和四个模型 bootstrap 全部通过，3103 随后清理；
- 生产切换：`current` 从 `20260820-195135` 切至 `20260820-201251`，`previous` 指向 `20260820-195135`；自动回滚未触发；
- 版本字段：切换复核发现辅助字段 `RELEASE_VERSION` 仍是旧值，已备份环境文件、同步为 `20260820-201251` 并重启复核；`APP_VERSION` 与 `RELEASE_VERSION` 现已一致；
- 技术验收：内部和 HTTPS 公开健康均返回 `20260820-201251`；HTTPS 独立页、microapp HTML/JS、四个公开 bootstrap 均为 200；HTTP 8081 兼容入口仍为 200；
- 产物验收：现网 standalone 主资源包含 `scene-library:navigate` 和 `expectedParentOrigin` 新诊断字段；
- 运行状态：服务 active/enabled，3102 仅监听 `127.0.0.1`，3103 无残留；Nginx 配置测试通过；
- 回滚信息：回滚目录 `/nlplabProj/scene-library/releases/20260820-195135`，切换前环境备份 `/tmp/scene-library-next.env.before-20260820-201251`；
- 联合验收状态：场景库侧发布完成；主平台必须按 09 文档修改并发布后，“查看资源详情”按钮才会收到导航结果。浏览器生成的 `event.origin` 不能由场景库版本改变。

### 2026-08-21 - Release 20260821-084944 一号机组 3D 浮层浅色化上线

- 用户反馈：一号机组 3D 视窗左上“当前检视”和底部时间轴为深色块，文字也偏深，导致信息不清；
- 源码处理：为两个浮层增加独立专用类，为历史/预测计数增加 `data-mode`，最终样式置于主题文件末尾，避免旧 HUD 和 Canvas 通用规则覆盖；
- 配色：浮层为冷调浅灰蓝 `rgba(241, 247, 250, 0.96)`，正文为深蓝灰 `#20384a`，边框为 `#9fb8c7`；历史/预测标签分别使用淡蓝与淡琥珀底；
- 影响边界：只修改一号机组两个 HUD 及时间轴控件，不修改 3D 模型、数据、预测、上传、布局、路由或其他页面；
- 部署源码提交：`41cec4ebf75ecd137fe026c26dbffaa446b881da`；
- 本地验证：导航专项类型检查、导航协议检查、HTTPS 集成检查和 standalone/microapp 双构建通过；构建后两套 CSS 均包含新专用类和配色值；
- 发布包：`scene-library-release-20260821-084944.tar.gz`，大小 7,638,951 字节、目录项 305、SHA-256 为 `1865D637E1CBE43F4572FBAFB33A5404387C5656A5A2641449759333831F3838`；
- 上传安装：远端 SHA-256 一致，新目录 473M；安装期间 `current` 保持 `20260820-201251`；
- 生产等价预检：临时 systemd 单元在 3103 启动成功，健康版本与四个模型 bootstrap 均通过，随后无 3103 残留；
- 生产切换：`current` 从 `20260820-201251` 切至 `20260821-084944`，`previous` 指向 `20260820-201251`；自动回滚未触发；
- 技术验收：内部及 HTTPS 公开健康均返回 `20260821-084944`；独立入口、`embedded=1&viewId=eq-18`、microapp HTML/JS 均为 200；
- 现网产物：公开 CSS `/cockpit/assets/index-BOWjSLtF.css` 已核对新选择器和 `#f1f7faf5`、`#20384a`、`#9fb8c7`；
- 环境一致性：`APP_VERSION` 与 `RELEASE_VERSION` 均为 `20260821-084944`；服务 active，3102 仅监听回环地址，Nginx 配置测试通过；
- 目视验收边界：当前无可连接浏览器实例，未完成截图级视觉检查；用户需强制刷新页面后确认实际观感。

### 2026-08-21 - Release 20260821-085852 一号机组深色 HUD 彻底移除

- 触发原因：Release `20260821-084944` 用户目视验收未通过；旧深色块仍在，并出现浅色层与深色层叠加观感，部分文字颜色接近背景而不可见；
- 根因确认：两个入口均已返回首版新哈希，并非主平台代理旧文件；组件仍同时保留旧 `unit1-predictive-hud`、`bg-slate-900/80`、`bg-slate-900/90` 等类，CSS 也保留三代旧 HUD 规则，形成层叠竞争；
- 代码修复：从 JSX 删除全部旧 HUD、深色背景、深色边框及浅字工具类；从 CSS 删除所有 `.unit1-predictive-hud` 历史规则；
- 文字防漏：显式覆盖“当前检视”标签、当前时间、时间轴说明、计数、历史起点、当前计算和预测终点；面板及全部后代默认使用深色、不透明、正常混合模式；
- 面板结构：只保留单层完全不透明浅色背景 `#f1f7fa`，正文基色 `#20384a`；不再使用半透明 HUD 背景或浅色文字；
- 部署源码提交：`e612f665874cfe983edf449674ca5cb96b08f6f2`；
- 本地验证：专项类型检查、导航检查、HTTPS 集成检查与双构建通过；构建 CSS/JS 均不存在旧 HUD；全部文字与专用深色规则均存在；
- 发布包：`scene-library-release-20260821-085852.tar.gz`，大小 7,646,650 字节、目录项 305、SHA-256 为 `A19133C58A88CD9845737745D621128A3F9C3B085356ED28918F9DD93734A07B`；
- 上传安装：远端哈希一致，新目录 473M；安装期间正式版本保持 `20260821-084944`；
- 生产等价预检：3103 健康版本与四个模型 bootstrap 全部通过，临时服务随后清理；
- 生产切换：`current` 从 `20260821-084944` 切至 `20260821-085852`，`previous` 指向 `20260821-084944`；自动回滚未触发；
- 技术验收：内部与 HTTPS 公开健康均返回 `20260821-085852`；独立页、`embedded=1&viewId=eq-18`、microapp HTML/JS 均为 200；
- 现网正向检查：公开 CSS 包含全部专用文字类、`#f1f7fa` 和 `#20384a`；公开 JS 包含五组完整界面文字；
- 现网反向检查：公开 CSS 与 JS 均不存在 `unit1-predictive-hud`；
- 环境与运行：`APP_VERSION`、`RELEASE_VERSION` 一致；服务 active，3102 仅监听回环地址，3103 无残留；Nginx 配置测试通过；
- 回滚：`/nlplabProj/scene-library/releases/20260821-084944`；环境备份 `/tmp/scene-library-next.env.before-20260821-085852`。

## 10. 每次更新本记录时必须遵守

- 只记录实际执行且有输出依据的结果；
- 不记录密码、私钥正文、Token 或完整秘密环境文件；
- 写清实际 Release ID，不用“最新版”代替；
- 写清切换前真实 `OLD_RELEASE`，不硬编码历史版本；
- 失败也要记录失败位置以及线上旧版本是否仍正常；
- 回滚时记录回滚原因、目标、结果和健康检查；
- 主平台若未完成适配，明确写“场景库侧已部署，主平台联合验收待完成”。
