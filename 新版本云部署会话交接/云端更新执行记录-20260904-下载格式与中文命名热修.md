# 云端更新执行记录：下载格式与中文命名热修

> 执行日期：2026-09-04（Asia/Shanghai）  
> Release：`20260904-152456`  
> 最终状态：Release `20260904-152456` 已上线并完成下载、浏览器和服务验收  
> 范围：仅场景库服务器 `47.122.104.52`；不登录、不修改主平台服务器

## 1. 发布内容

- 四个试点页的数据规范支持 PDF/DOCX/JSON，数据样例支持 CSV/XLSX/JSON/ZIP，预测结果支持 CSV/XLSX/JSON，分析报告支持 PDF/DOCX/JSON。
- 四类下载弹窗均可填写设备 ID、自由选择格式；默认带出当前筛选设备，未筛选时带出该模型第一台设备。
- 文件名统一为 `<模型ID>-<页面中文模型名>-设备ID-<设备ID>-<中文资料名>.<扩展名>`，ZIP 内文件使用同一规则，样例内容的 `device_id` 与下载选择一致。
- 修复覆盖同一设备后旧原始批次文件残留、损坏文件解析失败后 `.part` 残留；上传异常主动取消任务，服务启动时清理无法恢复的遗留分块。
- 试点仍仅为水轮机 `2326`、污水泵 `2328`、桥式起重机 `2316`、矿卡/牵引车 `2310`，没有推广到其余 102 页。

## 2. 本地验证

```text
npm run build:all=通过
npm run typecheck:navigation=通过
npm run verify:navigation=通过，roots=20 nodes=1050
npm run verify:model-loading=通过
npm run verify:model-stability=通过
npm audit --omit=dev --audit-level=low=0 个漏洞
git diff --check=通过
下载矩阵=4 模型 × 每模型 13 种格式，共 52 个文件通过
中文命名=RFC 5987 文件名、中文模型名、设备 ID、中文资料名均通过
产物内容=ZIP 内名称、DOCX XML、XLSX 工作表及 30 行 device_id、CSV BOM、JSON、PDF/ZIP 文件头通过
生命周期=覆盖旧批次清理、设备删除、失败解析临时文件清理、启动残留清理通过
Playwright=4 页 × 桌面/移动；格式列表、16 次实际下载文件名、上传、确认弹窗通过
```

全仓 `npm run typecheck` 仍只命中既有 7 个维护计划 `three-types.ts` 非法字符文件，本次新增代码未出现在错误中。首次同时运行多个本地开发实例时 Vite HMR 端口冲突产生 WebSocket 噪声；关闭旧实例、只保留一个实例后完整回归通过，不属于生产代码缺陷。

## 3. 发布包

```text
Release=20260904-152456
sourceCommit=04209ba1eda3a74ff54e0010befda078bfb6ac6b
工作区=dirty，按用户要求发布当前工作区快照；未清理或覆盖其他改动
package=scene-library-release-20260904-152456.tar.gz
bytes=53,741,485
entries=319
SHA-256=96624BAA3BA1008C9874ABC2EBBE2B943E1C4AE3A2F651822F99B6454E7D5FC3
```

发布包已独立解压并执行与生产一致的完整 `npm ci`，安装 413 个包；包内服务启动后健康版本和带设备 ID 的 DOCX 下载通过。仅安装生产依赖会缺少当前 systemd 启动所需的 `tsx`，因此继续遵循既有完整安装规范。

## 4. 云端只读基线

```text
current=20260904-131956
previous=20260901-093430
APP_VERSION=20260904-131956
RELEASE_VERSION=20260901-093430
service=scene-library.service，active/enabled
NRestarts=0
WorkingDirectory=/nlplabProj/scene-library/current
listen=127.0.0.1:3102
3103=空闲
health.version=20260904-131956
磁盘=40 GiB，已用 20 GiB，剩余 18 GiB，使用率 54%
nginx -t=successful
shared/data=cockpit:cockpit
shared/model-cache=cockpit:cockpit
```

最初按名称 `scene-library-next` 查询时现场返回单元不存在，但 3102 健康。继续只读检查进程 cgroup 和交接文档后确认真实单元为 `scene-library.service`，随后 active/enabled、启动命令与工作目录均通过；该差异只影响核查命令，没有修改现场状态。

## 5. 上传、安装与 3103 预检

```text
远端包 bytes=53,741,485
远端 SHA-256=96624baa3ba1008c9874abc2ebbe2b943e1c4ae3a2f651822f99b6454e7d5fc3
新目录=/nlplabProj/scene-library/releases/20260904-152456
所有者=cockpit:cockpit
npm ci=413 个包
tsx=4.23.12
Node=22.23.2
3103 health.version=20260904-152456
3103 下载矩阵=52/52
3103 生命周期=覆盖旧文件、设备删除、失败临时文件清理均通过
预检数据=/tmp/scene-library-preflight-data-20260904-152456，与生产 shared/data 隔离
预检完成后 3103 已停止
current=20260904-131956（安装和预检期间未改变）
```

依赖安装继续使用共享 npm cache。3103 从新 release 工作目录启动，避免非 `cockpit` 可访问目录导致派生进程失败；预检完成后按监听进程精确停止，生产服务未重启。

## 6. 正式切换

切换前再次解析现场 `current=20260904-131956`，并将其设为直接回滚目标。脚本备份环境文件、原子更新 `previous/current`，重启 `scene-library.service`，并在任一门禁失败时恢复链接、环境和旧服务。

第一次切换的新服务和 Nginx 已正常，但脚本错误地用 Express 直连根路径 `http://127.0.0.1:3102/` 检查静态页，得到预期的 404 并触发自动回滚。回滚后逐项确认：

```text
current=20260904-131956
previous=20260901-093430
APP_VERSION=20260904-131956
RELEASE_VERSION=20260901-093430
service=active
NRestarts=0
health.version=20260904-131956
```

随后将门禁修正为 Nginx 实际前缀 `/cockpit/`、`/microapps/scene-library/` 和 `/scene-library-api/health`，第二次切换一次通过：

```text
current=20260904-152456
previous=20260904-131956
APP_VERSION=20260904-152456
RELEASE_VERSION=20260904-131956
health.version=20260904-152456
```

两次均由计划内 `systemctl restart` 结束旧进程，没有应用崩溃；最终 `NRestarts=0`。

## 7. 切换后验收

```text
内部 health=HTTP 200，version=20260904-152456
公网 HTTPS health=HTTP 200，version=20260904-152456
公网 HTTP 8081 health=HTTP 200，version=20260904-152456
standalone /cockpit/=HTTP 200
microapp /microapps/scene-library/=HTTP 200
公网下载矩阵=4 模型 × 13 格式，52/52 通过
文件名=中文模型名 + 设备ID + 中文资料名，RFC 5987 解码逐项通过
Playwright=4 页 × 桌面/移动只读回归通过，桌面实际下载 16 个指定格式文件名通过
截图=桌面/移动下载弹窗无遮挡、无横向溢出
生产数据=四页均为 120 条/1 台设备/1 个批次，检查前后未变化
service=active/enabled，NRestarts=0
journal error=近 15 分钟无记录
listen=127.0.0.1:3102
3103=stopped
```

本轮公网验收只读取页面、接口并下载文件，不上传、覆盖、重置或删除生产数据。浏览器中系统可能把“设备 ID”本地化显示为“设备编号”，字段值和服务端中文文件名没有被改变。

## 8. 回滚与边界

直接回滚目标为 `20260904-131956`。旧 release、两份环境备份、共享数据和共享模型缓存均保留；代码回滚不自动回滚或删除数据。

不得删除或覆盖 `/nlplabProj/scene-library/shared/data` 和 `shared/model-cache`。本次没有登录、修改主平台服务器 `8.146.211.204`，菜单结构和主平台交付内容不变。
