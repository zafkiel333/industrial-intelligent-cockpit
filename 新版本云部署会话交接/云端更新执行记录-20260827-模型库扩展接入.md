# 云端更新执行记录：模型库扩展接入

> 执行日期：2026-08-27（Asia/Shanghai）
> 部署对象：场景库服务器 `47.122.104.52`
> 最终状态：Release `20260827-142935` 已切换并通过技术验收；主项目服务器未修改

## 1. 发布内容

- 新增 99 个唯一页面—模型绑定，连同既有 4 个样例，共 103 个模型展示页；
- 新增 `eq-unit1-model`，对应模型 `6691`；
- 保护页面 `eq-18` 的组件、数据逻辑和菜单位置均未改动；
- 仿真分析前四页位置不变；
- 各栏目已将接入模型库的页面前置，未接入页面后移并保持相对顺序；
- 模型详情跳转白名单已由 103 模型目录统一生成；
- 主项目模型 API 无需配合修改；主项目导航菜单与宿主详情白名单仍需对方团队按交付文档发布。

## 2. 代码与发布包

```text
发布源码提交：7e375ceb5e4aa16b47bad5fd7942d217def06e70
发布包：scene-library-release-20260827-142935.tar.gz
发布包大小：39,988,373 字节
发布包条目：325
SHA-256：9b6258539457d939d2f756dcf4a2185118c05fe98632beb0c24e9c83434eafdb
安装目录：/nlplabProj/scene-library/releases/20260827-142935
```

发布包内包含 standalone、microapp、服务端运行源码及锁定依赖。远端安装后共有 362 个依赖包，tsx 为 `4.23.12`，Node 为 `22.23.2`。

## 3. 分阶段执行结果

### 3.1 本地验证

```text
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1050
MODEL_LIBRARY_EXPANSION_VERIFY_OK bindings=99 showcasePages=103 menuNodes=1050
HTTPS_INTEGRATION_VERIFY_OK models=103 files=19
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=http://127.0.0.1:3000/api
```

- standalone 与 microapp 双构建通过，分别转换 5,530 与 5,525 个模块；
- 模型 `6691` 实际下载 2,134,604 字节，FBX 格式校验通过；
- C 级代表页 `pm-hydro-19` 的动态字段同步和诊断返回正常；
- 全量类型检查仍受既有 7 个 `Maintenance-plan-management/*/three-types.ts` 非法字符文件阻断，本次导航专项检查和双构建未新增错误；
- 本轮没有可用的交互式浏览器实例，因此未将 HTTP 与资源校验记录为像素级目视验收。

### 3.2 安装与切换

先发布 Release `20260827-102257` 完成 99 页验证。随后发现模型详情按钮仍受原四模型本地白名单限制，修复后生成并切换最终 Release `20260827-142935`。

```text
current  → /nlplabProj/scene-library/releases/20260827-142935
previous → /nlplabProj/scene-library/releases/20260827-102257
```

最终环境公开字段：

```text
HOST=127.0.0.1
PORT=3102
APP_VERSION=20260827-142935
RELEASE_VERSION=20260827-102257
```

环境文件备份位于：

```text
/tmp/scene-library-next.env.before-20260827-142935
```

切换脚本带失败自动回滚，本次未触发。旧 Release、临时发布包、共享数据和模型缓存均未删除。

### 3.3 切换后技术验收

```text
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=https://47.122.104.52/scene-library-api
```

- 99 个新增绑定的模型 ID、格式、文件大小及 Dashboard 字段均通过线上 API 校验；
- `eq-unit1-model/6691`、`eq-0/2353`、`pm-hydro-19/2296` 通过 3103 生产等价预检；
- HTTPS、HTTP 兼容入口、health、standalone、embedded 页面及 microapp 均返回 200；
- 微应用脚本 Content-Length 为 `18,072,302`，Content-Type 为 `application/javascript`；
- `scene-library.service` 为 active/enabled；3102 仅监听 `127.0.0.1`，3103 无残留；
- Nginx 配置检查通过，切换后服务错误日志无新增记录；
- `shared/data` 和 `shared/model-cache` 完整保留。

## 4. 正式菜单清单

```text
文件：主平台统一导航拆分方案-20260820/scene-library-menu.manifest.json
menuVersion：20260827-142935
sourceCommit：7e375ceb5e4aa16b47bad5fd7942d217def06e70
sourceDirty：false
根节点：20
总节点：1050
唯一 ID：1050
menuContentSha256：270ed2cd19452df4e6ec2c5916974042744cea6fdf01e76e544ddd62bbe88951
文件 SHA-256：c54f5375db0b99460d5848895b436b47ffe0848aea69590a7d3719e24b7d14bd
```

主项目团队应以该 JSON 为唯一机器可读菜单来源，并按 `主项目模型库扩展接入-20260826/07-主项目导航栏更新列表与发布说明.md` 原子替换旧菜单。不得把人工摘录表重新手录成菜单。

## 5. 回滚与剩余验收

直接回滚目标为 `20260827-102257`。回滚只切换应用 Release 和环境版本，不删除或恢复共享业务数据。

仍需完成：

1. 主项目团队导入 1,050 节点正式菜单，并发布 103 模型详情消息白名单；
2. 在目标浏览器抽查模型朝向、材质、缩放、热点文字和“模型展示/原业务页”切换；
3. 验收主项目 iframe 菜单点击、刷新恢复、`eq-18` 位置与仿真分析前四页顺序；
4. 若代码托管远端仍因 TLS 握手失败无法接收提交，网络恢复后补推；场景库生产发布不依赖该推送。

## 6. 关联交付文档

- `主项目模型库扩展接入-20260826/07-主项目导航栏更新列表与发布说明.md`
- `主项目模型库扩展接入-20260826/08-实施与云部署验收记录.md`
- `主项目模型库扩展接入-20260826/修改日志.md`
