# 模型兼容修复与中文排查（2026-09-08）

阶段：全部本轮修复已于 2026-09-08 19:45:45（北京时间）发布为 20260908-194130，线上三模型、三中文页面及四试点正常／异常图回归通过。直接回退 20260908-192630。106 页推广清单已准备，当前数据工作区仍为四页试点，其余 102 页尚未推广。

## 根因与修复

1. ia-turbine-wear / 2338：571113 字节源 FBX 内存在 51912 个顶点对应的位置分量 155736、三角索引 51912，Geometry 为 Mesh，但连接的 Model 被导出器标记为 Null。Three.js 按 Null 创建空组而忽略几何。兼容模块仅在确有单一网格连接时将类型属性的四字节 Null 改为 Mesh；不改原文件、顶点、材质或变换，缓存仍保存原始字节。
2. ia-ship-eeoi / 2362：首次请求确实返回 HTTP 502，上游模型存储返回“模型获取失败：下载错误，请联系运维人员处理”；不是前端解析失败。后续重试已恢复并持久缓存 1896784 字节，原船模型成功渲染。修正“尚无可用缓存”时仍提示继续使用上次版本的错误文案。没有登录或修改主平台服务器；仅通过现有模型 API 读取资源。
3. 扩展发现 sim-port-motion / 6639：42,478,808 字节、文件头 FBX 7400，但 step_to_fbx_png.py 导出使用非标准节点布局，标准加载器在偏移 155 失败。增加严格限定导出器签名、单网格、无附加变换的读取器，读取原始 631671 个顶点、1013118 个三角形及法线；该导出遗漏 UpAxis，按桅杆的 -Z 方向转为 Three.js +Y 向上。该文件原材质未给出颜色，使用中性材质，不更换模型或补造几何。网格数组、索引、范围和格式均验证。

另外按视口水平/垂直视场角适配包围球距离，避免模型虽然成功加载却被默认近距离视角裁切。

## 扩展审计证据

- 全部 106 页面对应 106 模型完整下载，699104059 字节；最终都可下载。不是只检查文件头或 bootstrap。
- 使用实际 Three.js FBXLoader 和兼容模块解析全部 106 文件、检查非空网格、顶点和有限包围盒；最终失败 0。
- 三个问题页面实际 Chrome 渲染、重新打开及截图通过。兼容专项与原有视图居中/旋转稳定性测试通过，原文件字节保持不变。
- 审计脚本 scripts/audit-model-binaries.ts、scripts/audit-model-parsing.ts，结果 .runtime-cache/model-repair/audit/。

## 中文显示

用户补充要求：页面主体应为中文，仅保留 ID、单位和必要专业缩写。闸坝页船名直接在源数据改为华运05号、长江号、金星号、蓝鲸号、江王号；状态内部值不变、使用中文展示映射。进出闸、闸室水位、节水、调度策略、图表图例与提示直接使用中文。

扫描 views/components 下 1945 个 TSX 文件中的可见文本及常见展示属性，识别业务文案与应保留的编码/单位；公共 operationalChinese 词表补充船名、状态、业务提示及常见技术词。船舶交通、海事安全页面的同类残留一并修复，61 条代表性中文及 ID/单位保留断言通过。扫描候选包含大量编码、单位、文件名和专名，候选数量不等于英文缺陷数量。运行时完整浏览器验证覆盖闸坝、海事安全、港口交通三页；未声称逐页浏览全部 1000 余页面。

## 推广准备

用户认可四页数据可视化，要求准备推广至计划页面，先解决上述问题。本轮不改 106 页面模型绑定与菜单，不将所有设备套用水轮机阈值。后续按每个模型配置数据字段、单位、参考限、采样频率、正常/大幅异常成对验收数据，并分批发布；eq-18 保留原页面。配套要求继续遵循外部模型数据上传与预测诊断-20260903/验收数据/README.md。

已生成 `外部模型数据上传与预测诊断-20260903/07-全计划页面可视化推广清单.md`，逐项列出 106 页、模型 ID 和四页已上线／102 页待推广状态。

## 首次发布与线上追加发现

- 20260908-144302：包 53807545 字节，325 条目，SHA256 F6F3A8273474E01E2948A9AFC35D1ECB493438C864081236ED808673B48F2FFE；19:21:24 切换，直接回退 093932。安装、52 项下载／协议生命周期、四页时序接口隔离预检通过；HTTPS、8081 双入口静态资源哈希与数据总量核对通过。
- 线上浏览器验收未通过：dashboard 上游请求持续超时（UPSTREAM_TIMEOUT），导致 bootstrap 的 Promise.all 整体失败，隐藏模型及本地数据区。不能把这次失败记成线上模型验证通过。
- 追加修复：bootstrap 在实时数据故障时返回明确不可用状态与空字段，保留模型信息和本地数据区；不生成假数值、不记作成功遥测、不将空记录加入诊断历史。页面显示尚无实时数值和自动重试；恢复后正常轮询数据替换空态。实时接口错误改中文。元数据与模型绑定校验仍保留。
- 独立故障注入测试 `scripts/verify-dashboard-isolation.mjs`：503 实时接口下 bootstrap=200、空值不伪造、本地数据 API 可用、上游恢复成功。目标类型检查、61 条中文断言、模型加载约束和双入口构建通过。全项目类型检查的既有问题未在本轮消除。
- 追加包 20260908-192630：53803071 字节，325 条目，SHA256 D9190FAD91E2DF36A79A87EE5BBE27689F8400C268FE68C4BBA690E85B1D4A62。

## 断连缓存补修

192630 隔离预检、生产切换和四页正常／异常图桌面窄屏回归通过。线上实时数据不可用时初始化已能成功，本地数据仍可查看。但模型二进制接口同样先请求上游元数据，仍会超时，故三个模型线上渲染验收尚未通过。

补修：持久缓存增加 modelId，断连时仅允许当前配置 ID 一致的缓存继续使用；有元数据时原指纹比较和改绑替换校验不变。加载错误绑定缓存时拒绝使用。旧缓存通过本轮 106 个已下载并解析文件的 SHA256、场景 ID 和模型 ID 完整比对后补齐 modelId，原索引备份于 shared/model-cache/manifest-backup-20260908-model-bindings，二进制未改动。专项测试增加离线模型字节一致性、bootstrap 可用和错误绑定拒绝，均通过。应用接口的 STALE 显示改为“应用接口数据未更新”。

20260908-193430 包：53808759 字节，325 条目，SHA256 176A241DF6F6E5402C1E6B3F909FC0DE7F4DF869F3C922D815574FEBE1474491。上传未结束时提前执行的一次安装被 SHA256 校验拒绝，未解包、未修改生产版本；待上传完成后必须重新校验再安装。

## 最终构建与传输方式

继续检查通用业务词汇，补齐发布、审批、安装、退役、参数不匹配、风险及维护状态；“Unit Tests”由错误直译改为“单元测试”。69 条中文及标识保护断言通过。

- 最终包 20260908-194130：53810029 字节，325 条目（316 个文件），SHA256 02D03275BD1C535EBB9DA6A382EAC4D2BFAEF3003464EAAEBAE251A38D87F1C8。
- 因 193430 全包上传速度降低，最终版复用该已传输基包中的相同文件。差异包 7358597 字节，SHA256 6bd158852a58af2ab3f66f1ae367914bdf06b614ffb30d839756acb4a9f0bbe6；全文件清单 SHA256 c4458942a2d844ea0f30ef469fef25bbca2aa680b9a12a65dfcc0059e505bdb6。
- 远端必须先验证基包、差异包、清单三个哈希，再按最终清单将 316 个文件逐一校验后构建新 Release；不复制旧目录的多余文件、不在 current 上覆盖。仍单独执行 npm ci、隔离预检、原子切换与线上验收。193430 未切换生产。

## 最终上线与验收证据

- 三份传输哈希通过，`COMPLETE_RELEASE_FILE_HASHES_OK 316`，依赖安装与 tsx 启动通过，锁文件不变。
- 隔离预检：52 下载、32 协议样例及生命周期通过；四模型历史／预测接口通过；三个问题模型二进制内容与原始缓存 SHA256 一致。
- 19:45:45 原子切换成功，current=194130、previous=192630；APP_VERSION 与 RELEASE_VERSION 同步。HTTPS 与 8081 的独立／微应用入口 HTML、脚本和样式均与本地最终构建哈希一致。
- 实际 Chrome 公网渲染通过：ia-turbine-wear、ia-ship-eeoi、sim-port-motion；三份截图人工查看，模型完整显示且方向正确。
- 实际 Chrome 中文页面通过：ia-lock-efficiency、cp-maritime-safety、sim-port-flow；截图船名、进出闸、闸室水位、状态、图表主体中文，S-8821、1500t 等标识和单位保留。截图与页面文字保存在 `.runtime-cache/model-repair/production-shots/`。
- 四页数据区桌面／窄屏回归 `STATUS_UI_OK`：正常与异常区分、预测越限、精确读数、误差区间开关、两种视图均通过；水轮机保留用户正常／大幅异常设备，其他三页不向生产额外导入验收数据。
- 服务 active/enabled、NRestarts=0，3102 仅回环监听，3103 临时预检已停止，nginx 配置正确。最终服务日志未见启动异常。
- 四份生产 state.json 的 SHA256 与发布前一致：水轮机 540 条／3 设备／3 批次，其余三页各 120 条／1 设备／1 批次。模型缓存索引变更有单独备份，运行数据未变更。
- 归档 `发布资料-20260908-194130/` 已保存菜单与全文件校验清单；本轮本机 4178 预览服务已停止。未登录或修改主平台服务器，未提交／推送工作区。

上游实时接口曾持续超时，本项目修复的是故障隔离和可靠缓存，不代表修复了上游服务本身。无有效缓存、首次资源不可用时仍应明确报错；不能用别的模型或伪造数值掩盖故障。中文检查包括全源扫描与代表页面浏览器验收，不等同于逐个点击全站所有交互状态。
