# 状态标签对比与数据管理弹窗更新

Release：20260909-181000。阶段：已切换并通过公网只读验收。

## 本次范围

- 将实测核验区域的“当前时间覆盖率”卡片替换为“本次对称误差（sMAPE）”，读取该次已计算的 smape；时间对齐及部分验证统计规则保持不变。
- 新增预测状态与实际状态并排大标签，分别显示正常/异常，并明确判断一致/不一致。未验证显示待验证，缺实际标签显示未提供标签，不按正常处理。
- 修复通用520px弹窗样式覆盖数据管理宽度的问题。宽屏最大1560px、最大880px高，窄屏接近满屏；标题与工具区保留，表头固定，批次表内独立横纵滚动，按钮并排避免行高过大。
- 仅修改当前三项展示，不恢复此前已停止的主平台排查或其他待办。没有改动预测算法、设备存储或核验结果。

## 产物

- 基准commit：5870ee02b123bbdbe379c018ff5439557fbafdea，工作区快照构建。
- 包：scene-library-release-20260909-181000.tar.gz。
- 大小：54,314,097字节，482条目。
- SHA256：C2E191BF29DF8C7492DFC1140ABAA2C689236C4040DB0E751D2783FB3F7ECEEC，本地与远端一致。
- 上线前版本：20260909-163928。

## 本地验证

- 双构建、定向TypeScript、diff格式检查通过。
- 使用隔离临时数据启动接口回归通过，未写生产数据。
- verify-hydro-tags-and-manager.mjs：正常/正常、异常/异常、缺实际标签、判断不一致四种渲染通过；不一致场景使用浏览器响应夹具，不改动任何保存预测。
- 1600与390视口下检查弹窗尺寸、边界、固定表头与表格内部滚动通过；逐状态及弹窗截图已目视核对，未发现页面脚本错误。
- 发现自动中文处理会移除sMAPE缩写，已给该指标名增加本地化保护。

## 线上验收

- 独立3103预检通过：健康接口版本正确，四试点模型完整读取成功。
- current切换为20260909-181000，previous保留20260909-163928，未触发回滚。
- 环境备份：/etc/scene-library-next.env.before-20260909-181000。
- 切换前后生产设备state文件哈希一致；原有6次预测快照及累计2次有效核验保留，没有向生产写入测试数据。
- 公网1600/390视口弹窗尺寸、固定表头、表格滚动和边界检查通过，无页面脚本错误。
- 服务active，NRestarts=0；正式监听127.0.0.1:3102，3103预检服务已停止。
