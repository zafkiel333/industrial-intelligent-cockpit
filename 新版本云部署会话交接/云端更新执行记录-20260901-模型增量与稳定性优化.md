# 云端更新执行记录：模型增量与稳定性优化

> 执行日期：2026-09-01（Asia/Shanghai）  
> 最终状态：Release `20260901-093430` 已上线  
> 范围：仅场景库服务器 `47.122.104.52`；主平台服务器未登录、未修改

## 1. 发布内容

- 替换两个元数据存在但下载内容不是有效 FBX 的资源：`eq-0/2353→2363`、`turbine-blade-erosion/2286→2306`；
- 吊装实训改绑唯一的 `7193 混流式转轮`；
- 从主项目新增 22 个模型中选择 `8741/8736/8740` 接入 `eq-15/eq-16/eq-17`；
- 新增 106 模型发布前文件头审计；
- 服务端增加 8 项/128 MiB 双预算 LRU，定时器只刷新驻留模型；
- 浏览器缓存降为一个已完成 ArrayBuffer，页面卸载释放 WebGL context，失败自动重试最多两次；
- 生成 `20260901-093430` 正式菜单和 106 模型主平台交付包。

## 2. 提交与发布包

```text
运行代码提交=07005f60131f8a202d6de1c257d098bfb2ad9ed0
发布包源码提交=aa244232d0f937e6ba2a7f6405873d6fc90b8bae
发布包=scene-library-release-20260901-093430.tar.gz
字节=39,995,208
条目=326
SHA-256=6dcaa849b8807be3747b11c813b262ec3307e7f58947a174a9bf00d21a043b25
安装目录=/nlplabProj/scene-library/releases/20260901-093430
```

本地和远端 `/tmp` 包的字节与 SHA-256 完全一致后才解压安装。依赖使用共享 npm cache，以 `cockpit` 用户执行 `npm ci`；新 Release 的 `data`、`model-cache` 指向共享目录，安装阶段没有改变生产 `current`。

## 3. 发布前基线

```text
current=20260828-145036
previous=20260828-103420
service=active/enabled
NRestarts=0
MemoryCurrent=469,688,320 bytes
MemoryPeak=474,189,824 bytes
主机内存=1.6 GiB，无 Swap
listen=127.0.0.1:3102
3103=空闲
nginx -t=successful
```

服务没有崩溃或重启，磁盘使用率 50%。高内存来自旧模型缓存策略，不是数据库或磁盘故障。

## 4. 3103 隔离预检

```text
health.version=20260901-093430
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=102 base=http://127.0.0.1:33103/api
eq-0=7,665,980 bytes
eq-15=8,192,944 bytes
eq-16=21,846,688 bytes
eq-17=22,175,856 bytes
turbine-blade-erosion=8,679,052 bytes
TurbineRunnerHoistingTraining=1,135,500 bytes
```

六个文件均为有效二进制 FBX。连续加载十个代表模型后缓存为 8 项/114,535,408 字节，低于 8 项/134,217,728 字节上限。旧 3102 生产服务全程 active；预检完成后停止 3103 和 SSH 隧道。

## 5. 正式切换

切换脚本保存旧链接和环境文件，任意链接、服务、健康、Nginx 或入口失败都会恢复旧版。本次一次通过，没有触发回滚：

```text
current=20260901-093430
previous=20260828-145036
APP_VERSION=20260901-093430
RELEASE_VERSION=20260828-145036
```

计划内 `systemctl restart` 给旧进程发送 SIGTERM，日志记录 `status=143` 后立即启动新进程；新服务持续 active，`NRestarts=0`，不是应用崩溃。

## 6. 切换后验收

```text
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=102 base=https://47.122.104.52/scene-library-api
standalone=HTTP 200
eq-15 embedded=HTTP 200
microapp HTML=HTTP 200
microapp JS=HTTP 200
公开 health=HTTP 200，version=20260901-093430
HTTP 8081 compatibility=HTTP 200
6 个关键模型公网完整下载=HTTP 200、有效 FBX、字节与预检一致
modelMemoryCache=8 项/80,564,628 字节，上限=8 项/134,217,728 字节
service=active/enabled
NRestarts=0
listen=127.0.0.1:3102
3103=stopped
nginx -t=successful
```

完成 102 页 Bootstrap 和关键模型请求后，服务 `MemoryCurrent=369,889,280`、`MemoryPeak=398,376,960` 字节，低于旧服务发布前的 469,688,320/474,189,824 字节。

## 7. 主平台交付与回滚

主项目团队应接收：

```text
主平台导航正式交付包-20260901.zip
主平台导航正式交付包-20260901.zip.sha256
ZIP SHA-256=9d68ba09b32e5be935d6d21077ede2ca3243a3216417758a82e5427fe3f75c09
```

ZIP 内 manifest 和 allowlist 必须一起更新，分别负责排序和 106 模型详情白名单。主平台自行备份、构建、清缓存、发布及回滚。

场景库直接回滚目标为 `20260828-145036`。旧 Release、共享缓存、上传包和环境备份均保留；本次没有删除生产数据。
