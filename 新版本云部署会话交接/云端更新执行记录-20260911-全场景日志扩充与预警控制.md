# 云端更新执行记录：全场景日志扩充与预警控制

执行日期：2026-09-11

最终 Release：`20260911-145044`

直接回退 Release：`20260911-083049`

场景库服务器：`47.122.104.52`

主平台服务器：本次未登录、未修改

## 一、发布内容

- 场景库全部 1,050 个页面的基础场景日志扩充至每页 34 条。
- 944 个普通页面按 20 个既定一级分类、页面名称和业务领域生成运行、复核、分析、计划、闭环和预警记录，时间跨度约 561 天。
- 106 个外部模型页面按逐页六项指标、设备部位、预测周期、正常状态和故障知识生成运行、预测及实测核验记录，时间跨度约 350 天。
- 每页基础记录固定保留 2 条普通预警，其中一条位于较近的运行周期；严重预警最多 1 条，且只允许用于具有明确严重后果的故障配置。
- 普通页面未匹配到明确设备关键词时，使用所属一级分类的专属业务对象和环节，不再回落为通用设备套话。
- 外部模型基础日志以 `operational-v2` 稳定编号写入模型目录，重复读取不会重复追加；既有预测、核验和运行日志全部保留。

## 二、本地验收

以下检查全部通过：

```text
全量日志规则：1,050/1,050 页通过
普通页面：944 页，每页 34 条、2 条普通预警
外部模型页面：106 页，每页 34 条、2 条普通预警，严重预警最多 1 条
普通页面基础日志总数：32,096
外部模型基础日志总数：3,604
模型接口回归：106/106 页通过，生成预测后共读取 3,710 条日志
浏览器抽检：20 个一级分类代表页 + 3 个外部模型页通过
模型库：showcasePages=106，menuNodes=1050
导航：roots=20，nodes=1050
standalone / microapp 双构建：通过
中文文案审计：通过
```

源码提交：

```text
920597a59a726a2133a9d1d2bc19ad836b446511
feat: 扩充全场景业务日志与预警记录
```

## 三、发布包

```text
文件：scene-library-release-20260911-145044.tar.gz
大小：54,358,172 bytes
条目：486
SHA-256：537C82D16F93F0BE5B7A3C0F97FBCA2A48C8AFBE29D8504591CAEEA43503E77E
```

发布包包含 standalone、microapp、服务端运行源码、字体、锁定依赖和水轮机预测核验参考数据。服务器 `/tmp` 中的包与本地大小及 SHA-256 完全一致。

## 四、云端基线与隔离预检

切换前生产状态：

```text
current  -> /nlplabProj/scene-library/releases/20260911-083049
previous -> /nlplabProj/scene-library/releases/20260910-194728
scene-library.service = active / enabled
NRestarts = 0
监听 = 127.0.0.1:3102
磁盘剩余约 5.1 GB
Nginx 配置检查通过
```

新 Release 安装后先在 `127.0.0.1:3103` 启动隔离预检。健康版本、四个代表模型的 Bootstrap 与完整 FBX、预测汇总接口均通过。四个代表模型的隔离日志结果均为 34 条基础记录、2 条普通预警、0 条严重预警。

## 五、正式切换与数据落盘

切换脚本先计算共享设备 `state.json` 哈希，再原子切换 Release 并重启服务。切换完成结果：

```text
current  -> /nlplabProj/scene-library/releases/20260911-145044
previous -> /nlplabProj/scene-library/releases/20260911-083049
scene-library.service = active / enabled
NRestarts = 0
监听 = 127.0.0.1:3102
health.version = 20260911-145044
共享设备 state.json 切换前后哈希一致
```

水轮机两份历史日志文件的属主原为 `root:root`，会阻止服务账户继续追加记录。本次仅将这两个日志文件的属主规范为 `cockpit:cockpit`，未修改文件内容；复核后 106 个日志目录下非 `cockpit` 属主文件数量为 0。

切换后主动访问全部 106 个模型的日志接口，使基础日志立即持久化，而不是等待页面首次打开：

```text
模型页面：106
新增基础记录：3,604
生产日志总记录：4,359
每页 operational-v2 基础记录：34
每页普通预警：2
启用严重预警的模型页：1
场景日志文件：106
```

总记录数包含此前已经存在的运行、预测和实测核验日志，因此部分页面显示数量会大于 34。水轮机页面发布后显示 53 条，新增基础记录仍为 34 条和 2 条普通预警，历史及实际操作记录未被覆盖。

## 六、发布后验收

生产 API 全页审计：

```text
scenes = 106
issues = []
```

生产公网浏览器逐页验收：

```text
MODEL_OPERATIONAL_UI_ALL_OK
通过 = 106 / 106
最慢页面 = sim-visual-wastewater-pump，3.222 秒
其次 = sim-visual-bridge-crane，3.004 秒
其次 = sim-visual-hydro-turbine，2.992 秒
```

场景日志专项页面检查：

```text
普通页面 mm-05：场景日志（34），普通预警 2 条
水轮机页面：场景日志（53），列表读取正常
页面脚本错误：0
```

公网入口：

```text
https://47.122.104.52/cockpit/                                  200
https://47.122.104.52/microapps/scene-library/                  200
https://47.122.104.52/microapps/scene-library/scene-library.js  200
https://47.122.104.52/scene-library-api/health                  200
http://47.122.104.52:8081/cockpit/                              200
```

切换后的服务日志未出现新的应用写入错误；systemd 在正常重启时记录的旧进程 `status=143` 属于停止旧服务的历史行，当前服务保持 active 且 `NRestarts=0`。

## 七、回退方法

如需回退，只切换应用 Release，不删除共享模型数据或日志：

```powershell
ssh -i C:\Users\Administrator\.ssh\scene-library-deploy-ed25519 root@47.122.104.52 "/tmp/deploy-unified-hydro.sh switch 20260911-083049"
```

回退后需重新检查健康版本、`current/previous`、服务状态、两个前端入口及共享日志目录。由于本次已向共享目录追加基础场景日志，应用回退不会删除这些日志。

## 八、边界

- 本次仅发布场景库服务器，没有登录、修改或发布主平台服务器。
- 未删除旧 Release、共享数据、模型缓存或远端发布包。
- 工作区中原有未跟踪的 Word、PDF、旧发布资料和历史压缩包未纳入提交，也未修改或删除。
