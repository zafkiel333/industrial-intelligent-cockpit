# 外部可视化模型仿真展示 —— 修改日志

> 对应计划：[`开发计划.md`](./开发计划.md)。
> API 调整说明：[`API接入与调整说明.md`](./API接入与调整说明.md)。
> 记录规则：按时间倒序追加；每项包含状态、涉及文件、修改内容、影响面、验证方式和备注。

---

### [2026-08-12 01:06] 清理外部模型四页非 3D 区域的深灰底板

- 状态：完成
- 涉及文件：
  - 修改 `components/remote-model-showcase/AssessmentPanel.tsx`
  - 修改 `components/remote-model-showcase/ConnectionDetailDrawer.tsx`
  - 修改 `components/remote-model-showcase/ConnectionStatusBadge.tsx`
  - 修改 `components/remote-model-showcase/DataProvenancePanel.tsx`
  - 修改 `components/remote-model-showcase/ProjectConnectionMap.tsx`
  - 修改 `index.css`
  - 修改两份配色与外部模型修改日志
- 修改内容：
  - 将诊断结论、故障预测、处置建议和等待状态中的深灰半透明背景替换为浅蓝灰信息块。
  - 将资源平台、安全接入服务和业务应用三个协同节点分别调整为浅蓝、浅暖黄和浅紫背景。
  - 将资源协同详情抽屉正文改为浅色表面，通道卡片和资源卡片统一使用浅蓝灰背景。
  - 将数据来源说明改为浅蓝/浅紫双面板，等待同步标签改为浅中性蓝灰。
  - 保留弹窗外层遮罩以及 3D 模型视窗、模型控制按钮的深蓝背景，避免削弱模型和操作按钮对比度。
- 影响面：仅调整四个外部模型页面的共享展示组件，不修改模型请求、缓存、解析、遥测、诊断或报告逻辑。
- 验证方式：`npm.cmd run build` 与 `git diff --check` 均通过，仅保留项目原有的大体积分包提示。

---

### [2026-08-12 00:49] 修复外部模型视窗配色回归风险并补充加载阶段诊断

- 状态：代码修复完成；上游模型文件下载服务当前仍异常，等待外部服务恢复。
- 涉及文件：
  - 修改 `components/remote-model-showcase/RemoteModelViewer.tsx`
  - 修改 `components/remote-model-showcase/RemoteMetricCard.tsx`
  - 修改 `components/remote-model-showcase/ProjectConnectionMap.tsx`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `index.css`
  - 修改 `外部模型仿真展示方案/3D模型请求失败错误报告.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
  - 修改 `平台视觉配色适配方案/修改日志.md`
- 修改内容：
  - 撤销全局 Canvas 的屏幕混合模式，外部 FBX/GLTF WebGL 画布强制使用 `mix-blend-mode: normal` 和完整不透明度。
  - 为外部模型视窗增加独立 `remote-model-viewer` / `industrial-visual-surface` 作用域，隔离普通页面的浅色背景兼容规则。
  - 在 Three.js 场景内将 API 提供的近黑背景提升为中深蓝灰，保持灰模轮廓并避免依赖浏览器 GPU 混合。
  - 将模型失败状态拆分为“模型资源请求失败”和“模型解析或渲染失败”，并向页面保留后端错误码。
  - 对四个页面的标题、资源协同、模型、实时参数、趋势和诊断区域进行浅蓝灰分层优化。
- 本次检查结果：
  - 水轮机本地模型代理返回 `502 MODEL_STORAGE_UNAVAILABLE`，远端消息为“下载错误，请联系运维人员处理”。
  - 水轮机 Bootstrap 同时出现 `504 UPSTREAM_TIMEOUT`。
  - 水轮机、污水泵、桥式起重机的当前模型缓存均为 `empty`，模型二进制通道为 `offline`；矿卡尚未在当前进程发起模型请求，状态为 `unknown`、缓存为 `empty`。
- 原因判断：
  - 当前模型不显示的直接原因是上游没有返回 FBX 二进制，本地运行期缓存也为空。
  - 配色扩展引入的 Canvas 混合模式属于独立的前端回归风险，现已移除；它不会产生后端 `502/504`。
  - 若开发服务在配色修改期间重启，原内存模型缓存会被清空，从而暴露当时已经复发的上游下载问题，因此时间上可能表现为修改后才不显示。
- 验证方式：
  - `GET /api/model-showcase/sim-visual-hydro-turbine/model`：当前返回 `502`。
  - 四场景 `GET /connection`：核对模型通道状态、错误码和缓存状态。
  - `npm.cmd run build`：通过，仅保留项目原有的大体积分包提示。
  - `git diff --check`：通过。

---

### [2026-08-10 13:49] 优化设备数字孪生资源协同模块业务文案

- 状态：完成
- 涉及文件：
  - 修改 `components/remote-model-showcase/ProjectConnectionMap.tsx`
  - 修改 `components/remote-model-showcase/ConnectionDetailDrawer.tsx`
  - 修改 `components/remote-model-showcase/ConnectionStatusBadge.tsx`
  - 修改 `components/remote-model-showcase/DataProvenancePanel.tsx`
  - 修改 `src/remoteModelShowcase/connectionRegistry.ts`
  - 修改 `src/remoteModelShowcase/useModelShowcaseConnection.ts`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `外部模型仿真展示方案/跨项目模型连接关系展示开发方案.md`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 将原页面模块名称“跨项目模型连接关系”调整为“设备数字孪生资源协同”。
  - 将三节点统一命名为“模型资源平台”“安全接入服务”“业务应用端”，不再向业务用户直接展示 `Source Project`、`Connection Layer`、`Consumer Project`、BFF、CACHE 和 SCENE 等工程术语。
  - 将中间接入能力命名为“模型数据安全接入服务”，并用“资源授权、文件校验、快速复用、异常保护”说明业务价值。
  - 将状态文案调整为“资源同步正常、资源已就绪、部分服务受限、资源暂不可用、等待首次同步”。
  - 将详情抽屉更名为“设备资源协同状态”，内部区域调整为“资源来源与应用去向”“资源同步与智能服务状态”“业务数据与分析成果说明”。
  - 将六类服务调整为“设备模型信息、设备三维模型、实时运行数据、典型仿真工况、运行数据一致性校验、健康评估与故障预警”。
  - 将数据职责明确为“模型资源平台提供”和“工业智能驾驶舱分析生成”。
  - 将按钮“查看来源模型”统一调整为“查看资源详情”，四个固定跳转 URL 和安全打开方式保持不变。
  - 将页面顶部技术标签调整为“资源来源：ICES-Union 3d2.0”和“接入服务：模型数据安全接入”。
  - 同步更新开发方案和 API 调整说明，使文档描述与实际页面一致。
- 影响面：
  - 仅调整用户可见文案、接口返回的描述性标签及对应说明文档。
  - 未修改场景 ID、模型 ID、来源详情 URL、接口路径、状态枚举、缓存逻辑、请求周期或诊断功能。
- 验证方式：
  - 检查四个共享组件中已无原技术化页面标题及英文节点名称。
  - 本次相关 TypeScript/TSX 定向类型检查通过。
  - `npm.cmd run build` 通过；保留项目既有 `/index.css` 和大包体积提示。
  - 直接生成连接快照核对描述字段，来源角色、安全接入服务、业务应用、六类服务名称及两类数据职责均返回新业务文案。
- 备注：
  - 文件名和内部类型仍保留现有工程命名，避免无必要的接口或引用迁移；业务页面全部使用新的资源协同命名。

---

### [2026-08-10 12:04] 完成跨项目模型连接关系、通道状态与来源模型跳转

- 状态：完成
- 涉及文件：
  - 修改 `server.ts`
  - 修改 `src/remoteModelShowcase/types.ts`
  - 修改 `src/remoteModelShowcase/modelCatalog.ts`
  - 新增 `src/remoteModelShowcase/connectionRegistry.ts`
  - 新增 `src/remoteModelShowcase/useModelShowcaseConnection.ts`
  - 新增 `components/remote-model-showcase/ConnectionStatusBadge.tsx`
  - 新增 `components/remote-model-showcase/DataProvenancePanel.tsx`
  - 新增 `components/remote-model-showcase/ProjectConnectionMap.tsx`
  - 新增 `components/remote-model-showcase/ConnectionDetailDrawer.tsx`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `外部模型仿真展示方案/跨项目模型连接关系展示开发方案.md`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 建立六通道运行期连接状态注册表，分别记录模型元数据、3D 模型二进制、Dashboard、工况、数据一致性和本地诊断的成功、失败、耗时、HTTP 状态、缓存及最近成功时间。
  - 在现有上游 JSON 请求、模型下载/缓存和本地诊断路径中写入连接观测；观测不改变原有业务响应，也不新增上游探测请求。
  - 新增只读 `GET /api/model-showcase/:sceneId/connection`，返回来源项目、本地 BFF、当前项目、通道状态和数据归属；错误消息经长度限制和对象 key 脱敏。
  - 前端新增 10 秒连接快照轮询和四场景应用运行期缓存；连接状态请求失败时保留最后成功快照，页面隐藏时暂停轮询。
  - 四页面标题区新增 `SOURCE: ICES-Union 3d2.0` 和 `VIA: LOCAL BFF` 标识，并在标题与 3D 区之间加入“来源项目 → 本地 BFF → 当前项目”三节点关系条。
  - 新增连接详情抽屉，展示六类通道状态、路径、耗时、缓存、最近成功时间、当前脱敏错误，以及外部原始内容与本项目派生内容边界。
  - 四场景目录增加固定 `sourceDetailUrl`；关系条、详情抽屉及初始化失败页均提供“查看来源模型”，在新标签页安全打开模型 2326、2328、2316、2310 的对应详情页。
  - 来源详情页只用于人工追溯；3D 模型、元数据、Dashboard、工况和一致性数据仍全部通过 API/BFF 获取，不请求或解析详情页 HTML。
  - 所有本次代码修改均添加 `2026-08-10 + 修改类型 + 功能说明` 注释。
- 影响面：
  - 只扩展既有四个外部模型仿真页面和对应 BFF，不新增菜单、页面 ID、依赖或数据库连接。
  - 连接状态仅保存在 Node.js 进程内存中；服务停止后自动释放，不写入磁盘。
  - 现有 3D、遥测轮询、运行期缓存、工况切换、诊断、校验和报告下载流程保持不变。
- 验证方式与结果：
  - 本次相关 TypeScript/TSX 定向 `tsc --noEmit`：通过。
  - `npm.cmd run build`：通过，Vite 完成 5,554 个模块转换；仅保留项目既有 `/index.css` 和大包体积提示。
  - 全项目 `npx.cmd tsc --noEmit`：仍被既有 `components/Maintenance-plan-management/*/three-types.ts` 非法字符错误阻断，本次未修改这些历史文件。
  - 在独立 3108 测试实例完成四场景 `bootstrap`、`model`、`diagnosis`、`connection` 联调；模型 ID 和四个来源链接全部匹配。
  - 四个模型实测字节数为 7,661,788、13,082,652、3,777,248、6,175,120；Dashboard/诊断通道连接成功，模型运行期缓存可用。
  - 四个连接快照均未出现 `file_url=` 或 `3d_model/` 对象 key；测试实例已停止，未影响 3000 端口进程。
- 备注：
  - “查看来源模型”是人工查看入口，不属于业务请求链；外部详情页不可用不会影响当前项目的数据、缓存、诊断或报告。
  - 最终视觉位置、窄屏抽屉和外部标签页行为建议由用户在目标浏览器中做一次人工确认。

---

### [2026-08-10 11:44] 新增跨项目模型连接关系展示开发方案

- 状态：完成方案设计，等待用户审批，未开始业务开发
- 涉及文件：
  - 新增 `外部模型仿真展示方案/跨项目模型连接关系展示开发方案.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 明确来源项目 `ICES-Union 3d2.0`、当前项目 `Industrial Intelligent Cockpit` 和本项目 BFF 三方关系。
  - 设计四页面共享的三节点连接关系条、来源标签、通道状态、数据来源面板和连接详情抽屉。
  - 设计场景级后端运行期连接状态注册表，记录元数据、模型二进制、Dashboard、工况、一致性和本地诊断通道。
  - 规划只读 `GET /api/model-showcase/:sceneId/connection`，只读取本地观测状态，不因前端轮询增加上游请求。
  - 定义 connected、cached、degraded、offline、unknown 五种状态及缓存、最近尝试、最近成功和错误语义。
  - 明确上游原始数据、本项目代理缓存和本项目派生诊断报告的数据所有权边界。
  - 列出前后端预计文件、实施阶段、异常验证、性能要求、安全边界、验收标准和十项审批内容。
  - 继续要求所有后续代码修改添加“日期 + 修改类型 + 简要功能说明；”注释。
- 影响面：
  - 仅新增开发方案并更新修改日志。
  - 未修改 `server.ts`、前端组件、API、依赖或运行配置。
- 下一步：
  - 等待用户确认来源项目/当前项目展示名、三节点位置、状态分类、接口边界和第一版实施范围。

---

### [2026-08-10 11:17] 补充全部代码修改的日期与功能注释

- 状态：完成
- 涉及文件：
  - 修改 `App.tsx`
  - 修改 `constants.tsx`
  - 修改 `server.ts`
  - 修改 `components/SciFiCard.tsx`
  - 修改 `components/remote-model-showcase/AssessmentPanel.tsx`
  - 修改 `components/remote-model-showcase/RemoteMetricCard.tsx`
  - 修改 `components/remote-model-showcase/RemoteModelViewer.tsx`
  - 修改 `src/remoteModelShowcase/diagnosticEngine.ts`
  - 修改 `src/remoteModelShowcase/modelCatalog.ts`
  - 修改 `src/remoteModelShowcase/types.ts`
  - 修改 `src/remoteModelShowcase/useRemoteModelTelemetry.ts`
  - 修改 `views/simulation/remote-model/index.tsx`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `外部模型仿真展示方案/开发计划.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 按用户新增要求，为本次外部模型仿真功能的全部代码修改补充“日期 + 新增/修复/调整 + 简要说明；”格式注释。
  - 为所有新增业务文件添加文件头用途注释。
  - 在既有文件的菜单、页面导入、路由、端口配置、BFF 和通用卡片收缩约束相邻位置添加修改注释。
  - 为运行期数据缓存、模型二进制缓存、模型失败自动重试、Canvas 高度反馈修复、页面布局边界和评估报告下载分别添加修复或新增注释。
  - 在开发计划新增第 10 节，将该格式登记为本对话后续代码修改的持续规范。
- 注释示例：
  - `// 2026-08-09 新增：注册四个外部模型数字孪生仿真页面组件；`
  - `// 2026-08-09 修复：按场景保留应用运行期数据，切页先展示旧值并同步请求最新数据；`
  - `{/* 2026-08-09 修复：固定模型与参数卡边界，超量数据仅在组件内部滚动； */}`
- 影响面：
  - 仅增加代码注释和开发文档规范，不改变页面功能、API、运行配置或依赖。
- 验证结果：
  - 本次相关 TypeScript/TSX 独立类型检查通过。
  - `npm.cmd run build` 通过，Vite 完成 5,549 个模块转换。
  - 保留项目既有的 `/index.css` 缺失提示和大包体积警告，无新增构建错误。
- 备注：
  - 当前 PowerShell 执行策略禁止未签名的 `npm.ps1`；验证改用同一 Node 安装中的 `npm.cmd`/`npx.cmd`，未修改系统执行策略。

---

### [2026-08-10 00:27] 补充调查 3D 模型下载自动恢复事件

- 状态：完成，事件进入恢复观察期
- 涉及文件：
  - 修改 `外部模型仿真展示方案/3D模型请求失败错误报告.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 用户反馈模型突然恢复后，重新完整请求四个上游 FBX，核对 HTTP 状态、响应类型、Content-Disposition、Content-Length、实际字节数和 FBX 文件头。
  - 重新请求四个本地模型 BFF，确认全部返回 200、字节数正确且 `X-Model-Runtime-Cache=HIT`。
  - 核对端口 3000 后端进程 PID 和启动时间，确认故障与恢复发生在同一服务进程内，没有通过重启或后端代码修改恢复。
  - 在错误报告新增第 16 节，记录故障前后对比、恢复时间窗、直接恢复机制、两级缓存行为、可确认与不可确认的原因、残余风险和后续监控建议。
  - 将报告状态从“未恢复”更新为“已恢复、观察中”，同时保留原始故障证据和上游根因闭环要求。
- 恢复证据：
  - 四个上游接口均返回 `application/octet-stream`，完整字节数为 7,661,788、13,082,652、3,777,248、6,175,120，与元数据全部一致，文件头全部为有效二进制 FBX。
  - 四个本地接口响应耗时为 90～127 ms，全部为 200 和运行期缓存 HIT。
  - 最后失败样本与首次本轮成功样本将恢复时间限定在 `2026-08-09 23:53:35` 至 `2026-08-10 00:27:38` 之间。
- 结论：
  - 恢复发生在上游模型下载/共享对象存储边界；现有自动或再次请求在上游恢复后取得文件并写入两级缓存，所以页面无需重启即可突然恢复。
  - 上游内部是服务重启、存储连接恢复、配置修复还是人为操作，仍需该时间窗内的上游日志和变更记录确认。
- 影响面：
  - 仅更新错误报告和修改日志；未修改任何业务代码、API、依赖或运行配置。

---

### [2026-08-09 23:53] 新增 3D 模型请求失败详细错误报告

- 状态：完成
- 涉及文件：
  - 新增 `外部模型仿真展示方案/3D模型请求失败错误报告.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 重新直连复测四个上游模型的基本信息、文件列表、Dashboard 和模型下载接口，并同步复测本项目四个模型 BFF。
  - 记录四个模型的文件 ID、文件名、声明大小、HTTP 状态、响应类型、实际字节数、请求耗时和业务错误正文。
  - 建立从浏览器、本项目 BFF、上游下载处理到 MinIO/对象存储的故障边界说明。
  - 详细说明为何可以排除文件过大丢包、CORS、前端窗口、FBXLoader、本地超时、模型 ID、中文文件名和本地缓存等原因。
  - 按优先级列出对象存储不可达、凭据/权限失效、对象丢失、元数据与对象不一致、下载处理异常等上游候选根因。
  - 提供上游运维日志检索、对象 stat/head、存储连接验证、接口契约修正、恢复方案、复现脚本和逐层验收标准。
- 复测结论：
  - 四个模型元数据和文件列表均成功；四个 Dashboard 均为 ONLINE，字段数为 6/6/5/6。
  - 四个上游文件下载请求均在 69～182 ms 内返回 HTTP 200、`application/json`、95 字节和 `code=-1`，正文均为“下载错误，请联系运维人员处理”。
  - 四个本地模型接口均返回 HTTP 502、`MODEL_STORAGE_UNAVAILABLE`，证明本地内容校验与错误映射正常。
- 影响面：
  - 仅新增错误报告并更新修改日志；未修改任何前端、后端、接口、依赖或运行配置。

---

### [2026-08-09 23:25] 3D 异常状态布局修复与评估预测报告下载补齐

- 状态：完成
- 涉及文件：
  - 修改 `components/remote-model-showcase/RemoteModelViewer.tsx`
  - 修改 `components/remote-model-showcase/AssessmentPanel.tsx`
  - 修改 `components/SciFiCard.tsx`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `外部模型仿真展示方案/开发计划.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 修复 Three.js 画布在高 DPI 设备上以绘图缓冲区尺寸参与页面布局、再被 `ResizeObserver` 重复放大的高度反馈循环。
  - 将 Canvas 绝对定位并固定为容器宽高，尺寸监听只在容器实际尺寸变化时更新绘图缓冲区；模型加载失败和每 30 秒自动重试均不再改变视窗高度。
  - 四个页面的模型卡固定为 492 px 高并启用布局/绘制隔离；实时参数卡同样固定外部高度，内容过多时只在卡片内部滚动。
  - 去除仿真页面自身的纵向嵌套滚动，统一交由应用主内容区滚动；为通用 `SciFiCard` 增加 `min-width: 0`、`min-height: 0` 约束，避免图表、长文本或缺失状态撑开 Grid/Flex 轨道。
  - 评估区域标题明确调整为“数据分析、设备评估与故障预测”，并说明该功能只依赖遥测数据，模型文件缺失不会中断诊断。
  - 复用项目既有 `downloadScenarioReport` 下载能力，新增“下载评估预测报告”按钮；报告包含当前参数、健康评分、风险等级、诊断置信度、故障概率、预测窗口、判断依据、处置建议及数据更新时间。
  - 数据请求异常但存在运行期缓存时，下载报告会明确标注使用最后成功数据及其更新时间。
- 根因记录：
  - `WebGLRenderer.setPixelRatio()` 后以 `setSize(width, height, false)` 设置 Canvas 绘图缓冲区，但未约束 Canvas 的 CSS 尺寸；Canvas 的高分辨率固有高度撑高父容器，`ResizeObserver` 再读取新高度并继续扩大，造成页面持续向下增长。
- 影响面：
  - 未修改后端或上游 API；诊断、预测和下载均继续使用现有遥测与 `/diagnosis` 输出。
  - `SciFiCard` 仅增加收缩约束，不改变已有页面颜色、边框、间距和业务逻辑。
- 验证结果：
  - 本次相关 TypeScript/TSX 独立类型检查通过。
  - `npm run build` 通过，Vite 完成 5,549 个模块转换；只保留项目既有的 `/index.css` 和大包体积警告。
  - 四个场景 Bootstrap 均返回 200，字段数为 6/6/5/6；随后四个诊断接口均返回 200，并各输出健康评分、风险等级和至少一项故障预测。
- 备注：
  - 上游 FBX 存储异常仍是独立问题；本次保证模型文件缺失时页面布局、遥测分析、故障预测和报告下载仍可正常使用。

---

### [2026-08-09 21:35] 运行期数据保留与 FBX 模型加载链路修复

- 状态：完成
- 涉及文件：
  - 修改 `src/remoteModelShowcase/useRemoteModelTelemetry.ts`
  - 修改 `components/remote-model-showcase/RemoteModelViewer.tsx`
  - 修改 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 修改 `server.ts`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 为四个场景分别建立浏览器应用运行期缓存，保留 Bootstrap、最后成功快照、最近 60 条趋势、工况、诊断和一致性结果；离开页面不再清空，停止或刷新整个应用后释放。
  - 每个场景首次只发起一次 Bootstrap；同场景在首次请求尚未结束时重新进入会复用同一 Promise，避免重复初始化请求。
  - 再次进入页面时先同步展示该场景缓存，同时立即请求 Dashboard/当前工况；新响应的数据签名发生变化才替换旧值和追加趋势，没有变化则保留原数据。
  - 数据请求失败时不清空现有页面，继续展示最后成功数据；页面新增“数据更新”和“最近检查”时间，错误提示同步显示上次更新时间。
  - 增强 JSON 响应校验：本地后端未重启、接口落入 Vite HTML/404 时返回明确提示，不再因错误结构覆盖页面状态。
  - 后端新增模型二进制运行期缓存与同场景并发请求合并；首次成功后，后续页面切换直接复用内存中的模型文件。
  - 后端在发送模型前校验 `Content-Type`、大小和 FBX/GLB/GLTF 文件头；上游返回 JSON 错误时改为稳定的 502/504 JSON 错误，不再把错误正文伪装成 FBX 交给前端。
  - 前端改为先读取并校验模型 `ArrayBuffer`，再调用 `FBXLoader.parse` / `GLTFLoader.parse`；成功的二进制在浏览器运行期缓存，切页后无需重新下载。
  - 模型加载错误区现在展示真实错误原因，支持手动重试并每 30 秒自动重试；同时明确区分“上游没有返回模型文件”和“FBX 解析失败”。
- 根因记录：
  - 现场复现时，上游模型下载接口 HTTP 200 实际只返回 95 字节 `application/json`：`code=-1`、`message=下载错误，请联系运维人员处理`，而水轮机 FBX 元数据声明大小为 7,661,788 字节。
  - 因此原空白模型不是窗口尺寸异常，也不是 Three.js 不支持 FBX，而是旧代理未校验响应内容，把上游错误 JSON 直接传给了 FBXLoader。
- 验证结果：
  - 本次相关 TypeScript/TSX 独立类型检查通过。
  - `npm run build` 通过，Vite 完成 5,549 个模块转换。
  - 更新后的测试服务上，四个 Bootstrap 均成功，字段数为 6/6/5/6；水轮机连续 Dashboard 请求的同步时间与转速均发生更新。
  - 当前上游模型存储仍未返回二进制；更新后的本地模型接口在 30 秒后正确返回 `504 MODEL_DOWNLOAD_TIMEOUT`，不再返回 95 字节伪 FBX。上游恢复后首次有效模型会自动进入两级运行期缓存。
- 备注：
  - 端口 3000 上仍是修改前启动的旧 `tsx server.ts` 进程；需要重新启动开发服务才能加载本次 `server.ts` 路由和模型校验逻辑。

---

### [2026-08-09 19:27] Phase 1～5 - 完成四个外部模型仿真页面、BFF、动态数据与诊断输出

- 状态：完成
- 涉及文件：
  - 修改 `server.ts`
  - 修改 `constants.tsx`
  - 修改 `App.tsx`
  - 新增 `src/remoteModelShowcase/types.ts`
  - 新增 `src/remoteModelShowcase/modelCatalog.ts`
  - 新增 `src/remoteModelShowcase/diagnosticEngine.ts`
  - 新增 `src/remoteModelShowcase/useRemoteModelTelemetry.ts`
  - 新增 `components/remote-model-showcase/RemoteModelViewer.tsx`
  - 新增 `components/remote-model-showcase/RemoteMetricCard.tsx`
  - 新增 `components/remote-model-showcase/AssessmentPanel.tsx`
  - 新增 `views/simulation/remote-model/RemoteModelSimulationView.tsx`
  - 新增 `views/simulation/remote-model/index.tsx`
  - 修改 `外部模型仿真展示方案/开发计划.md`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容：
  - 在“仿真分析”前四位加入已批准的四个页面名称和 ID，并在 `App.tsx` 注册独立页面入口。
  - 建立四场景白名单和模型目录，统一维护模型 ID、设备字段、趋势分组、风险方向、故障知识、预测建议与 3D 展示参数。
  - 在 `server.ts` 实现 `bootstrap`、`model`、`dashboard`、`scenario`、`data-sync`、`diagnosis` 六类同源 BFF 接口；业务代码只访问 `/three-model-api/api/...`，没有请求或解析任何参考详情页。
  - 模型文件 key 只由后端从白名单模型元数据解析；前端不能提交任意对象 key。模型代理支持 FBX/GLB/GLTF、10/60 秒超时、50 MB 限制、5 分钟元数据缓存、1 小时浏览器缓存及流式转发。
  - 建立最多 60 个快照的后端诊断窗口，按正常范围、趋势、波动、越限比例和设备故障知识生成健康分、风险等级、故障概率、预测窗口、证据、建议和置信度，并加入风险恢复迟滞以降低临界值跳变。
  - 建立共享前端 Hook：Dashboard/工况接口每 5 秒轮询，快照累计为滚动时序；若将来接口直接返回时序数组则按时间排序；仅在接口成功且字段只有范围时生成带 `range-simulated` 标记的范围模拟值；接口失败时保留最后快照并停止生成新值。
  - 建立 Three.js 模型查看器，支持远端 FBX/GLB/GLTF、加载进度、重试、自动居中缩放、光照、旋转/缩放/重置/全屏、异常着色、振动/转速联动、ResizeObserver 和卸载资源释放。
  - 建立共享页面布局，显示 API 在线状态、3D 模型、实时参数、三组趋势、工况切换、智能诊断结论、故障预测、维护建议、数据一致性和模型资源信息；前端未展示诊断模型结构、算法、权重或训练过程。
  - “矿卡”页面按批准方案使用模型 2310，并在资源区明确标注远端实际资源为“拖车牵引车”。
  - `server.ts` 的监听端口新增 `PORT` 环境变量支持，默认行为仍为端口 3000。
- 影响面：
  - 新增四个仿真分析页面及其本地 API，不改变已有页面 ID、已有接口或依赖版本。
  - 没有修改 `package.json` / `package-lock.json`，没有把远端模型文件落盘到项目。
- 验证方式与结果：
  - `npm run build`：通过，Vite 完成 5,549 个模块转换；保留项目既有 `/index.css` 缺失提示和大包体积警告。
  - 对本次新增的 9 个 TypeScript/TSX 业务文件执行独立 `tsc --noEmit`：通过，无新增类型错误。
  - 全项目 `npx tsc --noEmit`：仍被既有 `components/Maintenance-plan-management/*/three-types.ts` 非法字符错误阻断；报错文件不在本次修改范围。
  - 四个 `bootstrap`：均返回 200；模型名依次为水轮机总成、污水泵 KCM100HD、桥式起重机、拖车牵引车；字段数依次为 6/6/5/6。
  - 四个 `model`：代理下载字节数分别为 7,661,788、13,082,652、3,777,248、6,175,120，与上游元数据全部一致。
  - 水轮机故障工况：返回 3 个越限字段；诊断返回 `critical`、3 项故障预测；数据一致性接口返回 100%。
- 备注：
  - 当前诊断为本项目构造的仿真智能诊断服务，适合页面演示，不应作为真实生产设备检修决策的唯一依据。
  - 交付前建议在目标浏览器中按开发计划第 6 节人工复核四个模型的视觉角度和低配设备帧率。

---

### [2026-08-09] 审批记录 - 完整开发计划获准执行

- 状态: 完成
- 涉及文件: `外部模型仿真展示方案/开发计划.md`、`外部模型仿真展示方案/修改日志.md`
- 修改内容:
  - 用户认可模拟智能诊断服务方案，并明确授权开始开发。
  - 开发计划从“部分确认，尚未开始业务开发”转为“已确认，开发进行中”。
  - Phase 0 全部确认项标记为完成，从 Phase 1 开始执行。
- 影响面: 仅更新审批状态，业务代码将在后续步骤按计划修改。
- 验证方式: 核对用户明确指令“认可，开始开发吧”。
- 备注: 后续每个实际开发阶段完成后继续按时间倒序追加记录。

---

### [2026-08-09 18:49] 计划修订 - 确认请求与展示方案，改为后端模拟智能诊断服务

- 状态: 完成（仅文档修订，业务开发仍未开始）
- 涉及文件:
  - 修改 `外部模型仿真展示方案/开发计划.md`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容:
  - 将“全 API 请求并由本项目后端代理”标记为已确认。
  - 将 Dashboard 快照、API 时序和纯范围数据的展示方案标记为已确认。
  - 按用户要求将原“前端规则推演”调整为后端模拟智能诊断系统：后端读取当前快照和近期历史，像独立预测服务一样输出诊断及预测结论。
  - 规划新增 `POST /api/model-showcase/:sceneId/diagnosis`，返回健康分、风险等级、诊断结论、故障概率、预测窗口、维护建议和置信度。
  - 明确当前诊断服务不要求真实训练模型，内部可使用统计特征、趋势判断和设备故障知识构造稳定、合理的结论。
  - 明确前端只展示诊断输出，不展示预测模型、算法、特征权重、规则公式、训练过程或推理流程。
  - 将预计新增的 `riskAssessment.ts` 调整为后端 `diagnosticEngine.ts`，并补充历史缓冲、状态防抖和真实模型替换边界。
- 影响面（是否触碰现有功能/接口/样式）:
  - 仅修改本说明目录内的三份 Markdown 文档。
  - 未修改任何现有业务代码、接口或依赖。
- 验证方式（如何确认未破坏原功能）:
  - 检查开发计划审批项已将请求方案和数据展示方案列为已确认。
  - 检查计划与 API 说明均包含 `/diagnosis` 的输入来源、输出结构和前端展示边界。
  - 检查 Phase 1～5 仍未执行。
- 备注:
  - 等待用户复核模拟诊断服务方案并最终批准业务开发。

---

### [2026-08-09 18:19] 计划修订 - 明确全 API 请求链与数据展示设计

- 状态: 完成（仅文档修订，业务开发仍未开始）
- 涉及文件:
  - 修改 `外部模型仿真展示方案/开发计划.md`
  - 修改 `外部模型仿真展示方案/API接入与调整说明.md`
  - 修改 `外部模型仿真展示方案/修改日志.md`
- 修改内容:
  - 将第 0.2 节四个页面名称、ID 和顺序标记为“已确认”。
  - 将“继续使用模型 2310 承载矿卡页面”标记为“已确认”，并约定设备信息如实显示远端模型名“拖车牵引车”。
  - 明确四个详情页链接只用于需求阶段人工识别模型 ID，业务代码禁止请求详情页、解析 HTML 或抓取 DOM 数据。
  - 在开发计划中补充浏览器→本项目 BFF→上游模型 API 的完整请求设计，逐项列出模型信息、文件列表、模型下载、Dashboard、工况和一致性校验对应的 API。
  - 补充首次进入、3D 加载、动态刷新、工况保持和一致性校验四类请求时序。
  - 补充 Dashboard 快照、未来时序数组、纯范围字段三种数据形态的处理规则。
  - 补充 API 字段到同步条、参数卡、趋势图、3D、评估报告和一致性面板的展示映射。
  - 明确同一 API 快照同步驱动所有展示区域，不允许各区域独立生成随机数据；接口失败时不以本地模拟伪装在线。
- 影响面（是否触碰现有功能/接口/样式）:
  - 仅修改本说明目录内的三份 Markdown 文档。
  - 未修改 `App.tsx`、`constants.tsx`、`server.ts`、`views/`、`components/` 或依赖文件。
- 验证方式（如何确认未破坏原功能）:
  - 检查开发计划标题状态为“部分确认，尚未开始业务开发”。
  - 检查计划明确写出 `/three-model/detail` 不参与任何业务请求。
  - 检查 Phase 1～5 仍处于未执行状态。
- 备注:
  - 当前继续等待用户讨论并确认 API 请求、数据展示及评估预测方案。

---

### [2026-08-09 17:59] Step 0.1～0.3 - 完成需求调查并建立规划文档

- 状态: 完成（仅文档阶段，业务开发尚未开始）
- 涉及文件:
  - 新增 `外部模型仿真展示方案/开发计划.md`
  - 新增 `外部模型仿真展示方案/修改日志.md`
  - 新增 `外部模型仿真展示方案/API接入与调整说明.md`
- 修改内容:
  - 阅读用户提供的 `模型api接口说明文档.md`，整理模型基本信息、模型文件、Dashboard、场景切换、数据一致性校验和 3D 外部加载流程。
  - 参照 `场景库测试方案/开发计划.md` 与 `场景库测试方案/修改日志.md` 的结构，建立本任务独立的计划、状态跟踪、风险、验收和日志格式。
  - 对模型 2326、2328、2316、2310 的远端基本信息和 Dashboard 做只读核对，记录实际字段、范围、模型文件格式和文件大小。
  - 拟定四个页面名称和场景 ID，规划在“仿真分析”菜单前四位展示。
  - 发现并记录模型 2310 的重要差异：需求称其为“矿卡”，但远端实际资源名称为“拖车牵引车”、行业为“航运交通”。
  - 设计后端同源 BFF、共享前端页面、FBX Viewer、轮询时序、规则评估与故障预测方案。
- 影响面（是否触碰现有功能/接口/样式）:
  - 仅新增本说明目录及三份 Markdown 文件。
  - 未修改 `App.tsx`、`constants.tsx`、`server.ts`、`package.json`、`views/`、`components/` 或任何现有业务代码。
  - 未新增依赖，未下载或落盘远端模型文件。
- 验证方式（如何确认未破坏原功能）:
  - 检查新增目录只包含三份规划文档。
  - 计划中明确设置审批门槛，Phase 1～5 状态均未开始。
  - 远端查询仅使用 GET 请求，未调用场景切换、数据同步或任何写接口。
- 备注:
  - 下一步等待用户审批 `开发计划.md`，并确认模型 2310 是否继续使用。
  - 用户批准前不得开始业务实现。

---

（后续实际代码修改将在此处按时间倒序追加。）
