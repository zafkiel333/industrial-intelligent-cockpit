# 修改日志

## 2026-09-11（固定场景命名保护与 106 页全量验收，Release 20260911-083049）

- 场景库 20 个一级分类继续使用既定业务名称，不随全局术语规范化改写；其中第 19 类固定为“震动监测”。设备测量指标、频谱和幅值等技术术语仍按各页面专业语义使用，不反向修改一级分类名称。
- 外部模型页标题增加业务名称保护，页面显示严格采用逐页配置的实际标题；新增自动审计，同时校验 20 个一级分类顺序、外部模型标题保护和中文界面转换。
- 新增 106 页全量浏览器验收：逐页检查标题、简介、三个数据入口、六项测量卡、六张时序图、阈值诊断模型版本、专业化字段及禁止公开的内部措辞，并捕获页面脚本错误。
- 本地构建、模型库、导航、中文审计和 318 组预测回归全部通过；生产 API 审计 106/106 无问题，公网浏览器审计 106/106 通过，最慢页面 2.868 秒。
- 分区时延、统一水轮机工作流、专业文案等专项公网验收继续通过；正式入口与微应用入口均返回 200。
- Release `20260911-083049` 已上线，直接回退 `20260910-194728`；共享数据保持 106 个模型目录、315 套非水轮机配对数据、945 个配对文件及 106 份场景日志。
- 完整包哈希、预检、切换、验收和回退命令见 `新版本云部署会话交接/云端更新执行记录-20260911-固定场景命名与106页全量验收.md`。

## 2026-09-10（外部模型预测诊断全量推广，Release 20260910-194728）

- 将水轮机统一数据工作区推广到全部 106 个外部模型展示页，覆盖 16 类设备场景；历史传入、验证传入、数据管理、六指标图、预测结论、预警处置、实测核验、分区时延和两类日志使用统一页面逻辑。
- 为每个页面独立配置六项指标、单位、参考范围、Modbus 地址、IEC 61850 路径、故障知识、预警部位、检查建议、诊断版本和场景日志，不复用不符合设备实际的水轮机字段或结论。
- 除水轮机保留 50 组数据外，其余 105 页各生成三套观测/验证配对数据，一套正常、两套异常；生产目录已实际预置 315 套、945 个文件，并配套页面级规范、样例、算法和协议说明下载。
- 全 318 组盲测达到状态准确率 97.48%、综合结论准确率 97.48%、预测区间点级覆盖率 99.13%、平均归一化 MAE 7.74%；保留少量可见但不过量的区间外点。
- 所有页面的系统响应日志与场景日志均按模型目录持久化，并补齐跨越约半年的运行记录；严重预警只用于明确、持续且后果较重的异常。
- 生产水轮机早期联调设备编号规范为 `2326-JZ-01` 至 `2326-JZ-06`，原数据和预测结果保留，修改文件有独立备份；106 页公开文案、设备编号、批次名和日志复核无联调或内部实施措辞。
- 最终 Release `20260910-194728` 已上线，直接回退 `20260910-185005`；完整发布、持久化与验收证据见 `新版本云部署会话交接/云端更新执行记录-20260910-外部模型预测诊断全量推广.md`。

## 2026-09-10（系统响应日志与预测场景日志，Release 20260910-093903）

- 页面顶部新增独立“系统响应日志”，与“场景日志”分开；每次操作按`T_start`、`T_end`、`Δt`三行成组展示，结束记录后立即显示对应时延。
- 时延模块改为保留每次操作，完成、失败或中止后写入模型数据目录；记录功能名称、设备/任务上下文、起止时间、时延与状态，重复提交按`traceId`去重。
- 水轮机预测保存后写入场景业务日志，正常预测与异常预测均记录；实测核验记录预测tag、实际tag、覆盖率、判断一致性和sMAPE。
- 场景日志增加正常、信息、关注、预警、严重预警五级。红色严重预警仅限critical风险且具有严重持续越界，并要求连续不少于60分钟或至少两项持续越界。
- 两类日志分别保存为`logs/system-response-events.jsonl`和`logs/scene-prediction-events.jsonl`，位于模型共享数据目录，不随Release切换删除。
- 本地API、重复去重、正常/异常分级、日志文件、定向TypeScript、双构建、中文审计及1600/390浏览器完整闭环通过；全仓类型检查仍仅被既有7个维护计划非法字符文件阻断。
- Release `20260910-093903` 已切换上线，直接回退为 `20260910-003316`。公网系统响应日志、场景日志、双视口布局和时延算术通过，生产设备状态文件哈希保持不变。

## 2026-09-10（功能分区响应时延与诊断版本）

- 新增分区T_start/T_end/Δt显示，按用户补充不设置统一时延总览；上传与解析合并计时，文件选择后自动完成预检。
- 确认停留不计时，入库/验证从最终确认另起计时；结果显示经过绘制机会后结束，失败和中止单独标记。
- 预测、核验、历史、下载、三维资源及手动刷新在对应功能附近显示，删除重置不新建计时。
- 实际阈值诊断规则登记为HD-TD v1.0.0，在诊断区显示，并写入新预测与报告。
- 本地时间戳算术、注入延迟、排除人工等待、自动解析、SCL与协议预检、删除排除及双视口验证通过。目标发布20260910-003316，最终状态见同日执行记录。

## 2026-09-09（恢复单图，区分两类范围）

- 撤去指标下方独立误差图/面板，恢复每指标一张对照图；六指标仍同时显示。
- 正常范围用标值的绿色上下限线，预测误差范围用浅橙色包络带，取消正常范围底色，避免叠加。
- 偏差具体数值移入悬停说明，图下保留简短摘要；原有预测、阈值及统计不变。
- 本地单图结构、状态/偏差组合、双视口、构建和定向类型检查通过。目标发布20260909-201950，实际状态见对应执行记录。

## 2026-09-09（设备状态与预测偏差分层展示）

- 将主图两种重叠阴影改为上下限虚线、超限点和文字状态；每指标另设偏差图及判定摘要。
- 实测是否超限与预测偏差是否超出历史经验范围独立显示，补齐正常但偏差大、超限但偏差小和无验证数据状态。
- 长历史显示保留每个时间桶的极值及质量缺口，控制绘制规模；不修改原预测或统计。
- 新增11号说明、判定边界与浏览器四状态验收。目标发布20260909-193008，实际状态见同日云端执行记录。

## 2026-09-09（状态标签显式对比与数据管理弹窗）

- 按“去掉时间覆盖率指标，换一个指标”理解，替换为当前核验的对称误差sMAPE。时间覆盖仍用于核验完整性判断，不改变统计规则。
- 预测tag与实际tag使用并排大卡显式展示正常/异常及匹配结果；补齐待验证、缺标签状态。
- 数据管理弹窗独立加宽加高，修复520px通用样式覆盖，增加固定表头和内部表格滚动。
- 本地四种标签状态、双视口弹窗、构建及定向类型检查通过。发布状态见同日《状态标签对比与管理弹窗》执行记录。

## 2026-09-09（水轮机单历史、独立验证与六指标预测展示）

- 按最新意见拆出“导入验证数据”；历史追加/替换仅作用于设备唯一当前历史，取消参考数据组选择框。
- 六指标以两列三行/移动单列同时展示，取消逐指标切换，保持独立单位、纵轴及统一时间联动。
- 预测结果、预警处置和实测核验分别展示，增加方法/周期/回测跨度与误差、阈值/时间/峰值/部位/检查建议；正常状态使用常规检查项目名称。
- 水轮机新预测升级为谐波回归+AR(1)残差与非周期基线的时间留出选优；旧预测保留，新标识使用 hydro-harmonic-v3。同一设备历史跨版本核验不重复累计。
- 新增10号改进说明，明确用户和答辩评审可能质疑的统计与预测依据；本地接口、浏览器、正常异常、算法、下载及双构建通过。
- 目标发布 20260909-163928，实际生产状态详见对应云端执行记录。

## 2026-09-09（水轮机统一数据与预测核验重构）

- 实施 09 号方案：一个上传入口、统一设备/参考组、同一时序图，预测与核验跟随当前选择，数据管理收纳为弹窗。
- 移除页面固定 50 组目录、完成分母、独立历史上传和全组验证长表；样例规模与产品可处理组数解耦。
- 复用协议与通用格式分块导入；新设备归属、增加、覆盖与文件内设备映射在确认前展示。全部重复记录导入后定位原有效批次，避免切到空批次。
- 新增内容版本预测快照、后续实测部分/完整核验、无标签数值核验、幂等累计统计和当前预测多格式下载。旧验证数据保留。
- 本地接口/浏览器/算法与服务器 3103 正常异常核验通过。发布过程和最终状态见 `新版本云部署会话交接/云端更新执行记录-20260909-水轮机统一数据预测核验.md`。

## 2026-09-09（水轮机数据、预测与验证统一页面需求整理）

- 新增 `09-水轮机数据预测验证统一页面方案.md`，把数据管理与独立验证工作台合并为以当前设备和当前数据组驱动的统一流程。
- 明确单一上传入口、已入库参考数据选择、增加/覆盖/新增设备、预测快照与后续实测核验、动态累计统计及旧数据保留规则。
- 固定 50 组目录、0/50 进度、全组长表和重复历史图不再作为目标页面结构；50 组仅为样例数据规模。
- 本条记录为方案整理，尚未实施组件、接口、存储迁移或发布。

## 2026-09-09（水轮机 50 组预测验证工作台，Release 20260909-083801）

- 仅对水轮机 `sim-visual-hydro-turbine` / 模型 `2326` 增加两阶段预测验证：先导入 180 分钟观测窗口并冻结 6 小时预测、状态标签、故障类型和部位，再导入紧随其后的 360 分钟实测数据进行核验。
- 准备 `HT-01`～`HT-50` 共 50 组独立数据，每组绑定 `2326-VAL-xx` 设备编号；共 9,000 条观测记录、18,000 条验证记录，包含 27 组正常和 23 组异常，异常覆盖轴系不平衡、轴承温升、水力工况失稳和机组出力衰减，并包含弱先兆、工况恢复和故障类型边界情况。
- 观测文件不含实际标签；验证文件固定包含 `actual_tag`、`actual_fault_code`、`actual_fault_name`、`actual_fault_part`。接口校验组号、180/360 条数量、60 秒连续时间、六字段数值及验证标签一致性，拒绝错组、缺段和明显异常格式。
- 页面新增累计完成度、状态准确率、综合结论准确率、异常故障类型准确率、归一化误差、SMAPE 和预测区间覆盖率；按组展示两阶段上传、冻结结论、六指标预测/实测对照图、逐指标误差及 50 组验证台账。
- 验证结果持久化保存逐时点实测、冻结预测、文件摘要和误差；支持重新上传观测后清除该组旧核验、删除单组和重置全部，危险操作均一次确认。源数据与运行记录分目录保存。
- 盲测结果：状态准确率 96%、综合结论准确率 94%、异常故障类型准确率 91.3%、平均归一化 MAE 18.26%、SMAPE 20.20%、预测区间覆盖率 89.11%。结果保留 2 组状态误判和 1 组故障类型误判，不读取验证标签反推预测。
- 验证：数据生成器、50 组盲测、接口正负流程、单组/全组 ZIP 下载、定向 TypeScript、standalone/microapp 双构建、Playwright 桌面/移动端上传核验、六曲线、误差表、台账、确认弹窗和横向溢出检查均通过；浏览器测试记录已清理。
- 当前状态：已随 `20260909-083801` 上线；公网双视口显示 50 组且无横向溢出，生产验证台账保持 0 条，等待用户逐组导入观测与验证数据。

## 2026-09-09（试点页预测工作台重构，Release 20260909-083801）

- 四个试点页删除“正常运行 / 高负荷 / 故障演练”三个按钮及前端工况切换逻辑；页面只读取实际接口或已传入数据，不再提供工况演练入口。
- 按“左侧三维模型、右侧实测数据、下方预测与结论”的结构重排页面。实测数据按指标显示最新值、参考范围和近期曲线；原历史/预测混合图、数据一致性、模型与资源信息、旧设备评估面板从试点页移除。
- 新增独立预测数据区、预测结论、预警和预警部位。预测图保留各指标原始单位、模型参考带、随时间扩展的误差带、预测中值、持续越界点和时间游标；批次表“分析”按钮可联动所选设备及批次。
- 水轮机和污水泵预测窗口由 24 分钟扩为 6 小时，桥式起重机由 5 分钟扩为 30 分钟，矿卡由 10 分钟扩为 2 小时；对应预测点总数为 2160、2160、900、1440。
- 算法升级为最多 720 个连续有效点上的稳健平滑、阻尼趋势、周期趋势候选，并以三个滚动起点的多步 MAE 择优。周期候选需具备相关性且较非周期基线改善至少 8%；预测范围不按正常限裁剪。
- 预警要求预测中值持续越过模型参考范围，不以单点波动触发；输出首次时间、方向、峰值、持续时间和逐模型配置的设备部位。当前偏离、趋势、残差及未来持续越界共同进入故障风险。
- 修复上游新 Dashboard 结构缺少 `twin_status` 时首次进入崩溃的问题，在遥测入口补齐兼容状态、数据来源、点位数和时间戳。
- 修复上游三维模型元数据暂时不可用时 bootstrap 阻塞整页的问题，允许数据、预测和诊断区域独立加载；修复上传新设备后旧概览可能提前清空目标批次的问题，刷新完成后精确定位刚导入批次。
- 验证：本次文件专项 TypeScript 检查通过；standalone/microapp 双构建通过；算法周期留出、常量、非负边界、缺口和既有大幅异常数据回测通过；四模型接口预测范围与点数通过；Chrome 4 页 × 1440/390 两种视口只读与完整上传、自动定位、重置确认回归通过。全仓类型检查仍只被既有 7 个维修计划 `three-types.ts` 非法字符阻断。
- 当前状态：四页试点已随 `20260909-083801` 上线并通过公网双视口只读验收；其余 102 页仍按逐模型配置和分批确认原则暂不推广。

## 2026-09-08（模型修复、中文排查与推广准备，20260908-194130）

- 修复 2338 的 FBX Null/Mesh 导出兼容，扩展修复 6639 非标准 FBX；106 模型完整下载与几何解析审计通过，三处问题模型公网实际渲染通过。
- 排查上游模型下载／实时数据超时，隔离 bootstrap 故障，增加按模型 ID 验证的断连缓存；保留原始模型、历史数据和错误事实。
- 闸坝船名与主体中文修正，补齐通用业务文案；69 条中文／标识保护断言、三页中文浏览器验收通过。
- 四页正常／异常图桌面窄屏回归通过，原数据哈希不变。发布 194130，直接回退 192630；详见同日模型兼容执行记录。
- 生成 106 页推广台账和分批验收要求，四页已上线，102 页待专属配置与正常／大幅异常双样例验收；eq-18 保留。

## 2026-09-08（正常/异常区分与自适应预测，20260908-093932）

- 正常参考带绿色显示，越界历史红线/圆点，越界预测红菱形；读数显示正常/偏高/偏低与原值上下限。灰色预测误差带可选默认收起，与正常参考范围分离。
- 固定斜率预测升级为三起点滚动回测选择末值、阻尼趋势、周期模板；周期须具备重复相关性并改善基线 MAE 至少 15%。按实际连续采样训练，非负物理边界与正常参考限分离，不伪造随机波形。
- 正常/大幅异常成对数据成为后续开发验收要求；四模型各两份 240 点 CSV、生成脚本、成对 API 与图形验证已落地。预测边界、常量、周期留出及缺口测试通过。
- 云端隔离与正式切换通过，保留全部原数据及两组用户演示设备。直接回退 20260907-221805，完整证据见本轮部署记录。

## 2026-09-07（多指标时序与预测，Release 20260907-221805）

- 按用户修正，将具体数值、时间动态及历史/预测作为主展示；多指标共用一个画布与时间轴，各轨保留原始单位，支持归一化叠加联动与原值读数。
- 增加历史实线、预测虚线、估计区间及分界，共享时间游标、回放与字段显隐；预测复用既有模型，按设备/批次隔离，不足样本或末条不可用时不展示预测。
- 存储量、质量矩阵与范围条默认折叠；上传自动定位保留。
- 专项类型及边界验证、双构建、云端隔离 API、四页公网双视口和精确预测读数通过；生产数据哈希保持一致，回退为 20260907-210916。

## 2026-09-07（导入数据可视化检视，Release 20260907-210916）

- 新数据工作区以设备和批次为范围，用指标×时间段矩阵替代重复的趋势曲线/健康评分；点击色块查看数值、时间、单位、参考范围和质量。
- 增加最新值参考范围条、当前留存量、质量分布和超参考记录统计；参考值明确为模型配置，非现场报警设定值；不混合设备、不插值、不以色块推断故障。
- 上传后自动定位新设备、新批次；批次表增加“检视”入口。既有上方遥测和预测/报告下载保留。
- 新增 data/inspection 只读接口和独立聚合模块，单设备/批次隔离、质量、空时段、边界及 150000 记录验证通过。
- 本地完整上传交互、四页双视口、云端隔离真实导入与公网只读验收通过；生产四份数据哈希未变，回滚 20260907-153132。

## 2026-09-07（最终审查与生产发布 20260907-153132）

- 修复重复点最后一个值无效时仍使用此前有效值的问题；整个时间组全无效时也计入缺点拒绝统计，增加专项回归断言。
- 发布脚本补齐运行启动脚本，并强制核验试点服务源码、中文字体与许可文件。
- 本地及云端隔离环境通过 84 个下载组合、SCL 生命周期和 24 个协议格式真实导入/取消/归档保留组合。
- Release 20260907-153132 已上线，回滚目标为 20260904-173623；公网四页双视口、102 个模型绑定、双入口资源哈希、导航及微应用生命周期通过。
- 生产四份 state.json 哈希保持不变；NRestarts=0。两个环境版本字段统一为新 Release，未修改主平台，未推广到其余页面。
- 以下“未发布”描述为此前开发阶段记录；最终证据见云部署交接目录本日执行记录第 12–14 节。

## 2026-09-07（固定协议字段、SCL 永久保存与明显异常适配）

- Modbus 长表字段固定为 `timestamp,device_id,unit_id,function_code,address,raw_value,data_type,byte_order,scale,offset,unit,quality`；IEC 61850 长表字段固定为 `timestamp,device_id,ied,object_reference,functional_constraint,value,unit,quality`。允许扩展列，缺少固定列时预检失败并列明字段。
- IEC 61850 SCL 改为随导入任务独立分块上传；确认前保存在临时目录，确认导入后永久归档到当前模型 `protocol/scl/`，批次清单记录文件名、相对路径、大小和 SHA-256。删除运行数据、删除批次和重置不自动删除 SCL。
- 增加显而易见异常处理：简单格式规范化；空值、非数值、非有限值和明显越界值剔除；时间无效或必需点位不全的记录拒绝；同时间同点位重复时采用最后值。复杂单位推断、跨时间插值和自动补点不处理。
- 导入预检新增自动规范化、无效值、缺点记录和重复点位统计，并明确 SCL 的归档状态；用户确认前仍不改变当前数据或永久 SCL 目录。
- 每模型 `reference/` 新增《协议长表固定字段与异常处理》；下载数据规范同步增加协议字段、SCL 生命周期和异常规则；协议 XLSX 样例新增“固定字段说明”工作表，JSON 样例携带字段/异常规则，协议 ZIP 增加中文说明文件。
- 顶部“数据样例”下载弹窗新增通用表格、Modbus、IEC 61850 来源选择，三类来源均可自由选择 CSV、XLSX、JSON 或全格式 ZIP，文件名随来源变化。
- 本地验证通过：52 个既有下载组合、32 个协议样例组合、固定字段缺失拒绝、四类异常统计、SCL 确认前/后生命周期、删除设备后 SCL 保留、四页桌面/移动浏览器回归，以及 standalone/microapp 双构建；本次仍未发布云端。

## 2026-09-07（协议来源文件导入与预检）

- 四个试点页的数据上传入口新增“通用表格 / Modbus / IEC 61850”来源选择；三类来源均支持 CSV、XLSX、JSON。
- Modbus 文件按 `timestamp + address` 解析寄存器长表，支持 `raw_value`、数据类型、字节序、比例和偏移；IEC 61850 文件按 `object_reference` 解析对象长表，保留 IED、功能约束、质量和源时间戳。
- 上传完成后先生成导入预检：展示来源、映射点位、未映射点位、时间范围、质量分布、拒绝记录、重复时间和导入后数据量；用户点击“确认导入”后才写入当前数据。
- IEC 61850 模式支持可选 ICD/CID/SCD/SSD 文件 XML 检查，用于显示 IED、逻辑设备和数据对象摘要；SCL 不作为历史测量数据直接导入。
- 批次记录新增来源标记；每个模型的 `reference/` 目录增加 Modbus 与 IEC 61850 三格式样例，页面可下载当前来源样例。
- 本地构建、四模型 52 个原有下载、24 个协议样例下载、Modbus/IEC 61850 预检不写入、确认导入、来源批次和删除还原测试通过；未执行生产发布，待用户确认协议文件样例和预检界面后再发布。

## 2026-09-04（上传设备选择防误操作优化）

- 上传弹窗将目标设备拆分为“已有设备”和“新增设备”两个明确入口；默认使用当前筛选设备或该模型第一台已有设备。
- “已有设备”只能通过下拉框选择，增加或覆盖数据时不再允许自由输入设备编号，避免输入错误把同一设备的数据误建为新设备。
- “新增设备”使用独立输入框；若填写的设备 ID 已存在，页面立即显示错误并禁用“开始上传”，引导用户返回已有设备下拉框。
- 新增设备固定使用增加语义，“覆盖该设备”不可选；仅已有设备增加数据时显示重复时间处理策略，减少无关选项。
- 扩展 Playwright 回归，四个试点页在 1440/390 两种视口均检查已有设备选项、默认值、新增设备重复 ID 拦截、覆盖禁用和模式切换清理。
- 本地双构建、导航、模型专项、完整上传/重置生命周期和截图检查通过；Release `20260904-173623` 已发布，直接回滚目标为 `20260904-163312`。
- 3103 隔离预检、公网 52 种下载、4 页双视口浏览器回归和上传弹窗实图检查通过；生产四页数据均保持 120 条/1 台/1 批次，服务 `NRestarts=0`。

## 2026-09-04（下载选项表达与数据工作区视觉升级）

- 四类下载弹窗取消原生下拉框，改为可直接点选的格式选项；每项以 `.pdf`、`.docx`、`.csv`、`.xlsx`、`.json`、`.zip` 等真实后缀为主标题，并用“适合查看、打印和归档”“Excel 工作簿，可直接导入”等用途说明替代不直观的技术类型名称。
- 下载弹窗新增完整文件名预览；设备 ID 或格式变化时即时更新，仍严格采用“模型 ID + 页面中文模型名 + 设备 ID + 中文资料名 + 后缀”的实际下载命名。
- 数据工作区重新设计为浅工业蓝灰风格：增加链路状态、带语义色的六项指标、运行趋势与预测面板、健康评分条、故障概率条和结构化批次表；层级、间距、边框、按钮和字体与四页周边界面统一。
- 上传、下载和危险操作确认弹窗统一视觉体系；上传仍支持 CSV/XLSX/JSON、增加/覆盖、三种重复时间处理和上传/解析双进度，删除与重置仍为一次确认且默认焦点在“取消”。
- 增加桌面/移动截图模式，并扩展 Playwright 断言：逐格式检查后缀、选中状态、文件名预览和实际下载名称。
- 本地 `npm run build:all`、导航类型检查、1,050 节点导航校验、模型加载、模型稳定性、4 页 × 桌面/移动只读与完整上传回归、截图和 `git diff --check` 均通过；npm 依赖审计因仓库服务端长时间无响应主动终止，本轮依赖未发生变化。
- Release `20260904-163312` 已发布，直接回滚目标为 `20260904-152456`；3103 隔离预检、公网 52 种下载、4 页双视口浏览器回归、桌面/移动截图、双入口、服务与日志检查通过，生产四页数据均保持 120 条/1 台/1 批次。

## 2026-09-04（下载格式、中文命名与数据清理热修）

- 数据规范新增 PDF、DOCX、JSON 自由选择；数据样例新增 CSV、XLSX、JSON、三格式 ZIP 自由选择；预测结果新增 CSV、XLSX、JSON；分析报告新增 PDF、DOCX、JSON。
- 下载弹窗增加设备 ID 字段，默认使用当前筛选设备或该模型第一台设备；四类接口均要求设备 ID。
- 下载文件统一按“模型 ID + 页面中文模型名 + 设备 ID + 中文资料名”命名，ZIP 内部文件沿用相同规则；样例内容中的设备 ID 与下载命名保持一致。
- 修复同一设备覆盖导入后旧原始批次文件残留、损坏文件解析失败后 `.part` 残留；前端上传异常会主动取消任务，服务启动会清理无法恢复的遗留分块。
- 本地 52 个下载组合、ZIP/DOCX/XLSX 内容、覆盖/删除/失败/启动清理、双构建、导航类型检查、依赖审计和 4 页桌面/移动 Playwright 回归通过。
- 热修 Release `20260904-152456` 已上线，直接回滚目标为 `20260904-131956`；公网 52 个下载组合、4 页双视口浏览器、16 次实际下载文件名、截图、双入口、服务和错误日志检查通过，生产数据未变。

## 2026-09-04（发布后独立缺陷检查）

- 使用 Playwright、Chrome CDP 截图、线上真实 XLSX 上传还原、接口负向测试、systemd/journal 和独立临时目录复现再次检查 Release `20260904-131956`。
- 四页桌面/移动渲染、3D Canvas、图表、字体、响应式布局、上传/解析双进度、批次落表、错误输入防护和服务稳定性通过；线上验收设备已删除并恢复 120 条/1 台/1 批次。
- 确认覆盖设备后旧原始批次文件未同步删除，以及损坏文件解析失败后 `.part` 临时文件残留两项数据生命周期缺陷；当前影响为不可见磁盘占用，不影响活动数据或预测结果。
- 本轮按“检查”范围只记录缺陷，未修改业务源码、未再次发布；修复前不推广到其余 102 页。详细证据见云端更新执行记录第 9 节。

## 2026-09-04（P0 云端发布与公网验收）

- 发布场景库 Release `20260904-131956`，切换前现场版本 `20260901-093430` 已设为直接回滚目标；主平台服务器未登录、未修改。
- 远端发布包大小和 SHA-256 与本地一致；新 release 独立安装、固定 SheetJS 依赖加载和 3103 预检通过后才切换生产。
- 内部、HTTPS、HTTP 8081 健康接口和双前端入口均通过，服务保持 active/enabled、`NRestarts=0`，3102 仅监听回环，3103 已停止。
- 四页 API、16 个下载、协议映射与预测输出通过；生产 XLSX 上传使水轮机页从 120 条/1 台增加到 150 条/2 台，随后按唯一验收设备删除并恢复 120 条/1 台，原始验收批次同步删除。
- Playwright 公网只读回归通过 4 页 × 桌面/移动视口，页面无“模拟/仿真”可见字样、横向溢出或新增浏览器错误。
- 生产数据目录为 `/nlplabProj/scene-library/shared/data/model-showcase/`，四模型目录均由 `cockpit:cockpit` 持有并独立保存；发布和代码回滚不删除数据。
- 正式菜单版本为 `20260904-131956`、20 个根节点/1,050 个总节点；本次只发布场景库，未替主平台团队导入菜单。
- 现场 Modbus/IEC 61850 实机联调仍待设备地址、点表/SCL、通信参数与测试窗口；当前上线的是协议映射、双向转换和适配器统一写入能力。
- 完整命令证据与回滚信息见 `../新版本云部署会话交接/云端更新执行记录-20260904-外部模型数据试点.md`。

## 2026-09-04（P0 试点开发与本地验收）

- 修复 SheetJS 0.20.3 在 Node ESM 环境读取上传临时文件时未注册 `fs` 的问题；CSV/XLSX/JSON 的 12 组最终复测全部完成。
- 将官方 `xlsx-0.20.3.tgz` 固定保存在 `vendor/` 并纳入发布包强制校验，生产安装不再依赖临时访问 SheetJS CDN；生产依赖审计结果为 0 个漏洞。
- 浏览器验收脚本新增只读模式，生产验收时不会上传或修改业务数据；本地完整模式仍覆盖真实上传和危险操作确认。
- 完成 4 页试点：水轮机 `2326`、污水泵 `2328`、桥式起重机 `2316`、矿卡/牵引车 `2310`；其余 102 个外部模型页保持原状。
- 页面新增数据管理与预测诊断区、上传入口、设备 ID、覆盖/增加和三种冲突策略、独立上传/解析进度条、设备筛选、数据/预测图、健康评分、故障候选、批次表。
- 增加按设备覆盖、增加、批次删除、设备删除、全部删除和恢复初始数据；危险操作均为一次确认/取消弹窗，默认焦点为取消。
- 新增 CSV、XLSX、JSON 解析和每模型三格式样例；上传框设备 ID 覆盖文件内标签并写入本批次全部记录。
- 新增模型隔离持久化目录、原始批次、当前数据、参考资料、数据规范和算法说明；发布版本切换不删除共享数据。
- 新增每模型局部趋势预测、95% 残差区间、质量过滤、健康评分和故障规则；四模型分别配置采样周期、预测步数、字段范围与协议点位。
- 新增 Modbus/IEC 61850 点位表、两个方向的遥测转换和协议适配器统一写入入口。当前仅完成接口及映射验证；没有现场设备地址、点表/SCL 和通信参数，未执行实机验收。
- 新增数据规范 PDF、三格式样例 ZIP、预测结果 XLSX、分析报告 PDF 四类下载；中文 PDF 已渲染检查，XLSX/ZIP 已重新解析检查。
- 页面移除四个试点的“模拟/仿真”可见标识；系统生成、上传和协议入口数据进入同一存储、分析、展示与下载链路。
- `npm run build:all` 通过；Playwright 完成 4 页 × 桌面/移动视口、实际上传、进度显示、批次落表、危险操作默认焦点和页面横向溢出检查。
- 全仓 `tsc --noEmit` 仍被既有 7 组维护计划 `three-types.ts` 非法字符阻断；本次双形态构建和新增功能专项验证均通过，未改动这些历史文件。
- 详细实现与证据见 `06-试点实现与验收记录.md`。

## 2026-09-04（10 项需求确认）

- 用户确认协议为 IEC 61850；正式范围固定为 Modbus 与 IEC 61850 设备采集及两者遥测数据互转。
- 用户确认上传格式为 CSV、XLSX、JSON；每个模型为三种格式各准备一份可直接导入的数据样例。
- 用户确认一个模型可以对应多台同型号具体设备；每次文件传入使用必填输入框填写设备 ID，作为该批次全部记录的设备标签。
- 用户确认数据不限制大小、永久保存。取消 20 MiB、200,000 行、2 GiB 等业务配额规划，改为流式/分块处理、有限内存窗口、磁盘余量检测和磁盘不足时不改变当前数据。
- 用户确认删除只询问一次；覆盖、重置和删除均采用单次影响摘要及确认/取消弹窗，不做二次确认。
- 用户确认不需要登录、操作审计和数据访问权限隔离；从数据目录规划中移除操作审计账本。批次清单仅用于展示已有数据基本情况。
- 用户确认先试点 4 页；继续采用水轮机 `2326`、污水泵 `2328`、桥式起重机 `2316`、矿卡/牵引车 `2310`，最终确认后再推广全部页面。
- 用户确认各模型的预测未来时长和算法参数分别按照实际应用场景配置。
- 用户确认下载范围只包含数据规范、数据样例、预测结果和分析报告，不增加其他下载项。
- 开发计划更新为 v0.3；该条记录对应需求确认阶段，当时尚未修改业务源码、依赖、数据或部署环境。

## 2026-09-03（需求第二轮修订）

- 根据用户意见将规划版本更新为 v0.2，仍只修改规划文档，不进入业务开发。
- 取消页面、图表、预测结果和报告中的“模拟/仿真数据”标识要求；系统生成、上传、Dashboard 和协议数据在规范化后使用完全相同的处理、预测、诊断和展示链路。
- 将原“多个数据集选择/激活”方案改为“一页一 `sceneId`、一 `modelId`、一套当前数据”；不同实际设备使用 `device_id`，不同时间传入的数据以批次记录。
- 明确覆盖、增加、三种记录冲突策略、重置、删除批次、按设备/时间范围删除和删除全部的语义；所有危险操作使用影响摘要及确认/取消弹窗，不规划角色权限隔离。
- 增加已有数据概况设计：总记录、设备数、字段、时间范围、质量分布、设备时间覆盖、导入批次和最近变更。
- 扩展上传兼容范围，规划单/多设备宽表、长表、多文件批次、XLSX 多工作表、JSON 数组/包络/JSONL、字段别名、时间格式和单位映射。
- 新增 `05-协议设备接入与互转方案.md`，设计 Modbus 与第二协议的真实设备连接、统一点位模型、双向转换、转换防环和实机验收。
- 更正前次协议检索结论：IEC 官方存在 `IEC 61580:1995`，但其内容是波导及波导组件回波损耗测量，不是设备通信协议；第二协议连接器仍需实际设备通信手册。若设备实际采用 IEC 61850，则按 IEC 61850 适配。
- 确定 P0 先改造水轮机 `2326`、污水泵 `2328`、桥式起重机 `2316`、矿卡/牵引车 `2310` 四页；用户评审定稿前不推广到其余 102 页。
- 规划 P1 再选 10 个扩展页补齐 14 个算法族，剩余 92 页按四批、每批 23 页推广。
- 增加数据样例、数据说明、预测结果和分析报告四类下载要求。
- 当前仍未修改 `server.ts`、React/TypeScript 源码、依赖、现有数据、构建产物、模型目录、菜单或云端环境。

## 2026-09-03

- 根据用户需求建立“外部模型数据上传与预测诊断”独立规划目录。
- 读取并核对 `外部模型仿真展示方案/` 与 `模型库增量与稳定性优化-20260901/` 的现行数据链路、诊断逻辑、缓存约束和发布基线。
- 核对仓库既有 10 个普通场景 Excel 上传实现，确认其 `_1/_2/...` 表头、单文件读取和固定 75% 预测分界属于历史试点限制，不直接用于 106 个外部模型页。
- 确认当前范围为 4 个原始仿真页与 102 个扩展绑定页，共 106 个外部模型展示页；`eq-18` 不属于本范围。
- 设计统一数据来源、规范化记录、按 `sceneId + modelId` 隔离的数据目录、上传/解析任务状态和数据生命周期。
- 设计 14 类设备预测配置族、正常/渐退/故障三类仿真样例和可解释混合预测算法。
- 规划文件上传、数据可视化、预测诊断、Modbus/IEC 61850 适配、测试和发布阶段。
- 首次查阅 Modbus Organization 与 IEC 官方资料时只定位到 IEC 61850 电力自动化通信标准，未定位到 IEC 61580；该结论已在同日第二轮修订中补充纠正。
- 当前仅新增规划文档；未修改 `server.ts`、React/TypeScript 业务源码、依赖、现有数据、构建产物、模型目录、菜单或云端环境。

## 后续记录规则

- 按时间倒序追加，不覆盖历史；
- 分开记录计划、代码、本地验证、模拟器验证、实机验证、构建、部署和浏览器验收；
- 模拟器契约测试与现场协议实机验收分开记录，但不在业务页面给数据设置模拟/仿真标识；
- 不发布没有足够标签和回测依据的预测精度；
- 每批必须记录覆盖的 `sceneId/modelId`、数据 schema 版本、算法版本和验证结果；
- 每次云端发布必须另行维护对应的云端执行记录和回滚信息。
