/** Version of the implemented hydro threshold + persistence + fault-association rules. */
export const HYDRO_THRESHOLD_MODEL:Readonly<{name:string;version:string;legacyVersion:string}> = Object.freeze({name:'阈值诊断模型',version:'HD-TD v1.0.0',legacyVersion:'HD-TD v1.0.0'});
