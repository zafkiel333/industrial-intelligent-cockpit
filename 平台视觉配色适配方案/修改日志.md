# 平台视觉配色适配修改日志

> 配套计划：[开发计划.md](./开发计划.md)  
> 记录规则：最新修改写在前面；实际代码修改必须同时添加日期及功能注释

---

### [2026-08-21] 运维知识管理三维背景与按钮对比度修复（待发布）

- 状态：本地代码修复、专项校验和独立版/微应用版双构建已完成；本轮未生成发布包、未上传服务器、未切换云端版本；
- 页面范围：根据“运维知识管理”栏位的实际路由与组件注册关系，扫描 `components/knowledge-manage`，只处理其中 32 个真正创建原生 Three.js `WebGLRenderer` 的三维页面；其余无三维视窗的知识管理页面不做无关改动；
- 三维背景：将 32 个场景的 `scene.background` 统一为浅工业蓝灰 `#E8F1F6`（`0xe8f1f6`），并同步把 32 处 `THREE.Fog`/`THREE.FogExp2` 基色改为相同颜色；保留模型几何、材质、灯光、网格、粒子、动画、相机和业务数据；
- 按钮根因：平台通用 Canvas/HUD 规则会把深色视窗内的工具文字强制改为浅色，而按钮底色已被全局浅色主题映射为浅色；在普通、悬停、选中或禁用状态切换时，两套规则命中顺序不同，造成浅底浅字或文字随鼠标变化后不可见；
- 按钮修复：仅在 `ops-knowledge` 首页和全部 `km-*` 页面挂载 `knowledge-management-theme` 作用域；在样式文件末尾为该作用域下按钮及其图标/文字后代明确配对常态、蓝色选中、红色告警、悬停和禁用状态的背景、边框与深色文字，且禁用按钮不再通过低透明度隐藏文字；
- 防漏校验：新增 `scripts/verify-knowledge-management-visuals.ts` 与 `npm run verify:knowledge-management-visuals`，检查 32 个三维背景、32 处雾化颜色、栏目作用域和按钮各状态保护规则；
- 验证：专项结果为 `KNOWLEDGE_MANAGEMENT_VISUALS_OK scenes=32 fogs=32 color=#E8F1F6 buttons=state-paired`；既有模拟维修背景、全站背景、导航类型、导航协议、HTTPS 集成与 `git diff --check` 全部通过；独立版和微应用版双构建通过，分别转换 5528 与 5523 个模块；
- 目视边界：本地预览服务启动正常，但当前会话没有可连接的浏览器实例，因此未执行截图级自动复核；正式发布前仍应在目标浏览器抽查 `km-dam-seepage`、`km-turbine-cavitation` 及其他代表页面的按钮常态、悬停、选中与禁用状态；
- 影响边界：不修改其他栏位的三维视窗或按钮，不改变菜单、路由、HTTP/HTTPS 适配、模型库通信、API、仿真算法及云端服务。

---

### [2026-08-21] 模拟维修服务三维视窗浅色背景整改（待发布）

- 状态：本地代码修复、专项校验和独立版/微应用版双构建已完成；尚未生成发布包、上传文件或切换云端版本；
- 页面范围：只覆盖“模拟维修服务”栏位实际挂载的 `mm-01` 至 `mm-46` 共 46 个页面，不修改“应用维修服务”首页，也不处理当前未挂载到这 46 条路由的 `mining-comparison-v2` 备用组件；
- 背景修复：将 46 个 Three.js 场景的 `scene.background` 从黑色、近黑色和深色工业色统一为浅工业蓝灰 `#E8F1F6`（`0xe8f1f6`），使三维区域与主平台浅色内容区协调，同时保留模型材质、灯光、几何、网格、粒子和业务状态色；
- 雾效修复：同步调整 49 处 `THREE.Fog`/`THREE.FogExp2` 基色，其中包含港口天气页面在雨、雾、风暴和夜间状态下动态写入的雾色，避免页面初始为浅色但切换工况后重新过渡到深色；
- 防漏校验：新增 `scripts/verify-simulated-maintenance-backgrounds.ts` 与 `npm run verify:simulated-maintenance-backgrounds`，自动扫描目标栏位的 46 个渲染场景并检查全部背景与雾化颜色；
- 影响边界：不修改其他栏位、页面布局、路由、导航、模型库跳转、HTTP/HTTPS 适配、业务数据或云端服务；
- 验证：专项结果为 `SIMULATED_MAINTENANCE_BACKGROUND_OK scenes=46 fogs=49 color=#E8F1F6`；既有四类背景规则、导航类型、导航协议、HTTPS 集成与 `git diff --check` 均通过；独立版和微应用版双构建通过，分别转换 5528 与 5523 个模块；
- 目视边界：本地生产入口可访问，但本机无头 Edge 本次未稳定应用带多个查询参数的嵌入路由，因此不把其默认首页截图作为目标页验收依据；正式发布前仍应抽查 `mm-01`、`mm-03`、`mm-30` 与 `mm-44`。

---

### [2026-08-21] 零部件寿命预警前十页业务化重构（已发布：20260821-120215）

- 状态：源码提交、发布包校验、云端安装、3103 独立端口预检、生产切换和公网 HTTPS/HTTP 验收全部完成；`current` 为 `20260821-120215`，`previous` 为 `20260821-112458`；
- 页面范围：水轮机转轮叶片冲蚀、发电机定子线棒绝缘老化、主变压器套管、闸门启闭机钢丝绳、调速器伺服阀、进水口拦污栅、泄洪洞闸门止水橡胶、励磁系统功率模块、油压装置压力容器、冷却水泵轴承共十页；
- 重构目标：把原先以“大面积三维模型、少量即时数值”为主的展示页改为可支撑寿命管理业务的数字化工作台，同时保持场景库现有浅色工业风格、专属三维模型和模型库入口；
- 统一业务骨架：每页包含资产与专业信息、健康指数、剩余寿命及置信区间、预测置信度、检修窗口、四项 KPI、四路状态信号、三类退化机理、三级阈值、寿命历史与预测趋势、四项处置任务、算法输入、智能建议、人工决策边界、预期收益和业务依据；
- 场景差异化：十页分别围绕冲蚀/空化、绝缘热老化与局放、套管介损与含水、钢丝绳弯曲疲劳与断丝、伺服阀污染磨损、拦污栅堵塞与结构应力、橡胶压缩与泄漏、功率模块热循环、压力容器循环疲劳、轴承包络振动与润滑建立独立参数和处置逻辑，不使用仅换标题的数据复制页；
- 新增实现：原工程中的“励磁系统功率模块寿命预警”和“油压装置压力容器疲劳监测”只有菜单项、没有专属页面与三维组件，本次补齐两套 `View.tsx`、状态类型和 Three.js 模型；三维背景统一使用工业蓝灰 `#315268`；
- 数据边界：所有页面数值明确为数字化演示样例；阈值与剩余寿命不能直接作为现场停机或检修指令，正式应用必须结合制造商限值、适用标准、设备台账和现场基线确认；
- 业务依据：参考 USBR FIST 设备检修资料、USBR 变压器维护指南、DOE 水电空化寿命研究、OSHA 钢丝绳检查要求、NASA 功率电子器件预测数据和 SKF 轴承寿命/状态监测资料；
- 防退化：新增 `scripts/verify-life-warning-redesign.ts` 与 `npm run verify:life-warning`，自动检查十页配置数量、独立标题与建议、40 项 KPI、40 路信号、30 类机理、30 项阈值、40 项任务、趋势数据、三维入口和模型库场景编号；
- 验证：`verify:life-warning`、`typecheck:navigation`、`verify:navigation`、`verify:https-integration`、`verify:scene-backgrounds` 与 `git diff --check` 通过；场景背景检查结果更新为 `scenes=305 fogs=124 color=#315268`；双构建通过；全量 `typecheck` 仍只被项目既有 7 个维修计划 `three-types.ts` 非法字符文件阻断，本次文件未引入新错误；
- 视觉验收边界：本地生产预览入口为 HTTP 200，但当前会话没有可连接的浏览器实例，未执行截图级目视复核；云端切换后仍将执行独立端、微应用入口、脚本资源和健康接口的公网复核。

---

### [2026-08-21] 计算机视觉监测业务图片纠错与随机图源清理（待发布）

- 状态：本地图片资产与页面引用已完成整改；尚未提交、生成发布包、上传或切换云端版本，继续等待其他问题合并；
- 用户反馈：`cv-solar-panel` 光伏板页面左侧错误显示城市大楼，`cv-insulator-flashover` 绝缘子页面错误显示草地，`cv-transformer-leak` 变压器页面也无法保证图像与设备一致；
- 根因：相关页面将 `picsum.photos` 随机图片服务当作工业监控画面，即使 URL 含固定 seed，也只固定随机结果，不保证返回光伏板、绝缘子、变压器等业务对象；这不是主平台路由或主题样式问题；
- 扩展审计：计算机视觉监测模块共发现 7 处随机图引用，除截图三页外还涉及大坝裂缝、闸门止水密封和输送带异物检测，因此统一清除该模块全部 `picsum.photos`；
- 本地资产：新增 6 张无文字、无水印、可随独立版和微应用版构建发布的工业巡检图，分别对应光伏阵列、输电绝缘子串、油浸式变压器、闸门橡胶密封、大坝混凝土裂缝和矿料输送带异物；
- 页面调整：光伏、绝缘子、闸门页面各绑定对应业务图；变压器可见光与热成像视窗使用同一设备基准图并保留热成像色彩处理；大坝可见光与热感视窗绑定同一裂缝基准图；输送带告警缩略图改用明确的矿料输送带异物图；
- 稳定性：图片放入 Vite `public/assets/computer-visual-inspection/`，由 `src/assets/cvMonitoringImages.ts` 基于 `import.meta.env.BASE_URL` 为独立版 `/cockpit/` 与微应用版 `/microapps/scene-library/` 分别生成正确资源路径，不再依赖外网图片服务、随机内容、跨域或 HTTP/HTTPS 状态；图片保持为独立静态文件，避免以 Base64 内嵌导致微应用主脚本膨胀；
- 无障碍与语义：同步把英文占位 `alt` 改为明确的中文业务描述，避免图片加载失败时仍无法判断页面对象；
- 防退化：新增 `scripts/verify-cv-monitoring-images.ts` 与 `npm run verify:cv-images`，检查本地 PNG、最低分辨率、页面绑定、业务语义文本，并禁止计算机视觉监测模块重新引入随机图片或远程图片直链；
- 验证：`npm run verify:cv-images` 通过，结果为 `pages=6 assets=6 randomSources=0 baseUrl=verified`；独立版与微应用版双构建通过；两套产物均包含 6 张独立 PNG 和各自正确的资源前缀；微应用图片改为静态文件后，`scene-library.js` 从错误内嵌图片时的约 39.8 MB 恢复为约 18.1 MB；`git diff --check` 通过；
- 目视复核：使用本地 `dist-standalone` 生产构建和隔离的无头 Edge 实例，分别打开 `cv-solar-panel`、`cv-insulator-flashover`、`cv-transformer-leak`；三页均命中嵌入模式目标路由，业务图片正常加载；同时依据新绝缘子图构图将闪络与污秽识别框移至绝缘子串区域，避免识别框落在天空背景；
- 影响边界：除按新图构图校准绝缘子页两处识别框位置外，不修改三维模型、业务数据、图表、路由、主平台通信或模型库跳转协议；
- 图像来源：使用内置图像生成能力按各工业场景分别生成，并已逐张目视核对业务主体；原始生成文件保留在本机 Codex 生成目录，项目使用副本位于 `public/assets/computer-visual-inspection/`。

---

### [2026-08-21] 维修计划管理前十页业务化重构（待发布）

- 状态：`mpm-0` 至 `mpm-9` 本地重构完成，专项校验和独立版/微应用版双构建通过；尚未提交、生成发布包、上传或切换云端版本；
- 问题：原页面普遍采用“大面积三维模型 + 少量运行参数 + 简单按钮”的展示方式，计划窗口、关键路径、质量放行、资源约束、风险闭环和算法决策边界表达不足，导致页面空旷且难以支撑实际维修计划业务；
- 重构架构：新增 `MaintenancePlanningWorkbench` 统一浅色工业平台视觉、响应式卡片和基础交互；新增 `planningScenarioConfigs` 承载十个场景的独立业务模型，使页面保持同一产品语言但不复制业务内容；
- 页面范围：水轮发电机组大修、泄洪闸门维保、大坝主体加固、压力钢管防腐、主变预防性检修、泵站年度检修、水库清淤、船闸停航大修、液压启闭机维保、尾水隧洞排空检修；
- 内容增量：十页共配置 40 项场景 KPI、40 项状态触发信号、50 个计划阶段与质量门、30 项主要风险、40 项资源准备度，以及 10 套互不重复的“数据输入—算法建议—人工决策边界—预期收益”闭环；
- 差异化：机组页突出解体吊装和轴系找正；闸门页突出疲劳敏感区与防汛窗口；大坝页突出潜在失效模式和施工扰动；钢管页突出腐蚀分区、表面处理与涂层寿命；主变页突出 DGA 趋势与最小检修包；泵站页突出供水能力和效率；清淤页突出泥沙预算、船机产能与水质；船闸页突出船流疏导和恢复通航；启闭机页突出污染控制和双缸同步；隧洞页突出排空安全、外水压力与远程检查替代方案；
- 交互：支持关键路径阶段选择、质量门查看、批准基线/智能重排对比和方案冻结；所有智能建议均明确显示输入数据、适用约束及“不能替代现场工程判断”的边界；
- 三维延续：保留十页原有 Three.js 模型及模型库入口，将三维模型放入业务工作台作为计划对象和作业空间视图，不修改模型几何、材质、路由或通信协议；
- 业务依据：参考 USBR FIST 2-7、FIST 3-30/3-31、FIST 4-1A、USACE EM 1110-2-6054、FEMA P-1025、USBR 保护涂层/输水隧洞指南及 World Bank 沉积管理指南；页面数据明确标注为规划样例，不作为现场作业指令；
- 防退化校验：新增 `scripts/verify-maintenance-planning-redesign.ts` 与 `npm run verify:maintenance-planning`，检查十页配置、三维入口、模型库场景编号、KPI、信号、阶段、风险、资源和智能决策字段完整性；
- 验证：`MAINTENANCE_PLANNING_REDESIGN_OK pages=10 kpis=40 signals=40 stages=50`；`build:standalone` 与 `build:microapp` 均通过；三维背景专项检查与 `git diff --check` 通过；
- 目视边界：当前会话没有可连接的应用内浏览器实例，未执行十页截图和按钮点击验收；需在浏览器控制恢复后补做最终逐页目视验收，再进入发布阶段。

---

### [2026-08-21] 四类栏位 Three.js 三维视窗黑色背景批量修复（待发布）

- 状态：本地代码修复、自动校验、双构建与代表页面截图复核均已完成；遵照当前安排，尚未生成发布包、上传文件或切换云端版本；
- 涉及栏位：维修计划管理、零部件寿命预警、振动监测、维修培训；同时覆盖振动监测现行目录 `components/vibration-monitoring` 与仍被页面引用的历史目录 `components/Vibration monitoring`；
- 根因：这些页面的三维区域由 Three.js/WebGL 绘制，`scene.background` 会在 Canvas 内部直接清屏，优先于页面 CSS 背景；大量场景仍显式使用近黑色、纯黑色或透明背景，因此全局浅色主题无法可靠消除深色三维底板；
- 修复：将上述五个组件目录中全部 303 个原生 Three.js 渲染场景的 `scene.background` 统一为工业蓝灰 `#315268`（`0x315268`）；其中替换 268 处既有背景配置，并为 37 个原先未设置背景的场景补齐显式配置；
- 雾效一致性：同步将 122 处 `THREE.Fog`/`THREE.FogExp2` 的背景基色调整为 `#315268`，避免远景雾化后重新过渡到黑色；
- 防漏校验：新增 `scripts/verify-scene-backgrounds.ts` 与 `npm run verify:scene-backgrounds`，自动扫描五个目标目录，要求每个含 `WebGLRenderer` 的场景均设置标准背景，并阻止非标准背景色或雾效色再次混入；
- 影响边界：只修改 WebGL 场景背景及其雾效基色，不修改模型材质、灯光、几何、粒子、网格、业务状态颜色、数据、交互、菜单、路由或通信协议；个别模型本体仍为深色属于模型设计，不是背景残留；
- 验证：`npm run verify:scene-backgrounds` 通过，结果为 `scenes=303 fogs=122 color=#315268`；`build:standalone` 与 `build:microapp` 均通过；`git diff --check` 通过；
- 目视复核：本地无头 Chrome 分别打开 `mpm-0`、`generator-insulation-aging`、`vibe-TurbineShaft`、`TurbineRunnerHoistingTraining`，确认四类代表页面的纯黑三维背景均已替换为工业蓝灰，模型、网格、粒子和信息浮层继续正常显示。

---

### [2026-08-21] 主平台嵌入模式下三处 Three.js 视窗尺寸与遮挡修复（待发布）

- 状态：本地代码修复和验证完成，尚未生成发布包、尚未上传或切换云端版本，等待其他问题合并后统一发布；
- 涉及页面：`mm-03`“闸门启闭机结构损伤模拟维修”、`am-dispatch`“工单智能调度台”和 `km-flood-discharge`“泄洪消能防冲经验库”；
- `mm-03` 根因：Three.js 画布只读取组件首次挂载时的宽高并只监听 `window.resize`；主平台侧边栏、内标签页及微应用容器在挂载后改变尺寸时不会触发该事件，导致画布停留在约 160px 的首次宽度，页面只剩左侧黑色窄条；
- `mm-03` 修复：使用 `ResizeObserver` 直接监听三维挂载容器，实时同步渲染缓冲区、画布尺寸、相机宽高比；业务状态通过 `ref` 传给动画循环，状态切换不再销毁和重建 WebGL 场景；补齐观察器、动画帧、控制器和渲染器清理；
- `am-dispatch` 尺寸修复：采用相同的容器级尺寸监听，模型选中状态和点击回调改用实时 `ref`；射线拾取坐标改为按点击时的实际容器宽高计算，避免画布尺寸变化后设备点选错位；
- `am-dispatch` 遮挡根因：页面在 Canvas 后方又渲染一个占三维区域下半部的青色渐变装饰层，全局浅色主题将其映射为不透明浅色块，因此即使 Canvas 尺寸正确，下半区仍被遮住；
- `am-dispatch` 遮挡修复：删除无业务作用的下半区装饰遮罩，保留完整三维画布、模型库按钮和 HUD；
- `km-flood-discharge` 根因与修复：画布同样停留在约 140px 的首次宽度；增加容器级 `ResizeObserver`、实时仿真状态引用和完整资源清理，使泄洪模型随主平台内容区宽高持续同步且切换工况时不再重建场景；
- 影响范围：只修改上述三页的 Three.js 尺寸、资源释放、模型点击和无业务装饰层，不改变模型几何、业务数据、菜单、路由、通信协议或其他页面；
- 验证：相关 TSX 文件专项 TypeScript 检查通过；`build:standalone` 与 `build:microapp` 均通过；`git diff --check` 通过；本地无头 Chrome 逐页截图确认 `mm-03` 模型完整显示、`am-dispatch` 下半区浅色遮挡消失、`km-flood-discharge` 泄洪模型及水流粒子完整铺满视窗；
- 已知基线：项目全量 `npm run typecheck` 仍被 `components/Maintenance-plan-management/*/three-types.ts` 中既有非法字符阻断，与本次三个修改文件无关。

---

### [2026-08-21] 1号机组 3D 检视与时间轴浮层浅色化

- 首版发布：Release `20260821-084944` 已上线但用户目视验收未通过；
- 二次修复发布：Release `20260821-085852` 已上线，回滚版本为 `20260821-084944`；
- 二次反馈：首版发布后深色块仍存在，并出现浅色层与旧深色层叠加的观感；“当前检视”标签、时间轴说明等文字仍可能因颜色继承而不可见；
- 二次根因：JSX 仍保留旧 `unit1-predictive-hud`、`bg-slate-900/80`、`bg-slate-900/90` 及多组浅字工具类，CSS 中也存在三代针对旧 HUD 的深浅规则，形成竞争；
- 二次整改：从组件中删除全部旧 HUD、深色背景、深色边框和浅字工具类；移除 CSS 中所有 `.unit1-predictive-hud` 历史规则；标签、时间、说明、计数、历史起点、当前计算和预测终点均改用独立语义类；
- 防漏措施：浅色面板改为完全不透明单层 `#f1f7fa`，面板及全部后代先强制深色、不透明、正常混合，再对历史蓝和预测琥珀做深色状态区分，覆盖原先因颜色“隐身”的文字；
- 二次发布验证：现网 CSS 与 JS 均不再包含 `unit1-predictive-hud`；公开 CSS 含全部文字专用类、`#f1f7fa` 浅色底和 `#20384a` 深色字；公开 JS 含“当前检视、时间轴说明、历史起、当前计算、预测末”完整文字；
- 问题：3D 视窗左上“当前检视”和底部时间轴仍显示深蓝黑底，受全局主题与历史 HUD 规则叠加影响，部分文字也呈深色，造成深底深字；
- 修改：为两个浮层分别增加 `unit1-predictive-inspection-panel`、`unit1-predictive-timeline-panel` 专用类，并为计数标签增加明确的数据状态；
- 配色：浮层统一为略带冷蓝的半透明浅灰白 `rgba(241, 247, 250, 0.96)`，正文使用深蓝灰 `#20384a`，配柔和蓝灰边框与轻量阴影；
- 状态：历史时间使用深蓝色和淡蓝底，预测时间使用深琥珀色和淡暖白底，保留状态区分但避免突兀；
- 稳定性：最终规则放在主题样式文件末尾并使用页面专用作用域，避免旧深色 HUD 或 Canvas 通用规则再次覆盖；
- 影响范围：只调整一号机组页面这两个浮层及其时间轴控件，不改变 3D 模型、数据、预测算法、上传、布局、路由或其他页面。
- 验证：专项类型检查、导航检查、HTTPS 集成检查和双构建通过；3103 生产等价预检通过；现网独立/嵌入入口、microapp 与健康接口均为 200；公开 CSS 已命中新专用类和颜色值；
- 目视边界：当前无可连接浏览器实例，未执行发布后的截图级目视复核，需用户刷新页面后确认最终视觉效果。

---

### [2026-08-19] 全站白色主导配色改造

- 将页面、侧栏、顶栏、卡片、表格、表单、弹窗、图表提示和普通交互面板统一为白色或近白色表面，并使用深色正文保证对比度。
- 将原有深色三维视窗和 400 处固定 Three.js 场景背景改为近白色；极端天气场景保留非常轻微的冷暖白差异，画布 HUD 同步改为半透明白底深字。
- 业务状态色降为低饱和度、小面积提示，仅在进度条和状态点中保留柔和强调，避免大块高亮色破坏整体协调性。
- 生产构建通过；当前环境没有可用浏览器控制入口，未执行逐页截图级视觉验收。

---

### [2026-08-12 18:22] 完成第四轮结构性视觉浮层排查与修复

- 状态：源码排查、代码修复和生产构建完成；浏览器逐页截图验收仍待具备控制入口后复核。
- 涉及文件：
  - 修改 `views/cockpit/unit1-predictive/Unit1PredictiveView.tsx`
  - 修改 `index.css`
  - 修改 `平台视觉配色适配方案/开发计划.md`
  - 修改 `平台视觉配色适配方案/修改日志.md`
- 漏检根因：
  - 1 号机组的 `Unit1ThreeScene` 内部是深色模型区域，但“当前检视”和时间轴由父页面渲染为模型组件的兄弟浮层。
  - 既有 `.industrial-visual-surface ...` 只处理模型组件后代，无法命中兄弟 HUD；全局主题把 HUD 的 `bg-slate-900/80` 浅化，而 HUD 又从模型外壳继承浅色文字，最终形成浅底浅字。
  - 此问题属于 DOM 结构与选择器命中范围冲突，单纯扫描文字、背景类名无法发现。
- 指定页面修复：
  - 为柱状模型外壳增加 `unit1-predictive-visual-shell`，为“当前检视”和底部时间轴增加 `unit1-predictive-hud`。
  - HUD 固定使用 94% 不透明度的深蓝灰背景、浅色正文、可辨边框和轻量文字阴影。
  - 当前时间使用高明度蓝色，预测时间使用高明度黄色；时间轴说明、历史/预测刻度和滑轨分别使用浅灰及中蓝灰。
- 发散排查与通用修复：
  - 清点 11 个 React Three Fiber Canvas 文件、788 个原生 Three.js Renderer 文件和 1 个 HTML Canvas 文件；不同类型可能存在功能归属交叉，但均纳入结构规则复核。
  - 新增“包含 Canvas 的 `overflow-hidden` 视窗直属 `.absolute` 浮层”规则，覆盖 ThreeScene/Canvas 与 HUD 互为兄弟节点的结构。
  - 将模型内部 `text-slate-400/500/600` 从 `#94a3b8` 提升为 `#b8c8d2`，其在工业蓝灰背景上的对比度由约 3.75:1 提升至约 5.60:1。
  - 深色视觉区的蓝、绿、红、橙、紫文字分别恢复高明度版本，并同步恢复对应 `800/900` 状态标签的深色半透明背景，防止“亮字 + 被浅化背景”的二次冲突。
  - 复核 139 处内联颜色或透明度：Recharts Tooltip/Brush 已由第三轮统一规则覆盖；动态状态点、进度条、头像、模型材质及图表系列色属于视觉编码，未机械改写为文本色。
  - 检查标题、HUD 继承色、业务标签、透明遮罩、图表内联样式、弹窗遮罩、选中/悬停/禁用状态和空状态，没有修改业务交互与数据逻辑。
- 对比度：本轮主要视觉文字组合的理论对比度为 5.07:1～13.95:1；“当前检视”正文约为 13.95:1。
- 影响面：仅修改可视化浮层和深色视觉区配色，不改变 API、上传、预测算法、路由、图表数据或 3D 模型逻辑。
- 修改注释：所有 TSX 与 CSS 修改均添加 `2026-08-12 + 修复说明` 注释。
- 验证结果：
  - `npm.cmd run build` 通过，Vite 完成 5,555 个模块转换。
  - 构建后的 CSS 包含 `unit1-predictive-hud` 和 `:has(canvas) > .absolute` 结构规则。
  - `git diff --check` 通过，仅保留项目已有的大体积分包提示。
- 备注：当前会话没有可用的本地浏览器控制入口，因此无法把源码级覆盖声明为逐页像素验收完成；目标浏览器中仍需优先复核 1 号机组“当前检视”、预测/历史切换及时间轴拖动状态。

---

### [2026-08-12] 完成第三轮逐页文字对比度排查与通用修复

- 状态：源码排查与代码修复完成；本地浏览器截图验收待具备控制入口后复核。
- 涉及文件：
  - 修改 `views/cockpit/unit1-predictive/Unit1PredictiveView.tsx`
  - 修改 `index.css`
  - 修改 `平台视觉配色适配方案/开发计划.md`
  - 修改 `平台视觉配色适配方案/修改日志.md`
- 根因与指定页面修复：
  - “1号机组异常预测分析（全维推力瓦监控）”标题原来使用 `text-transparent + bg-clip-text + 浅色渐变`；全局主题将渐变背景浅化后，透明文字只能显示接近卡片背景的浅色渐变。
  - 将该标题改为实体深蓝色 `platform-readable-title`，并将两个操作按钮移出标题元素，避免透明文字和标题颜色继续影响按钮子元素。
- 全页面复扫与通用修复：
  - 发现 123 个页面文件使用相同的渐变透明文字模式，统一改为深蓝、深绿、深红、深橙或深紫实体语义色，不再依赖浅色背景裁剪文字。
  - 发现 42 个页面文件包含 20%～40% 透明度的单位或辅助文字，将文本元素最低视觉透明度提高到 72%；绝对定位水印、装饰图标和禁用按钮不受该规则影响。
  - 发现 54 个页面文件包含 `/40`、`/50` 等半透明业务文字，针对非装饰文本恢复完整的深色业务语义色。
  - 加深全局弱辅助文字变量，浅色卡片上的主要前景色实算对比度为 5.19:1～8.56:1。
  - 补齐 Recharts Tooltip 标签和数据项文字、光标遮罩及 Brush 时间窗轨道，解决组件内联“深色底 + 浅色字”与全局白色提示框冲突造成的白底浅字。
  - 继续保留 3D、Canvas、视频、图片遮罩和 HUD 的深色底板及浅色文字规则。
- 影响面：只修改文字、图表提示层和时间窗控件配色，不改变 API、数据计算、路由、上传、预测算法或 3D 模型逻辑。
- 修改注释：本次 TSX 与 CSS 代码修改均已添加 `2026-08-12 + 修复说明` 注释。
- 验证结果：
  - `npm.cmd run build` 通过，Vite 完成 5,555 个模块转换。
  - `git diff --check` 通过。
  - 构建后的 CSS 已包含 `platform-readable-title`、透明文字实体化、半透明文字和 Recharts 对比度规则。
  - 构建仅保留项目已有的大体积分包提示。
- 备注：当前会话没有可用的本地浏览器控制入口，因此本次完成的是注册页面范围内的源码级逐页排查与可构建验证；最终像素级视觉效果仍建议在目标浏览器中逐栏点击复核。

---

### [2026-08-12] 按注册页面清单完成第二轮逐页配色补漏

- 从 `App.tsx` 的普通导入、懒加载导入及 `import.meta.glob` 中解析页面清单，共检查 997 个实际注册页面模块。
- 对整个 `views` 页面源码继续分类扫描，发现上一轮遗漏主要集中于：
  - 278 个文件包含自定义十六进制深色渐变；
  - 209 个文件包含自定义径向渐变或暗角；
  - 661 个文件包含纯黑或半透明黑背景；
  - 21 个文件包含自定义近黑边框。
- 在 `index.css` 中补齐自定义深色渐变映射，普通页面的深色根背景、标题块和径向暗角统一转换为浅蓝灰渐变。
- 对 `bg-black`、`bg-black/10` 至 `bg-black/90` 进行结构化处理：
  - 普通信息块、列表项和数据组件转换为浅色表面；
  - 图片、视频、CSS 仿真、3D 视窗和 HUD 根据 `relative/absolute`、`overflow-hidden`、`aspect-video`、`backdrop-blur` 及 Canvas 结构保留工业蓝灰底板；
  - 固定全屏弹窗遮罩继续使用深色半透明层，弹窗正文保持浅色。
- 修复深色视觉叠层中的文字对比度，Slate 文字恢复为浅灰，蓝、绿、红、橙、紫业务文字分别使用高明度语义色。
- 统一自定义近黑边框和列表分隔线；将荧光绿文字与边框调整为浅色背景可读的成功色。
- 保留品牌蓝、状态点、图表数据色、3D 模型材质和业务计算逻辑，不修改 API、路由、报告下载及模型加载行为。
- 所有新增样式均添加 `2026-08-12 + 修改类型 + 功能说明` 注释。
- `npm.cmd run build` 验证通过，仅保留项目原有的大体积分包提示。
- 当前会话未提供本地浏览器控制入口，因此本轮结论为“逐页面源码审计完成、视觉截图验收待运行环境复核”，未将静态扫描描述为最终视觉验收。

---

### [2026-08-12] 全量细审并统一所有页面零散配色

- 全量扫描 `views`、`components` 和应用入口，确认零散深色组件主要来自未被精确规则覆盖的透明度变体，例如 `bg-slate-900/20`、`bg-indigo-900/20`、`bg-red-950/20` 和 `bg-stone-900/40`。
- 在 `index.css` 中改为按实际类名令牌覆盖深色层级，使同一颜色的任意透明度写法均能进入浅色主题，不再依赖逐个枚举。
- 统一普通容器的浅蓝灰层级，并按业务语义保留低饱和浅蓝、浅绿、浅红、浅橙和浅紫状态面板。
- 补齐深色中性边框、业务色边框、Stone 渐变及蓝绿红橙紫渐变，消除浅色卡片上的黑色分割线和深色标题块。
- 统一深色 hover 变体，避免鼠标经过列表、卡片和按钮时短暂闪回深灰色；操作按钮继续使用实色背景和白色文字，保证对比度。
- 将半透明白色高光转换为浅蓝灰分层，增强浅色卡片之间的边界；补齐接近纯黑的自定义 Tailwind 背景，同时排除品牌蓝和荧光状态色。
- 明确保留弹窗遮罩、3D Canvas、数字孪生视窗和 HUD 的中深蓝灰例外，避免全局浅色映射影响灰模、彩色模型和前后景层次。
- 检查内联深色背景后确认，除一处业务状态色外均属于 Recharts 提示框；提示框已由全局浅色规则统一覆盖，无需批量修改业务页面。
- 涉及文件：`index.css`、`平台视觉配色适配方案/开发计划.md`、`平台视觉配色适配方案/修改日志.md`。
- 所有新增样式均添加 `2026-08-12 + 修改类型 + 功能说明` 注释；未修改路由、接口、业务计算、报告下载和模型加载逻辑。
- `npm.cmd run build` 验证通过，仅保留项目原有的大体积分包提示；当前会话未提供本地浏览器控制入口，因此本轮采用全量静态审计和实际生产构建校验。

---

### [2026-08-12] 清理仿真分析前四页残留深灰文字底板

- 检查前四个外部模型页面及共享组件，确认残留深灰主要来自诊断面板、资源协同节点和详情抽屉中的旧 `bg-slate-950` 半透明背景。
- 诊断结论、故障预测、处置建议和等待状态统一改为浅蓝灰信息块，并增强边框层次。
- 资源协同三节点按业务角色调整为浅蓝、浅暖黄和浅紫背景，避免所有节点使用相似的灰色底板。
- 资源详情抽屉正文改为浅色表面，资源来源卡和服务通道卡改为浅蓝灰；弹窗外层遮罩仍保留半透明深色。
- 数据来源说明采用浅蓝与浅紫双面板，等待首次同步状态改为浅中性蓝灰标签。
- 3D 模型视窗及其控制按钮继续保留中深蓝灰背景，不受本次浅色内容区调整影响。
- 涉及文件：`index.css`、`AssessmentPanel.tsx`、`ConnectionDetailDrawer.tsx`、`ConnectionStatusBadge.tsx`、`DataProvenancePanel.tsx`、`ProjectConnectionMap.tsx`。
- 所有新增代码均添加 `2026-08-12 + 修改类型 + 功能说明` 注释。
- `npm.cmd run build` 和 `git diff --check` 验证通过，仅保留项目原有的大体积分包提示。

---

### [2026-08-12] 优化仿真分析前四页并修复 WebGL 配色回归风险

- 对仿真分析前四个外部模型页面增加独立浅色主题作用域，重点调整标题区、设备资源协同区、3D 模型卡、实时参数卡、趋势图和诊断区层级。
- 页面主体使用浅蓝灰渐变，标题和资源链路区使用不同深浅的信息面，3D 视窗继续保留中深蓝灰背景。
- 实时参数卡按正常和异常状态分别使用浅蓝与浅红语义背景，增强数值、范围和状态的辨识度。
- 撤销上一轮全局 Canvas 的 `mix-blend-mode: screen`，统一恢复为 `normal`；外部 FBX/GLTF 视窗增加独立覆盖规则，避免不透明 WebGL Canvas 出现 GPU 合成异常。
- 外部模型场景在 Three.js 内部将过暗背景提升到中深蓝灰，不再依赖 CSS 混合方式修改 Canvas 像素。
- 当前接口检查同时发现上游模型下载再次异常；该后端问题及证据已写入《3D模型请求失败错误报告》，与前端配色回归风险分别处理。
- 涉及文件：`index.css`、`views/simulation/remote-model/RemoteModelSimulationView.tsx`、`components/remote-model-showcase/RemoteModelViewer.tsx`、`RemoteMetricCard.tsx`、`ProjectConnectionMap.tsx`。
- 所有新增代码均添加 `2026-08-12 + 修改类型 + 功能说明` 注释。
- `npm.cmd run build` 和 `git diff --check` 验证通过，仅保留项目原有的大体积分包提示。

---

### [2026-08-12] 将确认后的浅蓝灰主题扩展到全部页面

- 将 `App.tsx` 的浅色主题作用域由第一栏路由提升到整个内容区，全部导航栏目和业务页面统一使用浅蓝灰页面外壳、顶部栏与文字基色。
- 保留当前已确认的视觉层级：页面底色、导航外壳、标题栏、信息条、内容卡片和输入控件分别使用不同深浅的浅色表面。
- 在 `index.css` 中补齐其余页面常见的 Slate、Gray、Zinc、Neutral 深色背景、文字和边框映射。
- 对项目中大量自定义近黑十六进制背景增加统一兼容规则，防止切换栏目后重新出现整块黑色内容区。
- 将深色 Slate/Cyan/Blue 渐变页头和分区背景转换为浅蓝灰渐变，同时保留正常、警告、危险等业务状态色。
- 增加 Canvas 3D 容器自动识别：
  - 3D 视窗继续使用中深蓝灰渐变和弱网格，不强制改为白底；
  - 对历史近黑 Canvas 使用蓝灰底板和屏幕混合方式抬升暗部；
  - 3D HUD 使用深蓝半透明底和浅色文字，保证灰模、彩色模型及叠加数据均清晰。
- 所有新增代码均添加 `2026-08-12 + 修改类型 + 功能说明` 注释。
- 涉及文件：`App.tsx`、`index.css`、`平台视觉配色适配方案/开发计划.md`、`平台视觉配色适配方案/修改日志.md`。
- 未修改菜单 ID、路由关系、API 请求、业务计算、数据状态、报告下载和 3D 模型交互逻辑。
- `npm.cmd run build` 验证通过，仅保留项目原有的大体积分包提示。

---

### [2026-08-12] 增加导航、标题栏和边界区域的浅色层级

- 根据反馈减少界面中的纯白区域，保留“白色与浅色为主体”的总体方向。
- 在 `index.css` 中增加多级浅蓝灰颜色变量：页面底色、普通表面、次级表面、导航外壳、强化外壳和信息条表面。
- 通用顶部栏由白色调整为浅蓝灰，并加强底部分隔和阴影。
- 侧栏主体、品牌区、菜单悬停、选中菜单、子菜单、数量标签和底部状态区分别使用不同深浅的蓝灰色。
- 通用卡片主体调整为极浅蓝灰，卡片标题栏使用更明显的浅蓝灰背景。
- 设备页面顶部标题区调整为三段浅蓝灰渐变，并强化外边框和左侧品牌色边界。
- 场景信息条增加 `platform-meta-bar` 独立样式，与普通内容卡片区分。
- 表格标题行和数据悬停行调整为浅蓝灰层级。
- 白色主要保留给输入框、选择框等关键交互区域，避免所有容器使用相同背景。
- 涉及文件：`index.css`、`src/scenarioLib/ScenarioMetaBar.tsx`。
- 所有新增调整均添加 `2026-08-12 + 修改类型 + 功能说明` 注释。
- 未修改业务逻辑、接口、路由、数据和 3D 模型行为。
- `npm.cmd run build` 验证通过，仅保留原有大体积分包提示。

---

### [2026-08-11] 优化浅色层级、文字对比度与 3D 灰模展示

- 重新明确视觉结构：页面主体使用雾蓝灰背景、白色卡片和浅色内部面板，企业蓝为主强调色，并搭配绿色、紫色、橙色和红色表达不同业务语义。
- 修复浅色背景下文字辨识问题：
  - 补充 Blue/Cyan/Sky、Green/Emerald、Red/Rose、Purple/Violet 等浅色文字类映射；
  - 状态文字改用深色语义色，状态背景改为低饱和浅色；
  - 修正删除、确认、报告等有色按钮的前景色和边框；
  - 保证正常、警告、危险状态不再出现背景色与文字色过近的问题。
- 调整辅助配色：
  - 暖色只用于温度、风险和重点指标，不再作为页面主体背景；
  - 图表暖色序列统一为低饱和橙色，与企业蓝形成辅助色搭配；
  - Stone 系列统一映射为中性灰。
- 优化通用 3D 区域：
  - 背景由近黑色改为中深蓝灰渐变，并加入低对比度网格；
  - 增加环境光、天空光、主方向光和轮廓光；
  - 启用 sRGB 输出、ACES 色调映射并调整曝光；
  - 提高通用灰模基础材质明度，增强灰色和金属模型轮廓。
- 优化矿山提升机和一号机组模型：
  - 提升机主体、笼体和轮廓光统一为青蓝色，灰色配重使用补光增强；
  - 一号机组轴体、推力盘和基础环提高材质明度；
  - 一号机组增加冷色补光并提高网格亮度。
- 涉及文件：`index.css`、`components/ThreeScene.tsx`、`components/cockpit/unit1-predictive/ThreeScene.tsx`。
- 所有新增调整均添加 `2026-08-11 + 修改类型 + 功能说明` 注释。
- 未修改接口、业务数据、路由和模型交互逻辑。
- `npm.cmd run build` 验证通过，仅保留原有大体积分包提示。

---

### [2026-08-11] 根据首轮截图优化第一栏视觉协调性

- 根据矿山提升机页面截图确认首版主要问题：橙褐色遗留面积过大、白色卡片缺少层次、暖色边框与蓝白外壳冲突、3D 线框及图表色不统一。
- 优化 `index.css`：
  - 页面底色调整为雾蓝灰，降低大面积纯白造成的突兀感；
  - 强制通用卡片使用统一白色表面和中性边框，避免页面自定义深褐背景覆盖；
  - 含主标题的设备页头统一为蓝白信息栏，移除深褐、橙色渐变；
  - 将装饰性 Amber/Orange/Yellow 和 Stone 色映射为品牌蓝或中性灰；
  - 告警红、正常绿继续保留，动态警告数字继续使用警告色；
  - 遗留暖色图表序列统一为品牌蓝；
  - 修正暖色按钮、边框和内部面板在浅色主题下的对比度。
- 优化 `views/MineHoistView.tsx`：
  - 3D 提升机线框由橙色调整为青蓝色；
  - 深度运行轨迹、坐标轴、网格和提示框改为企业蓝白配色。
- 所有新增调整均添加 `2026-08-11 + 修改类型 + 功能说明` 注释。
- 未修改接口、数据计算、路由、交互和模型加载逻辑。
- `npm.cmd run build` 验证通过，仅保留原有大体积分包提示。

---

### [2026-08-11] 完成通用组件与第一栏页面配色替换

- 实施范围：
  - 通用应用外壳：侧栏、顶部栏、页面背景和滚动条；
  - 通用组件：`SciFiCard`、3D 画布标记和模型库入口；
  - 第一栏“工业智能运维”总览页；
  - 第一栏 `eq-0` 至 `eq-18` 共 19 个子页面。
- 新增 `index.css`：
  - 建立企业蓝、页面背景、卡片、文字、边框、状态色和深色可视化变量；
  - 将第一栏页面现有的 Slate/Cyan 深色工具类适配为浅色企业主题；
  - 统一图表坐标、网格、提示框、表格、输入框、弹窗和悬停状态；
  - 对 3D 模型区域进行深色隔离，避免浅色规则覆盖模型画布。
- 修改 `App.tsx`：
  - 使用路由条件识别 `smart-ops` 和 `eq-*` 页面；
  - 第一栏启用浅色内容主题，其他栏目暂不进行页面级颜色替换；
  - 顶部栏改为企业蓝白风格。
- 修改 `components/Sidebar.tsx`：侧栏改为白色表面、企业蓝选中态和浅色子菜单。
- 修改 `components/SciFiCard.tsx`：通用卡片改为白色表面、柔和阴影和企业蓝强调线。
- 修改通用及一号机组 3D 场景组件：增加深色可视化区域标记。
- 修改 `src/scenarioLib/ModelLibraryLink.tsx`：统一模型库入口的企业蓝链接样式。
- 所有新增或调整代码均已添加 `2026-08-11 + 修改类型 + 功能说明` 注释。
- 未修改菜单 ID、路由关系、API、数据处理、模型加载、报告下载或其他业务逻辑。
- 验证结果：
  - `git diff --check` 通过；
  - `npm.cmd run build` 通过；
  - Vite 仅保留项目原有的大体积分包提示，不影响本次构建；
  - 页面最终视觉效果等待用户在本地运行环境查看并反馈。

---

### [2026-08-11] 精简配色适配文档

- 根据要求将配色适配材料缩减为《开发计划》和《修改日志》两份文件。
- 重新编写《开发计划》，保留并简化以下必要内容：
  - 改造目标与视觉方向；
  - 暂定色板及其非精确取色说明；
  - 前端主题变量和图表/Three.js 配色设计；
  - 分批实施顺序、代码注释规则与验收要求；
  - 参考平台截图需求和开发确认条件。
- 删除以下重复说明文件，其必要内容已经合并进《开发计划》：
  - `参考平台取色报告.md`
  - `配色与主题设计规范.md`
  - `实施调整说明.md`
- 本次只调整说明文档，未修改页面样式、业务代码、依赖、接口或部署配置。

---

### [2026-08-11] 建立平台配色适配初步方案

- 检查当前项目配色，确认主要使用 Slate 深色背景和 Cyan 强调色，且颜色分散在多个页面及组件中。
- 尝试访问参考平台；因当前环境无法稳定建立连接，未将候选颜色描述为精确取色结果。
- 初步确定“浅色管理界面 + 深色工业可视化区域”的改造方向。
- 暂定企业蓝、浅色页面、深色 3D 区域以及状态色的使用规则。
- 规定后续修改采用统一主题变量、分批迁移、逐批验证和可回退方式。
- 规定所有实际代码修改添加 `日期 + 修改类型 + 功能说明` 注释。
- 本阶段未修改任何业务代码或现有页面配色。

---

（后续开发内容将在此处按时间倒序追加。）
