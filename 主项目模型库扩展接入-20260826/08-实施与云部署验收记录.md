# 实施与云部署验收记录

> 执行日期：2026-08-27（Asia/Shanghai）
> 最终状态：场景库开发、提交、云端切换和技术验收完成；主项目导航发布待主项目团队执行

## 1. 开发结果

- 新增 99 个唯一页面—模型运行时绑定，连同原四个样例共 103 个模型展示页；
- 所有入选菜单入口只展示本轮新开发的三维模型页面；“模型展示 / 原业务页”切换栏已在后续修订中移除；
- 新增 `eq-unit1-model`，固定使用模型 `6691`；
- `eq-18` 不在模型绑定表中，原组件、数据逻辑和工业智能运维第 19 位均未改；
- 仿真分析前四页顺序未改；
- 12 个栏目的模型页按确认清单前置，其余页面保留并维持相对顺序；
- BFF、Dashboard 图表、诊断和 data-sync 已兼容主项目动态返回的 4～6 个字段；
- 场景库详情跳转白名单由同一 103 模型目录生成，避免新增页出现按钮自拒绝。

核心提交：

```text
4763252 docs: archive model library expansion plan
ff55753 feat: expand model library showcase to 99 pages
0d1f4ad fix: allow approved model catalog detail navigation
7e375ce chore: update scene library menu manifest
5fe817e docs: deliver model deployment and host navigation update
caa8829 fix: remove legacy business page switch from model views
```

## 2. 本地与实时接口验证

```text
NAVIGATION_SPLIT_VERIFY_OK roots=20 nodes=1050
MODEL_LIBRARY_EXPANSION_VERIFY_OK bindings=99 showcasePages=103 menuNodes=1050
HTTPS_INTEGRATION_VERIFY_OK models=103 files=19
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=http://127.0.0.1:3000/api
```

- standalone 与 microapp 双构建通过；分别转换 5,530 与 5,525 个模块；
- 1 号机组模型二进制实际下载为 2,134,604 字节，格式为 FBX；
- C 级代表页 `pm-hydro-19` 的 5 个动态字段 data-sync 为 `pass`，诊断正常返回；
- 全量 `npm run typecheck` 仍被项目既有 7 个 `Maintenance-plan-management/*/three-types.ts` 非法字符文件阻断；导航专项、构建和本次新增代码没有新增类型错误；
- 本轮没有可连接的交互式浏览器实例，未把 HTTP/资源检查冒充为像素级目视验收。

## 3. 最终发布信息

| 项目 | 值 |
|---|---|
| 最终 Release ID | `20260827-150658` |
| 发布源码提交 | `caa882935e2f9e85455f7579750e21217bf56a9c` |
| 发布包 | `scene-library-release-20260827-150658.tar.gz` |
| 发布包大小 | `39,988,294` 字节 |
| 条目数 | `325` |
| 发布包 SHA-256 | `06e3098d9ec069e53243b9c3ac7a1c46d53e6e7f11a327497c1bad07f5c3b1cd` |
| 安装目录 | `/nlplabProj/scene-library/releases/20260827-150658` |
| 目录大小 | 约 `504M` |
| 运行依赖 | 362 个包，tsx `4.23.12`，Node `22.23.2` |
| 菜单版本 | `20260827-142935` |
| 菜单节点 | 根节点 `20` / 总节点 `1050` / 唯一 ID `1050` |
| 菜单内容 SHA-256 | `270ed2cd19452df4e6ec2c5916974042744cea6fdf01e76e544ddd62bbe88951` |
| 菜单文件 SHA-256 | `c54f5375db0b99460d5848895b436b47ffe0848aea69590a7d3719e24b7d14bd` |

发布过程中先上线 `20260827-102257` 完成 99 页验证，随后以 `20260827-142935` 修复详情跳转白名单。根据后续界面修订，最终形成 `20260827-150658`：删除模型页顶部配对说明及“模型展示 / 原业务页”切换栏，绑定入口直接进入新开发的三维模型页面；菜单内容和模型 API 均未改变。

## 4. 云端切换与回滚

最终切换后：

```text
current  → /nlplabProj/scene-library/releases/20260827-150658
previous → /nlplabProj/scene-library/releases/20260827-142935
```

环境公开字段：

```text
HOST=127.0.0.1
PORT=3102
APP_VERSION=20260827-150658
RELEASE_VERSION=20260827-142935
```

环境备份：

```text
/tmp/scene-library-next.env.before-20260827-150658
```

切换脚本包含失败自动回滚，本轮未触发。旧 Release、临时发布包、共享数据和模型缓存均未删除。

## 5. 云端技术验收

```text
MODEL_LIBRARY_LIVE_VERIFY_OK bindings=99 base=https://47.122.104.52/scene-library-api
```

- 99 个新绑定页的 Bootstrap 全部通过模型 ID、格式、文件大小和 4～6 个 Dashboard 字段校验；
- `eq-unit1-model/6691`、`eq-0/2353`、`pm-hydro-19/2296` 在 3103 生产等价临时服务中通过；
- HTTPS 与 HTTP 兼容入口的 health、standalone、embedded 页面和 microapp 均返回 200；
- 微应用脚本为 `application/javascript`，Content-Length `18,072,302`；
- `scene-library.service` 为 active/enabled；3102 只监听 `127.0.0.1`；3103 无残留；
- Nginx 配置检查通过；切换后 10 分钟内服务错误日志无记录；
- `shared/data` 与 `shared/model-cache` 保留。
- 最终 standalone 与 microapp 构建产物中均不存在“原业务页”和“页面展示模式”文字；源码专项检查输出 `legacySwitch=hidden`。

## 6. 尚待完成

1. 主项目团队按 `07-主项目导航栏更新列表与发布说明.md` 发布 1,050 节点菜单并完成浏览器验收；
2. 若希望 99 个新增模型都能从 iframe 打开主项目详情页，主项目宿主需把消息白名单扩展到本轮 103 个模型；模型 API 本身无需修改；
3. 在目标浏览器抽查模型朝向、材质、缩放和热点可读性，并确认页面中不存在“原业务页”入口或双模式切换栏；
4. 本轮结束时 GitHub TLS 已恢复，此前累计 8 个本地提交已成功推送到 `origin/main`；无需再补推旧提交。

## 7. 后续模型加载优化发布

2026-08-27 针对部分模型长时间停留 0% 的问题发布 Release `20260827-155745`，不改变菜单内容、页面—模型配对、`eq-18` 位置或主项目 API。

```text
源码提交：6253a659b19b5539243320b4d1a4dce579d46092
发布包：scene-library-release-20260827-155745.tar.gz
大小：39,991,463 字节
条目：325
SHA-256：167f20b9e7e091138bac7df1c9ab1c704eef35c9a5fcd2b75cec4107d23c95d4
current：20260827-155745
previous：20260827-150658
环境备份：/tmp/scene-library-next.env.before-20260827-155745
```

- 浏览器模型请求支持无订阅者时取消，同一模型的未完成任务共享进度并跨版本别名去重；
- 页面显示准备、下载、解析三个阶段，增加准备超时和流停滞保护；
- 服务端版本化响应为 `private, max-age=31536000, immutable`，浏览器 ArrayBuffer 缓存限制为 3 个；
- 3103 预检、99/99 公网 Bootstrap、完整 25.2 MiB 模型传输、公网双入口、服务/Nginx、端口和共享目录验证通过；
- 无可连接的交互式浏览器，Network 队列和截图级目视验收仍保留为明确边界。
