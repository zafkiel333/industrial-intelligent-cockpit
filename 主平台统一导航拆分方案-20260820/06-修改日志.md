# 场景库导航拆分至主平台：修改日志

> 本文件是本需求必须维护的变更记录。规划、代码实现、部署和联调分别记录，不把“写完方案”误写成“功能已上线”。

## 状态说明

| 状态 | 含义 |
|---|---|
| 已规划 | 需求和方案已形成文档，尚未修改代码 |
| 开发中 | 正在修改代码或菜单数据 |
| 已开发 | 本地代码完成，尚未部署 |
| 已部署 | 对应平台已发布，尚可能等待联合验收 |
| 已验收 | 双方联合验收通过 |
| 已回滚 | 已恢复到指定旧版本 |

---

## 2026-08-20：方案建立

状态：**已规划，尚未修改业务代码，尚未部署。**

### 新需求

- 场景库不再在主平台 iframe 中显示自己的全局导航栏；
- 主平台负责场景库全部菜单导航；
- 主平台关于本项目只有一个一级菜单“场景库”；
- 场景库现有菜单树整体放到“场景库”之下；
- 原场景库根菜单成为主平台二级菜单；
- 原 children 继续成为三级或更深层菜单；
- iframe 只显示导航栏以外的业务内容。

### 方案决定

- 保持当前 iframe 集成；
- 不采用 qiankun；
- 第一版使用 `embedded=1&viewId=...` URL 参数；
- 主平台路由采用 `/scene-library?viewId=...`；
- 场景库无参数独立入口继续保留原导航；
- 不新增主平台到场景库的实时切页 `postMessage`；
- 现有场景库到主平台模型详情的消息协议保持不变；
- 菜单清单由 `MENU_ITEMS` 自动生成 JSON，避免人工录入；
- 场景库先发布，主平台后发布；
- 不改变场景库 Nginx、systemd、API 前缀和 release 目录。

### 新增文档

- `00-方案总览与文档交付清单.md`；
- `01-开发计划与责任分工.md`；
- `02-场景库端开发调整指导.md`；
- `03-主平台端开发调整指导.md`；
- `04-菜单清单与页面定位协议.md`；
- `05-双平台部署联调验收与回滚说明.md`；
- `06-修改日志.md`。

### 尚未完成

- [ ] 场景库代码调整；
- [ ] URL 参数解析测试；
- [ ] Sidebar 嵌入模式隐藏；
- [ ] manifest 生成脚本；
- [ ] 完整菜单 manifest；
- [ ] 场景库新版构建和部署；
- [ ] 主平台菜单导入/配置；
- [ ] 主平台宿主页调整；
- [ ] 主平台发布；
- [ ] 双方联合验收。

---

## 后续代码修改记录模板

### `<日期>`：场景库开发

状态：`开发中 / 已开发 / 已部署`

```text
Git commit：
涉及文件：
实现内容：
测试命令：
测试结果：
menuVersion：
manifest SHA-256：
场景库 Release ID：
回滚版本：
遗留问题：
```

### `<日期>`：主平台开发

状态：`开发中 / 已开发 / 已部署`

```text
主平台 commit/版本：
涉及文件：
菜单导入方式：
权限策略：
内标签策略：
测试结果：
主平台回滚版本/备份：
遗留问题：
```

### `<日期>`：联合验收

状态：`已验收 / 未通过 / 已回滚`

```text
场景库 Release ID：
主平台版本：
menuVersion：
验收环境：
通过用例：
失败用例：
处理结论：
双方确认人：
```

---

## 维护规则

1. 每次代码、菜单、协议或部署变化都增加一条，不覆盖旧记录；
2. 记录事实和版本，不只写“已修改”；
3. 尚未部署时明确写“未部署”；
4. 场景库与主平台版本必须成对记录；
5. 菜单变化记录 `menuVersion` 和 SHA-256；
6. 回滚也必须记录原因、目标和结果；
7. 密码、私钥、Token 和完整生产环境变量不得写入日志。

